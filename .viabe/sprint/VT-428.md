---
vt_id: VT-428
title: Owner portal — render SR activation unmet-prerequisites ("connect a data source to activate")
status: Queued
priority: Medium
sprint: "Sprint-Fazal"
type: Feature
area: [Frontend, Owner-Portal, Onboarding]
related: [VT-421]
exec_order: 97
created: 2026-06-25
last_updated: 2026-06-25
---

## Contract (Cowork GATE 2026-06-25, VT-421 follow-up). NOT blocking the VT-421 merge. The introspection is only worth building if the owner SEES it.

VT-421 built `agents/activation_registry.unmet_prerequisites(tenant, agent)` (human-readable reasons) but it is NOT
yet wired to any web/portal consumer — so there's no live "SR needs a connected customer-data source to activate"
render today (verified in the #510 adversarial review).

## Scope
- An owner-facing surface (dashboard/onboarding) reads the per-tenant unmet-prerequisites for SR (via an
  internal-secret orchestrator endpoint that calls `unmet_prerequisites`) and renders the reasons as the activation
  blocker ("connect a customer-data source", "complete onboarding", …) — not a silent inactive state.
- Tenant from the owner session server-side (never client-trusted). No PII beyond the static reason strings.

## Gate
Owner-portal read surface. Routine → Cowork gate (owner-auth path — confirm session-derived tenant).

## Status history
- 2026-06-25: Rostered + ID allocated (Cowork VT-421 gate follow-up). Portal render of the activation-prereq reasons.
