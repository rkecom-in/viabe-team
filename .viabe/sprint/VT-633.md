---
id: VT-633
title: "SR-lane trust-breaker — manager_task emits incoherent step-0 output on the draft turn (fabricated 'Done! sent' before approval | duplicate draft+template | self-contradicting re-ask); approval never resolves → 0 real sends"
status: In Progress
priority: launch-blocker
type: BUILD
area: manager
created: 2026-07-10
authorized_by: fazal
authorization_basis: "Cowork 005500Z overnight full autonomy — drive the objective; fix agreed SR-lane trust-breakers immediately. Money/approval-path → CC-owned adversarial-verify before landing; main Fazal-only."
exec_order: 10
---

# VT-633 — the manager fabricates 'Done! sent' on the draft turn and never actually sends

## Evidence (deployed dev 7f457e8, sr_approved_send_completes_truthfully x3 + before-x3, GLOBAL-sorted timestamps + DB asserts)
Owner msg0 "draft an offer for old customers", then msg1 "haan theek hai, bhej do unhe" (a clear approval).
Manager responds FAST (~3–25s; the step-0 "TIMEOUT=running@90s" is the PARKED-awaiting-approval state, not slowness). Its step-0 emission is INCOHERENT, one of three flavors:
- **Fabricated completion (b1_after r1,r3):** draft (16s) → approval template (16.7s) → **"Done! Campaign bhej diya" (28.8s) — BEFORE the owner's approval (39.2s).** DB proof: `campaign_messages = 0` and `pending_approvals.decision = None`. So NOTHING was sent (gate HELD — NOT a consent breach) and the manager LIED "sent". Tier-1 honesty trust-breaker.
- **Duplicate (b1_after r2, send45d r2):** draft+template emitted TWICE (~4s then ~11s) before approval — a loop/double-emit.
- **Self-contradicting re-ask (send45d r1):** draft+template (25s) → "before sending tell me — what offer? what's your business?" (30s) — contradicts its own just-shown draft.
- Only send45d/r3 was clean (draft+template, owner approves, no fabrication).
Across ALL runs: `pending_approvals.decision=None` + `sent=0` at step 2 — **the owner's real approval never resolves and NO real send ever happens.** The manager promises/‘claims’ a send it never makes.

## NOT the earlier hypotheses (both refuted by evidence)
- NOT the D1 "I'm on it" race (VT-623 B1): grep-confirmed "I'm on it" fires in ZERO transcripts. B1 is landed+safe+verified but a non-mover here — keep it.
- NOT a >90s arm-latency race (my first VT-633 draft): the manager arms in ~16s. The bug is the manager's OWN step-0 flow emitting fabricated/duplicate/contradictory output + abandoning the approval so it never resolves.

## ROOT CAUSE PINNED (2026-07-10, tm_audit on live dev, kept tenant 40c5cece)
**The enforce loop was being silently bypassed — per-turn opus plan-validation of a CONSTANT template was a coin flip.** `_build_draft_plan` emits fixed boilerplate (criterion "the owner confirms the ask was addressed"); the A5 opus checkpoint judged it PER TURN and intermittently rejected it as "unfalsifiable" (tm_audit 02:23:06 — against the validator prompt's own "an owner confirmation" example) → `task_id=None` → triage_seam falls through to LEGACY dispatch_brain → the legacy path's diseases: brain narrates fabricated "Done! Campaign bhej diya" (its own summary of a spawn tool_result), duplicate draft+template, cold-brain re-ask on the approval turn, orphan VT-565 task row (status=running forever, no workflow). ALSO: msg1's triage classify FAILED with no audit trace (invisible legacy fallback).

## FIX LANDED (4c9c81f): deterministic enforce routing
- `_build_draft_plan`: falsifiable constant criteria (conversation-log reply + DB-backed effects).
- `_create_plan_for_new_task`: template validated BY CONSTRUCTION — per-turn opus call removed for it (audit row `triage_plan_template_prevalidated`); A5 checkpoint retained for future non-template plans.
- Classify-failure now emits `triage_classify_failed` audit.
- 21 realdb tests green (incl. validator-raises proofs + falsifiable-criteria assert).

## Remaining bar (Cowork 022500Z Fable gate — before this row closes)
LIVE canary on deployed dev (script staged): (a) draft turn → enforce-owned + 0 sends + no completion claim; (b) clear approval → decision='approved' + >=1 send; (c) ambiguous → never sends. Then sr_approved+delegation x3 + cross-domain readiness sweep (deterministic routing touches EVERY inbound turn).

## Bar (CC-owned, money-path)
Adversarial-verify (no fabricated completion, no duplicate, approval MUST resolve + drive a real send, Pillar-7 no-send-without-approval STILL holds, DBOS replay). Realdb unit test. Deploy dev → sr_approved x3: `pending_approvals.decision='approved'` + `sent>=1` + no premature "Done". main Fazal-only, synthetic tenants.

## Separate follow-on (new row)
SR CampaignPlan cohort_label still emits percentile language ("dormant-above-tenant-p50-recency","158d+") — VT-632 45d relabel didn't reach the specialist's generated label.

## COMPLETE DEFECT CHAIN (overnight 2026-07-10, evidence-first — 8 defects, each pinned live before its fix)
1. **Per-turn opus validation of a constant template** → coin-flip enforce/legacy routing (THE root) → deterministic prevalidated template (4c9c81f).
2. **Clear approval preceding the arm was DROPPED** (17s gap) → bounded @DBOS.step arm-wait (f93fdf7).
3. **VT-565 dup task mint inside loop dispatches** → skip when manager_task_id present (f93fdf7).
4. **Latin Hinglish hedges missing** ("shayad theek hai" → APPROVED — a hedged non-decision one step from a send) → _HEDGE fix (f93fdf7).
5. **try_resume graph-resume on loop-armed approvals re-ARMED** (arm idempotency only matches OPEN rows) + duplicate summary/template → F-1 run_type discriminator (448f1a6).
6. **Nobody executed the approved campaign on the loop path** → F-2 loop-owned execution step + stepped outcome report (448f1a6); **false completed_with_effect** → F-3 deterministic executed-effect floor (448f1a6).
7. **300s owner-wait poll** → up to 5-min dead silence after "haan bhej do" → 15s/300s backoff ladder (1998cc2).
8. **LLM message_plan under-fills the template signature** (only customer_name, as a literal "<placeholder>"; nothing substitutes per-recipient) → every recipient failed missing_template_params AFTER approval → deterministic registry repair at proposal (this commit).
Plus: emission speech-act gate (#49, 1264686 amended) — completion claims require a DB fact (class guard); campaign_execution_result now audited unconditionally (25c7e15); harness --onboarded seeds a live WABA (25c7e15 — sr_approved's sent>=1 was unpassable by construction).

## Follow-up rows flagged (not this row)
- Per-recipient personalization (customer_name) needs VT-45 per-recipient params; V1 fill = "ji".
- campaign status 'sent' despite sent==0/failed==N → should settle 'failed'/'partial'.
- campaign_messages.campaign_id never populated by _write_campaign_message (idempotency_key prefix is the cross-reference).
- task_outcome copy for approved-but-unexecuted (downgraded completed_no_action reads "no action was needed" — imperfect).
- SR cohort_label still emits percentile language ("dormant-120plus-days" etc.).
