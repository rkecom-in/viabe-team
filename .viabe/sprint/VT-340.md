---
vt_id: VT-340
title: CI/portal polish — Lighthouse gate + green the e2e-playwright/pr-title PR checks
status: Backlog
priority: Low
sprint: Sprint 8 - Owner Surface & Billing
type: Chore
area: [Infra, CI, Frontend]
assignee: unassigned
parent: VT-245
sub_items: []
exec_order: 9.2
blocked_by: []
tags: [ci, test-hygiene, frontend]
---

# VT-340 — Lighthouse gate + green the e2e-playwright/pr-title PR checks

Two PR-only CI items (zero deploy impact — `pull_request`-only, NOT in Railway's wait-set;
confirmed 20260605T182027Z):
1. **Lighthouse-CI gate** (VT-338 scoped-out): a CI job that builds team-web + serves it +
   runs `lhci` asserting >85 (perf/a11y) on the dashboard index. Genuine infra (build+serve+
   lhci), not a no-op config — hence a row, not inlined into VT-338.
2. **e2e-playwright + pr-title** red on PRs (seen on #351/#354). Diagnose + fix (or mark
   non-required / paths-ignore). PR-signal hygiene only.

LOW — behind the feature queue (Cowork 20260605T182400Z).

## Phase 1 (DONE — PR-signal hygiene)
- **pr-title FIXED.** The gate regex was end-anchored with no suffix (`\(VT-[0-9]+(-fix-[0-9]+)?\)$`) → it false-failed nearly EVERY real title (`(VT-88 Phase 1)`, `(VT-90) (#327)`, `(VT-96; Cowork)`). Relaxed to `\(VT-[0-9]+(-fix-[0-9]+)?[ ;,)]` — requires a numeric `(VT-N` run followed by a separator, so a descriptive suffix is allowed and the ref need not be last, while text-suffix (`VT-Foundation`/`VT-CI`) and dotted-legacy (`VT-3.3a`) IDs are STILL rejected (VT-239 intact). Verified against the full accept/reject set.
- **e2e-playwright is HEALTHY** (not red): it `needs: changes` + `if: needs.changes.outputs.web/orchestrator/ci == 'true'` → it correctly SKIPS when no relevant paths changed (the VT-245 trigger-diet). The #351/#354 "reds" predate that; no fix needed.

## Phase 2 (DEFERRED — needs a Cowork ruling)
**Lighthouse-CI gate.** Wrinkle: the dashboard index is `requireOwnerSession`-gated, so `lhci` hits the login redirect, not the dashboard — a perf/a11y gate needs a test session (a CI-only signed cookie) OR to target a PUBLIC page (the marketing/signup page) instead. That's a real design choice + adds per-PR build+serve+lhci CI time for a pre-launch portal. Deferred pending Cowork's now/later + which surface (auth'd dashboard via a CI session vs the public page).

## Origin
VT-87/VT-338 deploy-topology diagnosis + the VT-338 Lighthouse scope-out.
