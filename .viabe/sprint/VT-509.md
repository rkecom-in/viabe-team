---
id: VT-509
title: "Discovery quality — LLM not-found rendered as a card + scraper doesn't reproduce manual knowyourgst search + hide source names"
status: In Progress
priority: Critical
type: BUG
area: team-orchestrator
created: 2026-06-30
authorized_by: fazal
authorization_basis: "Fazal 2026-06-30 live re-run: LLM 'not found' reasoning shown as a Found card; ScrapingBee returned nothing while manual knowyourgst.com search returns RKECOM; + don't show source names (web/scrapingbee) to the user — 'public source' only."
---
# VT-509 — discovery quality (3 fixes; raw-evidence bar)
DEFECT 1 (LLM card): the LLM leg dumped its 'I'll search… Not Found' monologue as a Found candidate named after the echoed query. Fix: LLM leg returns STRUCTURED {name, gstin} only; not-found → ZERO candidates; never echo the query / dump reasoning; only GSTIN-bearing candidates surface as Found.
DEFECT 2 (scraper, core): ScrapingBee returned NOTHING (both cards source=web) but Fazal's MANUAL knowyourgst.com by-name search returns RKECOM SERVICES (OPC) PRIVATE LIMITED + GSTIN 27AAKCR3738B1ZE. The scraper doesn't reproduce a manual search. DO a real run, CAPTURE RAW HTML, find the EXACT divergence (js_scenario form-fill/submit firing? 6s wait too short? _RESULT_RE markup drift? wrong URL/field?), FIX so it reliably returns RKECOM+GSTIN. (My VT-507 '5/5' was flawed — likely cached / a web-source result, not the scrape.)
DEFECT 3 (Fazal direct): never show the candidate SOURCE name (web/scrapingbee/knowyourgst/maps) to the user — genericize to 'public records'/'public source'.
PROOF: the REAL signup form for 'RKeCom Services Pvt Ltd' surfaces ONE clean candidate = RKECOM SERVICES (OPC) PRIVATE LIMITED WITH its GSTIN (from the scrape), no garbage LLM card, source shown as 'public records'. Raw HTML evidence for the scraper fix.
