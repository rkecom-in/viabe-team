---
vt_id: VT-379
title: pipeline_steps.error column — never redacted at write, never swept; 3 direct-INSERT writers bypass redaction
status: PR-Ready
priority: High
sprint: "Sprint-Fazal"
type: Bug
area: [Privacy, Orchestrator]
assignee: Claude Code
related: [VT-374, VT-102]
cl_decisions_checked: []
created: 2026-06-12
last_updated: 2026-06-12
---

## Finding (CC STEP-0 of VT-374, 2026-06-11 — out-of-scope flag, accepted by Cowork)

Double blind spot: `pipeline_steps.error` is (a) never redacted at write and (b) never
swept by the nightly Detector-5 (which scans envelopes only). Plus three direct-INSERT
writers with zero redaction: `error_router`, `self_evaluate_gate`, `collapse`. Exception
strings can carry raw message content / phone numbers into an unredacted, unswept column.

## Proposed contract (when dispatched)

Route the 3 direct writers through the redacting writer; redact `error` at write with the
name registry; extend Detector-5 to the error column; backfill-redact existing rows; tests.

## Status

**Queued — NEEDS-FAZAL dispatch ruling.** Outside the CL-435 A–D grant; rostered so it
isn't lost. Not gate-blocking VT-374.

## Status history
- 2026-06-12: rostered from CC's flagged finding (Cowork answer 20260611T193000Z).
- 2026-06-12: dispatched (CL-437, Fazal verbatim 'Dispatch now'); built by CC. All three
  direct writers routed through the shared redacting INSERT (write_redacted_step_row —
  exact row semantics preserved); error + decision_rationale + envelopes redacted at
  write WITH the tenant name registry (fail-SOFT posture documented vs the ops-API
  fail-closed; structured warning on registry outage); Detector-5 extended to
  error/decision_rationale/tool_calls; sentinel-guarded batched backfill script
  (dry-run default, counts only); inert-registry tripwire + 9-test realdb suite.
  Adversarial verify caught the decision_rationale registry gap (agent think_text was
  pattern-only) — fixed. Fresh-DB full suite 1938 green. PR-Ready → Cowork gate.
