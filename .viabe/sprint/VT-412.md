---
vt_id: VT-412
title: Ops/VTR console — COMPLETE + well-design all ~20 pages to VT-405 quality (audit-first; grouped RISK PRs)
status: Done
priority: High
sprint: "Sprint-Fazal"
type: Feature
area: [Frontend, Operator Surface, Knowledge Architecture]
assignee: Claude Code
related: [VT-405, VT-372, CL-390, CL-425, CL-426]
exec_order: 82
created: 2026-06-24
last_updated: 2026-06-24
---

## Contract (Cowork STARTUP 2026-06-24; ID reconciled from a 412 collision — see VT-413).

Bring the operator/VTR Ops Console (`apps/team-web/app/(app)/team/ops/`, ~20 routes) to COMPLETE +
well-designed, to the **VT-405 tenant-profile** quality bar. **Audit-first** (read-only inventory) → grouped
**RISK PRs → Cowork's gate (NOT self-merge** — operator surface over customer data).

**HARD PII projection (CL-390 / CL-425 / CL-426):** the ops/VTR console shows operators OTHER tenants'
customer data → it MUST be a REDACTED / de-identified projection (masked name, last-4 phone, no raw message
body); raw customer PII stays encrypted-from-VTR. The machinery that surfaces data must not leak a value.

## Audit (DONE 2026-06-24 — read-only, 20 routes, tsc clean)
- **broken_styling (5):** fleet, escalations, activity, monitoring, assignment — bare unstyled HTML
  (globals.css is 1 line). → PR-A.
- **PII: NO CRITICAL leak** — orchestrator redacts every envelope at the sink (key + pattern). 2 MEDIUM
  residuals: `decision_rationale` pattern-only redacted (→ PR-D tightens to key-aware); a gated/audited
  drafts-reveal (build-gate: verify orchestrator 403+audit wired).
- **Auth:** run/stream cluster (stream/history/runs/debug) = `requireFazal`-only → Fazal-only today; the
  cross-tenant IDOR is LATENT not live. 2 stubs (workflows/ingestion) ungated.
- **Gap:** no tenants index. → PR-C.

## Fazal rulings (2026-06-24)
1. workflows + ingestion stubs → **DELETE both** (vestigial, superseded by run-control). → PR-B.
2. behaviour → **KEEP, rename to "Decision Audit"** (it's a decision-audit panel, not agent training). → PR-B.
3. run-replay → **OPEN to VTR operators** = the risk hardening. → PR-D.

## Build grouping (each → Cowork gate, NOT self-merge)
- **PR-A** #494 `vt412-ops-styling-pass` — style the 5 broken pages + side-nav. (open, at gate)
- **PR-B** #495 `vt412-ops-cleanup` — delete 2 stubs + rename behaviour→Decision Audit. (open, at gate)
- **PR-D** #497 `vt412-runreplay-vtr` — RISK: requireFazal→requireOpsOperator + canAccessTenant + de-identified
  scoped read + key-aware decision_rationale redaction + IDOR self-tests. (open, at gate + independent adversarial verify) → risk-gate.
  Honest deferral: live STREAM page NOT opened (needs a Phase-2 SSE proxy — flagged to Cowork as a possible new VT row).
- **PR-C** #498 `vt412-tenants-index` — net-new scoped de-identified list (the audit gap). (open, at gate)
- **Build-gate items (orchestrator-side, not checkable from team-web):** (a) drafts-reveal 403+audit wired;
  (b) `require_vtr_action` fail-closed on every mutating endpoint.

## Ambiguous (parked, NOT built without Fazal): telegram (operator self-binds Telegram to ops bot — kept;
launch-scope is a product call). behaviour resolved → Decision Audit.

## Gate
RISK (operator surface, PII projection, auth/IDOR) — grouped PRs → Cowork's gate, Pillar-7 Fazal-authorized
merge. NOT self-merge.

## Status history
- 2026-06-24: ID reconciled (was a 412 collision; allocator-drift row → VT-413). Audit done; Fazal rulings
  collected; PR-A #494 + PR-B #495 opened to gate; PR-D building; PR-C queued.
