---
id: VT-580
title: "Shopify public-app compliance pack — GDPR webhooks + protected customer data + review submission + scope trim"
status: Rostered
priority: launch-blocker
type: BUILD
area: integrations
created: 2026-07-03
authorized_by: fazal
authorization_basis: "Fazal 2026-07-03 (terminal): public distribution chosen for the Viabe Team Shopify app; real-merchant installs require review."
---

# VT-580 — the public Shopify app becomes installable by real merchants

## Why launch-blocking
The Viabe Team Shopify app is PUBLIC distribution (Fazal 2026-07-03 — one app identity for life; dev stores install un-reviewed, REAL merchant stores are blocked until review passes). Any real concierge tenant with a Shopify store depends on this row.

## Scope
1. Mandatory GDPR webhooks: `customers/data_request`, `customers/redact`, `shop/redact` endpoints on the orchestrator (HMAC-verified), mapped onto the EXISTING DSR machinery (dsr_purge + the data-request export path); registered in the Partner app config.
2. Protected Customer Data approval: Partner-dashboard request (data-use declarations) — CC prepares the answers from the privacy posture (CL-390/425, DSR, RLS); Fazal submits (Partner console is Fazal-only).
3. Scope trim: drop `write_customers`, `write_orders` from the authorize scopes (detection + attribution need the three read scopes only; sends go via OUR WhatsApp) — code const + app config.
4. App review submission checklist: listing basics (unlisted is fine), install/OAuth flow demo, webhook verification, privacy policy URL.
