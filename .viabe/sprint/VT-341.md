---
vt_id: VT-341
title: Wire team-web DSR + report-download proxy routes (dead links — PORTAL GO-LIVE GATE)
status: Backlog
priority: Critical
sprint: Sprint 8 - Owner Surface & Billing
type: Bug
area: [Owner Surface, Frontend, Privacy]
assignee: unassigned
parent: VT-87
sub_items: []
exec_order: 7.2
blocked_by: []
tags: [owner-surface, privacy, launch-gate]
---

# VT-341 — dead DSR/download links: wire the team-web proxy routes (HARD go-live gate)

VT-338 (#354) shipped the settings page (links `/api/dsr/export` + `/api/dsr/delete`) and the
reports page (links `/api/team/reports/{ym}/download`). **None of those team-web proxy routes
exist → 404.** An owner clicking "Delete my data" → 404 is a DPDP/trust failure.

Confirmed missing: `apps/team-web/app/api/dsr/export`, `/api/dsr/delete`, `/api/team/reports/...`.
The orchestrator DSR logic EXISTS (`api/dsr.py`); the gap is only the team-web proxy wiring
(same shape as the owner-read proxies in VT-87/VT-338 — requireOwnerSession → derive tenant
server-side → forward X-Internal-Secret → return). The reports download = a tenant-scoped
short-TTL signed Storage URL (VT-9.7; the object path derived from the SESSION tenant, never a
client field — the IDOR discipline).

## HARD GATE
**BLOCKS portal-go-live.** Not live today (the portal is auth-gated + not surfaced to real
owners), so NOT a merge-blocker — but the portal CANNOT be shown to a real owner with these
links dead. Add to the launch-gate list (VT-89.md gate set).

## Origin
Cowork VT-338 #354 subagent (20260606T003500Z), roster #1.
