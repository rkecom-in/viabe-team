---
vt_id: VT-231
title: Provision viabe-team-prod Supabase project in ap-south-1 (Mumbai) — launch-blocking
status: Plan-Review
priority: Critical
sprint: Sprint 8 - Owner Surface & Billing
type: Infrastructure
area: [Infrastructure, Privacy, Legal/Policy, Database]
assignee: Fazal
parent: VT-2
sub_items: []
exec_order: 9
branch: ""
version: "v1.0"
created: 2026-05-29
last_updated: 2026-05-29T17:10:00+05:30
follow_up_to: VT-169
depends_on: []
authorization_basis: "CL-422 (2026-05-29 Fazal-issued, STANDING with launch-gate sunset)"
---

# VT-231 — Provision viabe-team-prod (Mumbai) — launch-blocker

## Why

CL-422 locks dev acceptance in Seoul (free-tier constraint) WITH a hard constraint: NO real customer data on dev. Prod substrate must exist in ap-south-1 (Mumbai) before any beta-partner data ingestion AND before Reports-Jun15 public launch.

## Brief contract — Fazal-side action (no CC work)

1. **Provision** a new Supabase project named `viabe-team-prod` on a paid tier that allows region selection. **Region: ap-south-1 (Mumbai).** Backup region preference: ap-south-2 (Hyderabad).
2. **Run all migrations** in order against the new project: every file under `migrations/*.sql` from `001_*.sql` through `042_owner_feedback_surfaces.sql` (current highest as of VT-198 ship).
3. **Generate new secrets** for prod: rotate from dev:
   - `DATABASE_URL` (Supabase-provided)
   - `SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `SUPABASE_SECRET_KEY` (sb_secret_*)
   - `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (sb_publishable_*)
   - `TEAM_PHONE_ENCRYPTION_KEY` (NEW Fernet key — DO NOT reuse dev's)
   - `OPERATOR_JWT_SECRET` (NEW — `openssl rand -hex 32`)
   - `TEAM_ADMIN_API_TOKEN` (NEW — `openssl rand -hex 32`)
   - `INTERNAL_API_SECRET` (NEW)
   - `TEAM_PHONE_HASH_SALT` (NEW)
   - `FAZAL_OWNER_UUID` (re-derived from Supabase Auth on prod project)
   - `FAZAL_TENANT_ID` (re-derived after creating RKeCom tenant on prod)
4. **Save to** `.viabe/secrets/supabase-prod.env` (gitignored).
5. **Provision Railway + Vercel** prod environments wired to these secrets.
6. **Run VT-169 canary** against `supabase-prod.env` — assertion is `aws_region_for_ip == "ap-south-1"`.
7. **Update** `decisions-ledger.md` with "VT-231 done YYYY-MM-DD; prod residency verified ap-south-1."

## Acceptance

- Supabase prod project exists in ap-south-1
- All migrations applied + canary green
- Secrets file at `.viabe/secrets/supabase-prod.env`
- Railway prod env wired
- Vercel prod env wired
- VT-169 canary against prod returns PASS with `aws_region_for_ip="ap-south-1"`
- CL entry filed with verification date

## Out of scope

- Customer data migration from dev to prod (none should exist; dev is synthetic-only per CL-422)
- DPDPA legal review (VT-115 covers separately at launch)
- Multi-region replication (post-launch concern)

## Dependencies (Cowork-side, not blocking provisioning)

- Document the provisioning steps in `docs/clau/dev-env-runbook.md` (extend with prod section)
- File follow-up for Railway service-pair provisioning if needed
- Coordinate VT-115 legal review post-provisioning

## Discipline locks

- **No beta-partner data on dev** between now and VT-231 close (CL-422 hard constraint)
- VT-231 closes BEFORE first real customer onboard at launch
