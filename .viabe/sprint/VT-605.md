---
id: VT-605
title: "Loop P2 — ManagerPlan/PlanStep models + additive migration + executable plan store (CAS, SID idempotency, per-tenant queue, supersede-not-edit revisions)"
status: Todo
priority: launch-blocker
type: BUILD
area: manager
created: 2026-07-05
authorized_by: fazal
authorization_basis: "Fazal 2026-07-05 loop-program grant — execution plan §2 + Package 2."
---
# VT-605 — the plan spine becomes executable (expert Package 2)
Strict ManagerPlan/PlanStep pydantic models (1..8 sequential steps, fail-closed validation,
redact-before-persist). SpecialistHandoff gains task_id/step_id/plan_revision/attempt/
acceptance_criteria/policy_ref. Migration (NUMBER VIA scripts/migration_id_allocate.py — never
hand-picked): manager_tasks + queued status, plan_revision, terminal_outcome, owner_notification_status;
manager_task_steps + plan_revision, specialist, superseded, advisory_tool kind, pipeline_step
evidence kind; RLS/purge/migration-tests updated same change. Typed store APIs: create_plan /
load_plan / revise_plan / claim_next_step / complete_step; task created BEFORE first dispatch,
plan persisted atomically; source message SID = idempotency key; ONE active objective-bearing task
per tenant, later objectives queued; revisions supersede + append, never edit history; every
plan/revision/decision/terminal → tm_audit (structured, never chain-of-thought). Replaces the
observe-only task_producer (compatibility adapter allowed, no duplicate tasks). Exec order 3.
