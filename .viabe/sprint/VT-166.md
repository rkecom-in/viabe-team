---
vt_id: VT-166
title: Agent-loop daemon — Python Agent SDK orchestrator (replaces bash watcher)
status: Done
priority: Critical
sprint: Hardening
type: Infrastructure
area: [Infrastructure]
assignee: Pipeline Engineer
parent: VT-14
sub_items: []
branch: ""
version: ""
notion_legacy_id: 36a387c2-cc5a-8135-bc6c-e95f359c4e53
created: 2026-05-24
last_updated: 2026-05-25T03:45:00+05:30
---

## VT-166 — DONE (2026-05-24 23:23 IST)
PR #54 merged at 2026-05-24T17:52:22Z, squash sha `3921e4f6b20b9f9fcced7957e092a938fd8d73c5`.
**Files on main** (2155 insertions): `.viabe/daemon/` ([agent-loop.py](http://agent-loop.py), [core.py](http://core.py), [hooks.py](http://hooks.py), [install-launchd.sh](http://install-launchd.sh), [com.viabe.team](http://com.viabe.team).agent-loop.plist, [README.md](http://README.md), tests/) + `.gitignore` updates.
**Merge friction worth recording** (not for the row, but flagged in Clau_Session_Log as CL-411 candidate): The merge took 3 retries because of GitHub's `pull_request:` event filter ignoring `edited`. Title-only edits don't re-trigger CI, so the stale `pr-title` check stayed red after the VT-AGENTSDK-LOOP → VT-166 rename. Also discovered the repo uses Rulesets (not classic branch protection), which means `--admin` doesn't bypass required checks. Final path: empty commit → fresh CI → standard merge. Follow-up VT row needed: add `types: [opened, synchronize, reopened, edited]` to ci.yml's `pull_request:` trigger so title fixes self-heal.
**Next step (Fazal-owned):** `bash /Users/fazalkhan/development/viabe-team/.viabe/daemon/install-launchd.sh` to install the launchd LaunchAgent. After install, the daemon replaces the bash watcher (and the Cowork-side poller's role narrows to surfacing-only since the daemon does its own dispatching).

**Expected Outcome:** Python daemon `.viabe/daemon/agent-loop.py` that embeds `claude_agent_sdk.query()`, maintains ONE persistent Claude Code session across all signals via session_id resume, enforces Pillar 7 via PreToolUse hook (gh pr merge denied unless type:task with authorized_by:fazal), polls gh pr view for merge-detection on in-pr tasks, runs as launchd LaunchAgent for survival across reboots, dispatches macOS notifications + Telegram pushes on priority:high notify signals (host-side since Cowork sandbox blocks network/osascript). 27 pytest pass; ruff clean.

**Notes:** Built 2026-05-24 by Claude Code under the watch-loop architecture. Plan reviewed by Cowork at 16:37 UTC; 4 conditions addressed inline (SDK-error backoff, notify short-circuit, USD↔tokens budget gate, _active_signal_context try/finally). PR #54 in flight on branch feat/vt-agentsdk-loop-daemon. Two commits: dc09638 (base daemon) + d8e8ca3 (macOS+Telegram dispatch). Retroactive Notion row created 2026-05-24 to fix the text-suffix-VT-ID violation; queue dir .viabe/queue/VT-AGENTSDK-LOOP/ should be renamed to .viabe/queue/VT-\<NewID\>/ after merge. Once merged, install via `bash .viabe/daemon/install-launchd.sh` retires the Phase-1 bash watcher.

## Status history
- 2026-05-25 03:45 IST: migrated from Notion (notion_legacy_id: 36a387c2-cc5a-8135-bc6c-e95f359c4e53)
