---
id: VT-493
title: "SR prompt schema conformance — stale hardcoded campaign_window + un-enumerated source_kind (LLM echoes invalid plan)"
status: Todo
priority: Critical
type: BUILD
area: team-orchestrator
created: 2026-06-29
authorization_basis: "Win-back re-drive on bogus fixture: SR grounds a real plan but it fails CampaignPlanProposed pydantic → no plan emitted. Launch-blocker for the win-back sign-off."
---
# VT-493 — SR prompt schema conformance (LAUNCH-BLOCKER)
SR (Opus) saw the VT-490 cohort + composed a grounded proposed plan, but parse_campaign_plan (campaign_plan.py:399) raised 3 ValidationErrors → no plan emitted → self_evaluate never reached. (A1) sales_recovery_v1.md:100-101 HARDCODES campaign_window start=2026-05-22/end=2026-05-29 (5wk stale) → LLM echoes verbatim → CampaignWindow validator (start>=now, campaign_plan.py:132-158) rejects. Fix: inject the CURRENT server date into the prompt + instruct start>=today (a hardcoded future date re-stales). (A2) EvidenceSourceKind allows only tool_call|l4_skill_corpus|l2_episodic_memory (campaign_plan.py:91) but the prompt never enumerates them → LLM invents off-enum source_kind. Fix: enumerate the allowed values in the prompt. Optionally feed the pydantic error on the retry_with_backoff re-prompt. Money-path prompt — Cowork audit-on-diff.
