---
id: VT-586
title: "Record ALL onboarding sends to conversation_log — complete the VT-584 substrate fix (walker + shopify resume gate)"
status: Done
priority: launch-blocker
type: BUILD
area: manager
created: 2026-07-03
authorized_by: fazal
authorization_basis: "Autonomous capability program (CL-2026-07-03-autonomous-capability-program); harness re-run finding."
---

# VT-586 — the deterministic send paths join the lifetime log

## Finding (post-VT-584 harness re-run against deployed dev e722a45)
VT-584 fixed the paced-flow BEATS. But the harness still read most flow scenarios "silent". Root: TWO more send helpers reached the owner's phone WITHOUT recording to conversation_log —
- journey._send (deterministic walker/opener/turn-brain-fallback), and
- shopify_onboarding._send (the ENTIRE resume gate: shop-domain retry, auth waiting line, not-connected re-prompt, connected confirm).
Both called send_freeform_message with NO tenant_id. Effect: the Team-Manager's 24h window lost the whole integration hand-off + the walker turns, AND the harness read every such reply as false silence.

## Fix
Thread tenant_id + surface='journey' through both _send helpers into the send choke (send_freeform_message / send_interactive_message already record via _record_owner_conversation_turn→record_turn when given a tenant_id, WITH the real message sid). 10 shopify caller sites + 4 journey caller sites updated. Regression tests assert both helpers thread tenant_id into the choke. 353 onboarding tests green, ruff clean.

## Note
This fixes the CAPTURE/memory substrate. The re-run will now show the TRUE remaining failures (canned re-prompts, store-URL re-ask) as content-assertion FAILs instead of "silent" — the real P1 intelligence work (brain owns off-script turns).
