---
cl_id: CL-62
entry_type: Decision
topic: Process
status: Superseded
session_date: 2026-05-17
created: 2026-05-17T14:50:58.366Z
source: Claude Code 2026-05-17 PR #6 delivery; Fazal merged via gh CLI
linked_tasks: VT-3.1 done; unblocks VT-3.2 (SubscriberState), VT-3.3 (Twilio webhook), VT-3.4 (langgraph_supervisor wiring); local DB tech debt logged separately
notion_legacy_id: 363387c2-cc5a-8144-9fb2-e593133f8f89
title: "VT-3.1 shipped: PR #6 merged. DBOS-wrapped LangGraph substrate live on main with 7 required CI checks (added orchestrator job)."
---

# CL-62 — VT-3.1 shipped: PR #6 merged. DBOS-wrapped LangGraph substrate live

PR #6 merged 2026-05-17. main now contains the DBOS+LangGraph substrate per VT-3.1 Notion body (no scope creep).

Delivered (skeleton only — zero LLM, zero reasoning, zero tools per Pillar 1):
- Dependencies pinned in apps/team-orchestrator/pyproject.toml + uv.lock: dbos 2.22.0, langgraph 1.2.0, langgraph-supervisor 0.0.31. Added langgraph-checkpoint-postgres + psycopg-pool for PostgresSaver.
- src/dbos_config.py: launch_dbos() function (never on import), 6-min workflow timeout.
- src/orchestrator/graph.py: OrchestratorState TypedDict (tenant_id, run_id, history), single placeholder_node, PostgresSaver checkpointer with thread_id == run_id, checkpoint-table RLS keyed thread_id → pipeline_runs (Pillar 3 inline).
- src/orchestrator/runner.py: @DBOS.workflow pipeline_run decorator, 3 @DBOS.step checkpoints, run_id idempotency via DBOS native semantics.
- tests/orchestrator/test_skeleton.py: 5/5 passed in CI — end-to-end, idempotency, cross-tenant checkpoint RLS, DBOS auto-resume after SIGKILL, langgraph_supervisor import (verifies dep pinned correctly).
- CI: new orchestrator job (pgvector/pgvector:pg16 + uv sync). Added to ruleset; 7 required checks now.

Scope held to Notion VT-3.1 boundary (after Clau's 3 brief overreaches were caught by Claude Code):
- NO SubscriberState/Phase enum → VT-3.2
- NO supervisor.py / specialists/registry.py / research_agent stub → VT-3.4
- NO anthropic dep / FastAPI / health endpoints / /api/team/health/deep update → VT-3.9 + VT-3.3 + VT-122

Flags surfaced during execution:

1. Context7 docs for DBOS were wrong on DBOS.SetWorkflowID and DBOS.SetWorkflowTimeout class attributes — they are module-level imports (from dbos import ...). Claude Code hit this in CI iteration, fixed, re-pushed, green. Discipline rule going forward: Context7 lags occasionally; when implementation diverges from Context7, live PyPI + GitHub source wins.

2. New env var introduced: TEAM_SUPABASE_DB_URL (Postgres DSN, format postgresql://user:pass@host:port/db) — distinct from TEAM_SUPABASE_URL (REST API URL, unusable as DSN). DBOS needs the DSN. Fazal needs to populate this in apps/team-orchestrator/.env.local from Supabase Dashboard → Project Settings → Database → Connection string → URI (Direct connection, not pooled).

3. Local DB verification impossible on Fazal's machine — no Docker, brew postgresql@14 broken (won't bind). CI orchestrator job is verification of record. All five smoke tests (including the load-bearing DBOS auto-resume after SIGKILL) passed against clean pgvector/pgvector:pg16 image in CI.

## Status history
- 2026-05-17T14:50:58.366Z: created (status=Superseded)
- 2026-05-25 03:55 IST: migrated from Notion (notion_legacy_id: 363387c2-cc5a-8144-9fb2-e593133f8f89)
