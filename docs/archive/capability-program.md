> **ARCHIVED 2026-07-17 — zero live authority; see docs/README.md.**

# The Capability Program — Team Manager to Claude-Code grade
**Authorized:** CL-2026-07-03-autonomous-capability-program (Fazal, autonomous mode, do-or-die).
**Owner:** Claude Code, autonomous. **Audit:** Cowork (audit-after). **This file = the resumable approach doc** — any future CC session or Cowork reads this first.

## The bar (Fazal's words, operationalized)
The Team Manager must handle business conversation the way Claude Code handles engineering:
1. **Context retention** — NEVER asks for what the conversation already contains (canonical failure: the owner sent his store link 3× on 2026-07-03; each seam read a different context substrate or none).
2. **Intent understanding** — natural phrasing everywhere in-session; keyword floors only for compliance (STOP/opt-out/DSR) and as fast paths.
3. **Honesty** — never claims an untaken action; never invents facts; substance-railed disclosures.
4. **Business judgment** — decides next steps, guides specialists, composes data-grounded month plans (never hollow), validates/accumulates specialist plans (CL-plan-governance).
5. **Sub-agents = specialized mini-managers** — own tools, own memory, readiness self-checks (CL-agentic-fleet-mandate).
6. **Every inbound gets a response.** Silent drops are architecture failures, not bugs.

## Root-cause thesis (why it kept failing)
Conversation ownership is FRAGMENTED across deterministic seams (journey walker, paced-flow beats, shopify resume gate, prefilter direct handlers, dispatch brain) — each owns a slice of messages with its own slice of context. Amnesia, canned copy, and silent drops are all symptoms of that fragmentation. The end-state: **ONE conversational brain (the Team Manager) owns every in-session turn**, reading ONE context substrate (conversation_log + memory + flows-as-state), with deterministic rails as gates/tools around it — flows inform the brain; they never compete with it for the microphone.

## Phases
- **P0 (in flight)** — land VT-582 (server harness: injected inbound → deployed dev brain → captured outbound; dev-only secret, mocked sends) + VT-583 wave 1 (top keyword surfaces → floor+converse; ALL silent drops fixed; context reads unified onto conversation_log). Evidence: suites + harness starter scenarios.
- **P1 — conversation unification (wave 2)** — the single-brain turn owner: journey/flow/integration state become CONTEXT for the manager brain; the walker/beat token paths demote to fail-soft floors; remaining canned copy → substance-railed composition (sweep inventory 2026-07-03 = the checklist, in keyword-surface-sweep's report, relayed to Cowork).
- **P2 — delegation + plan governance** — VT-578 (specialists propose; manager validates/accumulates/manages via the B2 spine), VT-573 (Sales Recovery tool belt + lane memory + readiness self-check), VT-572 (manager memory self-write + compaction).
- **P3 — exhaustive server-side verification** — harness scenario permutations: onboarding happy/hostile/topic-switching, consent natural-language, integration flows incl. re-sent links + mind-changes, status/plan/edit requests, stop/resume, multi-field answers, repeats. Hard asserts: no silent drop, no re-ask of in-context facts, substance rails, floors intact. Plus an opus LLM-judge rubric per transcript: context-retention / intent / honesty / helpfulness / progression, threshold ≥4/5 each. Regression: the whole pack runs before any future conversational change ships.
- **P4 — the ping** — Fazal gets transcripts + rubric scores + the change ledger, only when P3 passes.

## Standing constraints (unchanged by autonomy)
Effect gates deterministic forever (sends/allowlist, consent recording, money, DSR); main = Fazal-only; allowlist-only real sends; no real sends from harness (bogus non-allowlisted tenants; send-guard asserts); prod untouched; every wave: tests + smoke + lint + pre-push green, deploy verified, Cowork signaled.

## Model policy (this program — Fazal cost directive 2026-07-03: never a higher model where a lower one suffices)
- PRODUCT brains (deployed): as designed — sonnet conversational hot path, haiku classifiers/distillers/extractors, opus only for consequential judgment (entity adjudication, plan validation).
- TEST EXECUTION: the harness itself is CODE (zero LLM); scenario runs cost only the deployed product tiers. Mechanical subagent work (scans, suite runs, file inventories, verify-reruns) = HAIKU. Simple/obvious code changes = SONNET.
- HIGH-REASONING spend is reserved for: architecture/design builds (opus), adversarial review of consequential diffs (opus), and the transcript JUDGE (opus — but transcripts are BATCHED per judge call to amortize; hard asserts run first in code so the judge only sees survivors).

## Log (append per wave)
- 2026-07-03: Program started. P0 builders in flight. Ledger entries: fluid-consent, conversing-surfaces+harness, conversation-memory, plan-governance, populate-first, paced-needs-driven, fleet-mandate, autonomous-program. Sweep inventory delivered (14 keyword surfaces / ~30 canned / 5 silent-drop paths).
- 2026-07-03 (P0/P3 first harness pass — two real defects from the haiku runner, re-classified):
  - The runner's "all flows silent" headline was HALF-WRONG (flow replies reached Fazal's phone all day). Real cause = harness capture blindspot: paced-flow beats send via `_send_turn`→`send_freeform_message` with NO tenant_id and never call `_append_recent_turns`, so their turns never hit conversation_log (which the harness reads). NOT a real drop — but a REAL memory gap: readiness/integration/defer/all-set/pickup replies were absent from the Team-Manager 24h window (re-fragmenting the substrate VT-579 unified). **VT-584** — `_send_turn` gains `tenant_id`; the 5 flow beats pass it → assistant line records to conversation_log (surface='journey'), fail-soft; turn-brain path unchanged (records via `_append_recent_turns`, no double-log). Regression test proves a defer-beat reply is retrievable. 22 paced-flow tests green.
  - The runner BURIED the worst finding: scenario topic_switch step 1 "Yes connect my data" fired `team_dsr_acknowledgment` — the delete-my-data compliance handler — because dsr_keywords.yaml matched bare "my data". A real owner connecting a store would be told their DELETION request is acknowledged. **VT-585** (opus builder, in flight) — DSR must match deletion INTENT not the substring; exhaustive must-fire/must-not-fire tests both directions.
  - Runner-quality lesson: a low-cost test EXECUTOR is fine, but its verdict TAXONOMY (silent vs timeout vs capture-gap) and severity ranking need a reasoning pass — a haiku runner mislabeled a capture artifact as the headline and under-ranked a compliance misfire. P3 keeps the opus judge as the ranking authority; the runner only collects.
- 2026-07-03 (full 8-scenario harness re-run on deployed dev e722a45 — CC drove it as zero-LLM code, not a subagent):
  - VALIDATED on deployed dev: VT-585 (topic_switch step1 "Yes connect my data" → normal integration offer, NOT a DSR ack — PASS); VT-584 (flow-beat replies now recorded, surface='journey'); manager brain healthy (honesty_probe 2/2); natural stop honored (stop_intent XPASS, mocked MKDEV sid); ZERO real sends across the whole run (send-guard clean, no SM/MM sids).
  - THE core disease, now precisely reproduced: **deterministic seams SILENTLY DROP any off-script inbound instead of falling through to the (healthy) manager brain.** Two layers found:
    1. CAPTURE/memory substrate (→ VT-586, landed 485d1b9): journey._send (walker) + shopify_onboarding._send (the whole resume gate) reached the phone but never recorded to conversation_log — so the 24h window lost the turns AND the harness read them as silence. Fixed by threading tenant_id through both _send choke calls. (VT-584 had fixed only the paced-flow beats.)
    2. INTELLIGENCE (P1, next): even with capture fixed, the replies are WRONG — the Shopify resume gate answers an off-script question ("what do you charge?") with a canned "that's not a store address / reply 'done'" re-prompt, and _flow_offer_next_integration re-asks for a store URL the owner gave IN THE SAME MESSAGE (context_retention step1 — the canonical 3×-re-ask, reproduced). The fix is the program's end-state: off-script inbounds route to the manager brain (flows become context, not the microphone owner); the brain answers, then re-offers.
  - Post-VT-586, the harness measures TRUTHFULLY: the remaining flow failures will read as content-assertion FAILs (canned re-prompt / re-ask), not "silent" — those are the P1 targets.
- 2026-07-03 (P1 intelligence — VT-587 + VT-588, pushed c6c3aee):
  - VT-587 (P1a): the offer beat reads the store URL from the CURRENT inbound before the fragile conversation_log lookback (the URL turn IS logged but RLS/txn-invisible within the same webhook run — proven by a deployed-dev diagnostic on tenant d25cb1a1). Kills the canonical 3x-store-link re-ask at the source.
  - VT-588 (P1b): the program end-state, first slice — off-script inbounds route to the manager brain instead of a canned reprompt. (1) discovery resume gate scans the whole body for a domain + FALLS a question/proceed/chat THROUGH to the brain (return None); malformed domain attempts still get the specific reprompt (TLD-gated, so Hinglish "haan.theek hai" is NOT a false attempt). (2) _build_onboarding_state_block injected into dispatch_brain so it answers off-script AND guides the owner back, never fabricating. opus adversarial review: 1 moderate finding fixed (dotted-bigram false-positive), 5 clean.
  - Architecture note: this establishes the pattern for the rest of P1 — a seam handles its on-script token deterministically and FALLS THROUGH for everything else; the brain (flow-aware via the onboarding-state block) owns the off-script turn. The AUTH-phase resume gate + the --journey walker are the remaining seams to convert the same way (follow-ups). The --journey early-turn drops still need a no-teardown diagnostic to confirm real-vs-staging before touching them.
  - NEXT: harness re-run on deployed dev to validate the brain answers off-script well (read the actual replies — the scenario asserts are weak; P3 strengthens them + adds the opus judge).
- 2026-07-03 (P1 harness re-run on deployed dev c6c3aee — READ the actual replies, not just PASS/FAIL):
  - WINS (validated on dev): onboarding_happy 2→4 PASS + repeat_question_guard 0→3 PASS (VT-586 flipped the --journey "drops" — they were walker-_send capture gaps, not real drops); context_retention 0→2 PASS — **VT-587 killed the canonical re-ask**: "Yes lets connect. My store is probe-store-a.myshopify.com" → the reply MINTS the connect link with that address ("Got your store address from earlier — probe-store-a.myshopify.com. Tap this secure link…"), no re-ask. topic_switch off-script routes to [manager] (VT-588 fall-through works). No real sends anywhere (send-guard clean).
  - THE REMAINING BAR GAP (not a harness FAIL — the weak asserts + the D1 fallback MASK it): the manager brain, handed an off-script conversational question ("what do you charge?", "ok back to it lets continue"), COMPLETES WITH NO OWNER REPLY → runner.py:164 `_send_completed_no_reply_fallback` fires the generic "Got it — I'm on it and I'll update you shortly." So VT-588 ROUTES correctly (question → brain) but the brain does not ANSWER + re-nudge; the onboarding-state block isn't steering it. This is the core "answer like Claude" competence gap. `assert_no_silent` passes because the D1 fallback sends SOMETHING — P3 must add an assert that the reply ISN'T the D1 line for a question that deserves a real answer.
  - context_retention step2 "did you get my store address?" got the AUTH-phase honest waiting line (not the brain) — because step1 advanced to AUTH and VT-588 only converted the DISCOVERY gate. AUTH-phase off-script→brain is a follow-up (mirror VT-588 to the auth gate).
  - DIAGNOSIS IN FLIGHT: brain-noreply-trace (opus) tracing WHY dispatch_brain emits no owner reply for a plain conversational question + the smallest fix lever (system-prompt "always answer" vs compose fallback vs graph change). This is the next build (a new VT).
- 2026-07-04 (the brain-answers chain — VT-589 + VT-590 + VT-591; each layer found by READING deployed-dev replies, not the suite):
  - ROOT CAUSE (opus trace, file:line): the manager brain WRITES its answer as the final AIMessage.content, but nothing transmitted it. The manager holds NO send tool (tool_guardrail forbids it); compose_owner_output_tool only shapes canned text keyed by a routing intent (never the manager's words) + its output dead-ends; the no-spawn terminal node (routing.py) returned only {terminated_without_spawn} and discarded the AIMessage. So a handle-directly turn completed SILENT → runner.py D1 fallback fired "Got it — I'm on it" INSTEAD of the answer. VT-588 routed correctly but the owner still got no answer — this was the whole "brain doesn't answer" gap.
  - VT-589 (62df70d): dispatch_brain, on terminal_path=='terminal' & completed, reads the trailing AIMessage text + transmits via send_freeform_ack (records the assistant turn → D1 fallback auto-suppresses; specialist/collapse/escalated paths excluded → no double-send). No new manager tool = tool_guardrail invariant intact. 7 unit + 237 agent tests green.
  - VT-590 (three deploy passes 6d8862e→3709708→77c1083, each exposing the next layer in the dev replies): (1) prompt — the final message IS the owner reply sent verbatim; (2) strengthen — write the COMPLETE reply, never opener+dangling-colon; (3) REMOVED compose_owner_output_tool from the manager inventory (function stays for deterministic composers) so it can't defer the body + banned 3rd-person reasoning leaks. Pinned search_conversation_history (VT-579) in the tool-surface test (pre-existing drift). After VT-590, "what do you charge?" → complete + HONEST ("free during trial; I don't have exact plan pricing to hand — check viabe.in or I can flag it"), "amazon not shopify" → complete + honest, reasoning leak GONE.
  - REMAINING (→ VT-591, in flight): explainer questions ("how does it work?", "is my data safe?") STILL end at a dangling colon — the ReAct agent emits an intro (no tool call → loop ends) as its complete turn + stops; three prompt passes didn't fix the model behavior. VT-591 = in the VT-589 transmit seam, DETECT an incomplete trailing message (dangling colon / empty / trailing tool_call) and only then run ONE focused no-tools compose-completion LLM call (same hot-path tier) that emits the complete reply; good replies skip the extra call; fail-soft to raw text then D1. Validate on the 6-probe set on deployed dev.
  - Method note: the harness suite goes GREEN while replies are broken (weak asserts + D1 fallback sends SOMETHING). The only reliable signal was reading the actual dev replies per probe — P3 must add the assert "reply is not the D1 line for a real question" + the opus judge before the P4 ping.
- 2026-07-04 (BREAKTHROUGH — the "truncation" was a validation-harness display bug, and the manager is Claude-grade):
  - THE PHANTOM: from VT-591 through VT-593 I chased an "explainer colon-truncation" ("here's how it works:" then nothing) across SIX deploy cycles. It was NOT real. My probe piped the reply through `grep "→ owner"`, which prints only the FIRST line — cutting every MULTI-LINE reply at the first newline. A VT-593-DIAG log (draft/out/body first-70-chars) proved it: all three were `"...here's the short version:\n\nOnce you connect your Shopify..."` — a complete, multi-line reply. LESSON (binding for future sessions): when validating conversational replies, CAPTURE THE FULL MULTI-LINE TEXT (sed range / conversation_log full row), NEVER a single-line grep. A green harness AND a first-line grep both lie.
  - TRUE STATE (fixed full-reply probe, 8 diverse scenarios on deployed dev, sonnet-5): the Team Manager answers at Claude grade. Pricing → honest, no fabrication, re-nudge. How-it-works → structured connect→analyze→approve loop, honest read-only/approval rails. Data-safety → concrete 4-point. "Sounds like a scam" → "deserves scrutiny not blind trust" + 5 points + "disconnect anytime, you've paid nothing yet". "bhai ye kaise kaam karta hai" → full answer IN HINGLISH. "I sell on Amazon not Shopify" → honest ("not 100% sure of the Amazon flow, I'll check with the team"), offers options, asks the clarifying Q. Resume → context-aware ("since you mostly sell on Amazon"). This IS the bar.
  - WHAT ACTUALLY MOVED THE NEEDLE: VT-589 (transmit the brain's answer — the real D1-fallback fix), VT-590 (remove the dead compose_owner_output_tool + prompt: final message IS the reply), VT-592 (sonnet-5 — the replies above are on it, quality is high), VT-593 FAQ-never-escalate prompt (pricing no longer escalates to a human). REVERTED as redundant cost: the VT-591/593 ALWAYS-compose (a 2nd sonnet call per turn that just re-emitted the already-complete draft — Fazal's token-waste concern). Now: transmit the brain's raw reply as-is; compose ONLY in the rare empty-reply case. Removed the VT-593-DIAG logs (a commit-review PII-in-logs finding).
  - REMAINING before the P4 ping: (1) fold the FULL-reply capture into the committed harness/scenarios + add the "reply != D1 line" assert; (2) the opus transcript judge (rubric ≥4/5); (3) AUTH-phase off-script→brain (mirror VT-588) — still a follow-up; (4) the recent_owner_texts circular-import fail-soft in journey._recent_shop_domain (dev log noise, VT-587 has other fallbacks) — low-pri cleanup.
- 2026-07-04 (P2 delegation — first assessment on deployed dev 3cb8ffb):
  - Delegation WIRING works: a business-action inbound ("which of my customers have stopped buying? win them back") to an --onboarded tenant SPAWNS a specialist (dev logs: haiku classify → TWO opus calls = manager + specialist), which queried the ledger and honestly returned "You currently have 0 customers in your ledger." So the manager delegates + the reply is grounded/honest — NOT hollow, NOT fabricated.
  - GAP to validate delegation QUALITY (per Fazal's bar "guide the low-level agents… efficient execution plan… specialists = mini-managers"): the harness seeds NO business data, so every business ask hits the empty-ledger honest-gate. Need a fully-onboarded tenant WITH seeded lapsed customers + a connected connector + the onboarding-complete gate satisfied. seed-builder (sonnet-5 subagent) is adding `setup --seed-lapsed-customers N` to convo_harness. Then validate: does the manager pick the right specialist, hand it a good situation/outcome (not micromanage), does the specialist produce a GROUNDED win-back plan (real lapsed names, not hollow), does the manager validate/compose it.
  - Minor P1 polish noted: the empty-ledger reply ("You currently have 0 customers in your ledger.") is honest but TERSE — a warm manager would add "connect your store or add them and I'll spot who's gone quiet." Low-pri.
  - MODEL POLICY UPDATE (Fazal 2026-07-04): use sonnet-5 SUBAGENTS for the majority of work; reserve opus for only the most consequential review/reasoning. Kill/release done agents.
- 2026-07-04 (P2 close-out + P3 executed — the do-or-die session; local dev history 79933e9 → 9e786c0, all deployed+validated on dev):
  - **VT-594 landed** (collapse-path owner surfacing): the composer's AttributeError was observability-only; the REAL defect was that NO completed collapse terminal sent the owner anything (6 silent cases enumerated) — every one now gets an honest, PII-redacted, self-contained reply; the proposed path sends an in-chat plan summary → approval template → Pillar-7 pause. A 25-agent adversarial-review workflow caught 2 CRITICAL PII leaks (agent-authored free text to WhatsApp) + a double-send + false-promise copy BEFORE landing; a delta re-review caught cohort_label as a residual channel. Method lesson: the test suite was green through all of it — the review layer and the live pack are what caught everything.
  - **VT-595 landed**: `business_analysis` label (v4 classifier) — "which of my customers stopped buying?" routes to the brain (validated live both directions). **VT-596 landed**: SR dev slot → sonnet-5 (+3 prod pins ride to promotion) — and the upgrade shipped TWO latent breaks only the live pack caught: the 1024 per-turn cap truncated Sonnet-5's fuller plans (→ 4096 + bounded max_tokens CONTINUATION), and intermittent grounding drops (`proposed.evidence_refs: too_short` → ONE corrective retry naming the failing field paths). Winback then passed twice consecutively. **VT-597 landed**: AUTH-phase off-script → brain (classifier-'other' falls through; classifier-None keeps the honest floor).
  - **VT-598 executed** (the P3 gate): harness gained assert_not_d1 + --json-report + the dev-test consent-seed endpoint (fixes the seed salt-mismatch — TEAM_PHONE_HASH_SALT is sealed, so consent must be recorded server-side; endpoint is DEV-ONLY for ALL secrets, 404 off-dev); 11 new scenarios (19 total incl. the delegation suite); full pack run on deployed dev; **opus judge over all 19 transcripts** (rubric ≥4/5). Judge found what asserts couldn't: the status_query unknown-parse portal deflection + engineer jargon in the insufficient-data reply (**VT-600**), the pasted-URL scheme rejection (VT-600), the journey-queue re-ask (**VT-601**: 'about' cross-fill + skip-answered-fields advance), plus register/canned-copy polish items (below). **VT-599 landed** (found live): 23 lane tools took MODEL-authored tenant_id (crash on business-name + IDOR class) — now resolved from the run context, never the model.
  - **Env finding closed by use**: dev MARKETING_CONSENT_VERSIONS did NOT admit 'dev-test-v0' (the VT-396 convention never landed) — appended by-reference (CL-431 dev authority); cohort detection now returns real candidates.
  - **REMAINING (documented, not blocking the P2/P3 bar)**: (1) flow-beat canned copy answers Hinglish in formal English + step-1 hostile/skeptical asks get the value-prop nudge before the brain (register/copy polish — brain-composed per-language beats are the stated end-state); (2) onboarding_happy "what can you do" answer is thin; (3) Marketing/Accounting/Finance/Tech/CostOpt remain manager TOOL LANES, not autonomous mini-manager specialists (roster expansion = next phase; one SpecialistSpec each per VT-465); (4) VT-484 tool-error middleware should generalize to the six lane create_agent builds (vt599-builder flag); (5) queue_busy race-loser residual (VT-369, accepted); (6) transcript_judge CLI needs a key path for local runs (sealed on Railway; this session judged via opus subagents on the same rubric).
