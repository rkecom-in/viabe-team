---
vt_id: VT-416
title: LAUNCH-HARDENING EPIC — every single-tenant shim + dummy-data-as-real gap → real-tenant-correct (audit-first)
status: In Progress
priority: Critical
sprint: "Sprint-Fazal"
type: Epic
area: [Frontend, Backend, Security, Auth, Billing, Launch]
related: [VT-415, VT-412, VT-406, VT-408, VT-231, CL-431]
exec_order: 85
created: 2026-06-24
last_updated: 2026-06-24
---

## Contract (Cowork 2026-06-24; Fazal launch-blocker: "nothing pushed incorrectly... do it RIGHT"). AUDIT-FIRST → Cowork gate; ALL remediation RISK → gate, NOTHING self-merges.

Close to launch. Every single-tenant shim + dummy-data-as-real gap must be real-tenant-correct before the
Sundaram e2e. Discipline over speed.

## P0 — BILLING (flag loud, fix first)
`apps/team-web/app/api/team/razorpay/subscribe/route.ts:38` → `tenantId: process.env.FAZAL_TENANT_ID ?? ''`
= a real owner's subscription/payment attaches to FAZAL's tenant. **HARD launch blocker.** Fix = resolve the
tenant from the OWNER session (server-derived, never client — IDOR), RLS-scoped. Canary: two distinct owner
sessions → two distinct tenant subscriptions, never cross-attributed.

## STEP 1 — COMPLETE AUDIT (read-only) → categorized inventory → Cowork gate BEFORE any remediation
Sweep team-web + team-orchestrator for: FAZAL_TENANT_ID (7 source files seen + more); requireFazal as OWNER
auth (onboard/billing) vs OPERATOR (ops — VT-412 already converts, don't double-work); hardcoded
tenant/owner/customer IDs / env single-tenant reads / `.first()`/`LIMIT 1` single-tenant assumptions;
dummy/mock/stub/seeded/placeholder data RENDERED on a real surface (TRIAGE — not input hints, honest
social-proof, comments, fixtures); mock returns / TODO-tenant / hardcoded sample responses in prod paths.
Inventory: `file:line | category | launch-path | proposed fix | risk`. Categories: [P0 billing]
[owner-auth/session shim][ops-auth shim][dummy-data-as-real][single-tenant assumption][seed/dev-only].

## STEP 2 — REMEDIATE in priority order, each → Cowork gate + verified
P0 billing → owner onboarding (VT-415, fold in) → ops-auth (coordinate VT-412) → dummy-data-as-real →
single-tenant assumptions. Every fix: tenant from real session server-side, never client/hardcoded (IDOR);
RLS-scoped; real data only; verify 2-tenant ISOLATION (A's session never sees B's data) + render + canary.
Grouped PRs by category, not singletons. NOTHING self-merges.

## Gate
RISK (auth/billing/PII/multi-tenant). Audit → Cowork gate; remediation grouped PRs → Cowork gate; Pillar-7 merge.

## Status history
- 2026-06-24: Rostered (Cowork launch-hardening). Read-only audit dispatched (parallel). Child rows per category
  as the inventory firms up. VT-415 (owner onboarding) folds in.
