---
vt_id: VT-335
title: Edge-case PR-2 — adhoc_campaign (via approval gate) + template_error report
status: Backlog
priority: High
sprint: Sprint 8 - Owner Surface & Billing
type: Feature
area: [Owner Surface, Specialist Agent]
assignee: unassigned
parent: VT-9
sub_items: []
exec_order: 4.1
blocked_by: []
tags: [owner-surface, privacy]
---

# VT-335 — VT-84 PR-2: adhoc_campaign + template_error_followup

VT-84 PR-1 (#347) shipped the stage-2 router + classifier-v3 (all 4 intents) + exclusion +
status_query. `adhoc_campaign_request` + `template_error_followup` currently FALL THROUGH to
the agent; PR-2 wires their handlers into `edge_cases_router.route_edge_case`.

## Scope
1. **adhoc_campaign_request** — owner asks to run a campaign now. MUST route through the
   EXISTING approval gate (Cowork Q4 — a SEND must NEVER fire off a Haiku intent): queue an
   `owner_initiated` orchestrator dispatch → the agent builds a CampaignPlan → the standard
   weekly-approval flow (VT-83/VT-47 request_owner_approval) takes over → owner confirms →
   THEN it sends. `trigger_reason='owner_initiated'` (already in the enum). LOGS the request
   against the per-week budget (enforcement = VT-334, deferred).
2. **template_error_followup** — owner reports a bad message we sent. Insert a
   `template_error_reports` row (NEW migration via allocator — `tenant_id` + `owner_complaint`
   = PII → RLS + DSR `_PURGE_ORDER` + a hard-delete canary, the VT-323/325 lesson) with the
   most-recent outbound template_id (last 7 days). Alert Fazal (`_alert_fazal` / Telegram).

## Review focus (Cowork)
adhoc-through-approval (no Haiku-fired send) + template_error DSR hard-delete + PII handling.

## Origin
VT-84 split (Cowork 20260605T160000Z); PR-1 = #347 merged.
