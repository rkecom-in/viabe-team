---
vt_id: VT-430
title: Trial-notify dev-live enablement — flip approved_for_live + satisfy correctness params (correct render)
status: Queued
priority: High
sprint: "Sprint-Fazal"
type: Feature
area: [Backend, Owner-Comms, Send, Launch]
related: [VT-426, CL-422]
exec_order: 99
created: 2026-06-25
last_updated: 2026-06-25
---

## Contract (Cowork 2026-06-25 DEV-POSTURE-REVERSE §3; the dev recipient-allowlist was DROPPED per the HARD never-add-numbers RULE — Fazal owns recipient control by process). Risk(send) → Cowork gate. ENABLEMENT ON; CORRECTNESS KEPT + SATISFIED.

Reverse the dev posture for trial-notify: it SENDS on the daily cron — but ONLY to a Fazal-provided tenant
`whatsapp_number` (the never-add-numbers rule means no fabricated recipients ever exist; the e2e tenant + its number
come from Fazal). So flipping it on is safe: no trial-ending tenant with a real number exists until Fazal provides one.

## Scope
- **Flip `approved_for_live: true`** for `trial_ending` + `trial_subscribe_link` in `twilio_templates.yaml` (the VT-426
  fail-closed gate's enablement). The daily sweep then sends (per-feature gate ON).
- **SATISFY the correctness gates (don't skip):** ensure `validate_params` PASSES — supply `owner_name` +
  `trial_end_date` so the template RENDERS CORRECTLY (no sample-value "Hi Raj Cafe"). Keep `audience == 'owner'`. A
  CORRECT real send, not a skipped one, not a garbage one.
- KEEP all correctness/eligibility gates (validate_params, audience, idempotency). NO fabricated numbers (HARD RULE).
- Stacks on VT-426 (#513, the approved_for_live field) → branch from `vt426-trial-sweep-notify`.

## Gate
RISK (real owner send). Build → Cowork gate, NOT self-merge. The actual send only fires for a Fazal-provided onboarded tenant.

## Status history
- 2026-06-25: Rostered + ID allocated (Cowork DEV-POSTURE-REVERSE). Allowlist DROPPED (HARD never-add-numbers rule); this is the trial-notify enablement + param-correctness only.
