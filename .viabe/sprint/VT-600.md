---
id: VT-600
title: "VT-598 opus-judge findings — status_query unknown-parse falls through to the brain; insufficient-data reply is owner-register; pasted store URLs mint"
status: Done
priority: high
type: BUILD
area: manager
created: 2026-07-04
authorized_by: fazal
authorization_basis: "Autonomous capability program P3 — three quality gaps the VT-598 opus transcript judge surfaced that the hard asserts structurally could not."
---

# VT-600 — judge-found quality gaps (three fixes, one row)

1. **Portal deflection** (context_retention step 2): "did you get my store address?" classified
   status_query → keyword parse 'unknown' → canned "check your Viabe Team portal". Fix:
   `answer_status_query` returns None on unknown; the router falls through to the brain (VT-588
   seam pattern). Live re-proof @ 9e786c0: "Yes, got it — probe-store-a.myshopify.com — no need to
   resend it. 👍"
2. **Engineer jargon to the owner** (delegation_empty_cohort): the insufficient-data collapse reply
   passed agent-authored prose ("L4 skill-corpus", "expected_arrr", "target_cohort.customer_ids").
   Fix: one deterministic owner-register honest body; per-item detail stays in the VT-379-redacted
   observability rows. Live re-proof green.
3. **Pasted URL rejected** (silent_drop_probe): "https://sundaram-sweets.myshopify.com" earned the
   malformed-address reprompt (token scan left the scheme on; the strict bare-host validator
   refused; `_is_domain_attempt` then matched the scheme). Fix: the scan normalizes scheme +
   path/query per token; the OAuth-layer validator stays strict. 4-shape paste matrix in tests;
   live re-proof green (2 XPASS).

Commits: 8a3eeef (partial: gate tighten rode this sha), aea8462 (all three fixes on dev).
