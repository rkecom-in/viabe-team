---
id: VT-775
title: "viabe.ai/team 404s in production — the prod team app is live at its vercel.app URL but the branded funnel URL routes to the Reports project, so RKEcom cannot sign up through the real funnel"
status: Rostered 2026-08-20 by CC while opening P1.1. BLOCKS P1.1 and P0.5. The fix is a routing change on a LIVE customer-facing domain — Fazal-authorized, not CC's to take.
priority: Critical
milestone: M3
owner: Fazal (authorization) + CC (execution once authorized)
exec_order: 133
created: 2026-08-20
created_by: cc
authorization_basis: "Clau 0300Z post-promotion queue item 1: 'P1.1 — RKEcom tenant on prod ... signal me what HIS taps are the moment your lane is ready.' This is what my lane found."
---

# VT-775 — the funnel exists, and the front door does not open onto it

## Measured, not assumed

| URL | status |
|---|---|
| `https://viabe-team.vercel.app/team/signup` | **200** — the prod team app is LIVE and serving signup |
| `https://viabe-team.vercel.app/team` | **200** |
| `https://viabe.ai/team/signup` | **404** |
| `https://viabe.ai/team` | **404** |
| `https://viabe.ai` · `https://viabe.ai/reports` | 200 (served by the `viabe-web` project) |

Vercel holds four projects under `rkecom-projects`: `viabe-web` (serves `viabe.ai`), `viabe-team-web`
(prod team app, updated 8h ago), plus the two dev projects. **`viabe.ai` is owned by `viabe-web`, so
`/team/*` never reaches `viabe-team-web`.**

**Correction to CLAUDE.md:** it records the prod Vercel project `viabe-team-web` as *"not yet created
— creation + deploy wiring = VT-231 cutover scope."* It exists and it is deployed. VT-231 itself is
the **Supabase Mumbai** row, not the web one; there was no row for this at all, which is why the gap
survived to be found at P1.1.

## Why this blocks P1.1, and why the vercel.app URL is not a workaround

P0.5 requires RKEcom to onboard **through the real funnel, not seeded**, and
**CL-2026-08-20 makes RKEcom a pure tenant** — treated exactly as a stranger's business. A stranger
does not sign up at `viabe-team-web.vercel.app`. Onboarding through the raw Vercel URL would produce a
tenant row and prove nothing about the funnel a real owner walks, which is the same class of mistake
as the VT-725 canary that passed on a path no live turn takes.

So the vercel.app URL is available and is **deliberately not** being used to unblock this.

## Fazal's tap (the whole row)

Route `viabe.ai/team/*` to the `viabe-team-web` project. Two shapes, his call:
1. **Rewrite on `viabe-web`** — `/team/:path*` → `viabe-team-web`. Keeps one apex domain; the Reports
   site keeps serving everything else. Touches a live customer-facing project's config.
2. **Domain/path assignment on `viabe-team-web`** in the Vercel dashboard.

**Why this is not CC's to take despite CC having Vercel access:** `viabe.ai` is a live customer-facing
domain serving Viabe Reports. A routing change there can take the Reports site down, and that is
prod-impacting and outward-facing in the CL-431 sense. CC will execute it the moment Fazal says which
shape and authorizes it — **the config change itself is small; the authority is the gate.**

## After the tap, CC's lane resumes immediately
RKEcom signs up at `viabe.ai/team/signup` as prod's FIRST tenant (prod is empty today: 0 tenants, 0
journeys, 0 WABA accounts, 0 customers) → journey → **GST verify** → **ownership flip via VTR** →
**allowlist widened to RKEcom's consented leads**. Those are the owner taps Clau asked me to
enumerate, and they are listed in the signal so Fazal does them in one sitting.

## Exit gate
(a) `https://viabe.ai/team/signup` returns 200 and serves the prod team app. (b) `https://viabe.ai`
and `/reports` still return 200 — the Reports site is not collateral. (c) A signup completed at the
branded URL creates a real prod tenant row, verified in the prod DB.


## 2026-08-20 — URL CORRECTION (Fazal supplied the canonical dev domain; I had the prod one wrong too)

Fazal: *"The correct dev domain is viabe-team-dev.vercel.app."* Re-measured both environments against
the canonical hostnames, and one of my earlier readings was simply wrong:

| URL | status | note |
|---|---|---|
| `viabe-team-dev.vercel.app/team/signup` | **200** | canonical DEV signup — renders a real form (consent + DPDP + WhatsApp copy) |
| `viabe-team.vercel.app/team/signup` | **200** | canonical PROD signup — renders the same form |
| `viabe-team-web.vercel.app/team/signup` | **404** | **I reported this as 200 earlier. It is 404, confirmed on two retries.** The `-web` hostname is not the serving alias. |
| `viabe.ai/team` · `viabe.ai/team/signup` | **404** | unchanged — the row's actual finding |

**The row's finding is unchanged and now rests on correct URLs:** both apps are live and serving
signup on their Vercel aliases; **the branded funnel `viabe.ai/team` still 404s** because `viabe.ai`
is owned by the `viabe-web` (Reports) project. Fazal's tap is the same routing change.

**What I got wrong and why it matters:** I quoted a hostname I had guessed from the project *name*
(`viabe-team-web`) rather than from the serving alias, and one probe returned 200 for it once. A
hostname that answers once and 404s twice is not a fact, and I published it as one. The measurements
above were each repeated before being written down.
