---
id: VT-594
title: "Delegated business work does not surface in the conversation — SR spawn → compose_owner_output raises → owner gets the D1 'I'm on it' fallback"
status: Todo
priority: launch-blocker
type: BUILD
area: manager
created: 2026-07-04
authorized_by: fazal
authorization_basis: "Autonomous capability program — P2 delegation. Fazal bar: 'guide the low-level agents… create efficient execution plan'; the plan must reach the owner conversationally."
---

# VT-594 — the manager delegates, but the result never reaches the owner

## Finding (deployed-dev 3cb8ffb, seeded onboarded tenant, 8 customers / 6 lapsed)
"make me a plan to win back my lapsed customers this month" and "set up the win-back campaign" both:
- Manager (opus) SPAWNS sales_recovery correctly (log: spawn_sales_recovery validate_python).
- SR specialist runs (claude-sonnet-4-6) and produces a result (tagged-union validated).
- Then: `dispatch_brain: compose_owner_output raised; emitting empty envelope` (dispatch.py:1127) → no owner-facing message recorded → runner D1 fallback fires "Got it — I'm on it and I'll update you shortly."

So the specialist WORKS but the owner just gets "I'm on it" — the plan/approval never surfaces in-chat. This is the P2 analog of VT-589 (handle-directly path), but on the SPAWN/collapse path, which VT-589 deliberately excluded on the (wrong) assumption that specialists transmit their own owner message.

## To do
1. Get the exact compose_owner_output exception (it's swallowed into the observability envelope's `extra={"exc": repr(exc)}` — surface it / repro locally with the SR result shape).
2. Decide the correct owner surface for a delegated plan: an in-chat plan SUMMARY + the Pillar-7 approval prompt (the campaign approval gate) — NOT a silent async task. Make the collapse/campaign path record + send that owner message (so _brain_emitted_owner_reply is True and the D1 fallback doesn't fire).
3. Confirm whether the SR cohort was even non-empty (see VT-596 consent-gate) — a fix must handle both the has-plan and empty-cohort cases with a real owner message, never D1.
