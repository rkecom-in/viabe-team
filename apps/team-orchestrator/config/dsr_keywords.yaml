# Initial draft for VT-3.8. VT-8 admission gate will own production keyword
# curation. Adding/removing keywords requires Type 2 governance per Notion.
#
# Matching: case-insensitive, word-boundary, anywhere in the message body.
keywords:
  - data deletion
  - my data
  - GDPR
  - मेरा डेटा
  # VT-329 (Cowork adversarial review): code-switched / Hinglish DSR — real owners mix scripts
  # mid-sentence ("मेरा data delete karo" / "data delete karo refund"). Boundary-safe containment
  # (the lookaround patterns) keeps these conservative; the failure direction is fail-safe
  # (over-route a deletion request, never miss one).
  - data delete
  - delete karo
  - डिलीट
  - हटाओ
  - मुझे हटा
