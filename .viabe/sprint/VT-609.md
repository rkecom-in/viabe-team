---
id: VT-609
title: "Loop P4 (moved last of conversions) — Onboarding conversation moves into the specialist; journey becomes tools (carries amendment A2: full regression port + deterministic floor)"
status: Done
priority: high
type: BUILD
area: manager
created: 2026-07-05
authorized_by: fazal
authorization_basis: "Fazal 2026-07-05 loop-program grant — execution plan Package 4; CC amendment A2 (highest regression risk of the program — journey.py embodies dozens of shipped defect fixes)."
---
# VT-609 — onboarding specialist conversion (expert Package 4 + amendment A2)
Journey interceptor stops consuming ordinary owner messages pre-Manager; journey tables stay as
resumable specialist state; journey operations become context-scoped tools (read_onboarding_state,
extract_owner_answer, record_answer, next_required_question, record_skip, apply_correction,
profile_completion_check, activation_check). Specialist absorbs multi-field/out-of-order answers,
never asks for an in-context fact, returns needs_owner_input (one question) or completed with
deterministic evidence; completion/activation stay deterministic (model cannot assert them).
Policy-confirmation stage records machine-enforceable owner bounds; missing policy = deny-all.
BINDING A2: the ENTIRE existing journey regression suite green through the specialist path
(greet/bare-no/redelivery idempotency, VT-569a, VT-576 pacing, VT-601 cross-fill + skip-answered);
LLM-down/unclassified keeps the deterministic floor (VT-597 pattern). No test may count the legacy
interceptor as Onboarding-Agent evidence. Exec order 7.

## RESOLVED 2026-07-06 — landed (6ed8b16, faad51e, 711307b, 1fcedcf, 9b9da4f, e114283, 599bbee), migration 169
Onboarding is a real Manager specialist. 8 conversion tools + policy-propose replace the
deterministic interceptor; the interceptor is MODE-GATED (legacy/shadow byte-identical, enforce
routes to onboarding_conductor). Deterministic LLM-down floor (scripted next-question via
conductor.decide_next_question, never silence/canned, records the failure for observability).
Safety guards proven AT THE TOOL BOUNDARY (85-behavior mapping table, .viabe/recon/): never-assert
off-taxonomy, bare-rejection, populate-first never-downgrade+completion, dunder-field guard;
idempotency structural (no cursor); opt-out/DSR via unconditional pre_filter. 6 pure-sequencing
behaviors deferred to VT-611's real-scenario pack (VT-576 pacing, VT-583 intent, cross-fill etc.).
Policy-confirmation: the FIRST-EVER grant_business_policy caller — redesigned to Pillar-7 after
review caught a money-bearing grant-on-LLM-judgment CRITICAL. Final shape: propose_business_policy
(profile-complete-gated, enum-validated, ₹10k-clamped) arms an owner-approval row; the owner's yes
resolves through the DETERMINISTIC approval-glue (apply_business_policy_decision) — zero LLM in the
grant path; grant reads bounds off the row + granted_by=approval_id; row's STORED decision governs
(concurrent-reply race closed). Write+policy tools bind ONLY in enforce (legacy = pre-PR 2 read
tools). Review: 3 rounds — 9 findings (2 critical) + a resolve-never-fires critical + a race
hardening, ALL fixed+revert-pinned. Battery 3,600/0. Spawned rows: VT-612 (paced-flow bridge);
VT-611 pre-work items #6 (answer-threading) + #7 (agent_customer_send sweep bug).
