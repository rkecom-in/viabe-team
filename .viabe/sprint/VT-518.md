---
id: VT-518
title: "DSR purge gap — tm_audit_log + debug_events were not erased on right-to-erasure (privacy launch-gate)"
status: In Review
priority: Critical
type: BUILD
area: privacy
created: 2026-07-01
authorized_by: fazal
authorization_basis: "Cowork PII-boundary audit-after of VT-514/515 (Fazal authorized the observability build + asked for the PII audit), dispatched as task 20260701T170000Z. DPDP right-to-erasure defect in just-landed work → fix-immediately standing."
---

# VT-518 — DSR purge gap: tm_audit_log + debug_events not erased on DSR

## Problem (Cowork audit-after, evidence cited)
VT-514 mig 147 (`tm_audit_log`) + VT-515 mig 146 (`debug_events`) added tenant-scoped PII-bearing tables. NEITHER was in `dsr_purge.py` `_PURGE_ORDER` → `purge_tenant_data()` left both intact on a right-to-erasure DSR. mig 147 *documented* "DSR-purge-scoped via the VT-185 path" but the impl never delivered it (the recurring hand-maintained-`_PURGE_ORDER` drift). Redact-at-write + RLS is insufficient for ERASURE — redacted activity history is still the subject's data.

## Build (code + test + comment only — NO new migration; tables already live via 146/147)
1. **`_PURGE_ORDER`**: added `tm_audit_log` + `debug_events` BEFORE `pipeline_steps`/`pipeline_runs`. tm_audit_log.run_id → pipeline_runs(id) NO ACTION ⇒ MUST precede pipeline_runs; parent_audit_id self-FK resolved within the single tenant-scoped `DELETE … WHERE tenant_id` (NO ACTION checked at statement end, parent+child gone together). debug_events FK-free + NULLABLE tenant_id ⇒ order-insensitive, pre-tenant (no-subject) rows correctly retained.
2. **`test_dsr_purge_substrate.py`**: seed both in `_seed_full_tenant_data`; added both to `_PURGED_TABLES` (broad sweep + cross-tenant + audit-count `1+len(_PURGE_ORDER)` auto-cover); new focused `test_purge_hard_deletes_tm_audit_and_debug_events` (erasure + cross-tenant + in-`_PURGE_ORDER` drift guard).
3. **mig 146 header**: added Retention/DSR note (was silent; mig 147 already documented it).

## Out of scope (deliberate, documented)
Secondary web tm-audit route de-id: SKIPPED. `deIdentifyTmAuditForVtr` nulls input/result/reasoning_ref — a VTR-blinding de-id; the tm-audit route is the operator/Fazal VT-516 feed (operator_claim), not VTR-facing → applying it would strip data the operator feed exists to show + mutate the feed under Fazal's pending eye-check. Already protected by redact-at-write (CL-390) + ops-operator gate. Different audience defeats the symmetry argument.

## Validation (deployed dev — CL-2026-06-29; real purge, NOT mock)
CANARY GREEN on live dev Supabase via `railway run --service vt-orchestrator-service --environment development -- uv run pytest`: **6 passed, 0 failed (424s)** — new tm_audit/debug erasure test + broad sweep + cross-tenant + privacy_audit_log retention (count auto-adjusted to +37) + owner_inputs + episodic. Proves seed into both FORCE-RLS tables (BYPASSRLS service role), real `purge_tenant_data` → 0 rows in both, FK ordering holds, cross-tenant isolation intact. ruff clean; AST/order verified (tm_audit_log idx 25, debug_events 26, before pipeline_runs 28).

## Known edge (pre-existing, noted not fixed)
tm_audit_log.parent_audit_id self-FK has no same-tenant CHECK. A (schema-permitted but emit-contract-forbidden) cross-tenant parent chain would make the single-statement DELETE fail-closed (abort + rollback), never silent-leak. Not introduced by VT-518; out of scope.

main untouched. NO send. Cowork audit-after + Fazal eye-check pending.
