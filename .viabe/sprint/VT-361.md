---
vt_id: VT-361
title: Business verification (Option F) — GSTIN lookup + reverse-penny-drop bind via Sandbox by Quicko
status: In Progress
priority: High
sprint: Sprint 9 - Launch Surface
type: Feature
area: [Onboarding, Privacy, Billing, Integrations]
parent: VT-112
related: [VT-112, VT-303, VT-231, VT-267]
tags: [kyc, verification, vendor, pii, plan-first]
---
# VT-361 — instant business verification at signup (Option F)

Fazal ruling 2026-06-07: VT-112 closes as **Option F** (instant programmatic business verification),
vendor = **Sandbox by Quicko** (account exists, PAYG wallet). PLAN-FIRST (pii + new vendor).
This row CLOSES VT-112 + writes docs/team/kyc-decision.md.

## Design (TWO-TIER — Fazal re-ruling 2026-06-08; penny-drop CUT)
Tiers: unverified | gstin_verified ("yellow", active GSTIN lookup alone — no bind, Fazal accepted
impersonation residual + VTR backstop) | vtr_verified ("green", manual ops upgrade, gates nothing yet).
ACTIVATION GATE: card_captured → paid_active requires gstin_verified (server-side read, fail-closed,
vendor_down≠invalid_gstin). VTR override = ops endpoint (operator-JWT, audited). GST-OTP + domain/WHOIS
= evaluated-and-dead; reverse-penny-drop = deferred-not-dead (Sandbox RPD endpoint NOT documented).

## Allocations (CL-424, up-front)
- VT-361 (this row). Migration **120** (tenants verification columns).

## Plan-ready sent: see .running plan-ready 20260607T18xx. Awaiting Cowork ack (Fazal sees the gating fork).
## GATING FORK (Fazal's call at plan review): (A) mandatory-to-activate (gate the card_captured→paid_active
   edge in transitions.py) vs (B) badge-only (status product-visible, no gate). Both wirings presented.

## Sequence: plan-ready → Cowork ack → build → Cowork subagent at PR (pii: result storage, no PII in logs,
   name-match can't be gamed by crafted business names, fail-closed). Canary: real GSTIN 27AAKCR3738B1ZE.
