---
id: VT-556
title: "VTR ingest-strategy / behavioural-directive teach-loop — VTR teaches, Team Manager picks it up next run"
status: In Review
priority: launch-blocker
type: BUILD
area: agent-memory
created: 2026-07-02
authorized_by: fazal
authorization_basis: "Fazal 2026-07-02 (Cowork 20260702T100000Z PART-1 item 3): build the VTR-facing ingest strategy/behavioural directive surface the Team Manager picks up on its next run — the human-as-teacher input feeding C3 seedable memory; route through provenance/authority/tenant-scope + audit; confirm the agent consumes it."
---

# VT-556 — VTR teach-loop (human-as-teacher → manager retrieval)

## Why
VT-550 shipped the `agent_memory` MECHANISM but the teach→pickup loop was BROKEN: no VTR submit surface, `get_active_memory` was never called by the manager, no provenance column, no audit-on-write. This closes the loop end-to-end (the C3 learning-loop input; distinct from the plan-edit draft correction).

## Built
- **mig-157** `agent_memory += authored_by_operator_id UUID` (soft, no FK) + `authority TEXT DEFAULT 'seed' CHECK IN (seed|vtr|owner|system)`. Existing rows unaffected (defaults).
- **`agent_memory.upsert_directive()`** — SERVICE-path write (a VTR is an operator, not a tenant conn; the operator↔tenant assignment gate is enforced at the API layer BEFORE this runs), provenance (operator id + authority='vtr'), immediate `retrieval_eligible=true`, version+1 overwrite, PII-redacted content, fail-soft `emit_tm_audit(knows / vtr_directive_ingested)`. `get_active_memory` now surfaces `authority` and orders **vtr-first**.
- **Manager retrieval WIRED** (`dispatch.py::_build_manager_directive_block`) — a per-turn `## VTR directives` SystemMessage inserted after the VT-194 cached prefix (mirrors the L1 / business-context blocks; cache still hits), reflected in the GETS retrieval audit + snapshot hash. **Config-gated `MANAGER_MEMORY_RETRIEVAL` (default OFF)** — observe-first: the seam lands dark, flipped per-env (dev for the e2e) once validated; prod stays off until authorized. Double safety with the per-row `retrieval_eligible` flip (BOTH must be true to steer a decision).
- **API** `POST /api/orchestrator/ops/vtr-agent-directive` — `require_vtr_action` gate IS the authority + tenant-scope check; content scrubbed + PII-redacted; fail-loud ops_audit row (the plan-edit idiom).
- **team-web** — `orchestrator-client.vtrAgentDirective` + `ingestDirectiveAction` (session-derived operator, early assignment guard) + `DirectiveForm` on the tenant **agents** page.

## Contamination controls
Provenance (`authored_by_operator_id`) + authority class (`authority='vtr'`) + tenant-scope (require_vtr_action assignment gate + RLS) + PII-redact-at-write + audit (ops_audit fail-loud + tm_audit knows). Retrieval default-closed (flag + per-row).

## Verify
ruff clean; 27 pure dispatch tests (incl 3 new gate tests: off→None + no retrieval call, on→renders `[VTR] …`, empty→None); 9 agent_memory realdb (incl 4 new: provenance+retrieval, overwrite-version, vtr-first-order, pii-redact); 14 DSR purge substrate (agent_memory erasure intact w/ new cols); team-web tsc + eslint clean.

## Follow-up (not blocking)
Flip `MANAGER_MEMORY_RETRIEVAL=true` on Railway **dev** for the PART-2 VTR-journey e2e (proves teach→pickup changes the manager's next decision). Prod flip is Fazal-authorized. Seed CONTENT remains a Fazal/archetype follow-up.
