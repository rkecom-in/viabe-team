---
id: VT-558
title: "Run-control completeness — campaign true-kill + VTR takeover (B6; LANE freeze already exists)"
status: In Review
priority: launch-blocker
type: BUILD
area: reliability
created: 2026-07-02
authorized_by: fazal
authorization_basis: "Fazal 2026-07-02 (Cowork 20260702T100000Z PART-1 item 2): freeze/kill switches (global/tenant/lane/campaign) + VTR takeover."
---

# VT-558 — campaign true-kill + VTR takeover (B6)

## Reconcile (what already exists — no rebuild)
GLOBAL freeze (`AGENT_AUTONOMY_GLOBAL_FREEZE`) + TENANT freeze (`workflow_controls` pause per kind) + **LANE freeze** — the per-(tenant, agent) autonomy freeze (`tenant_agent_autonomy.frozen` + `cancel_open_batches`; agent==lane) ALREADY atomically cancels a lane's in-flight batches. Net-new = campaign-TARGETED kill + tenant-wide takeover.

## Built
- **Campaign true-kill** — mig-159 `campaigns.status += 'cancelled'`; `CampaignsWrapper.cancel()` CAS (killable only from proposed/approved); `execute_approved_campaign` observes 'cancelled' at ENTRY (abort, nothing sent) AND at each recipient boundary (in-flight stop — remaining counted `killed`), and a killed campaign is NOT advanced to 'sent' / does NOT emit CAMPAIGN_SENT. `_campaign_status` is FAIL-OPEN (an unreadable status never kills a live campaign). API `POST ops/run-control/kill-campaign` (tenant derived from the campaign row, transport-auth-before-derive, raw service-cursor CAS, audit-in-txn, 409 if not killable).
- **VTR takeover** — `agents/takeover.py::take_over_tenant` / `release_takeover` COMPOSE the existing primitives (no new gate): pause `agent_dispatch` (`workflow_controls`) + freeze EVERY registered agent (autonomy freeze → atomic in-flight cancel). The coordinator sweep already skips a paused workflow_kind + a frozen agent, so takeover is honored with no enforcement-path change. API `POST ops/run-control/takeover` + `/release-takeover` (tenant-scoped + assignment gate, audit-in-txn). Idempotent (ON CONFLICT single active hold).

## Verify
ruff clean; 18 execute unit tests (1 new: cancelled-campaign killed-before-start, not advanced) + the existing empty-cohort summary updated for the `killed` counter; 4 VT-558 realdb (cancel CAS kills proposed / no-op on sent+already-cancelled; takeover pauses+freezes-all / release reverses / idempotent); 251-test regression sweep (run_control + autonomy + campaign + vtr_console) green.

## Note (honest scope)
Takeover is deliberately composed over pause + per-agent freeze rather than a new "seize" substrate — the manual VTR surfaces (plan-edit, VT-556 directive, confirm) already let the human drive; takeover just halts the automation atomically + audits it.
