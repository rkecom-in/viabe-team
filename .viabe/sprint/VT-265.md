---
vt_id: VT-265
title: Migration 053 DROP CONSTRAINT → IF EXISTS (re-apply idempotency)
status: Done
priority: Low
sprint: Sprint 1.5 - Hardening
type: Chore
area: [Migrations]
assignee: unassigned
parent: null
sub_items: []
exec_order: 99
---

# VT-265 — mig 053 bare DROP CONSTRAINT lacks IF EXISTS

From the VT-262/263 adversarial review (dropped-low). migration 053
(skipped_send_status) uses bare `ALTER TABLE ... DROP CONSTRAINT <name>` — the
only migration that does so without `IF EXISTS`. Mild re-apply idempotency risk
(a partial/re-run could fail on the already-dropped constraint). Add `IF EXISTS`
to match house convention. The runner tracks by name so 053 won't re-run on a
healthy DB; this is belt-and-braces. Low priority, post-Sprint-2.
