---
vt_id: VT-390
title: Signup honesty — welcome_sent reflects reality + stale comment/taxonomy cleanup
status: Done
priority: Medium
sprint: "Sprint-Fazal"
type: Bugfix
area: [Owner Surface, Onboarding, Documentation]
assignee: Claude Code
related: [VT-371]
exec_order: 2
created: 2026-06-13
last_updated: 2026-06-13
---

## Contract (Fazal 2026-06-13: "get the gaps closed")

Three small correctness/honesty fixes on the signup path (verified on dev `0874370`). One
coherent PR. NONE of these wires the real owner-WABA send (that stays Fazal-gated) — this
is about not LYING about it.

1. **`welcome_sent=True` is misleading** (`onboarding/signup.py:273-287`). `_default_welcome`
   only logs intent (`TODO(owner-WABA)`) yet `run_signup` returns `welcome_sent=True`, so the
   API reports a welcome was sent when it wasn't. Make the returned flag reflect reality:
   return `False` (or a tri-state like `welcome_pending`/`welcome_stubbed`) when the send is
   the stub. The signup itself must stay successful regardless (the welcome is best-effort,
   non-terminal) — only the reported flag changes.
2. **Stale migration comment** (`migrations/096_vt82_tenant_trial_columns.sql:5`) still says
   "trial_started_at + 14d" / "Day-39 extensions" — contradicts the 30-day, no-extension
   policy (`config/trial.yaml`). Correct the comment (column is already right).
3. **Taxonomy mismatch** — `team_welcome` is registered `audience: customer`
   (`config/twilio_templates.yaml`) but targets the OWNER. Fix the label to `owner`.

## Acceptance
- A signup with the stubbed welcome no longer reports `welcome_sent=True` (test asserts the
  honest value); signup still returns success.
- mig-096 comment reads 30-day / no-extension; `team_welcome.audience: owner`.
- No behavior change to tenant creation, trial clock, or the (still-stubbed) send path.

## Merge class
[BUILD] — honesty/cosmetic, no security/PII/RLS/money behavior change. CC self-merge to
`dev` on green (CL-429).

## Status history
- 2026-06-13: Rostered + dispatched (Cowork) under Fazal's gap-closure batch grant.
- 2026-06-13: Done — self-merged → dev @ 67d1f32 (welcome_sent honest + mig-096 comment + team_welcome audience:owner). 3 real gates green.
