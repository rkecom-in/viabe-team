---
id: VT-722
title: "Single-voice S3b — ENFORCE-mode parity: the agent-pick / commitment writers are DORMANT on the live path"
status: DEV-PROVEN (CC 2026-07-30 — pick net at runner seam + trial-terms at chooser site; canary 6/6 on live ENFORCE incl. contradiction-substrate flip; full-pack ×3 CLEAN: 11 blocks = exactly the 4 pre-existing classes, zero new, zero contamination. Exit gate met.)
priority: Critical
owner: CC
exec_order: 131.5
created: 2026-07-29
created_by: clau
authorization_basis: "CC finding 2026-07-29T1800Z (VT-719 stage-2 forensics) + CL-2026-07-28-single-voice-manager. Re-ranked from backlog because it makes the asserted-facts ledger INCOMPLETE on the live path."
---

# VT-722 — enforce-mode writer parity (the ledger's live-path hole)

## Why this is now Critical, not backlog
CC's stage-2 forensics: on dev **enforce** mode, an exact "Sales Recovery" tap routes to the
BRAIN, not the journey flow beat. `maybe_handle_enforce_journey_turn` is the narrow gate; the
full walker — which hosts `_FLOW_AGENT_CHOICE` and the agent-pick writer — only consumes turns
in **legacy/shadow**. There are TWO chooser surfaces (VT-700 direct-handler chooser + the
journey flow beat) and **on enforce NEITHER records the pick deterministically** — the brain
does it free-form.

**Consequence:** `active_agent` / `trial_terms` assertions record only on legacy/shadow paths.
The asserted-facts ledger (VT-719) is therefore **incomplete exactly where it matters — the
live enforce path**. A ledger with holes cannot enforce "never contradict yourself"; it can
only enforce it on the paths that happen to write. That is a silent-failure class, so it is
re-ranked out of backlog and INTO the single-voice program (S3b, ahead of VT-721).

Counterpoint recorded (CC): the policy-grant writer (frequency caps + spend ceiling) is
deterministic approval-glue and DOES fire in every mode — that leg is live end-to-end.

## Scope
Make the deterministic commitment writers fire on the ENFORCE path — either by routing the
chooser surfaces through the single emission choke (VT-718) so the pick is recorded there, or
by giving the enforce gate the same writer coverage as the walker. **Prefer the choke**: one
recording point beats two parity-maintained ones (that is the S4 thesis, applied early).
Every agent pick / commitment stated to the owner on ANY mode must land in the ledger.

## Exit gate
On deployed dev in ENFORCE: agent-pick + trial-terms assertions recorded on the live path,
proven by canary (not unit tests alone) · no double-write when legacy/shadow also fires ·
full-pack ×3 (brain-touching) · the VT-719 contradiction check demonstrably fires on an
enforce-path flip.

## Sequencing
Between VT-719 (stage 3) and VT-721. Rationale: finishing the ledger's writers is worth more
than starting the 7-day plan — an incomplete ledger under-delivers the whole S3 promise.
Fazal may override the order.
