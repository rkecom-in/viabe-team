---
id: VT-606
title: "Loop P3 — the durable Manager loop: DBOS manager_task_workflow + manager_review + specialist→review edges + triage + TEAM_MANAGER_LOOP_MODE (carries amendments A1/A3/A4/A5)"
status: Done
priority: launch-blocker
type: BUILD
area: manager
created: 2026-07-05
authorized_by: fazal
authorization_basis: "Fazal 2026-07-05 loop-program grant ('enable loop engineering') — execution plan Package 3 + program amendments."
---
# VT-606 — the loop itself (expert Package 3 + CC amendments)
DBOS workflow keyed by task_id: load plan → claim step → validate capability/prereqs/policy →
dispatch specialist or advisory tool → consume typed SpecialistReturn → manager_review decision
(accept_step/revise_step/ask_owner/continue/complete/escalate). Graph: remove specialist→END,
every specialist → manager_review; only manager_review advances/terminates; approval interrupt +
campaign effect path preserved. Triage on every non-deterministic-rail inbound: direct_reply /
answer_pending / new_task / task_status / cancel_task (direct replies create NO task; side
questions never lose the active task). Limits 8 steps / 2 revisions per step / 6 cycles per run →
blocked + VTR incident, never silence. TEAM_MANAGER_LOOP_MODE=legacy|shadow|enforce, prod default
legacy. BINDING AMENDMENTS: A1 legacy-compat envelope adapter (shadow compares like-for-like — the
tagged-union → collapse → VT-594 surfacing stays byte-compatible until enforce); A3 stale-resume
re-engagement (>24h pause → approved template via the registry, then exact task/step resume); A4
stable message ids for ALL checkpointed-thread injections + DBOS step-retry-mid-graph tests (the
VT-602 class); A5 triage+review = Sonnet-5 tier, opus ONLY for plan validation at objective
creation + final completion verification; shadow divergence categories: safety=block, intent=review.
Exec order 4.

## RESOLVED 2026-07-05 — landed bd23512 (+bf86802 test follow-up), deployed dev, migrations 166/167 live
10 commits, 3 build rounds + full adversarial review cycle. The loop: DBOS manager_task_workflow
(claim → validate → dispatch → review → verify → settle/promote), manager_review + triage
(sonnet-5), plan-validation + completion-verification (the two opus checkpoints), specialist→review
edges, TEAM_MANAGER_LOOP_MODE=legacy|shadow|enforce (legacy default; legacy graph pinned
byte-identical by test), stale-resume via team_reengage, queued promotion, limits 8/2/6 →
blocked + limit_exhausted incident.
- Adversarial review (32 agents): 13 confirmed findings — 1 CRITICAL (loop thread key broke the
  approval-resume invariant → fixed with uuid5 loop_run_id as both thread_id and state.run_id),
  4 loop-correctness majors (shadow admission jam → 'shadow' status mig 167; queued force-start +
  wrong-task resume → planned-only start + claim guard + question-bound correlation; verification
  retry re-ran the whole plan → append_step carry-forward; REVISE never applied → replace_step +
  revised-outcome plumbing + the self-caught revision_counts step_seq rekey), 1 minor (CLARIFY
  without a question busy-spin → revise redirect), 7 test-adequacy gaps (all closed incl. the real
  dispatch under test, the runner skip-guard integration test, a real DBOS SIGKILL/replay test).
- Re-verify: 6/6 fixed, three proven by revert-testing.
- DEFERRED to VT-607 (flagged, not hidden): pending_approvals.run_id FK → pipeline_runs has no
  loop-minted row — enforce-mode approvals FK-violate until VT-607 mints a real pipeline_runs row
  per loop dispatch. Unreachable in legacy/shadow (verified). Minor residuals also carried:
  review.py:271 comment drift; replace_step supersede rowcount; dormant record_decision CLARIFY
  branch same-pattern.
