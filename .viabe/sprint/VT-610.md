---
id: VT-610
title: "Loop P7 — autonomy + VTR control: per-capability force_l3 (earning-bypass only), atomic takeover/release, Ops provenance"
status: Done
priority: high
type: BUILD
area: manager
created: 2026-07-05
authorized_by: fazal
authorization_basis: "Fazal 2026-07-05 loop-program grant — execution plan Package 7."
---
# VT-610 — autonomy/VTR (expert Package 7)
Keep 20-clean-approval earning + explicit owner opt-in for L2→L3. Add force_l3 per-capability VTR
override: verified VTR assignment + scrubbed reason + atomic audit; bypasses ONLY the earning
threshold + owner opt-in — never policy/consent/opt-out/caps/ownership/activation/effect rails.
Demote/revoke/freeze/unfreeze retained. Tenant takeover = tenant-wide emergency freeze: pauses
dispatch + cancels open effect batches atomically; release unfreezes, never promotes. NO tenant-
wide promotion. Ops UI shows earned vs VTR-forced provenance + last transition. Exec order 8.

## RESOLVED 2026-07-06 — landed (eb9ada3, bb35ae9, 47680f9), migration 170
VTR force_l3: sets level='L3' skipping the earned path (drops 20-clean-streak + owner-opt-in),
provenance columns l3_force_granted_at/by_vtr (mig 170) + Ops view/handler surface (earned vs
forced), NO batch cancel, frozen-agent refuses the force, tm_audit fail-closed. RAIL-PRESERVING
(proven per-rail): force grants LEVEL only, never immunity — Gate-0 activation, per-recipient
consent (Gate-4 SKIP_CONSENT proven under forced L3 — the core invariant), opt-out/caps, policy
boundary, always-confirm floor, business-impact gates, RLS, and regression-freeze ALL still bind;
takeover atomic (verified). Re-verify: NO earned-provenance reader exists → no forced-L3
autonomous-send bypass possible (all rails key off level+frozen+level-blind floor, not provenance).
Auth: force_l3 escalated to the Fazal-only EXCEPTION tier (team-lead decision — overriding an
owner's autonomy opt-in is exception-worthy; additive on top of assignment/JWT; Fazal may relax to
any-VTR). Other VTR actions unchanged. Battery 3,622/0.
