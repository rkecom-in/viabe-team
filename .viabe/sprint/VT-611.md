---
id: VT-611
title: "Loop verification programme — 120-scenario pack, 30-critical ×3, judge gates, adversarial tenant tests, DBOS-retry/restart matrix, evidence manifest"
status: Todo
priority: launch-blocker
type: BUILD
area: manager
created: 2026-07-05
authorized_by: fazal
authorization_basis: "Fazal 2026-07-05 loop-program grant — execution plan §4 + PLAN.md Verification; extends the committed VT-598 pack."
---
# VT-611 — the promotion gate (expert §4)
Full existing suites stay green. Add: state-machine tests (every legal/illegal task+step
transition); graph tests (all three specialists return to manager_review); restart tests at every
pause point + DBOS STEP-RETRY mid-graph (amendment A4); concurrency (duplicate messages,
simultaneous replies, stale workers → CAS); adversarial (foreign tenant UUIDs, prompt injection,
unsupported connectors, fabricated completion, direct effect attempts); advisory-tool tests (no
spawn/mutation/execution claims). Scenario pack ≥120 (40 manager / 25 onboarding / 25 integration /
30 SR+autonomy+rails); 30 critical ×3 runs; zero silent terminals / cross-tenant / unapproved
effects / repeated in-context questions / unsupported claims; judge ≥4/5 every dimension, mean
≥4.5 (fix the judge-CLI key path so it runs without opus-subagent standins). Redacted evidence
manifest (code+deploy SHA, migration version, flag mode, scenario counts, judge scores, canary
refs, transcript hashes, synthetic-teardown + zero-real-send confirmations). Shadow gate: ≥50
conversations, safety-divergence=block. Every enforce promotion cites this row. Exec order 9.

## PRE-WORK (consolidated deferrals — build/fix BEFORE the scenario pit, they gate its honesty)
Ordered; each is a landed-program deferral, not new scope:

1. **Owner-notification composer (VT-607, THE first build item — load-bearing for "truthful owner
   outcome").** Today the loop records manager_tasks.terminal_outcome + owner_notification_status=
   'pending' but NOTHING sends the owner anything — a completed/cancelled/blocked/needs-owner task
   never tells the owner. Build the composer: reads terminal_outcome (completed_with_effect /
   cancelled / blocked / verified-no-effect) + the task's own evidence, composes the owner-facing
   line, sends via the registry template chokepoint (never freeform; A3 discipline), flips
   owner_notification_status pending→sent idempotently (stable message id per A4). A CANCELLED task
   (owner rejected) must say declined, NEVER success. Tests: one per terminal_outcome asserting the
   RIGHT message class + idempotent re-send + registry-resolved (no freeform).

2. **test_vt384_l3_wire_realdb ordering flake (VT-608, 4th occurrence, isolation-proven 3×).** The
   fixture leaks shared-DB state so test_execute_item_l3_grant_lands_auto_send_pending fails only in
   full-suite ordering, passes isolated. Root-cause the leaked row/role + make the fixture
   self-isolating (own tenant, teardown). Kills a recurring false-red that masks real failures.

3. **VT-608 CRITICAL-2 coverage residuals (re-verify):** (a) a single write→durable-column→read
   round-trip test for the confirmed field_mapping (currently the two halves are tested separately,
   the join isn't); (b) partial-mapping sharp edge — a partial confirmed mapping silently drops
   unmapped identity columns (no alias fallback) — decide+enforce: either reject a partial mapping
   at confirm time or fall back to alias for unmapped columns, then test it; (c) server-side
   validation that a confirmed mapping's canonical VALUES are real CanonicalField literals (an
   invalid value currently silent-no-ops that column).

4. **Live Rule-15 canaries on DEPLOYED dev (VT-608).** The local canary ran 7/8; the 8th (Sheets
   authorize_url mint) needs GOOGLE_OAUTH_CLIENT_ID/REDIRECT_URI/CLIENT_SECRET present on Railway
   dev (sealed → verify by USE). Set/verify the dev Google OAuth vars (CC dev autonomy), then
   re-run canaries/vt608_integration_specialist.py ON deployed dev → 8/8. Manifest cites the run.

5. **VT-609 pure-sequencing behaviors (incoming).** The builder's 85-behavior mapping table will
   name behaviors deferred to this row's real-scenario pack (VT-576 pacing, VT-583 classifier
   intent, and any others that genuinely need the orchestrating turn). Fold each named behavior
   into the onboarding scenario set (25 onboarding scenarios) as a REAL-LLM assertion — this row is
   where they get their behavioral proof, per the VT-609 ruling.

6. **Answer-text-to-redispatch threading (VT-609 flag B — generic Package-3 gap, affects ALL
   specialists).** When manager_review decides ask_owner, it parks pending_questions + waiting_owner;
   the owner replies, correlate_reply records answer_text, _resume_step_after_answer flips the step
   pending→running, and _dispatch_specialist_step re-dispatches with ONLY the step's ORIGINAL stored
   situation/desired_outcome — it does NOT thread pending_questions.answer_text into the re-dispatch
   messages. So the resumed specialist never sees what the owner just said → it re-asks. This is the
   "stop re-asking known info" objective violated at the loop core, and it predates VT-609 (VT-606
   infra; SR + Integration ask_owner flows hit it identically). Fix: thread the answered question's
   answer_text (+ the question it answered) into the re-dispatch's situation/messages, so the
   specialist resumes WITH the owner's answer. Load-bearing for enforce promotion — must land before
   the scenario pack asserts multi-turn ask_owner→answer→resume.

7. **agent_customer_send timeout-sweep resume_run gap (VT-609 builder flag, pre-existing).** The
   30-min timeout sweep (scheduled_triggers.run_approval_timeout_sweep_body) calls resume_run
   unconditionally on every approval type, but agent_customer_send's run_id is also a checkpoint-less
   minimal run (per the existing code comment) — a timed-out agent_customer_send approval resolves
   fail-closed (mark_approval_resolved runs first) but then throws inside the swallowed try/except,
   logging a false "resume failed" + leaving the minimal run stuck 'running'. Same class VT-609
   fixed for business_policy_grant; pre-existing, unrelated to VT-609. Add the same short-circuit
   branch + a mocked sweep test proving resume_run is never called for agent_customer_send.
