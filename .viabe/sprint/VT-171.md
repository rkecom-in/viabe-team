---
vt_id: VT-171
title: Replace LangSmith with Pydantic Logfire (CL-56 hot-fix)
status: Done
priority: Critical
sprint: Sprint 1 - Foundation
type: Hot-fix
area: [Observability, Privacy, DBOS]
assignee: Claude Code
parent: VT-122
sub_items: []
exec_order: 4.5
branch: "feat/vt-observability-logfire-migration"
version: "v1.0"
created: 2026-05-26
last_updated: 2026-05-26T05:40:00+05:30
supersedes_implementation_of: [VT-101]
honors_decision: CL-56
merged_pr: 60
merge_commit_sha: 39ea42c37e6ec3562a93cb689c40d208ff39a44a
merged_at: 2026-05-26T00:09:25Z
ci_green_sha: 3e8f19b
---

# VT-171 — Replace LangSmith with Pydantic Logfire (CL-56 hot-fix)

## Origin

Hot-fix surfaced 2026-05-26 ~04:00 IST during VT-28 plan-review. Clau noted that VT-101 (PR #56 merged 2026-05-25 ~23:35 IST) shipped LangSmith integration despite CL-56 (Standing 2026-05-16) having replaced LangSmith with Pydantic Logfire 9 days earlier. CL-415 had explicitly flagged this drift. Cowork missed both. VT-101 / VT-102 / VT-103 / VT-104 all shipped to main on the LangSmith premise. Fazal authorized the hot-fix: rip out LangSmith, ship Logfire, preserve all downstream contracts (redactor byte-identical token format, pipeline_log schema, cost dashboard rollups).

**Independently re-validated by Cowork 2026-05-26 ~04:45 IST** — current pricing + DBOS first-party integration confirm CL-56's reasoning. Pricing now even more favorable: Logfire 10M spans/mo FREE (was 5M); $2/M overage; Team $49/mo for 5 seats. LangSmith equivalent at our projected scale = 40× more expensive. DBOS first-party integration: 3-line setup (`logfire.configure() + DBOSConfig{enable_otlp:True}`). Anthropic Messages SDK first-party instrumentation: `logfire.instrument_anthropic()` — we use the Messages SDK directly per CL-249, so this is a perfect fit.

## Brief artifacts — authored 2026-05-26 at queue-time (Cowork) under new team model

- **Team (Fazal-set 2026-05-26):** Cowork = PM. Fazal = CEO (Pillar 7 merge auth). Claude Code = Tech Lead + Developer. Clau = architectural guide, escalation-only. No CodeX / CoderC / CoderX / Pipeline Engineer references in this brief.
- **Review path:** Claude Code self-reviews implementation + Cowork plan-review + Fazal Pillar-7 merge auth. Clau already weighed in escalation-mode at VT-28 plan-review surfacing this hot-fix; no further Clau involvement needed unless the migration uncovers a fresh architectural question.
- **Merge target:** `main` (per CL-132 Standing).
- **PR title:** `feat(observability): Pydantic Logfire migration (VT-171, hot-fix CL-56)`.
- **Branch:** `feat/vt-observability-logfire-migration`.
- **Region:** EU — `logfire-eu.pydantic.dev` (Fazal-set 2026-05-26). Aligns with VT-169 follow-up on Supabase region residency (currently aws-1-ap-northeast-2 Seoul; EU pick for Logfire is the conservative DPDP-friendly choice for observability data).
- **Credentials:** provisioned at `.viabe/secrets/logfire-dev.env` (`LOGFIRE_TOKEN`, `LOGFIRE_PROJECT`). Loading pattern matches existing vendor-scoped envs (`anthropic.env`, `supabase-dev.env`, `langsmith-dev.env`) — use the subshell-source pattern; never `export` to parent.
- **Decisions honored** (cl_decisions_checked): CL-36 (DBOS substrate), CL-56 (LangSmith → Logfire), CL-132 (merge to main), CL-137 (no deprecated APIs), CL-175 (langgraph.types.Command direct, not langgraph_supervisor), CL-205 (recalibration), CL-220 (CI gate verification), CL-248 (Haiku test / Opus prod model split), CL-249 (Anthropic Messages SDK), CL-260 (CampaignPlan v1.0 contract), DR-15 (canary mandatory), DR-16 (this brief itself was produced under DR-16; surfaced via `scripts/check_brief_against_ledger.py`).

## Dependencies

- **VT-101** (Done sha 449a98b) — LangSmith integration that this hot-fix supersedes. The `redact_for_langsmith` boundary established by VT-101 is the integration point; it gets renamed but the byte-identical token format is preserved.
- **VT-102** (Done sha e5c4cdc) — pipeline_log writer that depends on the redactor boundary. UNCHANGED by this hot-fix (different sink).
- **VT-103** (Done sha 95d9f29) — cost dashboard that pulls from pipeline_log + tenant_cost_views. UNCHANGED (different data path).
- **VT-104** (Done sha 3593ce7) — canonical PII redactor + reasoning trace. The `redact()` function in `src/orchestrator/privacy/pii_redactor.py` is the canonical entry point; `observability/pii.py` is a thin delegation wrapper. This hot-fix points the OTel boundary at Logfire instead of LangSmith; the redactor + token format stay byte-identical.
- **CL-36 Standing** — DBOS is the durable-execution substrate. Logfire's `enable_otlp=True` DBOS config is the integration mechanism.
- **CL-249 Standing** — Anthropic Messages SDK is the LLM client. `logfire.instrument_anthropic()` is the first-party integration.

## What Claude Code must produce

1. **`apps/team-orchestrator/src/orchestrator/observability/logfire.py`** — new module replacing `langsmith.py`:
	- `configure_logfire()` — calls `logfire.configure(service_name=..., advanced=AdvancedOptions(base_url='https://logfire-eu.pydantic.dev'))` (or env-var equivalent per Logfire SDK docs — verify exact EU base-url config at PICKUP). Token from `LOGFIRE_TOKEN` env. Project from `LOGFIRE_PROJECT` env. Idempotent.
	- `instrument_orchestrator()` — calls `logfire.instrument_anthropic()` + `logfire.instrument_pydantic()` + any other first-party instrumentations relevant to our stack.
	- `traced_node(fn)` — decorator replacing VT-101's `@traceable`. Wraps a LangGraph node / DBOS step in a Logfire span. Calls `redact_for_otel_span(args, kwargs)` BEFORE the span captures inputs/outputs (same byte-identical token format as VT-101's redactor — that contract is preserved).
	- Disable-on-no-credentials: if `LOGFIRE_TOKEN` is unset, `configure_logfire()` logs a stderr warning and emits no spans. Crucially: a Logfire ingest outage MUST NOT kill the pipeline (same posture as VT-101's LangSmith outage handling).
2. **`apps/team-orchestrator/src/orchestrator/observability/__init__.py`** — re-exports updated. Old `redact_for_langsmith` name kept as a deprecated alias for ONE cycle (with `DeprecationWarning`); removed in VT-172 (next-allocator follow-up).
3. **`apps/team-orchestrator/src/orchestrator/observability/pii.py`** — rename internal export `redact_for_langsmith` → `redact_for_otel_span` (vendor-neutral); keep `redact_for_log` unchanged (canonical name). Token format byte-identical to VT-104.
4. **DBOS OTel emission** — update `apps/team-orchestrator/src/orchestrator/dbos_config.py` (or wherever `DBOSConfig` is constructed) to set `enable_otlp=True`. Per DBOS docs, this routes DBOS workflow + step spans through OTLP exporter → Logfire.
5. **LangSmith removal** — delete `apps/team-orchestrator/src/orchestrator/observability/langsmith.py`. Update every import site (use `grep -rn 'from orchestrator.observability.langsmith'`). Update env-handling for `LANGSMITH_API_KEY` / `LANGSMITH_PROJECT` (loaders + canary env loaders): remove. Mark `.viabe/secrets/langsmith-dev.env` as deprecated (do NOT delete the file — it stays in `.gitignore` and may be useful for audit; just stop sourcing it).
6. **Pyproject + requirements** — add `logfire>=4.0` (or current stable; verify at PICKUP). Remove `langsmith` if directly listed. Run `uv sync` (per CL-58 uv is canonical).
7. **Tests** — pure tests in `apps/team-orchestrator/tests/orchestrator/observability/test_logfire.py`:
	- `configure_logfire` idempotent
	- `traced_node` decorator redacts inputs before span capture
	- No-credentials path emits stderr warning + does not crash
	- Span attributes contain `service.name`, `run_id`, `tenant_id` when set in DBOS context
	- Deprecated `redact_for_langsmith` alias emits `DeprecationWarning` but still works (regression for any straggler import)
8. **VT-101 / VT-102 / VT-104 canary re-runs** — must still pass byte-identical against their original assertions. The token format contract is unchanged; only the destination span backend is different. This is Group A of the canary below.

## Architectural rules to enforce in this subtask

- **CL-56 Standing:** LangSmith stays OUT. Any new import of `langsmith` library = build break.
- **CL-36 Standing:** DBOS is the substrate. DBOS spans MUST go through OTLP exporter (no parallel custom instrumentation).
- **CL-249 Standing:** Anthropic Messages SDK is the LLM client. `logfire.instrument_anthropic()` is the standard hook.
- **CL-260 Standing:** CampaignPlan v1.0 schema unchanged. Logfire spans CAN capture CampaignPlan JSONB but only AFTER redaction (Pillar 7 PII never appears in logs).
- **CL-132 Standing:** All PRs to main, no `dev`.
- **CL-137 Standing:** No deprecated APIs / EOL packages. Logfire `>=4.0` ensures current SDK.
- **DR-15 (Rule #15):** real Logfire ingest in canary, not mock. Real DBOS workflow. Real Anthropic call.
- **Pillar 7:** every PR merge requires Fazal `type: task` with `authorized_by: fazal`. No autonomous merge.
- **Pillar 8 (one redactor):** the `redact()` function in `pii_redactor.py` stays the SINGLE canonical entry; logfire.py imports it (does NOT duplicate redaction logic).

## Acceptance criteria

- LangSmith client removed; no `from langsmith` imports anywhere in `apps/team-orchestrator/src/`.
- `logfire.py` module ships with `configure_logfire()`, `instrument_orchestrator()`, `traced_node()`.
- DBOS spans visible in Logfire EU workspace when a workflow runs locally.
- Anthropic Messages SDK calls auto-captured (token counts + duration) by `logfire.instrument_anthropic()`.
- `redact_for_otel_span` is the vendor-neutral boundary name; `redact_for_langsmith` is a deprecated alias.
- `redact_for_log` unchanged.
- Token format byte-identical to VT-104 (phone_tok_HEX, body_tok_HEX, `<redacted:customer_name:len=N>`).
- VT-101 + VT-102 + VT-104 canaries re-run post-migration: ALL pass with byte-identical assertion output.
- `.viabe/secrets/langsmith-dev.env` no longer sourced (file kept as deprecation audit trail).
- mypy --strict passes; ruff clean.
- All 12 CI checks green.

## Out of scope

- **Production Logfire workspace** — dev workspace only in this PR. Prod workspace + token rotation is a separate follow-up (VT-172 candidate when prod observability is wired).
- **Logfire MCP server integration** — the SQL-queryable MCP angle mentioned in CL-56 is a Phase 2 nicety; not in this hot-fix.
- **Per-tenant project separation in Logfire** — the LANGSMITH_PROJECT env var convention pattern. Logfire equivalent uses `service_name` + tagged attributes. Single project for Phase 1 (10 design partners) is fine; per-tenant only if needed at scale.
- **VT-103 cost dashboard re-skinning** — VT-103 pulls from pipeline_log (Postgres), not LangSmith. UNCHANGED. The "cost from LangSmith trace metadata" pattern that some Logfire users wire up is NOT in scope; we already have our own cost rollup.
- **LangSmith account closure** — keep the LangSmith dev account dormant for one cycle in case rollback is needed. Closure is post-VT-171-merge + post-soak-period.
- **Pydantic AI agent SDK adoption** — Logfire's `instrument_pydantic_ai()` only applies if we use the Pydantic AI agent framework. We use Anthropic Messages SDK direct (CL-249); Pydantic AI adoption is a separate architectural decision (not implied by this hot-fix).

## Canary (Rule #15 — mandatory)

Real DBOS + real Supabase + real Anthropic + real Logfire ingestion. NO mocks for the substrate. Fail-not-skip on any error.

**Canary file:** `apps/team-orchestrator/canaries/vt171_logfire_migration.py`
**Wall-clock budget:** ≤ 60 seconds (includes ~5-10s Logfire ingest latency wait).
**Cost budget:** < ₹1 captured Anthropic spend (one Haiku call).

**PREFLIGHT (assert before any assertion runs):**
- Supabase host resolved; print stripped DSN.
- Anthropic host resolved; print masked key prefix.
- Logfire base URL resolved (assert `logfire-eu` substring); print masked token prefix.
- DBOS configured with `enable_otlp=True`; print DBOS app name + database name.

**Group A — observability regression (3 assertions, NO mocks):**
1. **VT-101 token-format regression:** call `redact_for_otel_span` (the renamed boundary) and `redact_for_langsmith` (the deprecated alias) on the same input; assert byte-identical output (proves the alias still works AND the format didn't drift). Inputs include the VT-101 canary's exact phone + customer_name + body string.
2. **VT-102 pipeline_log regression:** run a synthetic DBOS workflow that writes a `pipeline_log` row through the redactor; query the row back from Supabase; assert payload contains `phone_tok_HEX` (not raw digits) AND the OTLP-exported span captured the SAME tokens in its attributes.
3. **VT-104 PII redactor regression:** scan a multi-pattern blob (PAN + Aadhaar + IFSC + GST + CC-Luhn + email + phone E.164 + 10-digit phone + customer-name bigram); assert every pattern redacted; assert idempotency (`redact(redact(x)) == redact(x)`); assert the BIGRAM customer-name parens-stripping fix (VT-104 bug #3) still holds.

**Group B — Logfire ingestion (3 assertions, real backend):**
4. **Real Logfire span emission:** call `traced_node` decorator on a synthetic function; assert `logfire.configure()` returned success; assert `logfire.instrument_anthropic()` returned success; assert a span object was created (use Logfire SDK's `span.context` introspection).
5. **Span attributes are redacted:** the synthetic function's input contains a phone number + customer name; verify the span's captured `input` attribute contains `phone_tok_HEX` (NOT the raw phone), and the customer name is `<redacted:customer_name:len=N>`.
6. **Logfire ingest verification (read-back):** sleep 5-10 seconds for ingest latency; query Logfire's read API (HTTPS, `LOGFIRE_READ_TOKEN` or write token if read-permissions allow); assert the synthetic span landed in the EU workspace with the redacted attributes. If the read API requires a separate read token, document the credential gap and Fazal-provision before merge.

**Group C — DBOS OTLP emission (2 assertions, real DBOS):**
7. **DBOS workflow span emitted:** start a synthetic `@DBOS.workflow` that runs 2 `@DBOS.step`s; verify the corresponding OTLP spans land in Logfire (read-back via Group B's read mechanism). Assert the workflow_id is captured as a span attribute.
8. **DBOS step + Anthropic call nested:** the synthetic workflow's step body makes a real Anthropic Haiku call; verify the Anthropic span (from `instrument_anthropic`) appears as a CHILD of the DBOS step span (full-stack tracing — the architectural win over LangSmith). Assert parent-child relationship in the span tree.

**Group D — Real Anthropic call with cost capture (1 assertion):**
9. **Real Haiku call inside workflow → cost < ₹1:** the synthetic workflow makes a real Anthropic Haiku call with a redacted prompt (PII stripped before send). Assert `anthropic_ok=True`, in_tokens > 0, out_tokens > 0, cost_paise computed from `model_pricing.yaml` < 100 (under ₹1 ceiling per DR-15). Assert the prompt SENT to Anthropic contains zero raw PII patterns.

**Group E — Failure modes (2 assertions, defensive):**
10. **Logfire outage simulation:** point `LOGFIRE_TOKEN` to an invalid value (`LOGFIRE_TOKEN=invalid-token-canary-test`); run the synthetic workflow; assert the workflow STILL COMPLETES (Logfire ingest failure does not propagate) and a stderr warning was emitted (not a crash).
11. **No-credentials path:** unset `LOGFIRE_TOKEN`; call `configure_logfire()`; assert it logs a "logfire-disabled" warning AND returns gracefully (no exception). This is the pattern from VT-101's LangSmith outage handling, ported.

**Audit artifact** (pasted in `pre-merge-result`):
- Wall-clock from `time` command.
- Resolved Supabase + Anthropic + Logfire-EU hosts at PREFLIGHT (credentials stripped).
- Per-assertion observed values for all 11.
- **Plus regression re-runs of VT-101 + VT-102 + VT-104 canaries** (proves the migration didn't break the existing observability stack).
- Anthropic cost captured (must be < 100 paise / ₹1).
- One sample Logfire span (JSON-exported attributes) showing redacted-only payload.
- Full stdout tail ≥ 150 lines + full log at `/tmp/vt171-canary-evidence.log`.

Summary-only `pre-merge-result` will be bounced. Standard tightened from VT-104 with the Logfire read-back verification step.

## Branch & PR

- Branch: `feat/vt-observability-logfire-migration`.
- PR title: `feat(observability): Pydantic Logfire migration (VT-171, hot-fix CL-56)`.
- Review path: Claude Code self-reviews implementation + Cowork plan-review + Fazal Pillar-7 merge auth.
- Merge target: `main`.

## Status history

- 2026-05-26 04:55 IST: created by Cowork as hot-fix for CL-56 drift. Allocated VT-171. Status flipped to Queued. brief-ready signal queued for CC.
