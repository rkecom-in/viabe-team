---
id: VT-631
title: Per-agent trial + subscription billing model (₹5000/mo/agent, 1-month free trial per agent on activation)
status: PARKED — launch-prep track (pre real-owner send), NOT an objective-blocker. Fazal-authorized model; plan-first + Fazal-authorized merge when picked up. Do NOT self-merge.
priority: High
type: BUILD
area: billing / subscriber state machine
created: 2026-07-09
authorized_by: fazal
authorization_basis: "Fazal 2026-07-09 confirmed the model; Cowork 20260709T035000Z parked it as a separate pre-launch build, NOT to be done now (proceed on the objective instead)."
---

# VT-631 — per-AGENT trial + subscription (a state-machine reshape, NOT config)

## The model (Fazal 2026-07-09, exact — goes to real owners)
- ₹5,000/month PER specialised agent.
- Each agent includes a ONE-MONTH FREE TRIAL, granted the first time the tenant ACTIVATES that agent.
- Per-agent + activation-timed: the free month is independent per agent, starts WHEN that agent is activated
  (activate Marketing in month 5 → that agent's month 5 free). NOT a single tenant-wide trial tied to signup.
- Once per agent per tenant; after the free month → ₹5,000/mo for that agent (or it lapses). No repeat trials.

## The gap (reconciled 2026-07-09 — a BUILD, not config)
Current billing = CL-433 TENANT-WIDE: `transitions.py` is ONE phase machine per tenant (onboarding→trial→
subscribe→paid_active→lapsed). One 30-day trial per tenant, one tenant-wide subscribe. There is NO per-agent
trial/subscription state (grep-zero). The "activation" concept that exists (`onboarding_gate.is_agent_eligible`,
activation_registry) is ELIGIBILITY prereqs, NOT billing. VT-619 (agent_cost_limits/tenant_agent_usage) meters
per-agent COST, not trial/subscribe lifecycle.

## What the build needs (scope, when picked up — plan-first, Fazal-authorized, money-path → no self-merge)
- Per-agent trial state: trial_start stamped when each agent is first activated; one-time per (tenant, agent).
- Per-agent subscription lifecycle: each agent independently trial → paid | lapsed.
- Tenant↔agent phase reconciliation: define the tenant phase when agent A is in trial + agent B is paid (does
  CL-433's tenant `paid_active`/`trial`/`lapsed` still mean anything, or move to per-agent phases?).
- Per-agent Razorpay mapping: today `payment.captured` → the tenant-wide `subscribe` event; needs per-agent.
- Reconcile with CL-442 (GST hard-gate at signup) + CL-433 (no card in trial, no refund).

Gates REAL launch (VT-231 lineage), NOT the manager-intelligence objective. See `.viabe/manager-objective.md`.
The manager already STATES the trial (VT-630 grounding); nothing enforces it until this lands.
