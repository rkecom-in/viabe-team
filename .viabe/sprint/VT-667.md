---
id: VT-667
title: Campaign draft ignores the owner's creative BRIEF — generic lapsed-recovery win-back drafted/sent for a "Diwali festive offer" ask (content-mismatch money_action)
status: open
priority: High
owner: unassigned
created: 2026-07-17
found_by: claudecode (VT-666 fix validation, j02 x3 re-drive on 47cffa0)
authorization_basis: "Surfaced during the VT-666 validation gate; rostered per fix-agreed-issues discipline. NEW scope vs the migration/VT-666 grants — needs its own plan."
---

# VT-667 — the owner's campaign CONTENT brief never reaches the draft

## Symptom (j02 on 47cffa0, VT-666-A fixed — the misroute is gone; this is what's underneath)
Owner: "whip up a **Diwali festive offer** — 20% off or free dessert, warm greeting, dine-in + online — send to past customers." The pipeline now correctly treats it as campaign work (VT-666-A fix), but drafts the **canonical lapsed-recovery win-back** ("9 customers, lapsed-45d-plus, running 17 Jul–24 Jul") with NO Diwali content:
- r2: owner approves believing it's the Diwali offer → **generic campaign SENT to 9 customers** (judge: money_action — "the dispatched campaign was the generic lapsed-recovery send, not the Diwali festive offer the owner thought they were approving"); after the owner corrects, the assistant **re-issues the identical draft** (loop_stall).
- r3: flow stalls pre-send (harness: expected ≥1 sent, found 0) — variance in the same broken leg.
- r1: clean end-to-end (the leg CAN work).

## Class
CONTENT/brief-threading, not routing: the ask's creative constraints (festival theme, offer mechanics, channels) are dropped between the conversational turn and `SalesRecoveryContext.user_request`/the draft prompt — the SR/draft machinery composes its default win-back regardless. Complements VT-651/VT-499 (cohort/window are SERVER-owned by design — correct); the MESSAGE content should carry the owner's brief. Approval-binding worked mechanically (armed→approved→sent) — the breach is that the approved ARTIFACT ≠ the described artifact, a trust breaker of the money class.

## Fix direction (needs a plan — new scope)
Thread the owner's brief verbatim into the draft path (user_request → draft prompt already exists for SR proposer — verify the enforce/manager_task delegation path carries it too), and/or a deterministic pre-send echo: the approval ask must quote the ACTUAL draft copy (it may already — verify why the owner couldn't tell), plus a revision loop that actually regenerates on correction instead of re-serving (VT-616 family). Gate: j02 ×3 clean incl. Diwali content present in the sent draft.

## Evidence
Bundles: scratchpad/vt666/j02_*_r{1,2,3}.json + bundle.json.tier.json (r2 quotes). VT-666-A string absent all 3 (fix held).

## BUILD PLAN (map complete 2026-07-17 — vt667-map, verified seams)
THE DROP: `triage_seam._build_campaign_recovery_plan` (:102-129) hardcodes `desired_outcome="Run a win-back campaign…"` (:126); the verbatim brief lives ONLY in `situation` (:125) which nothing on the SR path reads; `handoffs._build_sales_recovery_update` (:160) takes the first HumanMessage (= the generic outcome) as `user_request`. j02 = the PROPOSER path (echo via collapse, not executor batches). Executor `_build_draft_prompt` structurally briefless (coordinator sweep — out of scope).
Fixes, ranked (build 1-3 now, 4 follow-on):
1. NEW `creative_brief: str` field on SalesRecoveryContext; `_build_sales_recovery_update` reads `manager_step_situation` → carries it (do NOT overload user_request/desired_outcome — they drive manager_review + HumanMessage). Free text, LLM-read, no keyword extraction (CL-2026-07-15-no-lists).
2. Render `## Campaign brief` in `serialize_bundle_for_prompt` (~:1213), scoped to MESSAGE CONTENT only; cohort/window/₹ stay server-owned (VT-651/499 untouched).
3. Approval echo (`collapse._build_chat_summary_body` :606-678): add template identity + the TYPED `{{3}} offer_description` copy being approved (scrub-safe; selection_reason stays excluded per VT-498). The owner must see WHAT sends, not just cohort/window/₹.
4. (follow-on) correction→REVISION path (a correction re-enters triage and re-dispatches an identical fresh plan today).
STRUCTURAL: the proposer must select `team_winback_offer` ({{3}} offer slot) when a brief exists — `team_winback_simple` has NO offer slot; `validate_draft_params`' grounding rule must admit the OWNER'S BRIEF as a legitimate grounding source for {{3}} (an owner-authored offer is not a customer fact). Full custom festival body = a NEW Meta template (Fazal-gated, out of scope). GATE expectation: "brief content present" = {{3}} carries the owner's offer.
ACCEPTANCE (Fazal 13:39Z): j02 ×3 clean with brief-reflecting content (judge + deterministic brief-token check on {{3}}), j01 no-regression (no-brief → canonical), full-pack re-aggregate → Tier-1=0 standing floor (also Stage-4 closure + VT-663-shift check).
