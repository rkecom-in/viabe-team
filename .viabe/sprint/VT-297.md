---
vt_id: VT-297
title: Ops Console — Telegram VTR bot (ops parity-by-function)
status: Review
priority: Medium
sprint: Sprint 2 - Owner Surface
type: Feature
area: [Orchestrator, Owner Surface, Infrastructure]
assignee: claudecode
parent: VT-189
blocked_by: VT-290
sub_items: []
exec_order: 8
related: [VT-189, VT-290, VT-298]
created: 2026-06-02
---

# VT-297 — Ops Console — Telegram VTR bot

## Why
Fazal: "all ops functions/analysis/actions can also be carried out by the VTR through
Telegram." A VTR is often mobile/away from the web console; Telegram gives parity-by-function.

## What
- Telegram bot exposing ops functions: alerts, quick-actions, simple queries.
- Confirmed split: alerts + quick-actions + simple queries on Telegram; deep analysis on
  web — but **every ops function reachable by some path** (parity-by-function, not
  pixel-parity).
- Actions route through the same orchestrator RLS/audit paths as the web console.

## Acceptance criteria
- VTR identity → assigned-tenant scope enforced on Telegram (RLS fail-closed; a VTR can't
  query/act on an unassigned business via the bot).
- A quick-action via Telegram writes through the same path + audit-log as the web action.
- PII de-identified in bot output (CL-426).
- (Bot token: sandbox/mock acceptable until the token lands — flag if needed.)

## Out of scope
- The autonomous watchdog→alert push (VT-298); web pages (VT-290..296).

## Notes
Build deferred behind launch queue. Reuses the existing Telegram MCP/bot plumbing if
present. One PR (CL-424).

## Implementation — 2026-06-03, Review (full bot, security-first; team-web hosted)

Fazal build-go (full bot). Identity-binding is the IDOR-crux; built + proven FIRST.
- mig 076 (operator_telegram += telegram_user_id, verified partial-unique = takeover guard);
  mig 077 (telegram_update_replay, deny-all).
- `lib/telegram/identity.ts` — resolveOperatorFromTelegram: verified binding → operator → UNCACHED
  TTL=0 revocation → role (Fazal-UUID→VTAdmin, no stored col) → assigned scope. Fail-closed.
- `lib/telegram/webhook-handler.ts` + `app/api/telegram/webhook/route.ts` — secret_token verified
  (constant-time, fail-closed if env unset) BEFORE body/DB; update_id replay dedup; identity-gated
  dispatch. Never 5xx.
- `lib/telegram/commands.ts` (read: /alerts /status /runs /help — scoped + de-id), `actions.ts`
  (/ack /resolve — IDOR-safe: reference resolved WITHIN the operator's scoped fetchEscalations set,
  acts on server-resolved tenant/id + ops_audit), `verify.ts` (/link single-use), `send.ts`
  (HTML-escape + clamp), `issuer.ts` + `app/(app)/team/ops/telegram/*` (VTR-authed link-code mint —
  closes the open-enrollment hole). Nav entry added.
- Tests: 39 team-web vitest (identity fail-closed incl. revoked/Fazal-VTAdmin, secret fail-closed,
  replay no-op, /link single-use + takeover, IDOR-safe actions, HTML-escape) + 4 real-PG canary
  (mig 076 verified-only resolution + takeover partial-unique). tsc/eslint/ruff green.
- FLAGGED: bot token (TELEGRAM_OPS_BOT_TOKEN) + webhook secret (TELEGRAM_OPS_WEBHOOK_SECRET) +
  setWebhook bootstrap are sandbox/mock until provisioned (sends/verify inert without them, fail-closed).

## Status history
- 2026-06-03: Review (full bot built security-first; 39 vitest + 4 real-PG canary; PR open →
  Cowork security re-review (IDOR-class) → Pillar-7)
