---
id: VT-602
title: "Lane sub-graph exceptions hang the run (DBOS retry loop, owner silence) — VT-492 convert-don't-orphan net + fix the non-consecutive-system-messages crash in the lane agent build"
status: Todo
priority: launch-blocker
type: BUILD
area: manager
created: 2026-07-04
authorized_by: fazal
authorization_basis: "Autonomous capability program — VT-598 live pack (consent_natural step 3 'go plan that Diwali campaign'): after VT-599 removed the UUID crash, the SAME path now dies on `ValueError: Received multiple non-consecutive system messages` in the marketing-lane agent invocation → unhandled out of graph.invoke → DBOS retry loop → the run never terminates and the owner gets SILENCE. vt599-builder flagged the structural gap: none of the six lane create_agent builds carry the VT-484 tool-error middleware, and nothing converts a lane-node exception to a clean terminal."
---

# VT-602 — no lane exception may ever hang the run

## Two parts
1. **Structural net (the priority):** any exception escaping a lane sub-graph node resolves to the
   clean 'escalated' terminal (the VT-492 convert-don't-orphan pattern — close_webhook_run records a
   terminal, maybe_escalate_support acks the owner; never a DBOS retry loop, never silence). Place it
   where dispatch/supervisor invokes lane nodes (or per-lane node wrapper) so the whole class is dead
   regardless of the specific exception.
2. **The specific crash:** `ValueError: Received multiple non-consecutive system messages` in the
   marketing-lane agent build (langchain message assembly — likely the lane's system prompt +
   a handoff/context injection both entering as system messages non-consecutively). Diagnose from
   the lane build (marketing_lane.py `create_agent` + the handoff update builder) and fix the
   assembly; regression test with the exact message shape that crashed.

## Evidence
Dev logs on 9e786c0: `dispatch_brain unhandled exception; DBOS will retry` +
`ValueError: Received multiple non-consecutive system messages` ×2 (retries), on the consent_natural
step-3 "great, now go plan that Diwali campaign" spawn_marketing path. Harness: run stuck
run_status='running' past 360s, zero owner reply.
