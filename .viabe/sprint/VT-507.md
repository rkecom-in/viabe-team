---
id: VT-507
title: "knowyourgst discovery fails SILENTLY in the live signup (RKeCom → couldn't find) — key + scrape reliability"
status: In Review
priority: Critical
type: BUG
area: team-orchestrator
created: 2026-06-30
authorized_by: fazal
authorization_basis: "Fazal 2026-06-30 LIVE signup 'RKeCom Services Pvt Ltd' → 'couldn't find your business' AGAIN. Verification standard = the LIVE deployed signup, NOT a canary. No claims without raw evidence."
---
# VT-507 — knowyourgst live-signup discovery (PROVE IT, raw evidence; Fazal HOLDING his run)
entity_match._knowyourgst_candidates (entity_match.py:185) runs FIRST but fail-soft: no SCRAPINGBEE_API_KEY → return [] (line 206); any scrape error → except→warning→[] (210). So a missing key / scrape flake / timeout / parse-drift → silent "couldn't find." ROOT CAUSE #1: SCRAPINGBEE_API_KEY was NOT set on Railway dev (CC set it by-reference + redeploy 2026-06-30). Then PROVE the deployed live signup surfaces GSTIN 27AAKCR3738B1ZE for "RKeCom Services Pvt Ltd" 5+ times with RAW evidence (ScrapingBee status + HTML + parse + candidates). Reconcile the 154700Z canary HONESTLY (did it truly scrape-and-parse, or chain a known GSTIN?). Harden: bounded retry; if the headless render_js scrape (120s) is too slow/fragile for inline signup, propose tighter/async/cached. NO claims without live evidence.

## RE-OPENED (Fazal failed AGAIN, key IS set): root cause is NOT the key (it works) — it's the FORM-PATH TIMEOUT. team-web /api/team/onboard/entity-candidates/route.ts → fetchEntityCandidates (orchestrator-client.ts:36) forwards with AbortSignal.timeout(_FORWARD_TIMEOUT_MS) [short] + no Vercel maxDuration; the knowyourgst scrape is ~90s uncached → the forward TIMES OUT → reason='timeout' → fail-closed {candidates:[]} → 'couldn't find'. My 5/5 hit the BACKEND endpoint directly (no timeout, waited 90s) — wrong path. FIX (plan-first, architecture): the 90s synchronous scrape can't sit in the signup request — async discovery (return fast + background fetch + poll/surface) OR cache-warm + tight inline timeout + a 'searching…' state. PLUS: track the discovery outcome+reason (Fazal's DBOS expectation — it's a sync request not a workflow, only a log warning today; add structured reason+impact). Prove on the REAL FORM path, not the backend endpoint.
