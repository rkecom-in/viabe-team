---
vt_id: VT-377
title: VTR Run-Control Panel Phase D — multi-VTR capability-complete (vtr_assignments + assignment-scoped views + Gap-6 Devanagari validator close)
status: Dispatched
priority: Critical
sprint: "Sprint-Fazal"
type: Feature
area: [Ops Console, Orchestrator, Privacy]
assignee: Claude Code
related: [VT-374, VT-375, VT-376, VT-381, VT-370, VT-281]
exec_order: 5
cl_decisions_checked: [CL-16, CL-79, CL-82, CL-88, CL-132, CL-191, CL-220, CL-252, CL-260, CL-278, CL-423, CL-424, CL-426, CL-428, CL-429, CL-431, CL-432, CL-434, CL-435]
created: 2026-06-12
last_updated: 2026-06-12
---

## Contract (plan §9 + CL-426 preconditions; Fazal ruling: capability-complete, NO human onboarding)

1. **`vtr_assignments` table** (allocator migration): operator_id ↔ tenant_id, granted_by,
   granted_at, revoked_at. RLS+FORCE, `_PURGE_ORDER` same migration, audited writes only.
2. **Assignment-scope ALL `vtr_*` views** (the CL-426 second-VTR precondition):
   `WHERE tenant_id IN (SELECT ... FROM vtr_assignments WHERE operator = current AND
   revoked_at IS NULL)` — across the Gap-6 five, `vtr_step_timeline`,
   `vtr_workflow_controls`, and `ops_top_tenants_today`. **VTR#1/admin tier keeps
   all-tenants** via the existing audited exception mechanism — not a bypass flag.
3. **Close the Gap-6 Devanagari validator gap** (the OTHER second-VTR precondition,
   pre-promotion list): the VTR-edit re-grounding validator must scan `text_hi` /
   Devanagari content (proper-noun blindness). This also CLOSES one tracked pre-promotion
   item — note it in the row on completion.
4. **Panel honors assignments**: tenant tiles list assigned tenants only (admin sees
   all); `require_vtr_action` already assignment-shaped — verify end-to-end with a
   real-DB two-operator test (operator A cannot see/touch operator B's tenant: views,
   timeline, mutations, ops_top_tenants_today).
5. **Capability-complete boundary:** NO onboarding flow, NO operator-creation UI, NO
   second human configured. The capability is proven by the two-operator TEST, not a
   second person.
6. **VT-381 RIDER** (closes the VT-376 gate conditions): F1 stale-'running' TTL/sweep +
   kill→TTL→rerun-succeeds test; F2 mig-133 existence-guarded anon/authenticated REVOKEs
   + the MULTI-VTR PRECONDITION note; F3 `ops_top_tenants_today` real-DB test (zero-run
   tenant listed, EXECUTE denied for app_vtr_role/anon).
7. Phase-C nits (422-button fallback, stale comment).

## Merge class

**RISK row — NOT self-merge** (RLS/view changes on PII surfaces + validator change).
plan-ack BEFORE build (view-scoping migration approach + validator fix approach +
two-operator test design). pr-ready → Cowork executed gate → Cowork-authorized merge.
3 real gates green. This is the BATCH-CLOSING row.

## Status history
- 2026-06-12: dispatched on VT-376 merge (0192d60) per CL-435 batch sequencing.
