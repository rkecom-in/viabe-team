---
id: VT-584
title: "Paced-flow sends record to conversation_log — close the harness/memory blindspot"
status: Done
priority: launch-blocker
type: BUILD
area: manager
created: 2026-07-03
authorized_by: fazal
authorization_basis: "Autonomous program (CL-2026-07-03-autonomous-capability-program); harness finding."
---

# VT-584 — flow-beat replies enter the lifetime log

## Finding
The harness read "all flows silent" — but the replies DID reach the owner's phone (mocked on dev). Cause: _send_turn → send_freeform_message with NO tenant_id, and paced-flow beats don't call _append_recent_turns → their turns never hit conversation_log. Real effect (beyond the harness blindspot): the readiness/integration/defer/all-set/pickup replies are ABSENT from the Team Manager's 24h window — re-fragmenting the very substrate VT-579 unified.

## Fix
_send_turn gains tenant_id (kwarg); flow beats pass it → the assistant line records to conversation_log (surface='journey'), fail-soft. The turn-brain path keeps recording via _append_recent_turns (no double-log). Regression: a flow-beat send is retrievable from conversation_log.active_window.

## Resolution (CC)
_send_turn gains tenant_id kwarg; 5 paced-flow beats pass it -> assistant line records to conversation_log (surface='journey', fail-soft). Turn-brain path unchanged (records via _append_recent_turns). Regression test proves a defer-beat reply is retrievable. 22 paced-flow tests green. Status: DONE, pending batch push.
