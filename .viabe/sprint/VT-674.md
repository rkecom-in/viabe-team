---
id: VT-674
title: On-demand agent-memory READ tool — L3 priors reachable mid-loop
status: open
priority: Medium
owner: unassigned
created: 2026-07-18
found_by: VT-669 capability-gap gate (Fazal 2026-07-18 "fail loudly")
authorization_basis: "Surfaced by VT-669's fail-loud sufficiency gate. Capability EXPANSION (follow-on), not a regression — build on a Fazal grant."
---

# VT-674 — on-demand agent-memory read tool

## Gap (`KNOWN_CAPABILITY_GAPS[on_demand_memory_read]`)
L3 priors are context-ASSEMBLED — pre-baked into the bundle the Manager hands down. Only L4 skills is
a callable tool. A specialist cannot query memory ON DEMAND mid-loop (e.g. "have we tried this play on
this cohort before"). The prior is frozen at bundle-assembly time.

## Build
Add a scoped memory-read tool (`read_agent_memory` / `query_memory`) — reads the anonymized L3 priors
(k-anon-gated aggregates; NEVER tenant/customer PII — same discipline as `knowledge/l3_construction.py`)
on demand. Catalog it (READ kind). This is a READ, not a write — no L3 mutation from a specialist.

## Gate / auto-close
Tool cataloged → gap auto-drops; delete `on_demand_memory_read` from `KNOWN_CAPABILITY_GAPS` in-PR.
Must preserve the L3 k-anon contract (≥ contributor floor per cohort; aggregates only) — a memory read
that could leak a single-tenant prior is a hard no.
