import { FoundingCounterWidget } from './founding-counter-widget'

/**
 * Viabe Team landing page.
 *
 * Lives here permanently — product-specific marketing belongs to Team.
 * Phase 1 scaffold: structure only, no business logic.
 */
export default function TeamLandingPage() {
  return (
    <main>
      <h1>Viabe Team</h1>
      <FoundingCounterWidget />
    </main>
  )
}
