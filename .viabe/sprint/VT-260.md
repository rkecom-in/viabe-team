---
vt_id: VT-260
title: Pre-push hook DB coverage (run orchestrator + migrations jobs vs local pg)
status: Backlog
priority: Medium
sprint: Sprint 1.5 - Hardening
type: Chore
area: [CI, DevEx]
assignee: unassigned
parent: null
sub_items: []
exec_order: 99
---

# VT-260 — pre-push hook DB coverage (Actions-diet root-cause fix)

Root cause of the ~17% CI PR-failure rate: the pre-push hook runs only the
dep-less smoke subset, NOT the orchestrator (DBOS+postgres) / migrations jobs —
exactly where failures land (vt-163 failed orchestrator ×2). Extend the hook to
run those against a local pg16+pgvector WHEN reachable (graceful-degrade like the
docker step). Plan: `.viabe/queue/VT-260/plan.md`. APPROVED by Cowork, HOLD build
for Fazal prioritization — Cowork leans BUILD THIS FIRST (root-cause). Ruling:
exclude the known-flaky test_dbos_auto_resumes_after_sigkill from the hook subset.
