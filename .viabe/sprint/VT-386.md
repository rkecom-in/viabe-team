---
vt_id: VT-386
title: PII redaction registry health/coverage — outage escalation (uncached fail-soft), name-blind Detector-5, runner webhook_received gap
status: Queued
priority: Medium
sprint: "Sprint-Fazal"
type: Bug
area: [Orchestrator, Privacy]
assignee: unassigned
related: [VT-379, VT-305]
cl_decisions_checked: []
created: 2026-06-12
last_updated: 2026-06-12
---

## Source: VT-379 gate conditions (2026-06-12)

1. A permanently-broken name registry = log-only visibility (the fail-soft path is
   UNCACHED — per-write warning, no alert escalation); Detector-5 is name-blind
   (phone/digit patterns only) so name leakage during an outage never fires `pii_in_log`.
   Fix: registry-failure counter → alert trigger (CL-428 discipline: Literal + CHECK in
   the same PR if a new kind), or fold a name-scan sample into Detector-5.
2. `runner.py` `record_webhook_received` writes input_envelope pattern-redacted with NO
   name registry — same gap class.

## Status
Queued — rides the next batch with spare capacity (Cowork-sequenced; in the CL-438 batch
spirit but not gating it).

## Status history
- 2026-06-12: rostered from the VT-379 gate verdict.
