---
vt_id: VT-247
title: Document deploy topology in CLAUDE.md (drift-prevention)
status: Done
priority: Medium
sprint: Sprint 2 - Owner Surface
type: Docs
area: [DevOps, Docs]
assignee: Claude Code
parent: VT-CI
exec_order: 1
branch: "docs/vt-247-deploy-topology"
version: "v1.0"
created: 2026-05-30
last_updated: 2026-05-30
authorization_basis: "Fazal 2026-05-30: redundant railway-up debugging cost time because deploy topology wasn't written down. Prevention: document it."
---

# VT-247 — document deploy topology

## Why
The VT-246 railway-up redundancy cost debugging time because the deploy
topology lived only in the Railway dashboard — no fresh session could
reconcile against it. Fix: a "## Deploy topology" section in CLAUDE.md so
"the deploy is failing" first checks the documented ground truth.

## Change
Added "### Deploy topology (ground truth, 2026-05-30)" to CLAUDE.md (under
the Merge-workflow notes):
- Orchestrator → Railway native GitHub auto-deploy (no CLI `railway up`,
  no RAILWAY_TOKEN; removed VT-246).
- team-web → Vercel CLI job in deploy-dev.yml.
- deploy-dev.yml = pre-deploy-checks + Vercel; keep green so Railway's
  "Wait for CI" lets the native orchestrator deploy fire.
- Discipline: check topology before fixing a redundant deploy step.

## Merge
Docs-only. ci.yml paths-ignored for CLAUDE.md → 0 heavy jobs. No protection
→ merge after read-back.
