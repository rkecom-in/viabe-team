---
id: VT-690
title: Observability → Honeycomb EU (drop Logfire SaaS; keep logfire lib for LLM I/O capture)
status: In Progress (core PROVEN on dev; prod key + phase-2 pending)
priority: High
exec_order: 30
created: 2026-07-20
---

## What (Fazal 2026-07-21: day-1 observability of agent decision-making)

Swap the tracing BACKEND from the paid Pydantic Logfire SaaS to Honeycomb EU (free tier), keeping
the `logfire` LIBRARY (free/OSS) for its best-in-class auto-instrumentation. Single chokepoint:
`observability/logfire.py` → `configure_logfire()` now runs `logfire.configure(send_to_logfire=
False, additional_span_processors=[BatchSpanProcessor(Honeycomb OTLP)])`. Enable-switch:
`HONEYCOMB_API_KEY` (was `LOGFIRE_TOKEN`). Captures: DBOS workflow/step spans (global provider
pickup), `instrument_anthropic` (full LLM prompt->response = the agent's thinking I/O),
`instrument_pydantic`. EU endpoint `https://api.eu1.honeycomb.io`; dataset derived from
service.name; content shipped (Fazal ruling), logfire default scrubbing keeps our own secrets out.

## Status

- Core LANDED: logfire.py rewritten + dbos_config comment + 11 unit tests green + ruff clean.
- Canaries: `canaries/vt690_honeycomb_span.py` (wire proof), `LOGFIRE_TOKEN` unset on prod.
- **ROOT CAUSE (Fazal, 2026-07-21): the console's "Key Secret" is ALREADY {id}{secret} concatenated**
  — so id+secret concatenation DOUBLED the id (96 chars) and 401'd. Fix = use the SECRET ONLY
  (64 chars). (The doc's "concatenate id+secret" is for the API-create response where `secret` is
  the raw tail; the console pre-concatenates.) dev HONEYCOMB_API_KEY re-set to secret-only.
- **WIRE PROOF PASSED on dev (2026-07-21)**: canary direct OTLP export -> SpanExportResult.SUCCESS
  (Honeycomb EU accepted the span, HTTP 2xx). Key is an EU key (eu1 endpoint correct; us 401s).
- **SECURITY**: the dev ingest key SECRET was pasted into chat during debugging -> ROTATE it (dev
  ingest keys are write-only/low-blast, but exposed). Regenerate + re-set secret-only after.
- **PROD**: Fazal to set prod HONEYCOMB_API_KEY = its OWN key's SECRET ONLY (currently the doubled
  id+secret = will 401). Authorized action.

## Next

- Fazal re-sets dev HONEYCOMB_API_KEY -> re-run vt690 canary -> PASS + marker visible in Honeycomb
  EU -> then prod key (authorized). 
- **PHASE 2 LANDED (2026-07-21)**: `tm_audit._mirror_to_span` mirrors every decision-audit event
  (GETS/KNOWS/DECIDES/DOES + summary/decision/reasoning_ref/action/result) onto the CURRENT OTel
  span as a redacted `tm_audit.<layer>.<kind>` event — so "why the agent decided X" (incl.
  reasoning_ref) rides the Honeycomb trace beside the LLM prompt/response (instrument_anthropic) +
  DBOS workflow spans, correlated by run_id. Same _redact_fields as the DB write; best-effort
  (never touches the audit path); no-op when tracing off / no recording span; large fields capped
  at 4KB. 4 unit tests + 77 consumer tests green. Live Honeycomb eyeball of a real journey trace
  = Fazal confirm once dev redeploys.
