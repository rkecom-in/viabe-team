---
id: VT-500
title: "Win-back grounding substrate — L4 kirana-winback benchmark + offer-elicitation + N=1 ARRR strictness (PRODUCT call)"
status: Plan-Review
priority: Critical
type: BUILD
area: team-orchestrator
created: 2026-06-30
authorization_basis: "Re-drive after VT-499: parse closed; self_evaluate correctly REVISEs→insufficient_data because expected_arrr can't be defended (N=1, no L3/L4 benchmark, no owner offer). The gate WORKING, not a defect — reaching an armed send needs grounding substrate + an owner offer (product/content), or a strictness decision."
---
# VT-500 — win-back grounding (the gate is CORRECT; this is product/content + a strictness call)
After VT-499 the win-back plan PARSES + reaches self_evaluate, but the gate REVISEs→insufficient_data: expected_arrr not defensible (N=1 cohort, 'no L3/L4 benchmarks'), empty attribution, empty template_params (no owner offer). This is the gate correctly refusing to ship a FABRICATED campaign. To reach an armed send, NOT a code bug fix:
- (a) seed an L4 'kirana_winback_recovery_benchmark' skill (domain grounding for recovery rates) — content/infra.
- (b) run the conversational offer-elicitation (the agent asks the owner 'what offer?') so template_params has a real offer.
- (c) PRODUCT CALL: should self_evaluate soften expected_arrr strictness for a small/N=1 cohort, or is 'insufficient_data until grounded+offered' the correct terminal? Fazal/Cowork decide.
DEPENDS ON: a Fazal/Cowork product decision (not autonomous — it's the money-path campaign-quality bar).

## FAZAL CHOSE (2026-06-30, Cowork 065500Z): 'Calibrate gate to campaign scale' — grounding proportional to CLAIMS + SCALE; safety rails UNCHANGED. SIMPLE win-back (team_winback_simple, no offer, cohort<=threshold): ships on genuinely-lapsed (VT-485) + consented + eligible + message grounded to bundle facts; expected_arrr NOT a ship-blocker. OFFER/LARGE (team_winback_offer/money_bearing OR cohort>threshold): KEEP full ROI/ARRR + owner offer + always-confirm floor. self_evaluate branches on (template_type, cohort_size). STAYS untouched: consent/opt-out/PII-scrub/onboarded/send-checkpoint + anti-fabrication grounding (no invented facts/offer). Plan-first → Cowork gate → build → re-drive.
