---
id: VT-681
title: O10-1 — per-tenant capability registry, ENFORCED AT THE PROMISE (live/advisory/disabled, resolve_for(tenant, env))
status: In Progress
priority: High
owner: claudecode
created: 2026-07-18
found_by: O10-1 ground-truth scout (2026-07-18, dev 42bd7e6) — verdict distributed-with-gaps
authorization_basis: "Cowork 223500Z (O10 addendum): 'if gaps, roster the closing row, size it.' Rostered + sized; BUILD starts on the O-drive sequencing (after O2/O6/O5 per 220837Z order) unless Fazal pulls it forward."
---

# VT-681 — the B-track capability contract does not exist yet; build it (size: L, 4 phases)

## Ground truth (scout, code-verified)
The contract — "a capability registry declares, per function and tool: live/advisory/disabled mode,
prerequisites, effect class, policy rail, verifier, rollback, environment availability; the manager
may promise only capabilities marked live for that tenant and environment" — is ABSENT as a unified
enforced property. `capability/registry.py` (VT-528) is the only contract-shaped artifact and it is
a 2-entry DEAD scaffold (wrong mode taxonomy = autonomy posture; NO tenant axis; zero live callers —
workflow.py:154 self-describes it as dead). The live pieces are distributed and POST-promise:
activation (per-agent, 2 agents) · tool_catalog (inert inventory) · advisory_registry (static) ·
roster (flag-not-tenant filtered) · entitlement (SOFT-OPEN — always True, billing unwired) ·
_validate_step + GateFacade chokes (real, per-tenant, fail-closed — EXECUTION is safe).
**The gap is HONESTY-AT-PROMISE:** a disabled capability can be verbally promised, then silently
blocked at execution. One hand-rolled instance exists (D2 capability_disclosure net).

## Phases (one coherent PR each; ONE row)
1. **(M)** Registry entries 2 → the promise-relevant capability set (join to the tool catalog /
   3 specialists / advisory lanes). Keep the CapabilitySpec shape; fix the taxonomy.
2. **(M)** `mode` → real live/advisory/disabled tri-state + `resolve_for(tenant, env)` joining
   is_agent_eligible + entitlement + freeze/kill state. (Registry today is env-only by design.)
3. **(M/L)** The PROMISE seam: per-tenant filter of the Manager's tool/spawn/advisory surface +
   a capability-truth context slice, generalizing the D2 net. Brain-touch → ×3 rail.
4. **(S)** Consolidate `_validate_step` onto the registry (its flagged future) or delete the dead
   scaffold — no two sources of capability truth.

## Notes
- Entitlement soft-open (always True) is a NAMED sub-gap — phase 2 must not silently launder it as
  "resolved"; keep it soft-open but visible in resolve_for's reasoning until billing wires.
- Safety framing honest: execution gates already fail closed; this row buys promise-honesty, not
  send-safety.

## Log
- 2026-07-18: Phase 1 LANDED (local commit): entries 2→14 (3 live agents + live integration
  Tools + 5 advisory functions + D2 ad-boost declared-DISABLED), mode taxonomy →
  live/advisory/disabled, 4 new deterministic verifiers, graduation-ratchet invariants
  (advisory-mode never effectful; empty envs only disabled; disabled defers verifier never the
  rail). 19 tests. No live callers (phase 3 wires) — boot validation is the only behavior change.
- 2026-07-18: Phase 2 LANDED (local commit): resolve_for(tenant, env) — mode × env × activation
  (fail-closed) × entitlement (called live, soft-open ALWAYS a visible reason — the named
  sub-gap honored); freeze/kill recorded as absent-subsystem reason (none exists tree-wide,
  audited). resolve_all_for = phase-3's one-call read. 26 tests total.
- NEXT: phase 3 (promise seam — brain-touch, ×3 rail) + phase 4 (consolidate _validate_step or
  delete scaffold note — scaffold is now ALIVE, so phase 4 = consolidation, not deletion).
