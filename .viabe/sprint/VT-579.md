---
id: VT-579
title: "Conversation memory architecture — lifetime log + 24h/20-turn active window in the Team Manager's context (mig 164)"
status: In Progress
priority: launch-blocker
type: BUILD
area: manager
created: 2026-07-03
authorized_by: fazal
authorization_basis: "CL-2026-07-03-conversation-memory-architecture (Fazal, Standing, verbatim spec)."
---

# VT-579 — the conversation becomes memory

## Honest current state
No lifetime transcript exists (inbound bodies PII-redacted at persistence; fragments scattered across owner_message_audit/notifications). The Team Manager dispatch has ZERO conversation window — every turn is amnesiac beyond context blocks. Onboarding's recent_turns (mig 162) was the local fix.

## Scope
Mig 164: `conversation_log` (tenant-scoped, RLS+FORCE, DSR-erased; role/text/message_sid/created_at; indexed tenant+created). Writers at the chokepoints: inbound (post-prefilter, real conversation only) + every owner-facing send seam. Active window: last ≤20 turns ∩ 24h ALWAYS injected into dispatch_brain's context (per-turn SystemMessage after the cached prefix, GETS-audited, snapshot-hashed — the directive/lessons idiom); the onboarding turn brain reads the SAME substrate (journey window becomes a view over it; mig-162 column kept read-compatible during transition). Brain-commanded retrieval: manager tool `search_conversation_history(query|range)` over the lifetime log. Compaction: VT-571 distiller pattern on window overflow → durable summary. Effect gates untouched.
