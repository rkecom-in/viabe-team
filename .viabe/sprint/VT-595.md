---
id: VT-595
title: "Analytical questions mis-route to status_query raw count — 'which customers stopped buying?' → 'You currently have 8 customers in your ledger.'"
status: Done
priority: high
type: BUILD
area: manager
created: 2026-07-04
authorized_by: fazal
authorization_basis: "Autonomous capability program — P2/P1 boundary. The classifier tags an analytical/action question as status_query and short-circuits to a raw count."
---

# VT-595 — status_query short-circuit swallows analytical intent

## Finding (deployed-dev, seeded tenant)
"which of my customers have stopped buying?" → edge_cases_router Haiku classify → status_query → owner_inputs/status_query.answer_status_query → literal "You currently have {n} customers in your ledger." (status_query.py:55). The owner asked WHICH customers are LAPSED (an analysis the Sales-Recovery lane owns); it got a raw total count. Keys on the word "customers".

## To do
Either (a) the classifier must not route analytical/action phrasings ("which stopped buying", "who's gone quiet", "win them back") to status_query; route them to sales_recovery / the brain; or (b) status_query's answer must itself surface the lapsed cohort, not a bare count. Prefer routing to the lane that owns the analysis. Keep the pure "how many customers do I have" count for genuine status asks.

## RESOLVED 2026-07-04 — landed on local dev @ 65beafa (option a, refined)
- New `business_analysis` label (Classification Literal + v4 classifier prompt,
  classify_owner_message_v4.md): WHICH/WHO/WHY analysis asks. status_query tightened to pure
  count/fact lookups; boundary guidance + worked examples (incl. Hinglish) added. "win them back"
  stays adhoc_campaign_request.
- Zero router change needed: edge_cases_router's default `return None` falls business_analysis
  through to the manager brain (the lane owner), with the classification riding intent_sink as the
  brain's prior. Deliberately NOT in _ROUTINE_INTENTS → opus-tier brain for delegation turns.
- Consumers swept: approval_resume decision map correctly falls to None; pre_filter_gate's
  status_query string is an unrelated regex concept; VT-146 owner_input_classifier untouched.
- Tests: label membership, router fall-through (the exact regression phrase), status_query
  fast-path regression, opus tiering; 92 passed. LIVE Haiku behavior validated in the VT-598
  deployed-dev pack (drive both phrasings, assert routing from logs).
