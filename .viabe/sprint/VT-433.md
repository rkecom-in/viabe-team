---
vt_id: VT-433
title: Enable dev public signup (ENABLE_PUBLIC_SIGNUP) + wire the inbound webhook dev-secrets — CL-422 internal-only
status: Queued
priority: Critical
sprint: "Sprint-Fazal"
type: Feature
area: [Backend, Frontend, Onboarding, Webhooks, PII, Launch]
related: [dev-activation, CL-422, VT-422]
exec_order: 102
created: 2026-06-25
last_updated: 2026-06-25
---

## Contract (Cowork GATE 2026-06-25, dev-activation Top-3 #3 BLESSED with CL-422 nuance). Needed for the genuine Sundaram e2e (real signup→onboard). Risk(PII) → Cowork gate.

- Set `ENABLE_PUBLIC_SIGNUP=true` (+ `ENABLE_WAITLIST_CAPTURE` per Cowork) on dev so the inbound signup→onboard→pipeline
  path runs. **CL-422 / HARD RULE:** dev signup uses ONLY Fazal-controlled INTERNAL identities (no fabricated numbers,
  no real external customer data on Seoul-dev) — state in the row; Fazal provides the identities.
- Wire the 4 inbound webhook dev-secrets (NAMES, by-reference, sealed — verify functionally):
  - Twilio inbound (team-web): `TWILIO_AUTH_TOKEN` (signature validation) — Fazal-provided.
  - Razorpay (team-web/Vercel): `RZP_WEBHOOK_SECRET` (fallback `TEAM_RAZORPAY_WEBHOOK_SECRET`) — Fazal-provided.
  - Shopify (orchestrator): `SHOPIFY_API_SECRET` (HMAC, VT-422) — Fazal-provided.
  - Telegram ops (team-web): `TELEGRAM_OPS_WEBHOOK_SECRET` — Fazal-provided OR CC-dev-set.
  - + the orchestrator↔team-web forward: `INTERNAL_API_SECRET` — CC-dev-set OR Fazal.
  CC sets the flags (config); Fazal sets the sealed secrets (Cowork relays which). NEVER fabricate a secret value.

## Gate
RISK (PII / inbound). Plan/config → Cowork gate. The Fazal-set secrets relayed by Cowork.

## Status history
- 2026-06-25: Rostered + ID allocated (Cowork dev-activation Top-3 #3, BLESSED with CL-422 internal-only nuance).
