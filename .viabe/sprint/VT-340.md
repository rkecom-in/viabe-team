---
vt_id: VT-340
title: PR-check cleanup — e2e-playwright + pr-title red on PRs (zero deploy impact)
status: Backlog
priority: Low
sprint: Sprint 8 - Owner Surface & Billing
type: Chore
area: [Infra, CI]
assignee: unassigned
parent: VT-245
sub_items: []
exec_order: 9.2
blocked_by: []
tags: [ci, test-hygiene]
---

# VT-340 — green the e2e-playwright + pr-title PR checks

`e2e-playwright` + `pr-title` are red on recent PRs (seen on #351). Both are `pull_request`-only
(ci.yml + pr-title.yml) — they do NOT run on a push to main, so they are NOT in Railway's
wait-set and have ZERO deploy impact (confirmed 20260605T182027Z). But a red PR check is noise
+ erodes the green-signal discipline.

Scope: diagnose + fix (or mark non-required / paths-ignore) e2e-playwright (likely a stale
selector or a missing dev-server step) + pr-title (the title-format lint — likely the numeric
VT-title convention vs the conventional-commits matcher).

LOW priority — PR-signal hygiene only (Cowork 20260605T182400Z). Behind the feature queue.

## Origin
Surfaced during the VT-87 PR-1 #351 / deploy-topology diagnosis.
