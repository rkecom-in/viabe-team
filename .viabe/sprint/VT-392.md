---
vt_id: VT-392
title: CI test-isolation flake — test_vt384_l3_wire_realdb leaks freeze/sweep state in full-suite order
status: Backlog
priority: Medium
sprint: "Sprint-Fazal"
type: Bugfix
area: [Observability, Agents]
assignee: Claude Code
related: [VT-384, VT-389]
exec_order: 60
created: 2026-06-13
last_updated: 2026-06-13
---

## Contract (rostered 2026-06-13 — CC-flagged during the gap-closure batch)

**Recurring CI flake (gate-blocking).** `tests/.../test_vt384_l3_wire_realdb.py::
test_execute_item_l2_tenant_still_l2_arms` fails ONLY in the full-suite CI run with
`'cancelled' == 'awaiting_approval'`. Proven order/isolation-dependent, not a real
regression: the test (and the file under any single-PR change) passes 35/35 fresh; the
full VT-384 wire file is 33/33 fresh. The failure surfaces only cross-file in full-suite
order — a prior test leaks state that cancels a fresh L2 tenant's batch.

**Prime suspect:** the owner-level opt-out freeze (VT-384, `kill_autonomy_by_keyword`)
cancelling ALL of a tenant's agents/open batches — a leaked freeze/sweep from an earlier
test bleeds into this one's tenant. Same flake CLASS as the earlier N2 kill-recover one.

**Fix direction (CC's call, log it):** isolate the realdb test's tenant/freeze state —
per-test tenant fixture teardown, or assert-and-reset the autonomy/freeze tables between
tests, so no prior test's freeze/cancel leaks. Do NOT weaken the production freeze
semantics to make a test pass.

## Acceptance
- The full orchestrator suite is green across repeated full-suite runs (no order-dependent
  cancel leak); the named test passes in full-suite order.
- A teardown/isolation assertion proves no freeze/sweep state crosses test boundaries.
- No change to VT-384 production autonomy-freeze behavior.

## Notes
- It already cost VT-389 two CI flakes this batch (greened on re-run). Worth fixing before it
  ambushes more PRs. Backlog — awaits Fazal prioritization.

## Status history
- 2026-06-13: Rostered (Cowork) from CC's gap-closure-batch flake flag (signal 20260613T183000Z).
