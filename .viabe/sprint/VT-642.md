---
id: VT-642
title: "Co-present 'send me the list' dropped when the win-back D3 net fires — honest can't-attach-names-yet ack (CD2 interim)"
status: Built + DEV-VERIFIED (honest ack live, f373f35, all 3 runs). Residual Tier-1 flag = a Fazal product/objective decision, NOT a manager bug — see "Dev result" below.
priority: Critical
type: Bug
exec_order: 1
owner: claudecode
created: 2026-07-15
source: "10-journey e2e sim j08 (Devanagari services), grounded in the deployed-dev j08_redrive transcript — Tier-1 ignored_speech_act NOT covered by VT-641"
---

## Problem (grounded in the deployed-dev j08 transcript, all 3 runs)

j08 turn-2, owner (Devanagari): "हां, वो 6 वाली लिस्ट भेज दो मुझे। और … वापस-लाने वाला ऑफर भी तैयार कर लो
— लेकिन अभी किसी को भेजना मत" = **"yes, SEND me that list of 6, AND prepare a win-back offer, but don't
send yet."** Two speech-acts: (a) send-me-the-list, (b) prepare-the-draft.

The D3 campaign net (`is_campaign_plan_imperative`) fires on the draft imperative → mints the SR dispatch
→ `skip_legacy_dispatch=True`. The async SR draft answers (b) — "I've drafted a campaign for 6…" — but
(a) the list-send request **vanishes silently**. That dropped speech-act is the Tier-1 breaker
(#4 ignored_speech_act) the blind judge flags on j08 3/3 (distinct from the VT-641 mis-route, now fixed).

Root cause: the D3 net owns the turn but only ever emits the campaign draft; a co-present list-send ask
has no path to the owner. We genuinely can't attach the individual customer names in chat yet — the
file-attachment path is **CD2 / VT-79 (still pending)** — so the honest move is to ACKNOWLEDGE the
list-send request rather than drop it (never a name dump; never a false "here they are").

## Fix (built — deterministic, single seam, money-gate untouched)

`onboarding/campaign_first_contact.py`:
- `mentions_customer_list_request(text)` — a co-present list/names cue detector (EN token set
  {list, lists, names, naam…} + Devanagari `keyword_match` pats {लिस्ट, सूची, नाम…}). Applied ONLY after
  `is_campaign_plan_imperative` already matched. FAIL-OPEN → False.
- `LIST_SEND_ACK_PREAMBLE` — honest interim copy: can't attach the individual names in chat **just yet**,
  confirms the cohort is in hand, bridges to the draft the owner also asked for, re-affirms the money gate
  ("nothing goes out until you say so"). Never a completion claim.

`manager/triage_seam.py` D3 real-cohort branch: when `mentions_customer_list_request(message_text)`, set
`direct_reply_text=LIST_SEND_ACK_PREAMBLE` on the returned `TriageSeamResult`. The runner emits it via the
replay-safe checkpointed `_send_owner_reply_step` (@DBOS.step, at-most-once) BEFORE dispatch; the async SR
draft still emits independently. So the owner gets **(1) the honest list-ack, then (2) the campaign draft**.

Why this is low-risk: it changes a REPLY STRING only — no plan/collapse/money-gate change. The send still
routes through the approval/consent/opt-out rails (`expect_sent_count=0` must still hold). The list-cue path
is strictly better than the pre-fix D1 "I'm on it" fallback (more honest + specific + addresses the drop);
the NON-list-cue path (`direct_reply_text=None`) is byte-identical to pre-VT-642.

## Verify
- 16 unit tests pass (predicate positives/negatives across EN/Hinglish/Devanagari + copy contract) + ruff
  clean + triage_seam suites green (5 passed, 22 dep-skipped).
- **PENDING: dev re-drive j08 x3** — transcript must show the honest list-ack AND the campaign draft AND
  `expect_sent_count=0` held; blind re-judge must clear the ignored_speech_act on j08.

## Dev result (f373f35 deployed, j08 x3, blind sonnet-5 re-judge)
The honest ack is LIVE and DETERMINISTIC — identical across all 3 runs:
> "I can't send the individual customer names as a list here in chat just yet — but I've got your
>  lapsed cohort and I'm drafting the win-back offer for them now. You'll see it to approve in a
>  moment, and nothing goes out until you say so."
- `expect_sent_count=0` held (0 db_assert failures). No fabrication, no silent drop.
- BUT the blind judge still flags 2/3 runs `ignored_speech_act`; the 3rd run — IDENTICAL manager
  behavior — scored CLEAN. So the residual is **judge variance on a rubric ambiguity**, not manager
  variance. Judge's `why`: "owner asked to receive the list first; the assistant deflected the direct
  request … instead of DELIVERING or clearly SCHEDULING it."

## This is a Fazal decision, not a manager bug (manager-gate-residual-is-product-decisions)
The owner's explicit ask — SEND THE NAMES LIST — genuinely cannot be FULFILLED right now:
1. CD2/VT-79 (the actual list delivery = WhatsApp file attachment) is unbuilt.
2. Your CD2-interim decision is "never dump raw customer names inline" (privacy) — so inline delivery
   is off the table without your override.
3. No lapsed-customers PORTAL view exists to link out to (only a generic `_DASHBOARD` pointer) — so a
   link-out would be dishonest ("see the list at <link>" when the link has no list).
So there is NO clean decision-independent manager fix. The manager is already doing the right honest thing.

The `ignored_speech_act` rubric has NO honest-capability-decline exemption (unlike `loop_stall`, which
explicitly exempts an interim-ack-then-delivery). An honest "I can't do X yet, here's what I can do" is
neither deception (fabrication) nor over-promise (impossible_promise) — it is the EXACT honest, bounded
behavior the objective is trying to produce, yet the rubric literally reads it as "didn't get the list."
Counting honest declines as Tier-1 makes Tier-1=0 require building EVERY capability a persona might ask
for — an impossible / wrong bar that penalizes honesty.

### DECISION NEEDED FROM FAZAL (pick one; I did NOT touch the judge to pass my own fix)
- (A, recommended) Rule that an HONEST capability-decline is Tier-2 (quality), NOT a Tier-1 trust-breaker.
  Then I add an exemption nuance to the `ignored_speech_act` rubric mirroring the `loop_stall` one. This
  is an objective-DEFINITION change (yours), not me gaming the scorer.
- (B) Build CD2/VT-79 now (real list delivery — file attachment OR a portal lapsed-view + link-out) so
  the ask is actually FULFILLED. Bigger, and needs your privacy ruling on the delivery form.
- (C) Approve inline names delivery (overrides the CD2-interim privacy hold). Fastest to "fulfilled" but
  reverses your privacy decision.

## Relationship to CD2 / VT-79
This is the honest INTERIM. The full fix (actually deliver the customer list — WhatsApp file attachment,
verified-owner + tm_audit) is VT-79/CD2, still pending. VT-642 ensures we never SILENTLY DROP the ask
while CD2 is unbuilt.

## Notes
Distinct from VT-641 (the Devanagari MIS-ROUTE, fixed d9f5506). VT-641 restored classification+delegation;
VT-642 handles the SECOND, co-present speech-act the delegation path swallowed.
