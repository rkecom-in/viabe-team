---
vt_id: VT-212
title: VT-207 PR-2 — real Google OAuth manual walk + integration verification
status: Done
priority: High
sprint: Sprint 2 - Integration Agent (re-anchored)
type: Feature
area: [Integration Agent, OAuth, Verification]
assignee: Claude Code
parent: VT-207
sub_items: []
exec_order: 8
branch: "feat/vt-212-oauth-manual-walk"
version: "v1.0"
last_updated: 2026-05-29T04:35:00+05:30
done_note: "OAuth substrate validated end-to-end against dev Railway env (token persisted; scope correct; callback 200). Apps Script step abandoned per CL-421 (zero-manual-paste connectors). Sheet integration redesign filed as VT-222."
---

# VT-212 — VT-207 PR-2: real Google OAuth manual walk

## Why this row exists

VT-207 PR-1 shipped the Sheet connector substrate (OAuth code paths + Apps Script template + HMAC verify + 5/5 canary). But canary asserted structural correctness against canned data — real Google OAuth round-trip was deferred.

This row closes the gap with a Fazal-driven manual walkthrough:
1. Fazal hits `/api/orchestrator/integrations/google_sheet/setup` (or web /team/onboard once VT-211 ships)
2. Redirected to Google OAuth consent screen
3. Grants access
4. Callback exchanges code → refresh_token → encrypted store
5. Sample pull on Fazal's dummy RKeCom Sheet returns rows
6. Apps Script template generated; Fazal pastes into Sheet; onEdit fires push; row lands in DB

## Scope

- Manual test session with Fazal driving the OAuth flow
- Document any code-path issues caught during real walk
- Fix bugs surfaced (if any)
- Re-run canary post-fix
- Update VT-207 status to Done after successful walk

## Acceptance criteria

1. Real Google account grants OAuth → refresh_token stored encrypted in `tenant_oauth_tokens`
2. `pull_sample` returns rows from Fazal's actual dummy Sheet
3. Apps Script template generates correctly; Fazal can paste + save in Sheet Extensions
4. `onEdit` event from Sheet → orchestrator endpoint → HMAC verifies → row lands in DB tagged `acquired_via='google_sheet'`
5. Re-auth flow works when refresh_token expires (simulated by clearing the row)

## Rule #15 Canary

No new canary file. Re-runs `vt207_sheet_connector.py` post-fix + manual verification log appended to `.viabe/queue/VT-212/task_log.md`.

## Dependencies

- VT-207 ✓ (PR-1 substrate on main)
- `.viabe/secrets/google-oauth.env` ✓ (Fazal dropped)
- Fazal availability for ~30 min manual walkthrough

## Branch & PR

- Branch: `feat/vt-212-oauth-manual-walk`
- PR title: `feat(integration): VT-207 PR-2 OAuth manual walk + fixes (VT-212)`

## Status history

- 2026-05-28 10:05 IST: filed by Cowork as VT-207 PR-1 deferral closure
