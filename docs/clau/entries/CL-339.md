---
cl_id: CL-339
created: 2026-05-22T13:30:32.465Z
notion_legacy_id: 368387c2-cc5a-81a5-8241-ecfda967940e
title: "CORRECTION (supersedes the 'existing no-raw-body architecture' premise in CL-330 and the CL-337 brief): CL-330 — Fazal's authoritative owner_inputs Type 3 decision — states the codebase has an 'existi"
---

# CL-339 — CORRECTION (supersedes the 'existing no-raw-body architecture' premise in CL-330

CORRECTION (supersedes the 'existing no-raw-body architecture' premise in CL-330 and the CL-337 brief): CL-330 — Fazal's authoritative owner_inputs Type 3 decision — states the codebase has an 'existing deliberate drop of the Twilio Body field' and that owner_inputs 'preserves the existing no-raw-body architecture intact.' Claude Code Step-0 ground-truth against main (git clean, HEAD post-019) proves this premise is FALSE. Raw owner-message text IS persisted on main today, plaintext, in TWO JSONB columns: pipeline_runs.trigger_payload and pipeline_steps.input_envelope. Trace: twilio_ingress.py:149-179 forwards full twilio_fields incl Body -> runner.py:158 captures body=str(fields.get('Body','')) into WebhookEvent.body -> runner.py:222-224 'tokenised = event.model_dump()' redacts ONLY sender_phone via hash_phone, body left plaintext -> runner.py:226/227 persists the tokenised dict (incl raw body) into both JSONB columns. The variable name 'tokenised' implies redaction; implementation redacts only the phone. CONSEQUENCE: (1) CL-330's structured-intent / no-raw-bodies TARGET architecture remains sound, but its rationale 'preserves existing no-raw-body architecture' has no existing architecture to preserve — there is a live raw-body RETENTION leak predating any owner_inputs work. (2) CL-337 brief Component 2's guarantee ('raw body never persisted, exists only in request-scoped variable') CANNOT be honored on top of main as-is — the body is persisted twice before any extraction logic runs. (3) The privacy notice must now describe THREE states not two: RETAINED (trigger_payload + input_envelope today), TRANSMITTED (to Anthropic via new extraction), DERIVED (structured owner_inputs row). 'Raw message text is not retained' is FALSE on main independent of any LLM transmission. SECOND FINDING (separate, DPDP-grade): the DSR/offboarding purge routine the CL-337 brief assumed 'exists' DOES NOT EXIST. migrations/010_dsr_tickets.sql is schema only; dsr_handler.py:24-48 INSERTs a ticket status='acknowledged', sends the DPDP ack template, performs NO deletion; dsr_tickets.status='completed' is unreachable from any code path; repo-wide grep for delete_tenant/tenant_purge/offboard/purge = zero hits. Net: DSR requests are acknowledged to the owner via template but nothing is ever purged — a live DPDP compliance exposure independent of owner_inputs. NEXT (Fazal-owned sequencing decision required before brief revision): (a) recommend a retention-fix 'Component 0' PR lands BEFORE owner_inputs — minimum scope: redaction step at runner.py:222 stripping body from tokenised before persistence, provenance preserved via MessageSid; backfill-redaction of existing rows = separate privacy-debt row. (b) DSR purge gap needs its OWN VT row — Clau will NOT fold a compliance gap into a feature PR. (c) owner_inputs one-PR lock (CL-331) is not violated by this — the lock covered owner_inputs, not the pre-existing pipeline_runs/pipeline_steps leak; these are revealed as distinct problems. owner_inputs brief revision deferred until Fazal rules on Component 0 sequencing — drafting it before that ruling would repeat CL-330's guess-at-premise error. Claude Code made no changes; main clean; awaiting revised scope.

## Status history
- 2026-05-22T13:30:32.465Z: created (status=)
- 2026-05-25 04:00 IST: migrated from Notion (notion_legacy_id: 368387c2-cc5a-81a5-8241-ecfda967940e)
