---
id: VT-485
title: "SR win-back grounding: purchase-ledger recency (not last_inbound_at) + self_evaluate context_summary plumbing"
status: Done
priority: High
type: BUILD
area: team-orchestrator
created: 2026-06-29
authorized_by: fazal
authorization_basis: "Cowork CLOSE-winback audit + last-mile drive: SR emits insufficient_data on a legit lapsed customer → self_evaluate correctly rejects → no gate-passed draft."
---
# VT-485 — SR purchase-recency grounding + self_evaluate context_summary
SR's ledger-summary recency uses customers.last_inbound_at (NULL for Shopify-sourced customers) instead of the purchase-ledger entry_date, so a lapsed-BY-PURCHASE customer surfaces no cohort → insufficient_data → self_evaluate correctly REJECTS (missing target_cohort/campaign_window/expected_arrr). Also self_evaluate.py:422 passes context_summary={} so the gate logs 'context_summary is empty' + can't verify grounding. Fix: SR recency from the purchase ledger entry_date; plumb a real context_summary into self_evaluate. Then a legit lapsed customer produces a gate-PASSED win-back draft. Depends on VT-484 (SR must be reachable to verify).
