---
vt_id: VT-372
title: /team landing page styling completely broken — diagnose root cause + fix rendering
status: Dispatched
priority: High
sprint: "Sprint-Fazal"
type: Bug
area: [Launch Surface, team-web]
assignee: Claude Code
related: [VT-95, VT-96, VT-97, VT-345]
cl_decisions_checked: [CL-2, DR-15, CL-16, CL-23, CL-28, CL-52, CL-59, CL-79, CL-82, CL-88, CL-97, CL-98, CL-132, CL-220, CL-252, CL-260, CL-274, CL-278, CL-424, CL-426, CL-428, CL-429, CL-431, CL-432, CL-433, CL-434]
created: 2026-06-11
last_updated: 2026-06-11
---

## Bug

Fazal reports the `/team` landing page (bilingual landing, VT-95) renders with completely
broken styling. No further detail given — CC diagnoses directly (CC has the Mac's network
egress + Vercel console access for the dev project).

## Fix contract

1. DIAGNOSE FIRST, on the rendered page (the `viabe-team-dev` Vercel deployment and/or a
   local `next dev`/`next build && next start` run). Identify the root cause class before
   changing anything:
   - code-level (missing/global CSS import, Tailwind config/content-paths, layout regression
     from a recent merge), vs
   - deploy-level (Vercel build settings, asset 404s, env) — dev-Vercel config is CC-managed
     autonomously per CL-431; report what was changed by NAME.
2. Fix so `/team` renders as designed: EN + HI variants, mobile + desktop widths, no
   browser-console errors, no 404'd assets.
3. Verification MUST be on rendered output (screenshot or DOM/CSS assertion against the
   running page), not just tsc/lint/vitest green — a styling bug cannot be proven fixed by
   a type-check.
4. If the root cause is a regression from an identifiable merge, name the PR/commit in the
   result signal.

## Scope guard

Styling/rendering of the `/team` landing surface only. No copy changes, no signup-flow
logic, no waitlist/launchMode behavior changes. Scope surprise → STOP + signal (CL-429 (d)).

## Canary

N/A external-API; the rendered-page verification in #3 is the acceptance gate.

## Merge class

[BUILD] — frontend styling; no money/auth/PII/RLS/classifier, no migration. PR into `dev`,
self-merge on full green per CL-429 if scope holds. If the fix turns out to be
deploy-config-only (no code diff), emit task-result with the console change by name instead
of a PR.

## Status history
- 2026-06-11: Fazal grant ("get CC to fix the styling of the page"); rostered + dispatched.
