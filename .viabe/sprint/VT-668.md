---
id: VT-668
title: Orphaned-approval silent no-op — approval TTL (48h) outlives the dead-lettered executor (~6h); owner approval resolves to NOTHING (no send, no reply)
status: open
priority: Critical
owner: unassigned
created: 2026-07-17
found_by: claudecode (approved→sent-leg verification on REAL Track B tenant 861a56a8, Fazal-approved 2026-07-17 13:49Z)
authorization_basis: "Found executing Cowork 20260717T134929Z (Fazal-directed sent-leg verification). Money-path Tier-1-grade (silent no-op after explicit owner approval). Rostered + surfaced; fix pending Fazal/Cowork confirm per fix-agreed-issues."
---

# VT-668 — owner approves; nothing happens; nobody tells him

## The live failure (REAL tenant 861a56a8, framework-routed SR path, flag ON)
- 07-16 16:41Z: SR proposal armed the Pillar-7 `campaign_send` approval (TTL 48h → 07-18 16:41Z); Fazal messaged the approval ask. The executing `manager_task` 49eff9a7 parks `blocked` on the approval.
- 07-16 ~22:40Z: the task is REAPED to **dead_letter** (`retry_budget_exhausted`, attempt 5/5, woken_from=blocked) — ~6h retry ladder, **15h before the approval TTL is half over**. `owner_notification_status=not_required` → death never surfaced.
- 07-17 13:49Z: Fazal replies "Approved" → `pending_approvals` resolves cleanly (decision=approved, +9s, owner SID bound) — **but no live workflow consumes it**. Campaign stays `proposed`; campaign_messages=0; drafts=0; **no assistant reply to Fazal at all**. He believes the win-back is going out.

## Class
Approval-window lifetime ≫ executor lifetime, and approval-resolution neither validates nor re-spawns a live executor. Result = SILENT no-op on the money path — the worst trust shape (worse than an honest failure): explicit owner authorization vanishes. Kin of the run-23 silent-drop class + VT-615 arm-durability, on the resume seam (`approval_resume` / manager_task lifecycle), NOT the send gates (which were never reached — nothing bypassed; fail-closed held, fail-SILENT did not).

## Fix direction (needs plan; correctness gates untouched)
1. **Approval-resolution must guarantee a consumer**: on resolve, validate the bound run/task is alive; if dead_lettered/absent → re-spawn/resume the execution leg (idempotent — the armed batch/campaign ids are in the approval row) OR honestly tell the owner it expired and re-arm. NEVER resolve into silence.
2. **Lifetime coherence**: an executor blocked on an owner approval must not burn its retry budget while waiting — `blocked-on-approval` should pause the ladder (or the TTL should clamp to executor lifetime).
3. **Dead-letter surfacing**: a dead_lettered task holding an armed approval must notify the owner (honest stall message) + cancel/mark the approval — `owner_notification_status=not_required` is wrong for this shape.
4. Re-drive the leg fresh on a synthetic tenant (approve within the live window) to prove the happy path exists at all on the framework-routed path.

## Fazal's live tenant — decision needed
His approved-but-orphaned campaign f6052899: (a) re-arm + he re-approves once VT-668 is fixed (clean), or (b) manual resume now. Recipients are 6 REAL-tenant customers — any actual send is HIS call (all sends on dev are transport-guarded, but this is his real data).

## Evidence
sentleg-audit report (6 checks; check-1 PASS, 2/4 FAIL, 3 vacuous, 5 partial, 6 = the root cause). Prior vacuous audit: scratchpad/audit861/. §7D nit reproduced (NULL run_id on plan_created + manager_review_decision; reasoning_ref joinable).

## FIX DESIGN (read-only seam walk 2026-07-17; primitives EXIST)
Facts: `task_store.py:36-38` — dead_letter is OPERATOR-REDRIVABLE by design (`redrive_task` :187-194 resets dead_letter/blocked → 'planned', keeps attempt). `orphan_reaper.reap_stalled_manager_tasks` (:106+, VT-557/560 ladder) sweeps ACTIVE stranded tasks; status taxonomy (:29 + :99 comment) DISTINGUISHES `waiting_owner` ("parked on approval") from `blocked` (retry rung) — but the incident task sat in `blocked` (woken_from=blocked, attempt 5/5) while parked on the approval → the ladder legitimately walked it to dead_letter. THE DEFECT = wrong parking status + no consumer-check on resolve + no owner surfacing.
Three-part fix (each seam small):
1. **Park approval-paused tasks as `waiting_owner`, not `blocked`** — find where the enforce loop records the `paused_approval` outcome onto the task (manager/workflow.py outer loop / task_store transition) and use `waiting_owner` (excluded from the stall ladder; verify the reaper's sweep predicate excludes it — :99 comment implies yes). The ladder stops burning while the owner thinks.
2. **Resolution guarantees a consumer** — at the approval-resolution seam (agent/approval_resume): after resolve, look up the bound task (approval.run_id / reasoning_ref linkage); if status ∈ {dead_letter, blocked} → `redrive_task` (existing primitive) + honest owner ack ("resuming your approved campaign now"); if task missing/cancelled → honest expiry reply + re-arm offer. NEVER resolve into silence.
3. **Dead-letter surfacing for approval-holders** — when the reaper dead-letters a task bound to an OPEN pending_approval: owner notification REQUIRED (honest stall message) + mark/cancel the dangling approval (else the 48h TTL invites exactly this incident). Flip `owner_notification_status=not_required` for this shape.
Acceptance: unit (park-status transition; resolve→redrive on dead task; resolve→honest-expiry on missing task; reaper notifies+closes approval); synthetic e2e: arm → let the task dead-letter (or force-status) → approve → campaign EXECUTES (redriven) + owner ack states truth; PLUS fresh happy-path (approve within live window) → sends mocked + count-bound ack. Fazal's real campaign f6052899: re-arm + re-approve after the fix (his call on timing).

## FAZAL RULING (2026-07-17 ~20:35 IST, Cowork 20260717T150940Z) — GRANTED, FIRST
Sequencing confirmed: 668 before 667's e2e validation. ACCEPTANCE: (a) fresh synthetic approve-within-window happy path; (b) synthetic orphaned-consumer → owner gets an HONEST message (never silence), approval not silently consumed; (c) **LIVE: re-arm campaign f6052899 on 861a56a8 → Fazal re-approves from his phone → sends execute (transport-blocked synthetics) → he receives the true-count confirmation** — closes VT-668 + the original sent-leg verification. LINKAGE: this is the dev incarnation of the VT-634 prod requirement — design the dead-letter/stall surfacing to feed the same VTR-console path later. Carried separately: §7D NULL-run_id nit (fold here if same seam, else own row); the misrouted Shopify-connect reply on 861a56a8 (class TBD — VT-666-family check).

## BUILD MECHANICS CORRECTION (builder research, dbos 2.22.0 — supersedes the naive design)
1. `redrive_task` alone is a DEAD END: nothing starts a workflow for a redriven 'planned' task (only triage's inbound path calls start_manager_task_workflow); the stall sweep would re-ladder it to dead_letter. Fix-2 must redrive AND start the workflow.
2. Same-SetWorkflowID restart is a SILENT NO-OP (owner_xid guard + terminal-status early-return) — the restart needs a FRESH workflow id (redrive-suffixed), gated on the redrive_task CAS actually flipping a row (= the double-start guard; downstream send idempotency unchanged).
3. NO stored approval→task join (pending_approvals has run_id=one-way uuid5(task,step,attempt), no task_id column; campaigns/manager_tasks likewise). Recovery = bounded enumeration over the tenant's tasks × steps × attempts ≤6 + hash-match (the forward direction already exists in verification.py). No schema change.

## FOLLOW-UP (cosmetic, not blocking): fix-2a's redrive resets a dead task to 'planned' with no actor — the sweep re-walks it to dead_letter in ~6h (harmless churn; fix-3 won't re-fire, approval closed). Refinement: drop the redrive from 2a, leave dead_letter (truthful, operator-redrivable) + the honest ack. One-liner; fold into the next manager-loop touch.

## CLOSED (2026-07-18, Fazal-directed)
Fix landed + PUSHED (ec25bc3 folded into f5c8d78 stack, dev). Deterministic proof: pre-push DB suite green incl. test_orphan_reaper (dead-letter-approval-holder surfaced + approval closed) + test_approval_resume (resolve→redrive+honest-ack; missing-task→honest-expiry; no-double-arm) — 32+ unit/DB tests. Composes clean with VT-667 fix-4 (correction cancels the waiting_owner task → terminal → orphan sweep skips it). REMAINING (optional, non-blocking): live-dev synthetic e2e + Fazal's phone re-arm of f6052899 = the end-to-end approved→sent proof (the accept subagent died on a transient rate-limit; unit/DB coverage stands). §7D NULL-run_id nit → fold into a §7D row if it recurs.
status: CLOSED (LIVE PROOF PASSED 2026-07-17 13:49-15:10Z — owner approved from phone, fixed consumer executed, 6 mocked sends, true-count confirm delivered 15:10:37Z; evidence in 20260718T055800Z signal)
