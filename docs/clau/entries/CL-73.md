---
cl_id: CL-73
entry_type: State Snapshot
topic: Architecture
status: Resolved
session_date: 2026-05-18
created: 2026-05-17T18:40:36.253Z
source: Clau Layer 1 audit, fresh session 2026-05-18, against commit 277723c (VT-3.3a)
linked_tasks: VT-3.3a (audited). Follow-up subtasks recommended: VT-3.3a-fix-1 (C1 GUC/RLS), VT-3.3a-fix-2 (C2+C3+C4 ingress hardening), VT-3.3a-fix-3 (H1/H2 CI grep + RouteToBrain handling), and one Notion-vs-shipped drift pass.
notion_legacy_id: 363387c2-cc5a-81b2-b0bb-de231cce5695
title: "Layer 1 audit complete (post-VT-3.3a) — findings report"
---

# CL-73 — Layer 1 audit complete (post-VT-3.3a) — findings report

Layer 1 audit per CL-69. Fresh session, no prior context, audited cumulative work through 277723c (VT-3.3a). Note: repo HEAD is actually 4106e29 (VT-3.3b already merged as PR #10) — VT-3.3b shipped before this audit ran, which the CL-69 sequence said should not happen. Either CL-69 was overridden or audit is one PR late. Fazal to confirm.

FINDINGS BY SEVERITY:

CRITICAL (4):
- C1: apply_transition + 9 other DB writers do not set app.current_tenant GUC; will fail in production under a non-superuser role due to FORCE RLS. Tests don't catch it because they all connect as superuser. Single biggest finding.
- C2: twilio_ingress.*record_inbound runs before DBOS.start_workflow + outer try/except returns 200 on failure. Lost-message hazard: row exists but workflow never started, next retry silently routes to dupe_handler.
- C3: empty MessageSid silently collapses to one workflow_id ('twilio_inbound*'); should be 400.
- C4: opt_out_handler/dsr_handler/status_ping_handler return confirmation_sent=True while only logging. Pillar 7 violation in return contract.

HIGH (6):
- H1: no-LLM CI grep missing twilio_ingress.py + runner.py.
- H2: RouteToBrain and Reject outcomes silently dropped in webhook_pipeline_run; substantive owner messages produce no observable record.
- H3: pipeline_steps lacks UNIQUE (run_id, step_index) constraint.
- H4: RLS attack tests only cover phase_transitions — 7 other tenant tables uncovered.
- H5: Handler TODOs say 'VT-3.3' but VT-3.3a+3.3b shipped without wiring Twilio sends; ambiguous traceability.
- H6: LangGraph checkpoint RLS lacks FORCE; intentional asymmetry vs. migration tables, fragile if orchestrator role isn't the table owner.

MEDIUM (7):
- M1: total_arrr_paise typo (three r's) propagated from Notion VT-3.2 body through code and test.
- M2: migration 000a comment still describes Apache AGE (abandoned per CL-66).
- M3: phase_transitions has no CHECK on from_phase/to_phase.
- M4: migration 007 comment says cust_tok_; implementation uses phone_tok_. Doc drift.
- M5: stub Twilio webhook in apps/team-web/app/api/team/twilio/webhook/route.ts is a confusion vector (potentially replaced by VT-3.3b at 4106e29 — not audited).
- M6: team_orchestrator package directory is a stub alongside the real orchestrator package; remove or use.
- M7: apply_transition TODO for pipeline_steps observability writes (VT-122).

LOW (3):
- L1: WORKFLOW_TIMEOUT_SECONDS not applied to webhook_pipeline_run; could hang indefinitely.
- L2: state.py docstring framing is slightly stale.
- L3: test_valid_transition_fires parametrization is heavy (21x fresh tenant inserts).

VALIDATIONS PASSED (22): Pillar 1 (no LLM imports anywhere; supervisor.py absent; get_opt_out_status absent; Pre-Filter Gate fully deterministic). Pillar 8 (one DBOS launch, one LangGraph init, one transitions module, one migrations dir; Tenant stub cleanly removed). FastAPI routes minimal (exactly 2). SubscriberState mutation locality holds. Real DBOS SIGKILL auto-resume tests pass for both workflows. LangGraph checkpoint cross-tenant test passes (with caveat H6). Phone-token salt required + salt-sensitivity tested. INTERNAL_API_SECRET uses hmac.compare_digest. CI test fixtures use TEST values. Env-var hygiene structurally correct + linted. Migrations 001-012 have inline RLS + appropriate CHECKs on the tables that need them. WebhookEvent fields all consumed downstream. pipeline_run vs. webhook_pipeline_run cleanly separated. OrchestratorState vs. SubscriberState cleanly separated. All 7 required CI checks present. PR-title check enforces VT-* refs. No secrets in git history.

NOTION-VS-SHIPPED DRIFT — INCOMPLETE: did not fetch the five subtask Notion bodies during this audit (tool-call budget). In-repo docstring evidence suggests strong conformance but recommend a follow-up pass (~15-30 min) that reads each Notion body and does a line-by-line scope check.

RECOMMENDED SEQUENCE:
1. Confirm production role (superuser vs. app role). If app role, C1 is launch-blocker.
2. One small PR: C2 + C3 + C4 (~30 lines, 'fix: move dedup into workflow + reject empty SID + correct send-pending return contract').
3. Second small PR: H1 + H2 (CI grep widening + RouteToBrain step record).
4. Third small PR: H4 (RLS test parametrization over all tenant tables).
5. Notion-vs-shipped drift pass as a one-off.

Full findings report delivered in chat 2026-05-18.

## Status history
- 2026-05-17T18:40:36.253Z: created (status=Resolved)
- 2026-05-25 03:55 IST: migrated from Notion (notion_legacy_id: 363387c2-cc5a-81b2-b0bb-de231cce5695)
