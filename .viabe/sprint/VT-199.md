---
vt_id: VT-199
title: VT-193 canary refinement — A5 in-process trip + A6 budget revision
status: Done
priority: Medium
sprint: Sprint 2 - Cost + Moat
type: Hygiene
area: [Orchestrator, Canary]
assignee: Claude Code
parent: VT-193
sub_items: []
exec_order: 6
branch: "feat/vt-199-canary-refinement"
version: "v1.0"
last_updated: 2026-05-27T21:30:00+05:30
---

# VT-199 — VT-193 canary refinement

## Why this row exists

VT-193 brain wiring shipped with vt193_brain_wiring.py canary at 4/6 PASS (A1-A4 load-bearing PASS). A5 was a conditional FAIL (cross-process trip limitation — canary patches `_NullDriver.cost_limit_paise` but the orchestrator subprocess loads its own class at boot). A6 was a FAIL because the brief's budget (<50 paise) didn't reflect real Opus 4.7 cost.

Both are canary-coverage gaps, not wiring defects. This row closes them.

## Scope

### A5 — in-process trip refactor

Restructure A5 sub-test to:
- Construct minimal `WebhookEvent` + `SubscriberState` fixture in-process
- Patch `_NullDriver.cost_limit_paise = 0` (or `tool_call_limit = 0`)
- Invoke `dispatch_brain()` directly (bypasses HTTP/DBOS)
- Assert `aborted_hard_limit` envelope row written + return value indicates hard-limit termination
- Restore class attribute after test (no global state pollution)

### A6 — budget revision

Update assertion bounds to match observed Opus 4.7 cost:
- Wall-clock: <60s (was <60s; matches)
- Cost: <1000 paise (was <50; revise to match observed ~895 paise with headroom)
- These bounds get further tightened after VT-194 prompt caching lands (target ~200 paise post-caching)

### Documentation

- Update canary file's module docstring with post-refactor expected ranges
- Note VT-194 will further tighten the budget after caching ships

## Acceptance criteria

1. A5 deterministically trips hard-limit + writes `aborted_hard_limit` envelope on every run
2. A6 budget assertions PASS on baseline Opus 4.7 run
3. canary 6/6 PASS overall on brain-wired main
4. No regression on A1-A4 load-bearing assertions

## Rule #15

This row IS a canary refinement; no new canary needed. Verification = run the existing canary post-refactor and confirm 6/6.

## Out of scope

- Pattern A (separate row)
- Prompt caching (VT-194)
- A6 tightening to post-caching levels (VT-194 owns that)

## Dependencies

- VT-193 ✓ (canary exists; refactoring its sub-tests)
- VT-194 (will tighten A6 further after caching ships; non-blocking)

## Branch & PR

- Branch: `feat/vt-199-canary-refinement`
- PR title: `chore(canaries): VT-193 canary A5 in-process + A6 budget revision (VT-199)`

## Status history

- 2026-05-27 21:30 IST: filed by Cowork per VT-193 task-result carryforward
