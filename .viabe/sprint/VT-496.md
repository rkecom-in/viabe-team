---
id: VT-496
title: "SR schema-reject field-path must survive redaction (diagnose the win-back parse failure on dev)"
status: Todo
priority: Critical
type: BUILD
area: team-orchestrator
created: 2026-06-30
authorization_basis: "Win-back re-drive #2: the dominant SR parse failure (4/5 drives, error hash 379f7e1) is UN-DIAGNOSABLE — the field path is SHA-redacted, the dev LLM key is dead, railway run doesn't expose the sealed key, logfire shows only span names. Prereq to fixing the win-back."
---
# VT-496 — SR schema-reject observability (PREREQUISITE, contained)
A pydantic ValidationError's field LOCATIONS (loc tuples, e.g. proposed.message_plan.X) are schema paths, NOT PII — but the FailureRecord message is SHA-hashed whole (pii_redactor.py:155), so the dominant win-back parse failure can't be diagnosed on dev. Fix: capture the ValidationError's field-path loc tuples as STRUCTURED non-PII metadata on the FailureRecord / pipeline_step that survives redaction (the field path + error type only — never the offending VALUE, which may be PII). Then the next re-drive can name the failing field. Contained + safe (non-PII field paths only).
