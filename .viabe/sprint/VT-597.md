---
id: VT-597
title: "AUTH-phase off-script inbounds route to the manager brain (mirror VT-588's discovery-gate conversion)"
status: Done
priority: high
type: BUILD
area: manager
created: 2026-07-04
authorized_by: fazal
authorization_basis: "Autonomous capability program (CL-2026-07-03) — P1 remaining item, named in the program log 2026-07-04: 'AUTH-phase off-script→brain (mirror VT-588) — still a follow-up.' Required for the context-retention bar."
---

# VT-597 — the AUTH resume gate still owns off-script turns

## Finding (harness re-run on deployed dev c6c3aee, 2026-07-03)
context_retention step2 "did you get my store address?" during the Shopify AUTH phase got the
deterministic honest-waiting line, not the brain — because VT-588 converted only the DISCOVERY
resume gate to fall-through. The AUTH gate still answers every off-script inbound itself, so a
question mid-auth gets flow copy instead of a context-aware answer.

## To do
Mirror VT-588's pattern in the AUTH-phase gate (shopify_onboarding): handle the on-script
token/expected reply deterministically; for everything else return None → dispatch_brain answers
with the onboarding-state block (flow-aware, guides back). Malformed on-script attempts keep the
specific reprompt. Tests mirror VT-588's (fall-through cases + on-script cases + regression on the
resume happy path). Validate on deployed dev with FULL-reply capture.

## RESOLVED 2026-07-04 — landed on local dev @ ca25204
- Discrimination rides the existing VT-583 `classify_auth_intent` (done | link | other | None):
  `done`/`link` stay deterministic (unchanged); `other` (positive off-script signal) now returns
  None → dispatch_brain answers with the PHASE_AUTH onboarding-state block (already covered since
  VT-588) and pending_owner_input stays live for the next done/link; `None` (classifier
  unavailable/failed) keeps the honest-waiting floor — no positive signal, no blind fall-through.
- AUTH has no malformed-token analogue (free-form intent, not a structured token) — the
  classifier-unavailable floor is the closest equivalent; preserved byte-identical.
- Red-then-green TDD; 26 shopify tests + 360 onboarding + 275 agent green; ruff clean.
  Deployed-dev proof (context_retention step2 now brain-answered) rides the VT-598 pack.
