---
id: VT-676
title: CD2 — customer-list WhatsApp file attachment to the VERIFIED OWNER (CSV + signed URL + media send + tm_audit)
status: built-pending-canary
priority: Medium
owner: claudecode
created: 2026-07-18
found_by: CD2 backlog (task #79); feasibility scouted 2026-07-18 (M, buildable)
authorization_basis: "Fazal 2026-07-18 'continue to close all open tasks faster' — CD2 is a long-standing rostered task. NOTE: '#79' is the TASK id; VT-79 is a taken/stale row — this row is the fresh allocation."
---

# VT-676 — deliver the customer list as a WhatsApp file, not an excuse

Owner asks "send me my customer list" → today VT-642 rides the honest can't-attach-yet ack
(LIST_SEND_ACK_PREAMBLE). This row actually delivers: CSV → private bucket → short-TTL signed URL →
Twilio media send to the OTP-verified owner number → tm_audit egress event. FIRST path where RAW
customer name+phone leaves the orchestrator boundary — full CL-390 rails.

## Feasibility (scouted, 2026-07-18)
EXISTS (reuse verbatim): report_storage.py private-bucket + 300s signed-URL + purge pattern (VT-86/341)
· CustomersWrapper reads (add list_lapsed_customers mirroring count_lapsed SQL) · _resolve_owner_phone
(OTP-verified, server-derived — task_outcome.py:396 precedent) · dsr_export audit precedent (counts/
shape, never content) · dev_send_guard passes media_url through (mock on dev unless allowlisted).
MUST BUILD: media_url= on a freeform in-session send in twilio_send.py (in-session media needs NO Meta
template approval) · CSV builder · `customer-exports` bucket clone · audit event · seam swap.

## Slice
`mentions_customer_list_request` fires (triage_seam D3 real-cohort branch) → build CSV → host →
freeform media send to `_resolve_owner_phone(tenant_id)` ONLY (`is_customer_send=False`; NEVER a
number from the message body) → emit tm_audit `customer_list_exported` (row count + file token only,
no content, no PII URL in logs) → KEEP LIST_SEND_ACK_PREAMBLE as the fallback on ANY gen/send failure.

## Rails (binding)
- Private bucket, 300s TTL, aggressive purge; leaked URL = PII doc.
- Never log file content or the signed URL.
- Recipient server-derived only. Owner's own tenant's data only (RLS-scoped read).
- Audit counts/tokens, mirroring dsr_export.
- Dev: mock-send proof first; the REAL media canary (Rule #15) = one send to Fazal's allowlisted
  +919321553267 on dev — needs Fazal receive-side. NO real send before that sign-off.

## Gate
Unit: builder PII-shape + recipient-derivation + fallback-on-failure + audit emission. Dev mock e2e:
ask → mocked media send recorded with signed URL + audit row. Live canary: Fazal receives the CSV on
WhatsApp, file opens, URL expires ≤300s after. Then close.

## PROGRESS (2026-07-18)
- Landed: 4ea2056 (build) + **acbb44e (fix-2)**. Fix-2 came from the dev mock-drive: the natural ask
  ("send me the customer list") routes to the R7 `lapsed_list` net which still carried the interim
  count+OFFER copy — export was only wired behind D3 campaign-imperative routing. Now BOTH doors
  deliver; CSV gained `last_purchase` + `lapsed` columns (wrapper `list_customers_for_export`, SAME
  canonical count_lapsed definition — file never diverges from chat).
- Infra: private `customer-exports` bucket created on dev Supabase; `SUPABASE_SERVICE_KEY` set on
  Railway development (was unset — NB monthly-report storage would also have failed on dev).
- **Mock e2e PASSED on deployed dev (acbb44e):** harness tenant, standalone list ask → reply "5 of
  your customers are lapsed … I've just sent you your full customer list as a file — the lapsed
  ones are flagged in it" (grounded: 5 = seed truth) + `customer_list_exported` audit row
  {rows: 6, MOCK sid MKDEV…, tenant-scoped path} + 435-byte CSV in the private bucket. Tenant torn
  down; export object purged.
- **REMAINING (Rule #15): live media canary** — one real send to Fazal's allowlisted +919321553267
  on dev; Fazal confirms file opens + URL expires. Then status → done.

## CLOSED 2026-07-18 ~18:20 IST — canary r4 PASSED (Fazal-confirmed: PDF arrived with the customer list)
Root-cause journey: r1 text/csv (MIME not on WhatsApp allowlist) → r2/r3 text/plain (Twilio
WhatsApp document support is effectively PDF-only) → r4 PDF via the monthly-report weasyprint
path = DELIVERED. Bycatch fixed en route: delivery-reconciliation wiring (status_callback env),
conversation_log illegal-surface silent drop, double-claim + TTL-confusion copy, ack honesty
under async uncertainty. Watch-item: r4 ledger row still 'accepted' — if callbacks never flip
'delivered', team-web webhook may not forward status posts (small follow-up).
