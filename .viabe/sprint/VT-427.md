---
vt_id: VT-427
title: Delete orphaned sales_recovery_stub dead code + the stale doc-comment
status: Queued
priority: Low
sprint: "Sprint-Fazal"
type: Chore
area: [Backend, Cleanup]
related: [VT-416]
exec_order: 96
created: 2026-06-25
last_updated: 2026-06-25
---

## Contract (Cowork GATE 2026-06-25, VT-416 sweep). Routine BUILD now — self-merge class into local dev (held).

`agent/sales_recovery_stub.py` (`build_stub_sales_recovery_agent`, `hardcoded_campaign_plan`, `00000000-…` UUIDs) is
FULLY ORPHANED — the supervisor dispatches the REAL `run_sales_recovery_agent` (`supervisor.py:128`). The only
reference is a STALE doc-comment in `sales_recovery.py:5` ("still routed through the stub" — false).

## Scope
- Delete `sales_recovery_stub.py` (verify zero live importers first — grep). Fix the stale `sales_recovery.py:5`
  doc-comment. No behavior change (dead code).
- Verify: full orchestrator import + the sales_recovery suite green (nothing imported the stub).

## Gate
Routine destub/dead-code → [BUILD] self-merge into LOCAL dev (held). No risk surface.

## Status history
- 2026-06-25: Rostered + ID allocated (Cowork VT-416 gate). Routine deletion.
