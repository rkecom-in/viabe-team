---
vt_id: VT-356
title: Fix 2 pre-existing mypy errors in runner.py (hygiene)
status: Backlog
priority: Low
sprint: Sprint 9 - Launch Surface
type: Chore
area: [Orchestrator]
assignee: unassigned
parent: ""
sub_items: []
tags: [tooling, hygiene, mypy]
---

# VT-356 — clear 2 pre-existing mypy errors in runner.py

Surfaced (not introduced) during the VT-334 review (Cowork 20260606T105500Z). `mypy
src/orchestrator/runner.py`:

1. `runner.py:267` — `Dict entry 2 has incompatible type "str": "str | None"; expected "str":
   "str"` (a dict literal with an Optional value where str is expected).
2. `runner.py:582` — `Incompatible types in assignment (expression has type
   "Literal['consent_required']", variable has type "Literal['direct_handler','brain','reject']")`
   — a route-decision Literal missing the `'consent_required'` member.

Both pre-date VT-334 (not in its diff). mypy is NOT pre-push/CI-gated today, so they don't block —
but leaving them rots the signal. Fix: widen the route Literal to include `'consent_required'`
(or correct the assignment) + handle the Optional in the dict at :267. Small, no behavior change.

## Origin
VT-334 #377 review (Cowork 20260606T105500Z) — "roster a tiny hygiene row so they don't rot."
