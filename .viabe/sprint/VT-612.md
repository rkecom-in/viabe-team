---
id: VT-612
title: "Enforce-mode onboarding→activation→first-value bridge — the paced-flow replacement (Manager-driven)"
status: Todo
priority: high
type: BUILD
area: manager
created: 2026-07-06
authorized_by: fazal
authorization_basis: "Fazal 2026-07-05 loop-program grant — surfaced by VT-609 (paced-flow has no enforce equivalent)."
---
# VT-612 — the profile→data→first-value bridge in enforce mode

Surfaced by VT-609: the deterministic VT-576 paced post-profile flow (readiness-ask → one-
integration-offer → data-landed plan-kickoff, all 24 test_journey_paced_flow.py tests) is 100%
owned by _maybe_handle_post_profile_flow, reached ONLY through maybe_handle_journey_reply — which
VT-609's mode-gate fully DISABLES in enforce mode. So once onboarding_conductor completes
profile+policy in enforce mode, NOTHING bridges "profile complete" → "connect data" → "first-value
plan fires". The loop is reactive to owner messages; the paced flow's value was PROACTIVELY nudging.

Scope: the Manager reasons the onboarding→activation→first-value sequence — after profile+policy
complete (activation_check), it spawns integration_agent to connect a data source, and once data
lands it triggers the first-value plan (sales_recovery). This is the Manager's own orchestration
intelligence (the objective's "efficient execution plan + delegate") replacing the deterministic
paced flow. Needs: a trigger for the post-profile handoff (a proactive tick or an onboarding-
complete signal the Manager consumes), the one-integration-at-a-time pacing preserved (no burst),
and the plan-fires-only-after-data-lands invariant (CL-2026-07-03 — a plan with zero connected data
is hollow). NOT a loop-proof blocker (VT-611 proves the loop mechanism); this is the enforce-mode
UX-completeness seam. The 24 paced-flow behaviors become this row's real-scenario assertions.
Exec order: after VT-611 (or folded into its onboarding scenario pack's forward-looking set).
