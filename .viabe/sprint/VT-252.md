---
vt_id: VT-252
title: Security — session-export secret leak (tokens + bypassPermissions)
status: Done
priority: Critical
sprint: Sprint 2 - SR Agent Skeleton
type: Security
area: [Security, Tooling, CI]
assignee: claudecode
pr: 173
version: "v1.0"
last_updated: 2026-05-31T03:35:00+05:30
---

# VT-252 — session-export secret leak

Automated security review found Supabase magic-link auth tokens (`token=...`
verify URLs) + `permissionMode: bypassPermissions` in Cowork session-export
artifacts.

## What I did (PR #173)
- Gitignored `cowork_sessions/session-export-*/` raw dirs (the untracked
  26-05-29 dir had 4 token URLs + bypassPermissions) — they can never be
  committed now. Curated `*.md` exports stay trackable.

## NOT done — needs Fazal/Cowork (flagged, out of my lane)
1. **Already-committed leak:** `cowork_sessions/viabe-team-session-export-2026-05-29.md`
   (merged in PR #141) contains magic-link tokens + bypassPermissions IN GIT
   HISTORY. Decide: history-scrub (filter-repo/BFG, rewrites shared history —
   Fazal-authorized) vs accept (tokens expired).
2. **Rotate** the exposed Supabase magic-link tokens IF any could still be valid
   (almost certainly expired — 2-day-old, magic-link TTL ~1h). Confirm.
3. **Fix the session-export tooling** to scrub `token=`/`access_token=` URLs +
   normalize `permissionMode` to a safe default BEFORE writing exports.
4. **CI guard:** add a token-scan (reject committed files matching `token=`/
   `access_token=` in URLs) — gitleaks may not catch URL query-param tokens.
