---
vt_id: VT-30
title: Unified-output composer
status: Queued
priority: High
sprint: Sprint 1 - Foundation
type: Feature
area: [Orchestrator, Owner Surface]
assignee: Pipeline Engineer
parent: VT-3
sub_items: []
exec_order: 7
branch: "feat/vt-orchestrator-output-composer"
version: "v1.0"
notion_legacy_id: 356387c2-cc5a-81be-bf36-df958788e1c2
last_updated: 2026-05-26T17:25:00+05:30
---

# VT-30 — Unified-output composer

## Brief artifacts — normalized 2026-05-26 at queue-time (Cowork)

The 2026-05-25 migrated body below names CodeX / CoderC / CoderX reviewers, a `dev` merge target, a `(VT-Orchestrator)` PR title, and treats `send_whatsapp_message` / `send_whatsapp_template` as "in progress." Three of those are pre-cutover artifacts; the fourth is a ground-truth error. Effective spec for this PR:

- **Team (Fazal-set 2026-05-26):** Cowork = PM. Fazal = CEO (Pillar 7 merge auth). Claude Code = Tech Lead + Developer. Clau = escalation-only. CoderX / CoderC / Pipeline Engineer / Frontend Engineer references in the migrated body are retired per CL-151. The historical "CoderX MUST review honesty-rule code paths" gate transfers to Claude Code (Tech Lead) + Fazal personal honesty-rule-tests review.
- **Merge target:** `main` (per CL-132 Standing).
- **PR title:** `feat(orchestrator): output composer as orchestrator-agent tool (VT-30)`.
- **Branch:** `feat/vt-orchestrator-output-composer` (as-spec).
- **Dependency reality (ground-truthed 2026-05-26):**
  - **`send_template_message` ALREADY EXISTS** at `apps/team-orchestrator/src/orchestrator/utils/twilio_send.py` as a `@DBOS.step`. 5 direct_handlers consume it (`template_error_handler`, `dsr_handler`, `opt_out_handler`, `status_ping_handler`, + 1 more). The brief's "VT-MCP-Tools VT-5.6 + VT-5.7 in progress" is **stale** — those Tier-A send paths shipped under VT-31 (Pre-Filter Gate). VT-30 composer uses the same `send_template_message` function for template sends; the `send_whatsapp_message` (free-form 24h-window) needs a NEW thin wrapper if not present (CC verifies at STEP-0).
  - **`apps/team-orchestrator/config/twilio_templates.yaml` ALREADY has all 8 Tier-A SIDs** (Fazal-approved templates; same 8 in `.viabe/templates.md` Cowork-managed reference). Brief's `template_routing.yaml` (intent → template_name mapping) is NEW — separate from the SID lookup. Composer reads BOTH: routing yaml for intent→name, twilio_templates yaml for name→SID.
  - **Tool inventory pattern established** at `apps/team-orchestrator/src/orchestrator/agent/tools/` (`self_evaluate.py` is the precedent). `compose_owner_output` registers the same way.
  - **VT-3.x ground-truth:** brief lists VT-3.1–VT-3.6 Done + VT-3.9 in progress. Post-migration: VT-31/32/33-37/39 all Done; VT-28/175/176 Done. VT-125 (orchestrator-agent prompt + tool inventory expansion) is Backlog exec 9 — composer-tool registration in this PR may anticipate VT-125's tool-inventory expansion OR coordinate with it. Surface at STEP-0.
- **Direct handlers existence changes the "SOLE message producer" framing.** Brief says: *"The composer is the SOLE producer of owner-facing message text."* That's NOT true post-VT-3.3c — 5 direct_handlers in `direct_handlers/` produce template-sends without going through any composer. Reconciled framing: **composer is the SOLE producer of orchestrator-agent-path owner messages.** Direct-handler templates (DSR ack, opt-out confirm, etc.) are deterministic pre-filter paths that don't go through the agent; they bypass the composer by design. The CodeX-rejection rule from the brief becomes: any NEW agent-path message MUST go through composer; direct_handlers remain as-is.
- **`.viabe/templates.md`** is the human-readable companion to `twilio_templates.yaml`; composer references both as needed.

## Parent task
VT-Orchestrator (respec'd 2026-05-12 for orchestrator-as-agent + DBOS + langgraph_supervisor).

## Concept reference
- `concept-team.md` §8 (orchestrator merges specialist response with state into single owner-facing message).
- `concept-team-pillars.md` Pillar 1 REVISED 2026-05-12: orchestrator-agent owns coordination + routing; composer is a DETERMINISTIC tool the orchestrator-agent invokes when its decision is "respond".
- `concept-team-pillars.md` Pillar 7 (owner is source of truth; messages must be honest).
- Clau_Session_Log CL-22.

## Dependencies
- VT-3.1–VT-3.6 Done.
- VT-3.9 in progress (orchestrator-agent system prompt; the agent calls compose_owner_output as a tool).
- VT-MCP-Tools VT-5.6 + VT-5.7 in progress (`send_whatsapp_message` + `send_whatsapp_template` delivery mechanisms).
- VT-OwnerSurface VT-9.4 in progress (edge-case handlers consuming composed outputs).

## What CoderC must produce
The DETERMINISTIC composer that takes a specialist agent's structured output (e.g., `CampaignPlan`) plus current orchestrator state (phase, escalation flags, pending approvals, error annotations) and produces ONE owner-facing message ready to send via WhatsApp.
**Architectural shift from prior framing:** the composer is now a TOOL invoked by the orchestrator-agent (or by deterministic state-machine paths like attribution-close confirmation). It is NOT a separate LangGraph node that fires automatically after every specialist invocation. The orchestrator-agent decides when to invoke it.
1. **Composer** in `apps/team-orchestrator/src/orchestrator/output_composer.py`:
	- Function `compose_owner_output(specialist_result: AgentResult | None, state: SubscriberState, intent_or_trigger: str) -> ComposedOutput`.
	- Registered as one of the orchestrator-agent's tools in VT-3.9's tool inventory.
	- Returns `ComposedOutput`: message body, message_type (`free_form_24h | template`), template_id, template_params, urgency, follow_up_required, follow_up_intent.
	- The composer is the SOLE producer of owner-facing message text.
2. **Composition rules** (deterministic Python, NOT LLM-generated):
	- If `state.escalation_pending`: prepend honest "the agent encountered an issue; here's what happened" framing.
	- If specialist_result has `terminated_by` (hard-limit breach): explain which of 5 limits (tokens/tools/depth/wall-clock/₹50 cost) was breached in plain language; offer Fazal escalation path.
	- If `state.phase == 'paid_at_risk'`: include soft retention nudge but NEVER apply pressure (Pillar 7).
	- If `state.phase == 'refunded'`: messages MUST acknowledge refund decision; cannot continue as if it didn't happen.
	- If outside 24-hour Twilio window: must use Meta-approved template (`message_type = template`); free-form rejected at tool level.
	- If inside 24-hour window AND no template applies: free-form with specialist content + orchestrator framing.
3. **Honesty rules** enforced in code (CodeX reviews these specifically):
	- The composer NEVER overstates ARRR. Uncertainty in attribution → message says so.
	- The composer NEVER hides a failure. Agent escalated → owner is told.
	- The composer NEVER applies retention pressure. "Are you sure you want to cancel?" allowed once; "Are you SURE? Look at all this value!" forbidden.
	- The composer NEVER claims certainty about customer intent ("this customer wants X") — always frames as "this customer's pattern suggests X."
4. **Language selection**:
	- Phase 1 supports Hindi + English via `tenant.preferred_language` (set during sign-up; defaults to English).
	- Templates pre-approved for both languages (VT-13.3). Free-form composed in preferred language.
	- Mixed-language replies (Hindi + English in same message, common Indian SMB pattern) ACCEPTED. Composer never enforces monolingual output.
5. **Template selection logic**:
	- Tier-A templates approved in VT-13.3: 5 essential. Tier-B and Tier-C added in later subtasks.
	- Composer maps `(intent_or_trigger, state.phase, escalation_pending)` → template_id when applicable.
	- Mapping table in `apps/team-orchestrator/config/template_routing.yaml`.
6. **Tool registration for orchestrator-agent**:
	- `compose_owner_output` registered as a tool the orchestrator-agent can call.
	- Tool description: "Compose an owner-facing WhatsApp message from a specialist result + current state. Returns structured ComposedOutput. Always call this BEFORE invoking send_whatsapp_message or send_whatsapp_template."
	- CodeX enforces: `send_whatsapp_message` / `send_whatsapp_template` tools (VT-5) reject calls where the message text didn't pass through `compose_owner_output` (via a hash signature in the call payload).
7. **Tests** in `apps/team-orchestrator/tests/orchestrator/test_output_composer.py`:
	- All Tier-A trigger→template mappings produce correct templates.
	- 24-hour window enforcement: synthetic state where last_owner_message_at > 24h forces template; <24h allows free-form.
	- Honesty rules: synthetic specialist_result with overstated ARRR gets corrected.
	- Refunded phase: messages always acknowledge refund decision.
	- escalation_pending: messages prepend honest framing.
	- Hindi + English: composer produces correct language for each preferred_language value.
	- Pressure avoidance: cancellation flow regex check rejects high-pressure copy.
	- Tool integration: orchestrator-agent can invoke `compose_owner_output` via supervisor; output flows back to agent's reasoning.

## Architectural rules to enforce in this subtask
- **Pillar 1 (revised):** composer is in orchestrator-agent's tool inventory. Specialists produce structured `AgentResult` objects, NEVER WhatsApp message text. CodeX rejects any specialist code that constructs message body strings.
- **Pillar 7 (owner-truth):** honesty rules enforced in code. CodeX reviews against explicit checklist.
- **Pillar 8 (no patchwork):** composer is SOLE owner-message producer. Other paths (Fazal escalations) use Telegram, not WhatsApp.
- Template routing config-driven. Adding new trigger→template = YAML change, not code change. New templates require Meta approval (VT-13.3) first.
- Composer is DETERMINISTIC. Same inputs → same output. No LLM call (the agent has already called LLMs to produce `AgentResult`; composer just packages).
- Mixed-language inputs from owners NEVER "corrected" by composer.

## Acceptance criteria
- `compose_owner_output()` exists; returns typed `ComposedOutput`.
- All Tier-A template mappings work; verified by routing tests.
- 24-hour window enforcement triggers correct message_type.
- Honesty rules enforced: regex tests reject pressure phrases, overstatement, certainty claims.
- Refunded-phase messages explicitly acknowledge decision.
- Hindi + English language paths tested.
- Composer is sole call site for `send_whatsapp_message` / `send_whatsapp_template`; CI hash-signature check enforces.
- Composer registered as orchestrator-agent tool (per VT-3.9 tool inventory).
- No LLM calls in composer.
- mypy --strict passes.
- PR title references `(VT-Orchestrator)`; reviewed by CoderX (mandatory — brand-critical); Fazal personally reviews honesty-rule tests.

## Out of scope
- Meta template wording / approval (VT-13.3).
- Tenant `preferred_language` field implementation (VT-9.2 sign-up).
- Owner-portal display of composed messages (VT-9.7).
- Image attachments outbound (Phase 1 text-only outbound).
- Voice notes outbound (Phase 1 text-only outbound).
- A/B testing message variants (Phase 1.5+).
- Composer invocation by deterministic state-machine paths (attribution close confirmation, monthly impact email body) — those are handled by separate composer entrypoints in VT-9.4.

## Branch & PR
- Branch: `feat/vt-orchestrator-output-composer`.
- PR title: `feat(orchestrator): output composer as orchestrator-agent tool (VT-Orchestrator)`.
- Reviewers: Pipeline Engineer (CoderC); Frontend Engineer reviews i18n + template_routing.yaml; CoderX MUST review honesty-rule code paths; Fazal personally signs off on honesty-rule tests.
- Merge target: `dev`.

## Canary (Rule #15 — mandatory)

Real Twilio Content API + real Supabase + DETERMINISTIC composer (no LLM, by spec). The composer outputs structured `ComposedOutput` — the canary asserts the deterministic-rule behavior AND that the resulting message can actually be sent via existing `send_template_message`.

**Canary file:** `apps/team-orchestrator/canaries/vt30_output_composer.py`
**Wall-clock budget:** ≤ 60 seconds.
**Cost budget:** **0 paise** (composer is deterministic Python — no LLM). Real WhatsApp send is OUT of scope for the canary (would actually message Fazal's phone); assertion is "send_template_message would-succeed if called" via dry-run flag.

**PREFLIGHT:**
- Supabase host resolved (for SubscriberState fixtures)
- Twilio Content API token present (for SID validation only — no actual send)
- ANTHROPIC env ABSENT (defense-in-depth: composer must have NO LLM path)
- `twilio_templates.yaml` parses + all 8 SIDs present
- `template_routing.yaml` (NEW, written in this PR) parses

**Group A — observability regression (3 assertions):**

1. **VT-104 redactor regression:** message bodies passed through composer + redactor → byte-identical token format preserved (`phone_tok_HEX`, `<customer_name>`, etc.).
2. **VT-102 pipeline_log regression:** composer invocation emits a `composer_invoked` pipeline_log event with redacted-only payload + ComposedOutput type.
3. **VT-171 Logfire regression:** `traced_node` span captured for composer invocation; span attributes redacted.

**Group B — honesty rules (5 assertions, deterministic):**

4. **No ARRR overstatement:** specialist_result with `attribution_uncertain: True` + `recovered_paise: 5000` → composed message says "approximately ₹50" not "₹50 recovered." Regex-asserted.
5. **No hidden failures:** specialist_result with `terminated_by: hard_limit_tokens` → composed message includes "agent encountered an issue" framing AND identifies which of 5 limits.
6. **No retention pressure:** synthetic cancellation intent → composed message must NOT contain pressure phrases (regex list).
7. **No certainty claims about intent:** specialist_result inferring customer intent → message uses "pattern suggests" / "looks like" framing, not "wants X."
8. **Refunded-phase acknowledgment:** state.phase = 'refunded' → composed message explicitly mentions refund decision (regex-asserted).

**Group C — routing logic (2 assertions, real templates):**

9. **24-hour-window enforcement:** synthetic state where `last_owner_message_at > 24h ago` → composed `message_type = template`; `< 24h ago` and no template applies → `free_form`. Verify against the 8 Tier-A templates in twilio_templates.yaml.
10. **Tier-A intent→template mapping:** for each of the 8 templates, the routing logic produces the correct `template_name` given a synthetic `(intent, phase)` tuple. NO Anthropic involvement.

**Audit artifact** (in `pre-merge-result`):
- Wall-clock from `time` command
- Resolved hosts at PREFLIGHT (Anthropic env ABSENT confirmed)
- Per-assertion observed values for all 10
- **6-canary regression sweep:** VT-102 + VT-103 + VT-104 + VT-171 + VT-28 + VT-176 (these run unchanged on current main against the composer's path through redactor / pipeline_log / Logfire)
- Sample `ComposedOutput` JSON for each of the 8 Tier-A intent paths
- Confirmation that `gate-no-llm-in-deterministic-triggers` would-extend to `output_composer.py` if scope expanded (composer is deterministic; gate could include `src/orchestrator/output_composer.py` whole-file)
- Full stdout tail ≥ 150 lines + log at `/tmp/vt30-canary-evidence.log`

Summary-only `pre-merge-result` will be bounced.

## Status history
- 2026-05-25 03:45 IST: migrated from Notion (notion_legacy_id: 356387c2-cc5a-81be-bf36-df958788e1c2)
- 2026-05-26 17:25 IST: queued by Cowork post-VT-176 merge. Brief artifacts normalized (CodeX/CoderC/CoderX retired, dev→main, PR title VT-30); dependency-reality corrected (send_template_message EXISTS; twilio_templates.yaml has all 8 SIDs; direct_handlers exist as bypass paths). Rule #15 Canary section appended (10 assertions across 3 groups). Rule #16 ledger check ran (30 active-context rows surfaced; cl_decisions_checked in brief-ready signal frontmatter). Status flipped Backlog → Queued.
