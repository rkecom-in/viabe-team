---
id: VT-497
title: "Decouple SR model-tier from VIABE_ENV — validate the win-back on the prod model tier without un-mocking sends"
status: Todo
priority: Low
type: BUILD
area: team-orchestrator
created: 2026-06-30
authorization_basis: "Win-back re-drive #2: dev validates the SR specialist on claude-haiku-4-5 (cheap test slot); Haiku emits a malformed plan in 4/5 runs. Prod uses Opus. The only model-tier lever is VIABE_ENV=production, which ALSO un-mocks the send guard (sales_recovery_executor.py:101) — so the loop can't be validated on the capable tier safely. NEEDS STEER."
---
# VT-497 — SR model-tier / send-guard env coupling (Cowork/Fazal steer)
The win-back last-mile validation is blocked by a coupling: the SR specialist's model tier AND the send-safety guard both key on VIABE_ENV. To test whether the win-back closes on the prod model tier (Opus), you'd flip VIABE_ENV=production, which un-mocks real sends — unsafe. Decouple: an independent SR-model override (e.g. SR_MODEL env) so dev can validate the win-back on Opus/Sonnet while the send guard STAYS mocking (EXPECTED_ENV=dev). Then re-drive on the capable tier confirms whether "last mile not closed" is a Haiku-tier artifact vs a real defect. STEER: Fazal/Cowork on the approach (likely SR runs the capable tier always, like the brain VT-480 — Haiku is too weak for CampaignPlan composition).

## UPDATE (VT-496 diagnostic): the win-back parse failure is a SINGLE mechanical field (campaign_window) → fixed by VT-499 (compute server-side), NOT by the model tier. VT-497 (SR on Opus) is likely UNNECESSARY for the win-back — revisit ONLY if other fields fail after VT-499 lands. Downgraded.
