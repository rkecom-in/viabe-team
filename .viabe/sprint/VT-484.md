---
id: VT-484
title: "Brain win-back mis-routes to spawn_integration + orphaned tool_use hangs the run (400 tool_use without tool_result)"
status: Done
priority: Critical
type: BUILD
area: team-orchestrator
created: 2026-06-29
authorized_by: fazal
authorization_basis: "Cowork CLOSE-winback audit (163000Z) + last-mile drive: the win-back (the sign-off SEND) can't reach SR — launch-blocker before sign-off."
---
# VT-484 — brain spawn_integration orphaned tool_use (LAUNCH-BLOCKER for the win-back sign-off)
Last-mile drive: the brain routes the win-back to spawn_integration (mis-route — should reach spawn_sales_recovery), which RAISES + orphans its tool_use → the next Anthropic call 400s ("tool_use without tool_result") → run hangs at 'running', never reaches SR. Deterministic, independent of tenants.phase. TWO defects: (a) a langgraph tool-error→tool_result handling gap (a raised tool must still emit a tool_result so the next turn is valid), (b) win-back routing mis-targets integration. Plan-first (deep brain/langgraph path). Adversarial-verify: a win-back intent reaches spawn_sales_recovery and the run completes (no 400, no hang).
