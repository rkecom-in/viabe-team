---
vt_id: VT-267
title: Owner business-profile onboarding experience (Integration Agent → identity)
status: Done
priority: High
sprint: Sprint 2 - Cost + Moat
type: Feature
area: [Orchestrator, Integration-Agent, Moat, Onboarding]
assignee: unassigned
parent: VT-204
sub_items: []
exec_order: 99
depends_on: [VT-195]
---

# VT-267 — productized L1 business_profile onboarding

RKeCom's L1 'business_profile' is seeded manually for launch (VT-195 Phase 3).
The productized path for EXTERNAL owners: extend the Integration Agent (VT-206)
from connectors → IDENTITY —
- draft a business_profile from public sources (website / Google Business
  Profile) + a few WhatsApp questions;
- owner confirms conveniently (WhatsApp one-tap / /team/onboard);
- write l1_entities via the Phase-3 L1 writer (`upsert_business_profile`,
  VT-195).

Depends on the L1 writer VT-195 Phase 3 ships. Parent VT-204/VT-9. CL-421:
zero-manual-paste, OAuth + auto-config only. "Take up next" per Fazal
(2026-05-31). Backlog — do NOT build until prioritized.

## PR-C (owner onboarding wizard) — 2026-06-03, Review

Built the owner-facing onboarding wizard (Fazal OAuth ruling: system-browser handoff).
- `lib/onboard/connect.ts` — startConnect (orchestrator mints VT-289 nonce → provider authorize
  URL opened in the SYSTEM browser) + checkConnection (fail-closed resume from
  tenant_connector_status / tenant_whatsapp_accounts).
- `lib/onboard/profile.ts` + `api/onboard_step.py::save_business_profile` — draft read (L1
  business_profile via serverSecretClient) + Review-&-Confirm save through the orchestrator
  (INTERNAL_API_SECRET, owner-editable allowlist, MERGE-not-clobber via upsert_business_profile so
  apify enrichment siblings survive).
- `app/(app)/team/onboard/wizard/{page,actions}.tsx` + `components/onboard/onboarding-wizard.tsx` —
  renders in the WhatsApp WebView; edit states; connect = tappable anchor (target=_blank, NO
  window.open popup) + "I've connected" re-check resume. Auth: requireFazal, tenant server-side
  (FAZAL_TENANT_ID Phase-1).
- Tests: 20 team-web vitest (handoff URL, fail-closed resume, draft map, allowlist save, no-popup
  source assertion) + 5 real-PG orchestrator canary (MERGE-not-clobber, secret gate, allowlist).
- FLAGGED seams: profile-draft enrichment is Apify-key-gated (displays existing L1, empty until
  provisioned); full Playwright WebView-UA E2E + real OAuth round-trip are vendor-gated → deferred.

## Status history
- 2026-06-03: PR-C built (Review) — Fazal OAuth system-browser-handoff ruling; PR open, awaiting Pillar-7.
