---
id: VT-715
title: "DPDP consent-record email (both channels) + owner-email substrate"
status: Built — awaiting promotion; email-capture placement = Fazal decision
priority: High
exec_order: 715
owner: claudecode
created: 2026-07-28
authorization_basis: "Fazal 2026-07-28 run-5 review #1: 'When we get the DPDP consent and privacy policy agreed by the tenant, either from the UI or through Whatsapp, in both cases we must send out an email to the tenant that will work as a record of the consent.'"
---

## What shipped

- **Migration 185** (applied dev): `tenants.owner_email` — the FIRST owner-email substrate in
  the product (monthly reports have passed owner_email=None since VT-86) — and
  `consent_records.record_email_sent_at` (the idempotency stamp).
- **consent_record_email module**: factual record (DPDP + residency consents, notice versions,
  capture channel, UTC timestamp, masked WhatsApp, business name; no customer PII) via the
  VT-86 Resend client. Idempotent (stamped once), fail-soft (never gates consent).
- **Hooks at BOTH seams**: web-UI create_signup_tenant (channel "web signup form (UI)") +
  WhatsApp create_whatsapp_signup_tenant (channel "WhatsApp 'I agree' button").
- **Pending semantics**: NO owner_email exists today on EITHER path (web form doesn't collect
  it either) → every record starts PENDING; `send_pending_consent_record(tenant)` retro-fires
  the moment an email is captured.

## OPEN — Fazal's call: where to CAPTURE the owner email

Options: (a) an onboarding journey question (costs one residual slot), (b) a post-activation
ask ("where should I send your records/reports?" — pairs naturally with the trial confirm),
(c) portal/web-form field (also fixes web signups). Until one ships, WhatsApp-consent records
stay pending. Recommend (b)+(c).

## Evidence

5 pins (renderer facts, pending-without-email, send+stamp-once, never-resend, fail-soft);
fresh-PG 29 migrations green; migration applied on dev; onboarding sweep 497 green.
