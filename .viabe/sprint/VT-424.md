---
vt_id: VT-424
title: Razorpay subscription — real razorpay.subscription.create (replace sub_stub_*), Idempotency-Key
status: Queued
priority: Critical
sprint: "Sprint-Fazal"
type: Feature
area: [Backend, Billing, Money, Launch]
related: [VT-416, CL-431]
exec_order: 93
created: 2026-06-25
last_updated: 2026-06-25
---

## Contract (Cowork GATE 2026-06-25, from the VT-416 sweep Row A). LAUNCH-BLOCKER money gap. Build the real code now; HOLD the live wiring/canary for Fazal's keys.

`_create_razorpay_subscription` (`api/razorpay_subscribe.py:63-88`) returns a STUB `sub_stub_<tenant>_<hash>` — at
go-live a trial→paid conversion does NOT create a real Razorpay subscription (cannot bill). Gated behind
`PlanIdNotConfiguredError`→503 (fails closed if keys absent).

## Scope
- Replace the stub with the real `razorpay.subscription.create` + an **Idempotency-Key** header (no double-subscribe
  on retry). Keep the fail-closed `PlanIdNotConfiguredError`→503 when plan IDs/keys are absent.
- **NEEDS-FAZAL:** live Razorpay keys + **plan IDs** + the money-canary (Cowork requesting). Build BEHIND that — the
  code path is testable with a stubbed razorpay client now; the LIVE call + canary wait on Fazal.
- Canary (Rule #15): a real (test-mode) subscription create → assert the subscription id shape + idempotent retry.

## Gate
RISK (money). Build → Cowork gate, NOT self-merge. Pillar-7 Fazal merge.

## Status history
- 2026-06-25: Rostered + ID allocated (Cowork VT-416 gate, Row A). Build the real code; live keys/plan-IDs NEEDS-FAZAL.
