---
vt_id: VT-401
title: Complete every sender's template_params + re-add strict fail-closed (no template ships Twilio samples)
status: Backlog
priority: High
sprint: "Sprint-Fazal"
type: Bugfix
area: [Owner Surface, Specialist Agent, Legal/Policy]
assignee: Claude Code
related: [VT-400]
exec_order: 2
created: 2026-06-23
last_updated: 2026-06-23
---

## Contract (follow-up to VT-400, 2026-06-23)

VT-400 fixed the welcome by mapping present params positionally + OMITTING missing ones (no regression).
But omission means any template sent with empty/partial params still renders Twilio's **sample values**
("Raj Cafe" class). Senders known to pass `{}`/partial today: opt-out, DSR, status-ping,
template-error confirmation handlers, and `team_weekly_approval` (request_owner_approval).

## Scope
1. **AUDIT** every live template send: which templates have variables that go UNFILLED on a real send,
   and what sample text they render. **Severity-rank** — compliance templates (opt-out / DSR
   confirmations) shipping sample text to real owners = treat as Critical; cosmetic = lower.
2. Complete each sender's `template_params` so every declared variable is supplied (real values).
3. **Re-add strict fail-closed** in send_template_message (a missing declared var → refuse to send,
   never emit samples) once all senders supply complete params.
4. Tests per sender; assert no template renders a Twilio sample for a real send.

## Acceptance
- No live template path can emit Twilio sample values.
- Strict fail-closed restored as the permanent guard.

## Status history
- 2026-06-23: Rostered (Cowork) — VT-400 deviation follow-up. Plan-first when picked up.
