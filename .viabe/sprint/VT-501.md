---
id: VT-501
title: "SR draft reliability — evidence_refs schema misses ~4/6 on dev (coerce-repair + confirm SR model tier)"
status: Done
priority: High
type: BUILD
area: team-orchestrator
created: 2026-06-30
authorization_basis: "Final win-back re-drive (VT-500 closed): a VALID grounded plan goes all the way to armed-send-ready, but the SR drafts a valid CampaignPlan only ~2/6 on dev — 4/6 emit invalid evidence_refs (too_short ×3, marker value_error ×1) and escalate cleanly (fails SAFE, no bad send). Reliability, not safety."
---
# VT-501 — SR draft reliability (evidence_refs schema misses; FAILS SAFE — reliability not safety)
The win-back last mile is CLOSED (VT-500): a valid grounded SIMPLE plan parses→PII-safe→ships→arms send-ready→pauses at owner-approval. BUT the SR LLM (Haiku on dev) satisfies the CampaignPlan schema only ~2/6: 4/6 emit invalid evidence_refs (min_length>=1 empty ×3; prose-marker⇄evidence_ref consistency value_error ×1, campaign_plan.py:312-336) → clean escalate (no bad send). The validators are CORRECT grounding rails (don't weaken). Options (plan-first): (a) a coerce-repair layer that heals the common evidence_refs misses (like VT-499 did for campaign_window — system supplies/repairs the mechanical part), and/or (b) confirm + fix the dev SR model tier (Haiku botches the schema — same root as the campaign_window VT-499 issue; re-raises the VT-497 spirit: SR should draft on a capable tier, OR validate on the prod tier). Determine the right lever (repair vs tier) — needs a Fazal/Cowork model-tier decision for (b).
