---
vt_id: VT-16
title: Cross-link pillars file from concept-team.md and CLAUDE.md
status: Cancelled
priority: Critical
sprint: Pre-Sprint 0 - Pillars & Setup
type: Documentation
area: [Documentation]
assignee: Fazal
parent: VT-1
sub_items: []
exec_order: 2
branch: "docs/vt-pillars-crosslinks"
version: "v1.0"
notion_legacy_id: 356387c2-cc5a-8134-90a0-e9d4bc679944
last_updated: 2026-05-25T03:45:00+05:30
---

# VT-16 — Cross-link pillars file from concept-team.md and CLAUDE.md

## CLOSURE (2026-06-03, Cancelled — superseded)
Superseded with parent VT-1: both cross-link targets are moot — `concept-team-pillars.md` is not
being authored, and `concept-team.md` doesn't exist. The pillars live in CLAUDE.md (Pillar 7),
decisions-ledger, and CL entries. (Cowork-confirmed 2026-06-03.)

## Parent task
VT-Pillars.

## Concept reference
- `concept-team-pillars.md` v1.0 (the file produced by the previous subtask under this parent).
- `concept-team.md` (full file — the cross-link goes at the top).
- `CLAUDE.md` (the Team product section — the cross-link goes at the top of that section, not the top of the whole file).
- `Viabe_AI_Technical_Reference_v2_0.md` (Reports' engineering reference — referenced for context, not modified).

## Dependencies
- `concept-team-pillars.md` v1.0 must be merged to `main` (the previous subtask under this parent).
- The exact source-of-truth banner wording must be available verbatim from the merged pillars file.

## What CoderC must produce
Two file edits in a single PR:
1. **Edit `concept-team.md`**: Insert the source-of-truth banner block at the very top of the file, above any existing content (including the existing introduction). The banner is a verbatim copy of the banner block at the top of `concept-team-pillars.md` — same wording, same hierarchy declaration, same formatting. After the banner, add a one-paragraph note pointing readers to `concept-team-pillars.md` for inviolable architectural rules and stating that any conflict between this file and the pillars file is resolved in favor of the pillars file.
2. **Edit `CLAUDE.md`**: Locate the Team product section. At the top of that section (not the top of the entire file — `CLAUDE.md` is cross-product and the banner is Team-specific), insert the same source-of-truth banner block, followed by the same one-paragraph pointer note as above. If the Team product section does not yet exist in `CLAUDE.md`, create it with a section header (`## Viabe Team — Source of Truth`) and place the banner + note inside.
The banner must be character-for-character identical across all three files (`concept-team-pillars.md`, `concept-team.md`, `CLAUDE.md`). Sloppy copies create future ambiguity. Use a copy-paste, not a re-typing.

## Architectural rules to enforce in this subtask
- Pillar 8 (no patchwork): the cross-links are structural, not editorial. The banner block is the same in every file. Do not paraphrase, summarise, or shorten the banner to fit context.
- The cross-link must be at the top of `concept-team.md` (the file as a whole), and at the top of the Team product section of `CLAUDE.md` (not the top of `CLAUDE.md` as a whole — it is cross-product).
- The pointer paragraph must explicitly state the conflict-resolution rule: pillars file wins over the file the reader is currently in.
- The PR must include a verification checklist: "Banner wording in `concept-team.md` matches `concept-team-pillars.md` byte-for-byte" and "Banner wording in the Team section of `CLAUDE.md` matches `concept-team-pillars.md` byte-for-byte." Verified by `diff` output in the PR description.
- No code changes. No restructuring of either file beyond the insertion. Existing content in both files is preserved verbatim.

## Acceptance criteria
- `concept-team.md` shows the source-of-truth banner block at the top, identical wording to `concept-team-pillars.md`.
- `CLAUDE.md` shows the source-of-truth banner block at the top of the Team product section (creating the section if needed), identical wording.
- Both files include the one-paragraph pointer note immediately after the banner, restating the conflict-resolution rule.
- `diff` between banner blocks in all three files returns empty (no whitespace differences, no line ending differences).
- Existing content in `concept-team.md` and `CLAUDE.md` is preserved — only insertions, no rewrites.
- Fazal personally approves the PR (Type 3 sign-off, recorded as a Notion comment on this subtask).
- PR merged to `main`, mirroring the merge target of the previous subtask under this parent.

## Out of scope
- Editing `Viabe_AI_Technical_Reference_v2_0.md` (Reports' engineering reference — untouched).
- Editing `Viabe_Team_Technical_Reference_v1_0.md` — that file does not yet exist; banner placement there is part of VT-EngineeringReference.
- Restructuring `concept-team.md` or `CLAUDE.md` beyond the banner insertion.
- Adding pillar implementation rules to either file.
- Updating any code, configuration, or CI to enforce banner presence — banner enforcement is documentation-only at this stage.

## Branch & PR
- Branch: `docs/vt-pillars-crosslinks`.
- PR title: `docs(pillars): cross-link pillars from concept-team and CLAUDE (VT-Pillars)`.
- Reviewers: Fazal (Type 3 sign-off, mandatory), CoderX (verifies byte-for-byte banner match).
- Merge target: `main` directly (same exception as the previous pillars subtask). Fazal personally executes the merge.

## Status history
- 2026-05-25 03:45 IST: migrated from Notion (notion_legacy_id: 356387c2-cc5a-8134-90a0-e9d4bc679944)
