---
id: VT-490
title: "SR cohort-surfacing — supply the dormant customer rows to Sales-Recovery (PII, plan-first)"
status: Done
priority: Critical
type: BUILD
area: team-orchestrator
created: 2026-06-29
authorization_basis: "Final win-back re-drive: the PRIMARY blocker to a gate-passed win-back draft."
---
# VT-490 — SR cohort-surfacing (LAUNCH-BLOCKER for the win-back sign-off; PLAN-FIRST, risk/PII)
build_sales_recovery_context (context_builder.py:595) surfaces only AGGREGATE percentiles — NO individual customer_ids/per-customer recency — and SR has no tools (sales_recovery.py:445 tools={}) to query. SR's prompt requires the cohort SUPPLIED → it correctly returns insufficient_data. VT-485 fixed the recency MATH; the cohort-SURFACING gap remains. FIX (plan-first — Fazal/Cowork design call): surface the dormant cohort customer rows into the SR context WITH CL-390 redaction / k-anonymity / token-budget / truncation-order design (it's customer PII into an agent prompt). Then SR grounds a 'proposed' CampaignPlan. Risk row — money + PII path.
