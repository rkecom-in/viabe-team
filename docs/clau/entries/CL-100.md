---
cl_id: CL-100
entry_type: Next Action
topic: Process
status: Resolved
session_date: 2026-05-18
created: 2026-05-17T23:21:35.817Z
source: Fazal 2026-05-18 in response to audit concern 3
linked_tasks: PR #15 (#11-stacked). Closes when dry-run result reported.
notion_legacy_id: 363387c2-cc5a-81da-89aa-d65c6ac78d36
title: "NEXT ACTION: Claude Code does local Docker-Postgres dry-run of PR #15 before #11 merges. 10-min timebox; if Docker unavailable, accept discovery latency."
---

# CL-100 — Local Docker-Postgres dry-run of PR #15

NEXT ACTION (Fazal 2026-05-18 in response to audit concern 3): Claude Code attempts a local Docker-Postgres dry-run of PR #15 BEFORE PR #11 merges, to surface any breakage in #15's new tests pre-merge.

WHY: #15's CI hasn't run (base branch is #11's, not main; GitHub auto-retargets and runs CI only post-#11 merge). Discovering #15 broken AFTER #11 is in main creates an awkward state: failing PR atop a merged predecessor, complicating rollback.

PROCEDURE (Claude Code-side):
1. Spin up Postgres in Docker: `docker run -d --name viabe-pg-test -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres:17`.
2. Set `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres` (or equivalent for your sandbox).
3. Apply migrations: from apps/team-orchestrator, `uv run python scripts/apply_migrations.py`.
4. From #15's branch (currently `fix/vt-orchestrator-brain-pending-record`, based on #11): run `uv run pytest tests/ -v`.
5. Report results back: green or specific failure.
6. Teardown: `docker rm -f viabe-pg-test`.

IMPORTANT CAVEAT (per Fazal): if Claude Code's sandbox can't run Docker (parity with Fazal's local-Docker-broken issue per open tech debt #1 in CL-66), DO NOT burn cycles fighting infrastructure. Report 'Docker unavailable in sandbox' and accept discovery latency. The fallback is: merge #11, then watch #15's auto-retargeted CI run; if it fails, fix in place.

No-tool-fight rule: 10-minute upper bound on the dry-run attempt. If it works in 5, great. If 10 minutes in we're still on infra issues, abort.

NOTE: #15's tests include the new `test_substantive_message_marks_run_escalated` + `test_status_callback_delivered_completes_clean`. These don't yet require migration 014's UNIQUE constraint (since #15's INSERTs ship without ON CONFLICT initially per CL-89). So the dry-run runs against pre-#16 schema and should pass cleanly. The CONFLICT-related tests get added in the post-#11+#16 rebase commit and won't run until then.

CLOSES: CL-90 concern 3 once dry-run reported (green or specific failure or 'Docker unavailable').

## Status history
- 2026-05-17T23:21:35.817Z: created (status=Resolved)
- 2026-05-25 03:55 IST: migrated from Notion (notion_legacy_id: 363387c2-cc5a-81da-89aa-d65c6ac78d36)
