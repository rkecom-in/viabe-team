---
vt_id: VT-306
title: Complete tenant-wrapper coverage + migrate call sites + flip lint to hard-fail
status: Backlog
priority: Medium
sprint: Sprint 7 - Knowledge Architecture
type: Chore
area: [Privacy, Database]
assignee: unassigned
parent: VT-72
sub_items: []
exec_order: 72
blocked_by: [VT-65, VT-66, VT-71]
---

# VT-306 — complete tenant-wrapper coverage + migrate call sites + flip lint hard-fail

Follow-up split out of VT-72 (Cowork 20260603T163500Z). VT-72 shipped the
`TenantScopedTable` primitive + 5 LIVE hot-table wrappers + the
`no-direct-tenant-db-access` lint in report/allowlist mode. This row completes
the layer-2 coverage.

## Why split (the no-stale bar)
- Wrappers for UNBUILT tables (EpisodicEvents/VT-66, KGEventsProcessed/VT-65,
  CompositionAudits/VT-71) can't be built until their substrate lands — wrapping
  unbuilt tables is the stale thing the bar forbids. `blocked_by` those rows.
- A full call-site migration (the ~22 allowlisted direct-access sites) is a large
  refactor; layer-1 RLS already protects them, so it's incremental — not a launch gap.

## Scope
1. Add wrappers for the remaining tenant tables as their substrate lands
   (EpisodicEvents, KGEventsProcessed, CompositionAudits, + any others).
2. Migrate the 22 allowlisted direct-access sites
   (see `scripts/check_no_direct_tenant_db_access.py` allowlist) to the wrappers.
3. Empty the lint allowlist + flip `gate-no-direct-tenant-db-access` to hard-fail.
4. Update `docs/team/cross-tenant-tables.md`.

## Dependencies
- VT-72 Done (primitive + wrappers + report-mode lint shipped).
- VT-65 / VT-66 / VT-71 (the unbuilt-table substrates).
