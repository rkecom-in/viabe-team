---
id: VT-607
title: "Loop P6 (moved up) — Sales Recovery returns to the Manager: SpecialistReturn adaptation + manager-review grounding validation — the first loop proof"
status: Done
priority: launch-blocker
type: BUILD
area: manager
created: 2026-07-05
authorized_by: fazal
authorization_basis: "Fazal 2026-07-05 loop-program grant — execution plan Package 6; CC merge-order amendment (cheapest, highest-value loop proof precedes the risky conversions)."
---
# VT-607 — first specialist through the loop (expert Package 6)
Preserve cohort detection, structured plan, grounding validation, self-eval, continuation handling.
SR consumes the Manager's desired outcome + acceptance criteria; returns typed SpecialistReturn; a
proposed campaign = effect_intent (never bypasses manager_review). Review verifies cohort grounding,
expected recovery, evidence refs, requested outcome BEFORE the approval/autonomous effect arms.
L2 pauses for approval; L3 only inside owner policy + all deterministic rails. Campaign + delivery
evidence recorded before task completion. Acceptance per execution plan Package 6 (incl. rejected/
revised/failed plans returning to the Manager). Exec order 5.

## RESOLVED 2026-07-05 — landed (a5e6aa1, ee4b631, 328b78a)
- FK fix: loop dispatches mint real pipeline_runs rows (run_type='manager_dispatch', full
  paused/escalated/completed lifecycle) — observability parity + the approval FK satisfied for real.
- Package 6: manager desired_outcome/acceptance_criteria threaded into the SR bundle; deterministic
  CampaignPlan→PlanSpecialistReturn adapter (no LLM re-interpretation); cohort grounding against
  REAL tenant rows (wrapper-layer) BEFORE review acceptance — hallucinated/foreign ids block.
- TWO criticals caught + fixed in review: (1) paused_approval resolution ignored the owner's
  DECISION (a rejected campaign settled completed_with_effect) → decision-aware routing
  (approved/rejected→cancelled/needs_changes→revise/timeout→blocked+incident), poll-polarity +
  per-decision revert-sensitive tests; (2) manager_review_outcome undeclared in AgentGraphState —
  LangGraph silently dropped it, every clean terminal read the escalate fallback (masked by the
  approval-pause paths) → declared + pinned.
- Battery: 3,491/0 full suite; re-verify 2/2 fixed. Residuals logged in program doc: the
  manager-task terminal→owner NOTIFICATION composer is unbuilt (VT-611 first build item);
  campaign_execution_blocked undeclared dead-write (declare on next touch).
