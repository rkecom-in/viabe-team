---
vt_id: VT-334
title: Weekly-approval "defer" decision + per-week messaging budget
status: Backlog
priority: Low
sprint: Sprint 8 - Owner Surface & Billing
type: Feature
area: [Owner Surface]
assignee: unassigned
parent: VT-9
sub_items: []
exec_order: 3.1
blocked_by: []
tags: [owner-surface]
---

# VT-334 — approval "defer" decision + per-week messaging budget (VT-83 deferrals)

VT-83 (#345) shipped the template-params fix + the deterministic approval-reply classifier.
Cowork deferred two enhancements as NOT launch-critical (scope ruling 20260605T152200Z):

## Scope
1. **"defer" decision** — owner replies later/बाद-में/next-week → the approval window
   EXTENDS 48h (max 2 defers), then treats as rejected (audit note). Today "timeout" is
   system-expiry, not an owner-initiated "later". Adding defer = a new `Decision` Literal +
   the `pending_approvals.decision` CHECK (CL-428 sync) + an extend-counter + the
   max-2-then-reject logic. Batch the CHECK with any other pending_approvals change.
   Add the defer keywords to `owner_inputs/approval_reply.py` (later/baad-mein/बाद में).
2. **Per-week messaging budget** — max 2 approval requests per owner per week
   (owner-fatigue guard). Cleanest in the weekly-cadence fan-out (skip tenants already at
   budget) vs in arm_pause_request. Documented in `docs/team/owner-messaging-budget.md`.

Neither is launch-critical (timeout covers expiry; the budget is belt-and-suspenders).

## Origin
VT-83 #345 scope ruling — Cowork deferred Gap #2 + Gap #3 (20260605T152200Z).
