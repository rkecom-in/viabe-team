---
vt_id: VT-422
title: Viabe Shopify OAuth app — public app, zero-paste OAuth install (Integration Agent connector)
status: In Progress
priority: Critical
sprint: "Sprint-Fazal"
type: Feature
area: [Backend, Integrations, OAuth, Auth, PII, Launch]
related: [VT-417, VT-415, VT-396, VT-421, CL-421, CL-390, CL-425]
exec_order: 92
created: 2026-06-25
last_updated: 2026-06-25
---

## Contract (Cowork 2026-06-25; Fazal: pivot from the dev-token/custom-app shortcut to a PUBLIC Viabe Shopify app, OAuth install — "read+write scopes"). PLAN-FIRST → Cowork gate. LAUNCH-BLOCKER. RETIRE the dev-token seed.

Build the REAL Shopify integration: a public Viabe app the seller installs (Install → Approve), Shopify hands us a
per-store token automatically — CL-421 zero-manual-paste. Replaces the `shopify-dev.env` custom-app token (test
fallback only; Fazal will NOT grant dev write-scope on it).

## Scope (plan maps EXISTS vs GAP)
- **EXISTS:** the connector ingestion — `shopify_webhook.py`, `connectors/shopify.py`, `ingest.py`, `backfill_orders`.
- **GAP (build):** the OAuth INSTALL flow (install URL → Shopify consent → callback → code→token exchange, with the
  `state` CSRF param); **per-tenant ENCRYPTED token storage** (keyed tenant_id, RLS-scoped, DSR-purgeable — the
  tenant's credential, CL-390/425); **webhook REGISTRATION on install** (orders/create → our endpoint); scope config;
  the onboarding wizard "connect Shopify" → triggers the OAuth install (reuse the VT-415 owner session).
- **Scopes = READ + WRITE:** read_customers/read_orders/read_products + write_orders/write_customers (+ future
  write-agent scopes — confirm the exact set). Write also unblocks the e2e backdated seed via the app token.
- **Distribution = UNLISTED public app** (custom distribution / install link) for launch — NO App Store review dep.
  App Store listing = a later fast-follow row (note it).
- **Secrets:** the ONLY secret is OUR app `client_secret` (one, ours, by-reference, CL-431/Rule-18 — never echoed).
  The tenant shares NOTHING.

## FAZAL-SETUP (CC cannot do — flag in the plan): create the app in the Shopify Partner dashboard; get
client_id/client_secret; set the dev (+ prod later) redirect/callback URL(s); pick the dev store to test against.

## Gate
RISK (OAuth / auth / PII / credential storage). Plan-first → Cowork gate; build (grouped) → gate, NOT self-merge.

## Status history
- 2026-06-25: Rostered (Cowork; Fazal "yes go ahead" — read+write public OAuth app). Plan dispatched (ultracode). Dev-token seed retired.
