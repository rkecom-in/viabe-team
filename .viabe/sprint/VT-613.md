---
id: VT-613
title: "campaign_messages.campaign_id never populated — broken message→campaign audit link (send-path)"
status: Todo
priority: High
type: BUILD
area: manager
created: 2026-07-06
authorized_by: fazal
authorization_basis: "Surfaced by VT-611 Package H1 (2026-07-06) while building the gate's DB-state effect asserts — a real pre-existing prod audit-trail gap, not introduced by the gate work. Rostered per 'fix agreed issues immediately'; scheduled behind the VT-611 gate run (workaround holds)."
---
# VT-613 — populate campaign_messages.campaign_id

## Finding (VT-611 H1)
`send_whatsapp_template.py:_write_campaign_message` (the SOLE campaign_messages INSERT path, line ~474)
**omits `campaign_id`** from the INSERT — so `campaign_messages.campaign_id` is NULL for every send, real or
dev-mocked. The message→campaign link is broken in the production audit trail: you cannot cleanly JOIN a sent
message back to its campaign / its approval.

## Impact
- Audit trail incomplete: no direct FK path campaign_messages → campaigns → pending_approvals.
- The VT-611 gate's `assert_no_unapproved_effect` / `assert_side_effects` work around it TODAY via
  `campaign/execute.py`'s own idempotency-key convention (`{campaign_id}:{customer_id}` LIKE-match; UUIDs never
  contain `:`). Mechanically sound + tested (21 realdb cases), but a workaround, not the fix.

## Fix
Thread `campaign_id` into `_write_campaign_message`'s INSERT (the value is available at the call site via the
CampaignPlan / execute path). Small + mechanical BUT send-path-touching → **plan-first, risk-row discipline**
(canary the real send after). Backfill is out of scope (forward-only column population is enough for the audit
link going forward). Then the H1 asserts can drop the LIKE-match workaround for a clean campaign_id JOIN.

## Does NOT block the VT-611 gate
The idem-key correlation is sound (single insert path, idempotency_key required), so the gate's effect asserts
are valid today. This is prod-observability hygiene, scheduled AFTER the gate run unless Fazal reprioritizes.
