---
id: VT-700
title: "Activation UX: Manager-introducing ACTIVATE TEAM button + post-grant specialist-agent chooser"
status: Built — awaiting dev push + promotion
priority: Critical
exec_order: 700
owner: claudecode
created: 2026-07-23
authorization_basis: "Fazal 2026-07-23 ~03:30 IST (live): the activation ask 'could have had a button for easy response' + 'this message needs to introduce the Manager to the Owner and then the owner must be able to choose from the list of specialist agents to activate'"
---

## What shipped

1. **The activation ask introduces the Manager** (`consent_required_handler._CONSENT_PROMPT`):
   "Hi! I'm your Manager — the head of your new Viabe AI team…" (runs tasks on WhatsApp, owner
   stays in charge, nothing to a customer without approval) → go-ahead ask. Consent-bearing
   sentences (process-messages/customer-data + STOP) carried VERBATIM; VT-272 interim-copy
   TODO stands.
2. **Tappable grant** — new `team_activate_button` Content object (canary-created,
   HXfbdb7ea9…): body {{1}} = the full prompt (var-1 recording keeps the enable phrase in
   conversation_log → runner ask-recognition unchanged); buttons "ACTIVATE TEAM" (the EXACT
   grant floor — zero classifier change) + "Not now" (deterministic decline path). Freeform
   fallback unconditional. Decline-ack prefix survives the interactive path.
3. **Post-grant agent chooser** (`data_inputs_enable_handler`): the grant confirm now carries
   the specialist-agent chooser in ONE message — trial terms plain (FREE 1-month per agent,
   paid after ONLY on explicit continue) + buttons [Sales Recovery / Customer Win-back /
   Campaigns] via journey_suggest_3; arms `__flow__=agent_choice` on the completed journey
   (fail-soft — activation never depends on journey state).
4. **`agent_choice` beat** (journey paced flow): an exact catalog tap records
   `activated_agents` into the draft (source=owner, dedup-append), confirms the trial + asks
   data-connection readiness in ONE message (Yes/Later buttons), hands to the EXISTING
   readiness machinery. "Later" defers; free text falls through to the brain (never hijacked)
   with the pick still armed.

## Catalog-sync invariant

`data_inputs_enable_handler._AGENT_BUTTONS` lowercased == `journey._AGENT_CATALOG` keys —
pinned by test. Catalog contents are Fazal-editable (initial three grounded in live SR arcs).

## Sequencing note (Clau ruling pending)

The consent gate outranks the paced flow, so unactivated owners meet THIS Manager-intro ask
before the VT-698 team_intro beat — per Fazal's ruling the intro now lives IN the ask, and the
post-grant chooser covers agent opt-in. The VT-698 beats remain for activated tenants. Whether
team_intro should be suppressed post-VT-700 (double-intro risk for owners who activate then
ack) is flagged to Clau.

## Evidence

FULL orchestrator DB sweep 5345 passed. New pins: test_vt700_activation_ux.py (8 — prompt
copy, interactive-first both handlers, fallbacks, decline-ack, chooser vars, flow arming,
catalog sync) + 3 agent_choice beat tests in test_journey_paced_flow.py; pre_filter handler
tests updated to capture the interactive leg.
