---
vt_id: VT-374
title: VTR Run-Control Substrate (Phase A) — controllable app-step executor, overrides, pause, app-level rerun, harness
status: Done
priority: Critical
sprint: "Sprint-Fazal"
type: Feature
area: [Orchestrator, Ops Console, Privacy]
assignee: Claude Code
related: [VT-375, VT-376, VT-377, VT-370, VT-369, VT-300]
exec_order: 1
cl_decisions_checked: [CL-24, CL-26, CL-29, CL-33, CL-35, CL-36, CL-41, CL-52, CL-56, CL-79, CL-81, CL-82, CL-88, CL-132, CL-220, CL-252, CL-260, CL-424, CL-426, CL-428, CL-429, CL-431, CL-432, CL-434, CL-435]
created: 2026-06-11
last_updated: 2026-06-11
---

## Contract

**THE PLAN IS THE CONTRACT: `.viabe/queue/VT-374/plan.md` (v2 + §12 amendments).**
Adversarially gated twice (GATE-FAIL v1 → redesigned → GATE-PASS v2 with §12 mandatory
conditions). Architecture: app-level run control — controllable/observed step registry,
consume-first overrides (recovery-idempotent, N2), boundary-only checkpointed pause with
opt-out/DSR exemption (I6), app-level `rerun_from` with fresh identity + side-effect
policy (I8), `vtr_step_timeline` keys-only view, step harness CLI. NO DBOS fork.

STEP-0 findings + the N1 `run_controls` retire-vs-coexist arm go in the plan-ack signal
BEFORE building.

## Merge class

Risk row (core seams + RLS + PII-adjacent): NOT self-merge. Build → pr-ready signal →
Cowork adversarial gate with executed evidence → Cowork-authorized dev merge (CL-435
blanket). 3 real gates green required (ci-success + migrations + orchestrator).

## Status history
- 2026-06-11: plan v1 gate-FAILED (post-hoc seam + DBOS-fork false assumptions); v2
  app-level redesign gate-PASSED w/ conditions; rostered + dispatched under CL-435.
- 2026-06-12: built by CC. STEP-0 4-way inventory → plan-ack (N1 RETIRE arm, Cowork-
  approved + amendments accepted); build wave (6 builders) → 4 adversarial verifiers
  (6 blockers/17 majors found) → fix wave (3 fixers + 3 test builders) → 2 re-verifiers
  (all 15 fix items confirmed) → test-gap patcher. mig-131 (workflow_controls +
  step_overrides + lineage cols + run_controls RETIRED + vtr_step_timeline/
  vtr_workflow_controls views). Full local acceptance green. PR-Ready → Cowork gate.
- 2026-06-12: Cowork gate APPROVE-WITH-CONDITIONS at head 1b62c69 (B1 stale-fetch cleared
  with origin evidence; 3 real gates green). MERGED #458 → dev @ b8bd111 (squash,
  Cowork-authorized task-merge under CL-435). Recorded Phase-B debt from the gate:
  C1 = F10 race acceptance (rerun racing approval-create; structural convergence holds
  via mig-128 partial-unique); C2 = explicit key projection for the 3 non-webhook
  passthrough kinds (agent_invocation / aborted_hard_limit / tenant_isolation_breach);
  notes = orphaned run-bound override sweep (hygiene) + panel copy must disclose
  consume-failure ⇒ pin silently not applied. Also Phase-B: bounded hold for
  non-webhook DBOS callers (re-verify flag).
