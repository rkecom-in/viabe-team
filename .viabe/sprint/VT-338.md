---
vt_id: VT-338
title: Owner portal PR-2 — customers/campaigns/settings/reports + i18n + Lighthouse
status: Backlog
priority: High
sprint: Sprint 8 - Owner Surface & Billing
type: Feature
area: [Owner Surface, Frontend]
assignee: unassigned
parent: VT-87
sub_items: []
exec_order: 7.1
blocked_by: []
tags: [owner-surface, frontend, privacy]
---

# VT-338 — VT-87 PR-2: the remaining dashboard pages + i18n

VT-87 PR-1 (#351) shipped the auth-gate + the data-spine (owner-read endpoint, mask-at-source)
+ the index page. PR-2 builds the rest, same data-path-A discipline (orchestrator owns the
reads + PII masking; team-web is a thin requireOwnerSession→forward proxy).

## Scope
1. **/dashboard/customers** — paginated list (orchestrator owner-read endpoint; phones MASKED
   at source, last-4; filters recency_band + spend_band; search by last-4).
2. **/dashboard/campaigns** — campaign history (status + cohort + ARRR/pending).
3. **/dashboard/settings** — read-only business profile + plan/trial dates + DSR-init buttons
   (link to the existing /api/dsr/* — owner-initiated, the only write-exception).
4. **/dashboard/reports** — monthly reports list + PDF download (mig 048 IS on main → unblocked).
5. **i18n** — locales/{en,hi}.json + a toggle (default tenant.preferred_language).
6. **Lighthouse** > 85 on the index (CI gate).

## Discipline (same as PR-1; Cowork subagents it)
- Server-side tenant derivation (NEVER a client field — IDOR). GET-only, zero mutating routes.
- Mask-at-source: raw phone never crosses the orchestrator→team-web boundary.
- Each new orchestrator owner-read endpoint: X-Internal-Secret, tenant-predicated wrapper, real-PG canary.

## nit-1 (from the VT-87 PR-1 #351 review — DO HERE)
`CustomersWrapper.top_customers_by_spend` skips `self._validate(out, tid)` — the Pillar-8
result-validation / tenant_isolation_breach backstop every other read calls. Add the one-liner
(ensure the SELECT carries tenant_id for the validate). Defense-in-depth on the isolation contract.

## Origin
VT-87 split (Cowork 20260605T174500Z); PR-1 = #351 merged.
