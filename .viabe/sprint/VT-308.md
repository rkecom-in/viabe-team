---
vt_id: VT-308
title: Platform-listing KG ingestion + emitter + HAS_THEME edges
status: Backlog
priority: Medium
sprint: Sprint 7 - Knowledge Architecture
type: Feature
area: [Knowledge Architecture, Ingestion]
assignee: unassigned
parent: VT-65
sub_items: []
exec_order: 65.8
blocked_by: [VT-6]
---

# VT-308 — platform-listing KG ingestion + emitter + HAS_THEME edges

Fast-follow split out of VT-65 PR-1/PR-2 (Cowork 20260603T173500Z). The
`platform_listing_updated` handler ships consumer-ready in VT-65 PR-1 but is
UNEXERCISED — there is no `platform_listings` source table/writer today, so PR-2
wired the other 7 emitters and left this one dormant (no source to emit from).

## Scope (when a platform-listing source lands)
1. The platform-listing source (table + writer) — apify_gbp / apify_food already
   write `business_profile` context via `upsert_business_profile`; this row adds a
   structured `platform_listings` source if/when product needs per-listing nodes.
2. Wire a `platform_listing_updated` emitter at that write site (outbox, same
   atomic pattern as the other 7).
3. Extend the `platform_listing_updated` handler with **HAS_THEME** edges
   (PlatformListing → Theme {theme, count, sentiment}) — deferred from PR-1.
4. Live-canary: a listing write → emit → KG PlatformListing node + HAS_LISTING +
   HAS_THEME edges, tenant-scoped.

## Dependencies
- VT-65 PR-1 Done (platform_listing handler consumer-ready).
- A platform-listing source (VT-6 ingestion family) — the per-edge emit point.
