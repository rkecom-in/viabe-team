---
vt_id: VT-342
title: get_business_profile VT-306 cleanup — SET ROLE app_role (RLS inert today)
status: Done
priority: Low
sprint: Sprint 8 - Owner Surface & Billing
type: Bug
area: [Privacy, Specialist Agent]
assignee: unassigned
parent: VT-306
sub_items: []
exec_order: 9.3
blocked_by: []
tags: [privacy, rls, tech-debt]
---

# VT-342 — get_business_profile: run under app_role (the VT-306 pattern)

`get_business_profile` runs on a raw `get_pool` + `set_config` WITHOUT `SET ROLE app_role`, so
RLS is INERT — isolation rests solely on the explicit `WHERE id = %s`. Pre-existing (NOT
introduced by VT-338; the VT-338 settings plan query correctly uses `tenant_connection`).
Bring it onto the `tenant_connection` (app_role + GUC) path so RLS is a real second layer, per
the VT-306 fix. LOW (not exploitable — the WHERE predicate scopes it; this is defense-in-depth).

## Origin
Cowork VT-338 #354 subagent (20260606T003500Z), roster #2.
