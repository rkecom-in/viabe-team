---
id: VT-522
title: "Restore dev pre-push green (2/2) — seed ownership_verified in SR-executor + campaign-complaint-freeze fixtures (VT-517 gate is intended)"
status: In Review
priority: launch-blocker
type: BUILD
area: tests
created: 2026-07-01
authorized_by: fazal
authorization_basis: "Phase-1 overnight build (Cowork 20260701T200500Z, CC owns execution). Row VT-522 — fix the second pre-push red without weakening the ownership gate."
---

# VT-522 — seed ownership_verified in the affected fixtures

## Problem
VT-517 (mig 148) added `tenants.ownership_verified` as a UNIVERSAL execution bar (`activation_registry.requires_ownership_verified=True`, checked in `onboarding_gate.py:100`). Tenants default `ownership_status='pending'` / `ownership_verified=false`. The end-to-end SR-executor + campaign-complaint-freeze fixtures seed journey-complete + gstin_verified + a connector but NOT ownership → the executor/campaign path now short-circuits `skipped_not_onboarded` before reaching the behavior under test. 5 pre-push failures (test_sales_recovery_executor ×3, test_campaign_complaint_freeze_realdb ×2).

## Fix (fixtures only — gate is CORRECT, not weakened)
Seed `ownership_verified=TRUE, ownership_status='verified'` on the tenant INSERT in both fixtures — representing a VTR-verified tenant, which is the precondition these tests need to exercise detection/drafting/complaint-freeze. **The ownership gate itself is untouched** (fail-closed: never bend a correctness/eligibility gate to pass a test — instead the fixture now represents a legitimately-onboarded+verified tenant).

## Verify
Both full files + the 2 single tests: 16/16 green on a fresh DB with mig 149. Combined with VT-521, all 7 pre-existing pre-push reds are resolved → dev pre-push is green again.
