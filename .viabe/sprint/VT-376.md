---
vt_id: VT-376
title: VTR Run-Control Panel Phase C — interactive controls (pause/override/rerun from the canvas) + C2 view hardening
status: Dispatched
priority: Critical
sprint: "Sprint-Fazal"
type: Feature
area: [Ops Console, team-web, Orchestrator, Privacy]
assignee: Claude Code
related: [VT-374, VT-375, VT-377, VT-380]
exec_order: 3
cl_decisions_checked: [CL-81, CL-132, CL-220, CL-240, CL-252, CL-260, CL-423, CL-424, CL-426, CL-428, CL-429, CL-431, CL-432, CL-435]
created: 2026-06-12
last_updated: 2026-06-12
---

## Contract (plan §9 + accumulated fold-ins from the A/B gates)

1. **Wire the controls into the canvas** via the Phase-A POST surfaces ONLY (pause/
   release, override set/cancel, rerun): every mutation audited with VTR identity,
   assignment-gated (Gap-6 pattern), no new mutation paths invented panel-side.
2. **Binding UX disclosures (the honesty layer):** rerun banner "outputs re-enter owner
   approval" (I2 visible); rerun-overlap escalation outcome surfaced (the C1-A guarantee);
   consume-failure disclosure (a pin may silently not apply on control outage — F-5);
   keys-only blind-write warning when editing values the VTR cannot see; observed-only
   steps show NO controls; non-rerunnable kinds (webhook/trial_sweep/campaign_send) show
   why.
3. **C2 fold-in (Phase-A gate condition):** explicit key projection for the 3 non-webhook
   passthrough kinds (agent_invocation / aborted_hard_limit / tenant_isolation_breach) in
   `vtr_step_timeline` — migration via the allocator; makes the leak structurally
   impossible (writers are constants-only today; this removes the trust).
4. **TOCTOU revisit (recorded in VT-375):** with the rerun button shipping, decide the
   gate-window edge. PROPOSE in the plan-ack — the B-analysis (gate-window lock ≠ the full
   guarantee; full-execution lock = unacceptable arm-path inversion) is the starting
   point. Cowork rules before build.
5. **`run_control_rerun_overlap` event-schema registration** (CC's rostered flag — the
   alert the panel surfaces must be a registered schema, not ad-hoc).
6. **VT-380 RIDER (batch-internal now):** assignments.ts eager `serverSecretClient()`
   default (hard-500 → degrade) + sticky-banner `toLocaleTimeString()` hydration #418
   (kills the e2e allowlist exception). Close VT-380 with this row.

## Merge class

**RISK row — NOT self-merge** (mutations + a view migration + gate-adjacent TOCTOU).
Plan-ack BEFORE build (TOCTOU proposal + C2 migration approach + mutation-wiring map).
pr-ready → Cowork adversarial gate with executed evidence → Cowork-authorized merge.
3 real gates green. Rendered verification with the hydration allowlist REMOVED.

## Status history
- 2026-06-12: dispatched on VT-375 merge (9213024) per CL-435 batch sequencing.
