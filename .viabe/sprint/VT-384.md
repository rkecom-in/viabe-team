---
vt_id: VT-384
title: Gap-5 PR-3 — the L3 autonomous send wire (Act mode for customer sends), C2-gated for real sends
status: Done
priority: Critical
sprint: "Sprint-Fazal"
type: Feature
area: [Orchestrator, Agents, Privacy]
assignee: Claude Code
related: [VT-369, VT-383, VT-385, VT-377]
exec_order: 7
cl_decisions_checked: [CL-330, CL-390, CL-425, CL-426, CL-428, CL-429, CL-431, CL-432, CL-433, CL-434, CL-435, CL-437, CL-438]
created: 2026-06-12
last_updated: 2026-06-12
---

## Preconditions cleared (CL-438): F1 ✓ (SIDs, VT-383) · F6 ✓ (signed off) · F3 ✓
(delegated defaults: 20-clean streak, L3 daily cap 50, weekly 1, 30d recontact, 90d
ceiling 2). **C1–C3 counsel OUTSTANDING → `MARKETING_CONSENT_VERSIONS` stays EMPTY: this
row builds the wire; real customer sends remain structurally impossible until counsel.**

## Contract (the deferred Gap-5 PR-3 scope, handoff §8 + the original plan §5.6-5.7)

1. **The L3 auto-send wire:** replace the `NotImplementedError` arm with the real path —
   eligibility (tenant_agent_autonomy: L3 granted + not frozen + streak/caps) →
   `team_l3_presend_notice` send → the F6 delivery-anchored 2h hold (durable wait) →
   silence proceeds → `agent_send_draft` (ALL existing gates re-evaluated at send time).
2. **Any owner inbound during the hold demotes to L2** — row-lock/CAS so a window-expiry
   send can NEVER fire over an in-flight objection (the original race requirement).
3. **Owner kill-keyword detection:** the standing "stop"-class keywords on OWNER inbound
   trigger the substrate freeze (atomic, cancels in-flight holds same-txn). Deterministic-
   first per the classifier pattern; the authoritative opt-out/DSR matcher stays FIRST.
4. **The L3 grant flow:** `team_autonomy_offer` send on streak threshold → ENABLE reply
   → autonomy_upgrade approval row (C3 consent evidence shape per the substrate);
   classifier recognizes ENABLE (+ HI variants) — copy/classifier lockstep (CL-438 drafts).
5. **Winback template-signature check:** executor WINBACK_TEMPLATE_PARAMS vs the
   registry/approved variables — hard-refuse on mismatch BEFORE any send.
6. Acceptance (real DB): the hold parks + survives restart; owner-inbound-during-hold
   demotes (race-tested, CAS); kill keyword freezes + cancels; consent empty-set still
   yields ZERO candidates end-to-end with L3 granted (the C2 stop proven against the
   FULL wire); caps enforced; money-bearing (winback_offer) NEVER auto-sends (always-
   confirm floor — CL-438 non-bypassable).

## Merge class
Money/PII/send-path risk row → plan-ack BEFORE build → Cowork executed gate →
authorization. Builds AFTER VT-383 merges (the wire needs the armed registry).

## Status history
- 2026-06-12: rostered + dispatched (CL-438 — Act mode for sends; deferred PR-3 now
  buildable, C2 still gates real sends).
- 2026-06-12: built by CC (3 builders → 2 verifiers → 2 fix rounds). The wire: L3 arm in
  the executor (is_always_confirm floor BEFORE arming) → auto_send_pending + presend
  notice → delivery-anchored config-driven hold (DBOS checkpointed poll; anchor on hold
  ENTRY) → wake-side row-locked CAS (FOR UPDATE across check+send+flip; demote takes the
  same lock — either order, no send-after-demote) → all gates re-evaluated at send.
  Demote-to-awaiting_approval (owner_disengaged kind — the only non-cancelling regression);
  C-c collision queues + the coordinator sweep re-arms with the REAL uuid5 dispatch run id
  (the stranded-batch gap found post-verify + closed). Kill/ENABLE handlers registered +
  rule-order pin (opt-out/DSR first, structural). Winback signature registry-as-canon
  closed (the F1 carry-forward). L3 daily cap enforced. VT-72 lint: pending_approvals
  reads routed through PendingApprovalsWrapper (4 new methods). C2 frozenset untouched —
  the centerpiece test proves zero sends end-to-end with everything armed. Fresh-DB 1992
  green. FLAGS for the gate: record_silent_l3_notice dead code (the owner_disengaged-at-
  threshold regression never fires — wire or drop, your call); DBOS teardown log noise on
  parked holds (cosmetic). PR-Ready → Cowork executed gate.
- 2026-06-13: Done — gate BLOCK (F1: owner STOP/DSR did nothing to an armed hold — the
  natural phrasings routed past the kill handler while the copy promised instant stop) →
  the ruled STRONG ARM (opt-out strictly stronger than kill; freeze-after-commit ordering;
  the promise is now REAL; owner-level freeze APPROVED as the honest read — per-agent
  granularity = a VT-385 design input). F4 threshold path dropped, counter kept (VT-385
  input). Live owner canary still deferred on Fazal's owner_phone dev action — fires
  through THIS wire's arm path. Merged #465 → dev @ 20a7106 (squash, head b590bca
  verified). **PR-3 COMPLETION: all three CL-434 customer-send stops are now
  flip-controlled — #1 fell here (the L3 caller exists, gated), #3 fell with VT-383
  (SIDs), #2 (the C2 consent frozenset) is THE last stop. Counsel C1–C3 is the only
  thing between the system and real customer sends.**
