---
vt_id: VT-393
title: Wire the real owner welcome WhatsApp send (close the owner-WABA stub) — canary-gated
status: Queued
priority: High
sprint: "Sprint-Fazal"
type: Feature
area: [Owner Surface, Onboarding]
assignee: Claude Code
related: [VT-390, VT-326]
exec_order: 1
created: 2026-06-13
---

## Contract (Fazal 2026-06-13: signed up as Sundaram, got no WhatsApp — "close the gap"). PLAN-FIRST + CANARY (Rule #15).

**Why no message today (diagnosed, confirmed in code):** the post-signup welcome is a stub —
`onboarding/signup.py:_default_welcome` only logs and returns `False` (VT-390 made the flag
honest; the send was always a stub). The real send path **already exists and is live**:
`utils/twilio_send.py:send_template_message(template_name, lang, recipient, vars)` sends a real
WhatsApp template via Twilio (`from_=TEAM_TWILIO_FROM_NUMBER`, content_sid from the registry),
and is Pillar-7-honest — if the template has no approved `content_sid` it makes NO Twilio call
and reports the error. The orchestrator holds the Twilio creds on Railway, so it can send.

**Scope:** wire the welcome seam to actually send `team_welcome` (lang SID, vars
`{owner_name, trial_end_date}`) to the owner's number via `send_template_message`. Build it as a
small **reusable owner-template-send helper** so trial/lifecycle notifies (trial_sweep, same stub
pattern) can reuse it later — but DO NOT wire those other callers in this row (one coherent change).
Keep the send GUARDED + non-terminal: a send failure must NEVER 500 a committed signup; `welcome_sent`
returns `True` ONLY on a confirmed send, else `False` (the VT-390 honesty invariant holds).

**Canary (MANDATORY, Rule #15 — CC runs it; the sandbox can't reach Twilio):**
1. Confirm `team_welcome` EN+HI `content_sid`s are Meta-APPROVED/sendable. If NOT approved →
   STOP and surface as NEEDS-FAZAL (submit team_welcome to Meta); do not fake a send.
2. Real send of `team_welcome` to **Fazal's own number** (the dev owner tenant — Fazal is signing
   up as Sundaram with his real number). Verify actual receipt on WhatsApp + a Twilio message SID.
3. Fail-not-skip on error.

## Acceptance
- A real owner (Fazal's number) receives the `team_welcome` WhatsApp after signup; `welcome_sent`
  reflects the confirmed send.
- Send failure (e.g. unapproved SID) degrades to `welcome_sent=False` + logged, signup still succeeds.
- No customer-PII / marketing path touched (this is an OWNER utility send — NOT counsel-C2-gated).

## Merge class
Real outbound send going live → **plan-first + Cowork review at PR + canary evidence** before merge
to `dev` (NOT blind self-merge). Owner-only utility; not money/customer-marketing.

## Status history
- 2026-06-13: Rostered + dispatched plan-first (Cowork) under Fazal's "close the gap" grant.
