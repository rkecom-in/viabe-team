---
vt_id: VT-425
title: Onboarding-agent connector stub tools — de-stub or DEFER cleanly (no stub on a live wizard path)
status: Queued
priority: Critical
sprint: "Sprint-Fazal"
type: Feature
area: [Backend, Agents, Integrations, Onboarding, PII, Launch]
related: [VT-416, VT-422, VT-417, CL-421]
exec_order: 94
created: 2026-06-25
last_updated: 2026-06-25
---

## Contract (Cowork GATE 2026-06-25, VT-416 sweep Row B — its OWN row, NOT folded into VT-417 which is merged/closed). LAUNCH-BLOCKER. DESIGN-FIRST → Cowork gate.

The 5 stub tools in `agent/integration_agent.py:238-247` (`start_connector_setup_stub`, `pull_sample_stub`,
`propose_field_mapping_stub`, `confirm_field_mapping_stub`, `dedupe_against_existing_stub`) are bound into the LIVE
`integration_agent`, invoked by `api/onboard_step.py:127` from the owner onboarding wizard. sample-pull returns 0 rows,
field-mapping returns `{}` — stubs on a live owner path.

## Scope (per-connector real-vs-DEFER — distinct from VT-417's ingestion-writer path; this is the interactive wizard agent-tool path)
- **Shopify connect = real via VT-422** (the OAuth install path).
- **Sheet / CSV**: build REAL sample-pull + field-mapping + dedupe, OR DEFER the connector cleanly (no stub on a live
  path — VT-417's contract rule). **Surface the per-connector real-vs-DEFER decision + the "which connectors at launch"
  question to Cowork's gate — that scope call may go to Fazal.** DESIGN-FIRST.

## Gate
RISK (PII / ingestion). Design-first → Cowork gate; build → gate, NOT self-merge.

## Status history
- 2026-06-25: Rostered + ID allocated (Cowork VT-416 gate, Row B — own row). Design the per-connector real-vs-DEFER → gate.
