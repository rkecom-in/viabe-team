---
cl_id: CL-358
created: 2026-05-22T22:23:18.819Z
notion_legacy_id: 368387c2-cc5a-81ba-b121-e36f32f6d3ef
title: "DECISION + CORRECTION (VT-146 SHIP GATE bypass — forward-fix finalized; refines blocker 368387c2-cc5a-81bf). FACTS IN: (1) Blast radius contained — grep confirms run_extraction_for_event has exactly O"
---

# CL-358 — DECISION + CORRECTION (VT-146 SHIP GATE bypass — forward-fix finalized; refines 

DECISION + CORRECTION (VT-146 SHIP GATE bypass — forward-fix finalized; refines blocker 368387c2-cc5a-81bf). FACTS IN: (1) Blast radius contained — grep confirms run_extraction_for_event has exactly ONE call site, runner.py:250 in webhook_pipeline_run (after record_webhook_received, before pre_filter); all other hits are import/re-export/docstring/pyc. Forward-fix is a one-line guarded change, no sprawl. (2) CORRECTION to the earlier runtime-impact framing: 'no API key = no transmission, table sits empty' describes the STORAGE axis only. Per the VT-146 commit message's own privacy posture, the writer TRANSMITS the raw customer message body over the wire to Anthropic Haiku for classification; only derived fields are persisted (migration 020 is derived-only, no body column — confirmed). The SHIP GATE's privacy notice is RETAINED/TRANSMITTED/DERIVED — the binding axis is TRANSMITTED: raw body to a third-party processor, which is exactly what the unexecuted Anthropic DPA+ZDR covers. So ANTHROPIC_API_KEY unset guards cross-border third-party PII TRANSMISSION, not storage — makes env-var-as-sole-control worse than stated, not equivalent. MECHANISM DECIDED (Clau-owned): guard runner.py:250 behind a code-level named constant OWNER_INPUTS_EXTRACTION_ENABLED default False (NOT an env var — env flags share the same invisible-in-diffs weakness as ANTHROPIC_API_KEY; the gate must live where a PR reviewer sees it). Flipping to True becomes a reviewed code change = real checkpoint. ANTHROPIC_API_KEY also stays unset in prod (defense in depth, two independent locks). Ship as a standalone tiny PR NOW (VT-146-track / VT-150-fix-2), not deferred to owner_inputs resume — main currently has the gate enforced by nothing. Commit also self-reports: replay duplicate-row risk (crash between classify and INSERT, no UNIQUE on tenant_id+message_sid — noisy not privacy) and that VT-146 inherits the DBOS workflow_inputs Body surface VT-150 just purged. STILL OPEN: is migration 020 APPLIED to the prod DB (vs just present in main) — determines whether the privacy notice's RETAINED column must describe a live empty table. UNCHANGED FAZAL-OWNED: SHIP GATE (DPA+ZDR executed, privacy notice signed) unsatisfied; forward-fix restores the checkpoint but does NOT clear the gate — owner_inputs cannot ship until Fazal closes both.

## Status history
- 2026-05-22T22:23:18.819Z: created (status=)
- 2026-05-25 04:00 IST: migrated from Notion (notion_legacy_id: 368387c2-cc5a-81ba-b121-e36f32f6d3ef)
