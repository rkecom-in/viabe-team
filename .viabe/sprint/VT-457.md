---
id: VT-457
title: "Dev-only GST-verify mock (Fazal-authorized e2e bypass, prod-safe)"
status: In Review
priority: High
type: BUILD
area: team-orchestrator
created: 2026-06-28
authorized_by: fazal
authorization_basis: "Fazal 2026-06-28: 'If the GST Api is still giving issues, bypass it for the test or treat GST verification optional.'"
---
# VT-457 — dev GST-verify mock (prod-safe)
`TEAM_SANDBOX_GST_MOCK_MODE=1` (dev only) → `sandbox_kyc.search_gstin` returns a fixture: the e2e test GSTIN
27AAKCR3738B1ZE verifies (RKECOM SERVICES (OPC) PRIVATE LIMITED, Active); any other valid-format GSTIN →
Inactive → invalid_gstin (reject path stays real). PROD-SAFE: flag added to auth/prod_safety._PROD_FORBIDDEN_FLAGS
→ prod REFUSES boot if set (verified). Enabled on Railway DEV. The e2e create→verify is now reliable regardless
of Sandbox GST uptime. Prod keeps the real Sandbox gate.
