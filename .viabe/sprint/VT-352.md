---
vt_id: VT-352
title: Razorpay webhook dead-letter table (durable replay) — SOFT GATE on live Razorpay
status: Done
priority: Medium
sprint: Sprint 8 - Owner Surface & Billing
type: Feature
area: [Billing, Database]
assignee: unassigned
parent: VT-330
sub_items: []
tags: [money, razorpay, dead-letter, soft-gate, pre-live]
---

# VT-352 — durable Razorpay webhook dead-letter + programmatic replay (Option B)

VT-330 closed the poison-pill with Option A (record-and-drop): on a parse-error the raw event
is COMMITTED to `razorpay_webhook_events` with a `{"_status":"dropped_parse_error","raw":…}`
payload marker + Fazal-alert + 200 (so Razorpay stops looping). That's the right TIGHT choice
pre-LIVE — nothing is lost; Fazal reconciles manually from the marked row.

Option B (this row, Cowork 20260606T074500Z) is the durable, programmatic-replay form for the
LIVE money path:
- a dedicated `razorpay_webhook_dead_letter` table (event_id, event_type, raw_payload,
  error_reason, retry_count, first_seen/last_retry timestamps) — claim the migration number
  via the allocator;
- a replay path (re-feed a fixed/parsed payload through razorpay_ingress);
- query/observability for stuck events.

**SOFT GATE:** the money path should have programmatic replay before real settlements — this
MUST land before `TEAM_RAZORPAY_LIVE=1`. Behind the queue; roster only.

## HARD pre-LIVE acceptance (VT-330 #370 subagent, Cowork 20260606T080000Z — F1)
VT-330's record-and-drop keys the dropped row on Razorpay's `event_id`. Replaying the CORRECTED
event with the SAME event_id hits the dedup → "duplicate" → the fee is NEVER applied → the
charge is silently lost on replay. (Nothing loses money TODAY — the drop path doesn't touch
fees and LIVE is off — so it's a pre-LIVE blocker, not urgent.) **Before `TEAM_RAZORPAY_LIVE=1`:
the replay path MUST re-process a dropped event** (re-process on `_status=dropped_parse_error`,
or via a distinct replay key that bypasses the dedup) **+ a test proving drop → replay → fee IS
applied.** VT-330 softened its comments so they don't promise a replay this row hasn't built.

## HARD pre-LIVE acceptance (VT-332 #375 subagent, Cowork 20260606T100000Z — F1)
The razorpay-subscribe vendor call sits INSIDE the DB txn (VT-331 pattern). Live-mode risk: the
vendor create succeeds → the commit fails → the jti (+ subscription row) roll back → a retry
creates a SECOND real Razorpay subscription (an orphan at the vendor). Stub-only today (no real
vendor). **Before `TEAM_RAZORPAY_LIVE=1`:** a pre-committed intent row carrying the vendor
sub-id (claim before the vendor call), OR a webhook/sweep orphan reconciliation, so a
commit-after-vendor failure can't double-create. Same cutover gate as the F1 replay contract above.

## F4 [Low, post-launch UX — VT-332 #375 subagent]
A team-web timeout AFTER the subscribe commits → the owner retries the deep-link → 403 "token
already used" although the subscription EXISTS (no money loss, just a confusing error). Post-launch,
consider: a same-tenant replay of an already-consumed jti returns `{status: exists}` (the existing
subscription) instead of 403. Fine to defer past launch.

## Origin
VT-330 A/B ruling (Cowork 20260606T074500Z) — Option A now, Option B pre-LIVE.
VT-332 #375 review (Cowork 20260606T100000Z) — F1 vendor-in-txn orphan + F4 replay-UX folded here.

## CUTOVER ACCEPTANCE (added in the build, Cowork bounce 20260606T222500Z — F7 + the canary)
Pre-`TEAM_RAZORPAY_LIVE=1`, in addition to the F1 replay-fee + F2 vendor-orphan contracts above:
- **F7 — wire the dead-letter sweep:** `billing/dead_letter.list_pending` + the orphan
  `reconcile_subscription_orphans` have NO caller yet (intentional — no real settlements pre-LIVE).
  At cutover, wire them: a scheduled job that alerts on pending drops / detected orphans, OR a
  runbook step the operator runs. Detect-only (no auto-remediation).
- **Idempotency-Key real-API canary:** prove Razorpay actually honours the `Idempotency-Key` on
  subscription.create (same key → same sub). If it does not, fall back to a pre-committed intent
  row before flipping live.
