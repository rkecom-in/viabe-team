---
id: VT-675
title: Promote the richer reads (recent-campaigns / attribution / ledger) into the COMMON set
status: done
priority: Medium
owner: unassigned
created: 2026-07-18
found_by: VT-669 capability-gap gate (Fazal 2026-07-18 "fail loudly")
authorization_basis: "Surfaced by VT-669's fail-loud sufficiency gate. Capability EXPANSION (follow-on), not a regression — build on a Fazal grant."
---

# VT-675 — promote richer reads into the common surface

## Gap (`KNOWN_CAPABILITY_GAPS[richer_reads_into_common]`)
`get_recent_campaigns` / `get_attribution_data` / `query_customer_ledger` EXIST (cataloged as lane
tools) but are NOT in the Manager-scoped COMMON read set every specialist reaches. A win-back
specialist needs prior-campaign history + attribution + ledger reads as COMMON operational data, not
lane-local. (Distinct kind from VT-672/673/674: the tools exist — this is a PROMOTION, not a build.)

## Build
Promote the three reads into `agent_framework.tools_common` (the common surface), preserving each
tool's PII posture: `query_customer_ledger` is operator-role substrate (phone-token input, customer_id
UUIDs out — CL-390) and is allowlisted in `check_no_direct_tenant_db_access.py`; do NOT widen its
scope, just make it common-reachable. Update `COMMON_READ_TOOLS` + the catalog holders.

## Gate / auto-close (ABSENT_FROM_COMMON)
Gap closes only when ALL THREE names appear on the common-read surface (`_common_read_names()`), not
just cataloged. Then delete `richer_reads_into_common` from `KNOWN_CAPABILITY_GAPS` (honesty test).
SR's `required_tools` can then be widened to declare them (currently SR requires only the 2 reachable
reads); `required_tools_reachable` will verify the widened set.
