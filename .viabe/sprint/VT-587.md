---
id: VT-587
title: "Offer beat reads the store URL from the CURRENT message — kill the canonical re-ask"
status: Done
priority: launch-blocker
type: BUILD
area: manager
created: 2026-07-03
authorized_by: fazal
authorization_basis: "Autonomous capability program (CL-2026-07-03-autonomous-capability-program); harness re-run finding (the canonical 3x-store-link re-ask, reproduced)."
---

# VT-587 (P1a) — use the URL the owner gave in the same message

## Finding (deployed-dev diagnostic, tenant d25cb1a1)
context_retention step1: owner "Yes lets connect. My store is probe-store-a.myshopify.com by the way" → the offer beat RE-ASKED ("Your store address looks like yourstore.myshopify.com — find it..."). The URL turn IS in conversation_log (VT-586 records it), but _recent_shop_domain's lifetime-log lookback missed it — the current turn's conversation_log row is uncommitted / RLS-invisible within the same webhook run (a fragile substrate).

## Fix
_recent_shop_domain scans the CURRENT inbound body FIRST (pure regex, most reliable — no cross-txn/RLS timing), then the log lookback as fallback. body threaded through _flow_offer_next_integration (3 callers). Pure regression test. 24 paced-flow tests green, ruff clean.
