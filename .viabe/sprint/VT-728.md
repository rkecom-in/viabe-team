---
id: VT-728
title: "SUPERSEDED by VT-729 — dev degradation root-cause"
status: Superseded (never dispatched; VT-729 closed the same finding with a better answer)
priority: n/a
owner: n/a
exec_order: 135.5
created: 2026-08-03
created_by: clau
authorization_basis: "Retired by Clau 2026-08-03 on reconciling VT-729 (CC, authorized directly by Fazal in terminal: 'Go ahead with your pick on Dev degradation')."
---

# VT-728 — retired, do not work this row

I rostered this to root-cause an apparent dev degradation (90s timeouts, `route='none'` on
still-running turns) and hypothesised orphaned-workflow queue pressure.

**CC got there first and the real cause was neither degradation nor orphans — it was me.**
See **[[VT-729]]**: Railway's native auto-deploy fires on EVERY push to `dev`, including the
docs-only pushes the VT-245 CI trigger-diet deliberately skips. A skipped CI run does not skip
the deploy. My `docs(sprint): …` push at 14:41 restarted the orchestrator at 14:42:58, mid-run,
splitting one measurement across two services.

VT-729 fixed it structurally rather than by discipline: `run_critical_x3.py` now reads
`dbos.application_versions` before and after each scenario and labels a redeploy-straddling run
**CONTAMINATED** rather than BLOCKED.

The orphaned-workflow observation from my row remains real but unattributed to any degradation,
and belongs to **VT-634** (prod failed/orphaned workflow handling, launch-blocker) — not here.
