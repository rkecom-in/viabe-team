---
id: VT-702
title: "e2e WhatsApp onboarding simulator (mock owner persona against deployed dev)"
status: Built — iterating (runs 1-2 done, fixes applied)
priority: High
exec_order: 702
owner: claudecode
created: 2026-07-24
authorization_basis: "Fazal 2026-07-23: 'Why don't you run an e2e simulation? have a process that can work as mock whatsapp and respond to the Managers questions'"
---

## What

`canaries/e2e_wa_sim.py` — drives the COMPLETE new-owner arc against deployed dev: signup
front door (unknown bogus +1-555 number → consent tap) → onboarding → activation → agent
chooser → connection ask. Owner = persona LLM (sonnet, local key layered in-process);
deterministic overrides at the fixed decision points (Yes on the card / ACTIVATE TEAM /
Sales Recovery); one scripted "What does that mean?" confusion probe. Button taps simulated
by sending the title (echo semantics). Dev send-guard mocks every outbound. Transcript +
arc checks printed; **STANDING (Fazal): every run ships with a graphical conversation-flow
artifact** (memory sims-ship-with-flow-diagram).

## Runs so far

- Run 1: front door PROVEN (session → consent → tenant); crashed on conversation_log.id
  being UUID (cursor now created_at). ENABLE_LLM_DISCOVERY found unset on dev → set + redeploy.
- Run 2: persona key path bug ("ok" spam — fixed parents[3] + export-prefix parsing). STILL
  found two real defects: the polite re-ask loop repeats ~13× verbatim-class on a
  non-answering owner (observation), and the confusion probe REPRODUCED the prod
  "What does that mean?" dead-end deterministically → VT-703.
