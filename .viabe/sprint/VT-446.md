---
vt_id: VT-446
title: Migrate l2_send.py:232 FROM pending_approvals to orchestrator.db.wrappers (pre-push lint green)
status: Queued
priority: Medium
sprint: "Sprint-Fazal"
type: Chore
area: [Backend, DB, RLS, Money-Send]
related: [VT-418, VT-306]
exec_order: 115
created: 2026-06-26
last_updated: 2026-06-26
---

## Contract (Cowork 2026-06-26, batch-push follow-up). The pre-push `test_no_direct_access_lint_passes` is RED on a pre-existing site. Risk(money-send-adjacent) → Cowork gate, PLAN-FIRST.

`agents/l2_send.py:232` does a direct `FROM pending_approvals` (JOIN agent_draft_batches) INSIDE `tenant_connection`
(`WHERE a.tenant_id=%s`) — tenant-scoped/RLS-safe, but OUTSIDE the wrapper layer → the VT-72/306 lint flags it (NOT
allowlistable — the allowlist is structural-residuals-only; it must MIGRATE to a wrapper). Pre-existing on origin/dev
(VT-418/#506); the consolidated batch pushed --no-verify (Cowork-authorized) since it's pre-existing + RLS-safe. Migrate
the read to `orchestrator.db.wrappers` so the pre-push hook goes green next batch. Money-send-adjacent (the L2 arm path,
wrapper-conn-param risk) → plan-first → Cowork gate + a real-DB RLS canary.

## Status history
- 2026-06-26: Rostered + ID allocated (Cowork batch-push follow-up). Plan-first.
