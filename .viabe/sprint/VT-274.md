---
vt_id: VT-274
title: Ledger entry-key disambiguation — distinguish genuinely-repeated identical entries
status: Deferred
priority: Low
sprint: Sprint 3 - IntegrationInvention
type: Chore
area: [Ingestion, Database]
assignee: unassigned
parent: VT-273
blocked_by: null
sub_items: []
exec_order: 99
---

# VT-274 — ledger idempotency-key disambiguation (Cowork N1, VT-273 review)

VT-273's idempotency key `entry_key = sha256(tenant:customer:date:amount:type)`
makes re-photographing the same ledger a no-op (no double-count). KNOWN LIMITATION
(accepted Phase-1 default, Cowork N1): two GENUINELY-separate identical entries
(same customer, same day, same amount, same type) collapse into ONE — an
under-count.

Accepted because under-count << the re-ingest double-count it prevents. Future
fix (only if it matters in practice): add a stable within-source ordinal to the
key (e.g. the entry's row position within the source upload) so two real
identical entries get distinct keys while re-ingest of the same upload stays
idempotent. Needs the ingestion adapter to carry a deterministic per-entry
ordinal. Do NOT build until data shows it matters. Documented in
migrations/061_customer_ledger_entries.sql + integrations/ledger.py.

## DEFER (2026-06-03, Cowork close-out)
Deferred-by-design (Cowork-N1): accepted under-count << prevented double-count;
needs a stable per-entry ordinal not in `LedgerEntryIn`. Build only if data shows
it matters. Status flipped Backlog→Deferred so the board shows intentional-not-
pending (close-out signal 20260603T114000Z).

## RECONCILE 2026-06-07 — still-deferred (VT-108 close + Deferred-sweep, Cowork 20260607T170000Z; Rule-14 verified)
Reconciled 2026-06-07 per Rule #14 ground-truth audit:
- Status Deferred CONFIRMED — no code changes, no ordinal field added, no superseding CL.
- Idempotency strategy (5-field sha256 key) remains unchanged since VT-273 landed (2026-06-01).
- Known limitation (genuine-duplicate under-count) explicitly accepted in VT-273 review + migration 061 + migration 062 N2.
- No dependencies; not launch-gating (Team-Jul15 soft launch proceeds with current phase-1 default).
- Genuine deferral (parked pending data signals showing under-count matters). UN-PARKS when/if production ledger shows duplicate-amount-same-day collisions warranting ordinal investment. No blocker; no change needed.
