Sprint-8 critical path is <b>complete</b>; the Jun-6 autonomous batch merged 11 rows — the 5 Meta templates live (EN+HI), in-window acks → free-form bilingual, signup OTP-gate (dark), kg_events DSR sweep, Razorpay webhook hardening, bilingual landing + signup form, waitlist mode, the trial-end single-use token, and weekly-approval defer + budget. Next in queue VT-98 → VT-119 → VT-336 → VT-328 → <b>VT-329 (Critical, Hindi/Hinglish classifier)</b>. Launch is gated on: live Razorpay keys + KYC, the signup go-live flip (needs VT-329), VT-231 Mumbai prod DB, and the public legal pages (VT-353).
