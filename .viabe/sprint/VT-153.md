---
vt_id: VT-153
title: Pre-#45 backfill scrub — purge plaintext bodies from rows written before c7135da
status: Done
pr: 302
priority: Critical
sprint: Hardening
type: Infrastructure
area: [Privacy]
assignee: claudecode
parent: VT-8
sub_items: []
branch: "vt-153-backfill-body-scrub"
version: "v1.0"
notion_legacy_id: 369387c2-cc5a-81e7-b59d-e1d6f3a72abc
last_updated: 2026-06-04T05:55:00Z
---

## BUILT 2026-06-04 — pre-#45 plaintext-body backfill (gate 3 of 3, closes the OPEN-LAUNCH-GATE set)
VT-144 (#45) stopped body persisting FORWARD; this scrubs the historical population. **mig 090** — idempotent
JSONB top-level key-strip of the VT-144 family ({body, message_body, raw_text, content}) from
pipeline_runs.trigger_payload + pipeline_steps.input_envelope + output_envelope (defensive; PII-detector scans
it). `?|` (has-any-key) guard → touches only still-dirty rows = idempotent + 0-row re-run. FTS:
pipeline_steps.envelope_search_tsv is GENERATED ALWAYS STORED (mig 038) → the UPDATE auto-recomputes it from
the scrubbed envelopes, so body tokens leave the GIN search index too (no separate refresh). Canary
(test_vt153_body_backfill.py, CL-422 synthetic): seed body-bearing rows → run mig 090 → ZERO body-family keys
both tables + clean-row byte-identical + FTS no longer matches 'secret' + 2nd-run 0 rows (idempotent). Passes
on fresh DB; ruff clean. VT-147 retired as dup. Dev synthetic (CL-422); scrubs prod's pre-#45 population at deploy.

# VT-153 — Pre-#45 backfill scrub — purge plaintext bodies from rows written before c7135da

(This page is blank in Notion; metadata captured above.)

Expected Outcome (from Notion):
All rows written before PR #45 (c7135da) on deployed environments have their plaintext message body scrubbed from pipeline_runs.trigger_payload + pipeline_steps.input_envelope JSONB. Verified by a count query showing zero rows with a non-null body key in those JSONB columns. Idempotent migration; safe to re-run.

Notes (from Notion):
Source: CL-383 closing snapshot, item 5 (referenced by Clau as 'Pre-#45 backfill scrub' / 'Pre-c7135da backfill scrub'). PR #45 (c7135da) shipped writer-side body redaction but only forward-going. Pre-existing rows on deployed envs still carry plaintext body. Separate data migration, not a code feature. Rostered by Cowork (delivery captain) 2026-05-23 23:00 IST per Clau's explicit delegation.

## Status history
- 2026-05-25 03:45 IST: migrated from Notion (notion_legacy_id: 369387c2-cc5a-81e7-b59d-e1d6f3a72abc)
