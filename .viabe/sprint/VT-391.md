---
vt_id: VT-391
title: Ops run-control — stale "Phase B / zero mutations" page header vs shipped Phase-C controls
status: Done
priority: Low
sprint: "Sprint-Fazal"
type: Bugfix
area: [Frontend, Owner Surface]
assignee: Claude Code
related: [VT-376, VT-377]
exec_order: 3
created: 2026-06-13
last_updated: 2026-06-13
---

## Contract (Fazal 2026-06-13: "get the gaps closed")

**Discrepancy (verified on dev `0874370`):** `app/(app)/team/ops/run-control/page.tsx:7`
header comment still asserts *"NO mutations in Phase B … this page ships zero POST calls,"*
but Phase-C shipped the mutation server-actions (`run-control/actions.ts`:
pause/release/override/rerun) — so the header contradicts the deployed reality.

**CC: first determine which is true on the deployed canvas, then fix accordingly:**
- If the page DOES wire the Phase-C controls (expected): correct/remove the stale Phase-B
  header so docs match behavior. Doc-only → **[BUILD], self-merge on green**.
- If the page is genuinely still the read-only Phase-B canvas and the controls aren't wired
  into the deployed surface: that's a real wiring gap touching the Ops Console (PII-wall /
  authz-sensitive) → **do NOT self-merge; signal Cowork** for the risk-row gate before any
  change to mutation wiring.

## Acceptance
- The run-control page header/comments accurately describe what the page does.
- If wiring changes are required, they came through the Cowork gate (not self-merged).
- No change to the PII-wall projection, assignment scoping, or the re-run/approval invariants.

## Merge class
[BUILD] if doc-only; ESCALATE to risk-row (Cowork gate) if real mutation-wiring change.

## Status history
- 2026-06-13: Rostered + dispatched (Cowork) under Fazal's gap-closure batch grant.
- 2026-06-13: Done — self-merged → dev @ 4067bc9 (doc-only — controls confirmed wired (Step-0); page+canvas headers corrected). 3 real gates green.
