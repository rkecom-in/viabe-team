# CL-428 — Session log: Sprint-3 ingestion batch + Shopify + VT-267 kickoff (2026-06-01 → 06-02)

**Type:** Session-log entry (handoff substrate). **Author:** Claude Code. **Reconciled against `git log` on main HEAD 4725539.**

## What shipped (merged to main)
The full **Sprint-3 ingestion engine** + Shopify connector, in one autonomous Cowork↔CC batch:
- **Two-surface transaction model:** VT-276 `imported_transactions` (migration 062, raw-import surface, idempotent on (tenant,source,provider_ref)) alongside the clean VT-273 `customer_ledger_entries`. VT-275 **attribution bridge** (migration 064 `attribution_status`/`match_confidence`) — scores unattributed imports vs the attributed ledger, sets a **tentative** link only (no auto-write to the clean ledger; promotion needs confirm/VPA — D2/N3 ruling).
- **Ingestion methods (1-9 except QR/VT-60):** VT-63 owner-typed NL, VT-58 KOT/POS, VT-57 UPI CSV, VT-59 cash-book photo+voice, VT-62 GBP, VT-61 Swiggy+Zomato. All on the shared `ingest_entries` seam; POS/UPI unattributed rows park via `park_unattributed`. Privacy: Zomato strips reviewer identity pre-LLM, persists aggregate only.
- **Live-verified:** Anthropic vision canary GREEN; Sarvam voice fixed (`saarika:v2`→`v2.5`, caught by the live canary) + GREEN; Shopify VT-208 **client_credentials** grant (paste-token dead) + VT-213 live walk GREEN (4 scopes, 200 pulls, 201 webhooks).
- **Docs/decisions:** CL-425 (owner_inputs = sufficient DPDP basis), CL-426 (VTR decaying human-on-the-loop + PII-encrypted-from-VTR), CL-427 (connector-audit gate + CL-421 "Shopify conforms" correction). Rostered VT-276/277/278/279/280/281/282/283/284/285. Closed stale PR #61 (VT-172 orphan).

## Decisions locked this session
- **CL-426 VTR** — Fazal=VTR#1; decaying human net; three-way routing (agent/VTR/owner); PII encrypted from VTR.
- **CL-427** — every connector must pass the "owner only approves" audit before reaching owners; Shopify client_credentials is dev/same-org only.
- **VT-267 D1 (Fazal):** business_contact (WhatsApp) = mandatory UNIQUE tenant identity; owner_contact optional/nullable. Supersedes CL-76 DC2 (whatsapp_number was deliberately non-unique). Built as PR-A2 (#224).
- **VT-59 Sarvam, VT-275 suggest-only, VT-58 N1/N2, VT-57 D2-refunds** — all Cowork-ruled inline (see the respective PRs).

## In flight at handoff
PRs #222 (VT-213→Done docs) / #223 (CL-427 docs) / #224 (VT-267 PR-A2) awaiting task-merge. Plans posted: VT-283 (Shopify OAuth managed-install), VT-85 (consent-capture). VT-267 PR-B (intent/method_selector/floor) pending 4 build-confirms; PR-C (web wizard) + PR-D (VT-268) follow.

## Carry-forward gotchas (operational)
- Pre-push DB jobs need the app_role/app_operator_role DROPPED before re-create (migs 015/027 non-idempotent CREATE ROLE, VT-271) — the hook does it; a manual run must too.
- Workflow/`.github` + general pushes use `git -c credential.helper='!gh auth git-credential'` (embedded PAT 403s).
- `mypy --strict` is non-blocking (CL-83); pre-existing `.fetchone()` Optional-index notes in l1.py are accepted.
- `upsert_business_profile` now top-level JSONB merges (merge-not-clobber, PR-A) — siblings preserved.
- Sarvam key = `SARVAM_API_KEY` in `.viabe/secrets/sarvam.env`; Shopify STORE_DOMAIN=kk4xva-di in shopify-dev.env (own dev store = same-org → client_credentials only; OAuth-install needs a separate merchant store).

## Next session
Cowork: merge the 3 PRs, tick PR-B confirms, draft privacy/consent copy, VT-189 wireframes. CC: build PR-B → PR-C → PR-D, build VT-85 + VT-283 on plan-approval. Launch-blocker = VT-231 Mumbai prod (Fazal, parked).
