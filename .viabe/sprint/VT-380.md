---
vt_id: VT-380
title: /team/ops surface hygiene — eager serverSecretClient default (hard-500) + sticky-banner hydration mismatch (React #418)
status: Done
priority: Medium
sprint: "Sprint-Fazal"
type: Bug
area: [Ops Console, team-web]
assignee: Claude Code
related: [VT-375, VT-370, VT-360]
cl_decisions_checked: []
created: 2026-06-12
last_updated: 2026-06-12
---

## Findings (CC e2e run during VT-375, 2026-06-12 — both pre-existing, out of VT-375 scope)

1. `lib/ops/assignments.ts:21` — `client = serverSecretClient()` default argument
   evaluates EAGERLY; with SUPABASE_URL/SECRET unset, every `/team/ops/*` page using it
   hard-500s instead of degrading (`requireOpsOperator` only catches UnauthorizedError).
   Fix: lazy default / move past the VTADMIN early-return.
2. `components/ops/sticky-banner.tsx:69` — `toLocaleTimeString()` SSR/client mismatch =
   React #418 hydration error on EVERY `/team/ops/*` page (currently allowlisted in the
   VT-375 e2e spec with a comment). Fix in the banner (stable formatting or
   suppressHydration on the timestamp node).

## Dispatch trigger

Queued. Dispatch becomes batch-internal when Phase C/D rendered verification needs a
clean console (the #418 allowlist is acceptance friction); otherwise NEEDS-FAZAL like any
out-of-grant row. [BUILD]-class, trivial.

## Status history
- 2026-06-12: rostered from CC's VT-375 e2e findings.

## Status history
- 2026-06-12: closed as the VT-376 rider (#460 → dev @ 0192d60): assignments.ts resolves
  its Supabase client lazily (credless envs degrade, no hard-500); sticky-banner renders a
  deterministic UTC timestamp (React #418 dead); the e2e hydration allowlist removed —
  the full CI board went green including e2e (the A13 flake surface died with the fix).
