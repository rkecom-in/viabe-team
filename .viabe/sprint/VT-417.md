---
vt_id: VT-417
title: REAL customer-data ingestion — replace the Phase-1 dedupe stub (Shopify FIRST) → live writers
status: In Progress
priority: Critical
sprint: "Sprint-Fazal"
type: Feature
area: [Backend, Integrations, PII, Ingestion, Launch]
related: [VT-396, VT-418, VT-325, CL-390, CL-421, CL-431]
exec_order: 86
created: 2026-06-24
last_updated: 2026-06-24
---

## Contract (Cowork 2026-06-24; Fazal: "build real connectors ... no more dummy or stubs, preparing to Go live"). PLAN-FIRST → Cowork gate, then build. Nothing self-merges (PII/ingestion = risk).

The VT-396 connector audit proved every inbound connector (`sheet_push`/`integration_push`/Drive/Shopify
webhook/scheduler `pull_full`) routes to the Phase-1 stub `integrations/dedupe.py:49-122` (writes only
phone_token_resolutions — NO customers, NO sale ledger, NO consent). The LIVE writers (`dedup_and_merge` +
`record_ledger_entries` entry_type='sale' + `consent_capture`) have no production connector trigger. Replace
the stub so connect→ingest actually lands customers + sales.

## Scope
- **Shopify FIRST** (creds in `.viabe/secrets/shopify-dev.env` — keys SHOPIFY_API_KEY/API_SECRET/WEBHOOK_SECRET/
  STORE_DOMAIN; by-reference only, CL-431/Rule #18): real webhook (order topics) + connector + initial backfill →
  order → `customers` (phone_e164, name) + sale `customer_ledger_entries` (amount/date) + consent (source TBD —
  the key decision). HMAC verify (WEBHOOK_SECRET).
- **Replace `dedupe_customer_row`** with a real `ingest_customer_rows` (canonical-row→writers mapping defined ONCE,
  mirror `_image_adapter`) on EVERY inbound path.
- **De-stub the rest:** sheet_push, drive_push, scheduler pull, `methods/*` (fix `upi_export` entry_type='payment'
  → correct 'sale' mapping; `contacts` identity-only = fine). Genuinely-not-go-live → flag DEFERRED, don't leave a stub.
- **PII/DPDP (CL-390/425/426):** persist only the writers' schema; phone tokenized; no raw PII beyond schema.
- **Canary (Rule #15):** real Shopify dev-store order → real customers row + sale ledger entry in dev DB (CL-422 dev = synthetic).

## Gate
RISK (PII + ingestion + sales/money-adjacent). Plan-first → Cowork gate; grouped PRs by connector → Cowork gate; Pillar-7 merge. NOT self-merge.

## Status history
- 2026-06-24: Rostered (Cowork, Fazal "no stubs on go-live"). Plan-first dispatched (design-only). Shopify first.
