# VT-632 lapsed/dormant window inventory (92 hits) — safety-gate sweep for the consistency directive

Generated by a 6-agent parallel sweep + opus completeness critic (2026-07-10). Classifies every hit that ENCODES a window value/label into the 3 overloaded concepts. See the to-cowork signal 20260710T0010Z for synthesis + the product-fork recommendation. This is the machine-generated full list.

## Concept 1 — customer purchase-recency (canonical 45d, LAPSED_WINDOW_DAYS)  (6)

- **`apps/team-orchestrator/canaries/scenarios/sr_cohort_surfacing_ask_list.json:19`** — cur=`45 (post-VT-632-fix, correctly tied to LAPSED_WINDOW_DAYS-backed lapsed_count fast-path)`
  - `"note": "VT-632 (Fazal 2026-07-09: lapsed = no purchase in 45d): the LAPSED count is the dormant subset, NOT the total seeded. ... Now fast-paths to lapsed_coun`
  - → No change — this is the corrected, already-consistent state (explicitly the VT-632 fix this sweep is protecting). Good exemplar wording; keep as the canonical phrasing template for any future scenario notes touching the concept-1 count metric.
- **`apps/team-orchestrator/src/orchestrator/db/wrappers/__init__.py:280`** — cur=`45 (docstring reference only)`
  - `had NO sale in the last ``days`` (45 = ``LAPSED_WINDOW_DAYS``). So: has >=1 'sale' ledger`
  - → Docstring mentions the value for clarity but the code takes days as a param and the only caller passes LAPSED_WINDOW_DAYS — not a second literal definition. No action.
- **`apps/team-orchestrator/src/orchestrator/owner_inputs/status_query.py:29`** — cur=`45`
  - `LAPSED_WINDOW_DAYS = 45`
  - → Already canonical single source of truth (VT-632, Fazal 2026-07-09). Verified via grep: no second hardcoded '45' day-window exists anywhere in src/orchestrator. No action needed — this is the reference implementation the rest of the sweep should be checked against.
- **`apps/team-orchestrator/src/orchestrator/owner_inputs/status_query.py:110`** — cur=`LAPSED_WINDOW_DAYS (45)`
  - `n = cw.count_lapsed(tenant_id, days=LAPSED_WINDOW_DAYS)`
  - → Correctly references the constant, not a re-literal. No action.
- **`apps/team-orchestrator/src/orchestrator/owner_inputs/status_query.py:114`** — cur=`LAPSED_WINDOW_DAYS (45)`
  - `f"within the last {LAPSED_WINDOW_DAYS} days."`
  - → Owner-facing copy correctly interpolates the constant. No action.
- **`apps/team-orchestrator/tests/orchestrator/owner_surface/test_edge_cases.py:325`** — cur=`45`
  - `assert "45 days" not in _lapsed  # NOT the fabricated "everyone bought within 45 days" claim`
  - → Correct as-is — guards the empty-ledger honesty path against a fabricated LAPSED_WINDOW_DAYS-shaped claim. Keep asserting via the source constant's value, never hand-roll a second '45' elsewhere. Sibling routing tests at same file lines 34-44 (qtype 'lapsed_count') carry no window value themselves — noise, not reported separately.

## Concept 2 — SR send cohort (percentile-gated; '60d' is cosmetic label)  (56)

- **`.viabe/live-e2e-winback-runbook.md:177`** — cur=`60`
  - `(last visit >60 days ago). This bypasses the connector but lets the SR detect + win-back`
  - → Runbook defines the manually-seeded lapsed customer as >60 days since last visit — an SR detection window in operator docs. Align with the actual SR percentile gate (there is no fixed 60 in code — the gate is percentile-based); the runbook's '60 days' can mislead an operator into thinking 60 is the threshold.
- **`.viabe/live-e2e-winback-runbook.md:230`** — cur=`60`
  - `2. Dormant customer DETECT — finds +917738859946 as lapsed (>60 days since last visit/purchase)`
  - → Same runbook restating a fixed >60d dormancy for the SR detect step. Correct the doc to say the cohort is percentile-gated, not a fixed 60-day cutoff.
- **`.viabe/sprint/VT-38.md:56`** — cur=`60-90`
  - `Expected: `status == 'proposed'` with a normal `CampaignPlan` for 60-90-day dormants.`
  - → Sprint-doc test expectation referencing a 60-90-day dormant cohort. concept-2. Documentation only; keep aligned with the recency band vocabulary.
- **`.viabe/sprint/VT-68.md:55`** — cur=`60_90d_dormant`
  - ``cohort_key` TEXT NOT NULL (canonical cohort identifier, e.g., `"cafe|tier_1|60_90d_dormant"`)`
  - → Design-doc spec of the k-anon cohort_key format carrying the 60_90d_dormant recency band. concept-2/PRIVACY band — canonical name; ensure RECENCY_BANDS in l3_types.py and the k_anonymity recency_band literals match this doc exactly.
- **`apps/team-orchestrator/canaries/drill_seed_lapsed_customer.py:30`** — cur=`45 (chosen to echo LAPSED_WINDOW_DAYS, but NOT a reference to it)`
  - `_DAYS_DORMANT = 45`
  - → This value only feeds detect_lapsed_customers -> CustomersWrapper.lapsed_candidates (the concept-2 percentile-gated SR SENDABLE cohort, used here to arm a REAL live drill send to an allowlisted phone). Own docstring (line 14, 'single-customer percentiles = own values ⇒ passes floors') admits the gate would pass at ANY days value — 45 is narrative-only, hand-typed, not imported from status_query.LAPSED_WINDOW_DAYS. Rename away from generic 'lapsed'/'_DAYS_DORMANT' toward an SR-scoped name (e.g. SR_DRILL_DORMANCY_DAYS) or add an explicit comment that the numeric coincidence with the canonical 45d customer-count window is deliberate-for-narrative-only, not a wiring dependency — otherwise a future reader will assume it's LAPSED_WINDOW_DAYS-driven.
- **`apps/team-orchestrator/canaries/scenarios/efficient_planning_batch_campaign_constraints.json:10`** — cur=`60 (owner-stated recency constraint, one of three bundled asks)`
  - `"message": "Set up a win-back campaign for anyone who hasn't ordered in 60 days, keep any discount under 15%, and skip anyone who's complained before",`
  - → Leave — tests batch-constraint retention (no re-asking), not window enforcement. Same caveat as vt140: SR cohort gate is percentile-based, not keyed to a literal owner-stated day count.
- **`apps/team-orchestrator/canaries/scenarios/sr_cohort_surfacing_recency_hinglish.json:10`** — cur=`60 (Hinglish '60 din', VT-490 cohort-surfacing LIST ask)`
  - `"message": "kaafi customers hain jinhone 60 din se zyada order nahi kiya — unki list bhej do mujhe",`
  - → Leave — NLU-robustness test for a recency-framed cohort-surfacing (LIST, not count) ask; real gate stays percentile-based. Fine as free text; just don't let '60' drift into being cited as if it were a real SR threshold (see also notes at lines 7 and 13 of this file, same '60 days' framing).
- **`apps/team-orchestrator/canaries/vt140_e2e_sr_agent.py:135`** — cur=`60 (owner-message free text driving a real dispatch_brain -> SR delegation run)`
  - `body="Recover my dormant customers from the last 60 days",`
  - → Leave as simulated NL input — the seed is built to elicit ANY recovery-campaign proposal, and nothing downstream asserts the agent literally honors '60 days' (lapsed_candidates is percentile-gated, no fixed cutoff exists to check against). Flag only: don't let this literal '60' get mistaken elsewhere for a real SR window constant — none exists.
- **`apps/team-orchestrator/canaries/vt190_self_evaluate_retrofit.py:179`** — cur=`"60-90 day dormants" (fixture cohort_label string fed into SelfEvaluateTool)`
  - `"cohort_label": "60-90 day dormants",`
  - → cohort_label is schema-unconstrained free text (orchestrator/collapse.py:389, min_length=1 only) so this passes regardless of wording. But for VT-632 hygiene, note the SAME 60-90-day segment is spelled three different ways across the repo: this fixture ('60-90 day dormants'), the real SR prompt example in agent/prompts/sales_recovery_v1.md ('dormant-60d'), and the privacy k-anon RECENCY_BANDS enum in knowledge/l3_types.py ('60_90d'). Align this fixture's string to whichever canonical label format Fazal settles on so a reader doesn't infer three different windows exist.
- **`apps/team-orchestrator/canaries/vt566_flywheel_readback_dev.py:99`** — cur=`45 (WhatsApp template param value)`
  - `"params": {"days_since_last_visit": "45"}`
  - → WhatsApp template param days_since_last_visit=45 driving a real dev readback. It happens to echo LAPSED_WINDOW_DAYS=45 but is not a reference. If it means the customer-recency the owner asked about, source from LAPSED_WINDOW_DAYS; if it's a send-template display value, tie to the SR cohort's actual per-customer days_since_last_sale, not a hardcoded 45.
- **`apps/team-orchestrator/src/orchestrator/agent/marketing_lane.py:163`** — cur=`"lapsed_60d" (illustrative example, LLM tool docstring for draft_campaign_plan)`
  - `segment_label — the human label of the customer segment this targets (e.g. "lapsed_60d",`
  - → Same inconsistency as onboarding_conductor.py — this is a live @tool docstring fed to the marketing-lane specialist. Standardize to the same illustrative string used in the SR prompt (sales_recovery_v1.md's "dormant-60d") so the marketing and SR specialists don't drift toward different segment-label vocabularies for the same underlying free-text concept.
- **`apps/team-orchestrator/src/orchestrator/agent/onboarding_conductor.py:367`** — cur=`"lapsed_60d" (illustrative example only)`
  - `_MAX_SEGMENT_LABEL_LEN = 64  # a segment is a free-form label (e.g. "lapsed_60d") — no fixed`
  - → Not a real constant — comment explicitly confirms 'no fixed taxonomy exists anywhere in this codebase', so this is genuinely free text. However the ILLUSTRATIVE example string here ("lapsed_60d") differs from the example used elsewhere for the identical concept: sales_recovery_v1.md / tests / twilio_ingress use "dormant_60d" or "dormant-60d". Since this docstring is LLM-facing (tool-adjacent policy proposal), standardize the illustrative string across all owner-policy / SR / marketing docstrings to avoid nudging different specialists toward inconsistent label spelling.
- **`apps/team-orchestrator/src/orchestrator/agent/onboarding_conductor.py:432`** — cur=`"lapsed_60d" (illustrative example, LLM tool docstring)`
  - `"lapsed_60d" — there is no fixed segment list). ``frequency_caps`` —`
  - → Same as line 367 — this is the propose_business_policy @tool docstring the LLM actually reads. Align the example string with the canonical "dormant_60d"/"dormant-60d" convention used in sales_recovery_v1.md + tests to keep the free-form-label illustration consistent across every specialist surface.
- **`apps/team-orchestrator/src/orchestrator/agent/prompts/sales_recovery_v1.md:105`** — cur=`dormant-60d`
  - `"cohort_label": "dormant-60d",`
  - → Own clearly-named constant, not a bare example string. This is illustrative JSON inside the SR agent's system prompt (the model is shown this as the shape of a cohort_label it should emit) -- touches the SEND path (decides framing of who the send rail targets) but carries no k-anon PII itself. Cross-ref: apps/team-orchestrator/src/orchestrator/owner_inputs/status_query.py:28 already annotates this exact string as 'a cosmetic SR-side label, not this count metric' -- Fazal/Cowork already flagged it as intentionally distinct from LAPSED_WINDOW_DAYS=45. Real gap: the actual k-anonymity recency band in code (orchestrator/knowledge/l3_types.py RECENCY_BANDS) is bucketed 0_30d/30_60d/60_90d/90d_plus, not a single 'dormant-60d' cutoff -- this prompt example uses different vocabulary than the real band names. Reconcile: either replace the literal example with the real band token (e.g. '60_90d') or introduce a single named SR_TARGET_DORMANCY_LABEL the prompt example is generated from, so prompt copy and the real k-anon band can't silently diverge.
- **`apps/team-orchestrator/src/orchestrator/agent/prompts/sales_recovery_v1.md:105`** — cur=`dormant-60d`
  - `"cohort_label": "dormant-60d",`
  - → concept 2 — LLM prompt example cohort label states a fixed 60d window that DOES NOT EXIST in code (detection is p75-percentile-gated, see executor:139). This 'dormant-60d' string is the origin of the 60d myth (also cited in status_query.py:28 and sprint docs). Fix: rewrite the example to reflect the percentile/tenant-relative basis (or generic 'dormant') so the model doesn't emit a literal 60d cohort_label that contradicts the real gate. SEND-path prompt (drives target_cohort).
- **`apps/team-orchestrator/src/orchestrator/agent/prompts/sales_recovery_v1.md:107`** — cur=`Inactive >=60d`
  - `"selection_reason": "Inactive >=60d, opted-in for promos [E1]."`
  - → Same example JSON block as line 105; the free-text selection_reason repeats the same 60d figure. If the cohort_label above is renamed/aligned to the real RECENCY_BANDS vocabulary, this companion example text must move in lockstep or the two drift apart inside the same prompt.
- **`apps/team-orchestrator/src/orchestrator/agent/prompts/sales_recovery_v1.md:107`** — cur=`>=60d`
  - `"selection_reason": "Inactive >=60d, opted-in for promos [E1]."`
  - → concept 2 — same 60d inconsistency as line 105, in the selection_reason exemplar the model imitates. The real selection is p75 days-since-last-sale + p50 spend, not '>=60d'. Rewrite to the percentile basis. SEND-path prompt copy.
- **`apps/team-orchestrator/src/orchestrator/agent/prompts/sales_recovery_v1.md:123`** — cur=`dormant_recovery_v1`
  - `"template_id": "dormant_recovery_v1",`
  - → Weaker hit -- a template_id example name (not a numeric window), but it embeds 'dormant' as if it's a real send-template name. Grepped .viabe/templates.md (the canonical template_name->SID registry) and apps/team-orchestrator/src -- this string does not exist anywhere as a real template; it is purely illustrative. Low priority: the prompt itself already warns a few lines below not to copy template_id values verbatim into a real plan, so drift risk is limited to a model imitating the naming pattern. No action required beyond confirming it stays fictional; do not let a future real win-back template accidentally take this exact literal name without going through the templates.md allocator.
- **`apps/team-orchestrator/src/orchestrator/agent/tools/prompts/self_evaluate_v1.md:54`** — cur=`90-180 day`
  - `- draft targets a 90-180 day dormant cohort but`
  - → CONFIRMED cross-file inconsistency: this self_evaluate consistency-check example uses a 90-180 day dormant-cohort window, while sales_recovery_v1.md's own example (line 105) uses 'dormant-60d' / '>=60d', and the real code band (knowledge/l3_types.py RECENCY_BANDS) tops out at '90d_plus' with no 180 upper bound at all. Three different numbers (60d, 90-180d, and the real 0_30/30_60/60_90/90d_plus bands) describing the same SR dormancy axis across two prompt files plus the code. This is illustrative example text (not a literal constant reference) so it won't break at runtime, but it actively teaches the judge model an inconsistent window. Fix: replace the ad-hoc '90-180 day' example with language anchored to the real named band vocabulary (e.g. 'targets the 60_90d recency band but attribution_snapshot shows zero customers in 60_90d'), or reference a shared SR_TARGET_DORMANCY_DAYS-style constant description so all three prompt/example sites can't independently invent different numbers.
- **`apps/team-orchestrator/src/orchestrator/agents/customer_send.py:74`** — cur=`30 (send-time gate-5 re-check, gate stack in module docstring lines 8-40)`
  - `RECONTACT_SUPPRESSION_DAYS = 30      # any agent contact <= 30d ago -> skip`
  - → SENSITIVE — this is the deterministic SEND choke-point re-check (agent_send_draft gate 5). Same name+value as sales_recovery_executor.py:146 but a genuinely separate Python object. Own a single clearly-named constant (e.g. SR_RECONTACT_SUPPRESSION_DAYS) in one module both send-time and detection-time code import, so the two suppression checks can never drift apart.
- **`apps/team-orchestrator/src/orchestrator/agents/customer_send.py:75`** — cur=`90 (rolling-90d contact ceiling)`
  - `MAX_AGENT_CONTACTS_PER_90D = 2       # lifetime-ish ceiling per rolling 90d`
  - → SEND-path 90-day rolling frequency-cap constant — the 90 is a named send window distinct from the dormancy window. Keep as its own constant; note it touches the SEND gate stack. List cited :74 and :100 but missed this named 90d ceiling.
- **`apps/team-orchestrator/src/orchestrator/agents/customer_send.py:298`** — cur=`90 (window param for the 90d cap query)`
  - `count(*) FILTER (WHERE sent_at > now() - make_interval(days => %s)) AS recent`
  - → Send-time query implementing the rolling-90d ceiling. Ensure the %s is fed from MAX_...PER_90D window constant, not a literal.
- **`apps/team-orchestrator/src/orchestrator/agents/sales_recovery_executor.py:139`** — cur=`0.75 (p75 days-since-last-sale)`
  - `DETECTION_RECENCY_PERCENTILE = 0.75`
  - → concept 2 — this is the ACTUAL SR dormancy/targeting gate (tenant-relative p75, NOT a fixed day window). SEND PATH: feeds _LAPSED_CANDIDATES_SQL WHERE days_since_last_sale >= recency_floor (percentile_cont). Keep the percentile mechanism but this is the source-of-truth for WHO GETS MESSAGED; it is INCONSISTENT with the '60d' copy in sales_recovery_v1.md. Give it a doc-comment cross-ref so no one 'fixes' the prompt to a literal that contradicts the code.
- **`apps/team-orchestrator/src/orchestrator/agents/sales_recovery_executor.py:146`** — cur=`30 (detection-time suppression, feeds lapsed_candidates() as suppression_days)`
  - `RECONTACT_SUPPRESSION_DAYS = 30`
  - → SENSITIVE — SEND path (gates who is even a lapsed/win-back CANDIDATE). Duplicate-name collision with agents/customer_send.py:74's independently-declared constant of the SAME name and SAME value — not imported from a shared location, so the two can silently diverge if only one is edited. Extract to one shared constant (e.g. a small sales_recovery config module) that both sales_recovery_executor.py and customer_send.py import.
- **`apps/team-orchestrator/src/orchestrator/agents/sales_recovery_executor.py:146`** — cur=`30`
  - `RECONTACT_SUPPRESSION_DAYS = 30`
  - → concept 2, SEND PATH — recontact-suppression window on the SR cohort (drops recently-contacted customers via _LAPSED_CANDIDATES_SQL make_interval(days => suppression_days); re-enforced at SEND time in customer_send.check_agent_send_caps). Already clearly named; keep as own constant. NOT the dormancy-target window — do not conflate with DETECTION_RECENCY_PERCENTILE; a rename toward 'SR_TARGET_DORMANCY_DAYS' would be WRONG here (this is frequency suppression). Flag: touches send path.
- **`apps/team-orchestrator/src/orchestrator/context_builder.py:334`** — cur=`90+ days (illustrative example only, no fixed threshold by design)`
  - `customer lapsed BY PURCHASE (bought 90+ days ago, never messaged) is a`
  - → This docstring for _build_ledger_summary explicitly documents a NO-FIXED-THRESHOLD, percentile-gated design (VT-312 brain-decides). The '90+ days' here is just an illustrative example, not a constant, but a stray day-count sitting next to the canonical LAPSED_WINDOW_DAYS=45 and the RECENCY_BANDS 90d_plus bucket creates confusion risk during a word-overload audit. Low priority: reword the example to avoid a bare day-count that reads like a threshold (e.g. 'bought long ago' instead of '90+ days ago').
- **`apps/team-orchestrator/src/orchestrator/db/wrappers/__init__.py:49`** — cur=`recency_pct percentile floor (the SR dormancy DEFINITION)`
  - `percentile_cont(%(recency_pct)s) WITHIN GROUP (ORDER BY days_since_last_sale::double precision) AS recency_floor_days`
  - → This _LAPSED_CANDIDATES_SQL block IS the sendable-cohort dormancy gate (percentile, not a literal). The actual SQL was missed (list only cited docstrings at :280/:283). SEND path — feeds lapsed_candidates(). Keep percentile-based, but ensure the recency_pct/spend_pct params are sourced from one clearly-named SR constant, not scattered call-site literals (executor.py:139 p75).
- **`apps/team-orchestrator/src/orchestrator/db/wrappers/__init__.py:66`** — cur=`recency_floor_days predicate (percentile-gated)`
  - `AND s.days_since_last_sale >= t.recency_floor_days`
  - → The dormancy filter predicate itself. concept-2: this decides WHO is dormant enough to message. Document it as the canonical SR recency gate; no literal to change but it's the definition the whole cohort hangs on.
- **`apps/team-orchestrator/src/orchestrator/db/wrappers/__init__.py:82`** — cur=`suppression_days (30 at call site)`
  - `AND acc.sent_at >= now() - make_interval(days => %(suppression_days)s)`
  - → SEND-path re-contact suppression window inside the sendable-cohort SQL. Bind suppression_days to the named SR constant (RECONTACT_SUPPRESSION_DAYS=30) at every caller, not a bare 30.
- **`apps/team-orchestrator/src/orchestrator/knowledge/l3_types.py:37`** — cur=`0_30d/30_60d/60_90d/90d_plus`
  - `RECENCY_BANDS: Final = ("0_30d", "30_60d", "60_90d", "90d_plus")`
  - → Already its own clearly-named constant (privacy k-anon cohort_key dimension, keyed on days-since-last-INBOUND, explicitly documented as a SEPARATE plane from the per-tenant percentile-gated SR detection in context_builder.py:341-344). No rename required, but note the boundary literals (30/60/90) are re-typed inline in recency_band() below rather than derived from this tuple — a future edit to one without the other would silently desync the bucket edges from the declared band names.
- **`apps/team-orchestrator/src/orchestrator/knowledge/l3_types.py:37`** — cur=`0_30d / 30_60d / 60_90d / 90d_plus`
  - `RECENCY_BANDS: Final = ("0_30d", "30_60d", "60_90d", "90d_plus")`
  - → concept 2 — PRIVACY/k-anon band. These are the canonical cross-tenant recency-band labels; recency_band() feeds cohort_key() (l3_construction:127) and privacy.k_anonymity CohortRow.recency_band (canonical hash string k_anonymity:89). MUST NOT be silently rewritten — changing a label changes the k-anon canonicalization/cohort_key. NOTE the live label is '60_90d', NOT the '60_90d_dormant' the sweep hypothesized (that suffixed form exists ONLY in sprint docs VT-68/VT-74, and VT-40 uses a different set dormant_30_60d/…). This is the single source of truth; reconcile the docs to it, not vice-versa.
- **`apps/team-orchestrator/src/orchestrator/knowledge/l3_types.py:43`** — cur=`days=90 boundary (inline)`
  - `if days_since_last_inbound is None or days_since_last_inbound >= 90:`
  - → Touches the k-anon PRIVACY recency_band plane (cohort_key dimension) used cross-tenant in L3. Consider deriving the 30/60/90 cut points from RECENCY_BANDS itself (e.g. parse the band name) instead of a parallel if/elif ladder, to remove the drift surface between the tuple and the bucketing function.
- **`apps/team-orchestrator/src/orchestrator/knowledge/l3_types.py:43`** — cur=`90`
  - `if days_since_last_inbound is None or days_since_last_inbound >= 90:`
  - → concept 2 — PRIVACY band boundary (>=90 -> '90d_plus'). Window value 90 for the k-anon recency band. Keep aligned with RECENCY_BANDS labels; centralize the 30/60/90 boundaries so labels and cutoffs can't drift. Privacy-touching (cross-tenant cohort dimension).
- **`apps/team-orchestrator/src/orchestrator/knowledge/l3_types.py:45`** — cur=`30`
  - `if days_since_last_inbound < 30:`
  - → concept 2 — PRIVACY band boundary (<30 -> '0_30d'). Same centralization note as line 43. Privacy k-anon dimension; not owner-facing lapsed metric.
- **`apps/team-orchestrator/src/orchestrator/knowledge/l3_types.py:47`** — cur=`60`
  - `if days_since_last_inbound < 60:`
  - → concept 2 — PRIVACY band boundary (<60 -> '30_60d', else '60_90d'). Same centralization note. Privacy k-anon dimension. This is the ONLY live source of a '60d' recency cut, and it's a k-anon band boundary — distinct from the SR target gate (p75) and the owner lapsed metric (45d).
- **`apps/team-orchestrator/tests/agent/test_autonomy_rails_vt474.py:78`** — cur=`lapsed (bare token, no window suffix)`
  - `p = _policy(allowed_action_types=["customer_send"], allowed_segments=["lapsed"])`
  - → business_policy.allowed_segments is a free-form string set with NO fixed taxonomy anywhere in the codebase (confirmed via src/orchestrator/agent/onboarding_conductor.py:367 comment, which itself gives 'lapsed_60d' as an example — a FIFTH distinct spelling). This bare 'lapsed' policy-grant token has no enforced relationship to the owner_inputs 'dormant_60d' segment or the campaign_plan 'dormant-60d'/'60-90 day dormants' cohort_label — a real send would only match this policy grant if the runtime segment value happens to equal 'lapsed' exactly. This is the highest-risk finding in this surface: verify what string value SR actually passes as attrs['segment'] at send time and whether it can ever silently fail to match an owner's granted 'lapsed'/'dormant_60d'/'all' policy row. Same pattern recurs at test_onboarding_conductor_write_tools_tenant_scope.py:484 (+5 more lines in that file) and test_vt610_force_l3_rails.py:274.
- **`apps/team-orchestrator/tests/orchestrator/agent/test_onboarding_conductor_write_tools_tenant_scope.py:484`** — cur=`lapsed (bare token)`
  - `"allowed_segments": ["lapsed"],`
  - → Same bare-'lapsed' policy-segment finding as test_autonomy_rails_vt474.py:78, here at the onboarding_conductor.propose_business_policy call site (the actual owner-grant authoring path). Repeats identically at lines 499, 516, 540, 574, 596 in this same file. Resolve alongside the VT-474 finding — this is where an owner's spoken grant ('message lapsed customers') gets tokenized to a policy string that must eventually match whatever segment value a real SR send uses.
- **`apps/team-orchestrator/tests/orchestrator/agent/test_sales_lane_agent.py:128`** — cur=`>60d dormant`
  - `target_framing="lapsed high-value customers, >60d dormant",`
  - → Sales-lane recommend_sales_play fixture text delegating to sales_recovery — free-text 'target_framing', not asserted against a structural window. Notably uses the bare word 'lapsed' here to mean the SR/dormant concept (concept 2), the same word concept 1's owner-facing count feature uses for its OWN 45-day metric — exactly the overload Fazal flagged. No code fix needed here (it's prose input to a tool), but worth naming explicitly as a source of the ambiguity.
- **`apps/team-orchestrator/tests/orchestrator/agent/test_sales_recovery.py:778`** — cur=`dormant-60d`
  - `"cohort_label": "dormant-60d",`
  - → Yet another spelling variant ('dormant-60d', hyphenated) vs. 'dormant_60d' (owner_inputs segment), '60-90 day dormants' (campaign_plan schema tests), '60_90d'/'60_90d_dormant' (k-anon/L3). Four+ distinct token shapes for what appears to be the same ~60-90-day SR concept. Recommend one canonical machine-readable constant (e.g. SR_TARGET_DORMANCY_DAYS or an enum) that owner_inputs.segment, k_anonymity.recency_band, and l3 cohort_key all derive from; cohort_label (LLM/free-text prose) can stay free-form since it's display-only.
- **`apps/team-orchestrator/tests/orchestrator/agent/test_sales_recovery.py:797`** — cur=`dormant_recovery_v1`
  - `"template_id": "dormant_recovery_v1",`
  - → WhatsApp template_id fixture in the same CampaignPlan test as 'dormant-60d' (line 778) — verify this template_id exists in .viabe/templates.md's canonical template_name -> SID map (per repo rule, no hard-coded SIDs/names should drift from that registry).
- **`apps/team-orchestrator/tests/orchestrator/agent/test_scope_discipline.py:171`** — cur=`Dec dormants 60-90`
  - `"campaign_name": "Dec dormants 60-90 reactivation",`
  - → Another free-text campaign_name carrying the 60-90 window as prose, not a structured value. Consistent with cohort_label being LLM-authored display text elsewhere — leave as-is, but see the cross-file constant recommendation above for the underlying numeric window.
- **`apps/team-orchestrator/tests/orchestrator/agent/test_self_evaluate_gate.py:920`** — cur=`90-180 day dormants`
  - `"target_cohort.cohort_label '90-180 day dormants' but context_summary "`
  - → A DIFFERENT band than the '60-90 day dormants' used elsewhere in the same test file (line 81, 750). Confirms cohort_label is genuinely free-text (SR/gate can reference any band an LLM invents) rather than a fixed enum — fine if intentional, but means there is no single 'the SR window' to reconcile against; documenting this as the expected behavior (not a bug) would remove ambiguity for the sweep.
- **`apps/team-orchestrator/tests/orchestrator/agents/test_sales_recovery_executor.py:222`** — cur=`30d (RECONTACT_SUPPRESSION_DAYS=30)`
  - `_seed_agent_contact(dsn, t, contacted, days_ago=10)  # inside the 30d suppression`
  - → A DIFFERENT, correctly-named window than LAPSED_WINDOW_DAYS/dormant_60d: sales_recovery_executor.RECONTACT_SUPPRESSION_DAYS=30 is the recently-agent-contacted SEND suppression, exercised here (real named constant already, good). Flag only because the numeral '30' now coincides with the unrelated 30-day trial window (concept 3) — pure coincidence today but worth a comment at the constant definition noting it is NOT the trial window, to prevent a future reader conflating them.
- **`apps/team-orchestrator/tests/orchestrator/knowledge/test_l3_construction.py:130`** — cur=`60_90d`
  - `assert _pattern_rows(pool, f"{bt}|tier_2|60_90d") == []  # dropped: <10 contributors`
  - → L3 cross-tenant cohort_key bucket is FIXED at '60_90d' (recency_days=75 seeded to land in it; same cohort_key reused lines 141, 181). This is a THIRD independent place '60_90d' is hard-coded (also test_k_anonymity.py, k-anon recency_band). Confirm this L3 bucket boundary and the k-anon recency_band and the SR percentile-gate (which is NOT a fixed day-count, see test_sales_recovery_executor.py) are meant to line up — today they're three separately-typed literals with no shared constant.
- **`apps/team-orchestrator/tests/orchestrator/manager/test_shadow_eval.py:61`** — cur=`60d dormant cohort`
  - `"situation": "60d dormant cohort",`
  - → Shared fixture string, identically reused at test_review_extraction.py:54 — good, these two agree with each other. Just free-text manager-turn situation, not a structural assertion; no action needed beyond the cross-file constant consolidation noted above.
- **`apps/team-orchestrator/tests/orchestrator/owner_inputs/test_owner_inputs_substrate.py:217`** — cur=`dormant_60d`
  - `segment="dormant_60d",`
  - → Same 'dormant_60d' literal as test_twilio_ingress.py/test_writer_unit.py (also asserted line 239 in this file). Consolidate onto one named constant/enum shared by writer + ingress + this substrate test rather than three independent hard-coded copies of the string.
- **`apps/team-orchestrator/tests/orchestrator/owner_inputs/test_writer_unit.py:65`** — cur=`dormant_60d`
  - `'{"intent": "winback", "segment": "dormant_60d", '`
  - → Same literal again (also line 73). Third independent copy of 'dormant_60d' across writer/ingress/substrate tests — collapse to one shared fixture/constant so a future window-name change doesn't require editing N test files in sync.
- **`apps/team-orchestrator/tests/orchestrator/owner_surface/test_approval_reply.py:186`** — cur=`60-90 day dormant`
  - `assert params["1"] == "60-90 day dormant"`
  - → This is the WhatsApp approval-request TEMPLATE PARAM #1 (real send path, owner-facing). Singular 'dormant' vs. the 'dormant**s**' plural used in every other cohort_label test fixture in this surface (test_collapse.py:139, test_output_composer.py:487, test_campaign_plan.py:71, etc.). Confirm this discrepancy is intentional (label passed through verbatim from whatever the LLM authored) and not a copy-paste drift risk on the one param that actually reaches a customer-facing WhatsApp template.
- **`apps/team-orchestrator/tests/orchestrator/privacy/test_k_anonymity.py:74`** — cur=`60_90d_dormant`
  - `"recency_band": "60_90d_dormant", "signed_up_before": _CUTOFF,`
  - → K-anonymity CohortPredicate recency_band — this is the PRIVACY-moat naming plane. Same file also uses the shorter '60_90d' (no '_dormant' suffix) at lines 125 and 136 for otherwise-identical predicates — two spellings of the same band inside one test file. Pick one canonical band-label format (e.g. SR_RECENCY_BAND = '60_90d') and use it everywhere in this module.
- **`apps/team-orchestrator/tests/orchestrator/privacy/test_k_anonymity.py:125`** — cur=`60_90d`
  - `"business_type": "cafe", "city_tier": "tier_2", "recency_band": "60_90d",`
  - → See line 74 finding — this is the shorter spelling of the same recency band used a few lines away as '60_90d_dormant'. Same literal repeats at line 136. Normalize to one label.
- **`apps/team-orchestrator/tests/orchestrator/test_collapse.py:139`** — cur=`60-90 day dormants`
  - `cohort_label="60-90 day dormants",`
  - → CampaignPlan.target_cohort.cohort_label free-text fixture. Same string reused verbatim in test_output_composer.py:487, test_campaign_plan.py:71 (the schema's own test), test_self_evaluate_gate.py:81, test_collapse_reply_transmit.py:63 — but test_approval_reply.py:186 (the actual WhatsApp template param on the SEND path) uses the singular 'dormant' (no 's'), and test_sales_recovery.py:778 uses 'dormant-60d', and test_scope_discipline.py:171 uses 'Dec dormants 60-90'. Five+ spellings of the same 60-90-day concept across the SR pipeline; cohort_label is free-form LLM-authored prose by design (not a rendered constant), but the send-path template-param test disagreeing in pluralization from every schema-level test fixture is worth a deliberate look.
- **`apps/team-orchestrator/tests/orchestrator/test_twilio_ingress.py:694`** — cur=`dormant_60d`
  - `intent="winback", segment="dormant_60d", occasion="diwali"`
  - → Own a named constant (e.g. SR_TARGET_DORMANCY_DAYS/segment enum) instead of the free-text literal 'dormant_60d' — this exact literal also asserted at lines 719 and 770 in this file (owner_inputs classification -> Composer read-path, an actual ingress e2e). Touches the SEND-eligible classification path; treat as privacy/send-sensitive.
- **`apps/team-orchestrator/tests/orchestrator/test_vt485_winback_grounding_realdb.py:186`** — cur=`90-day purchase-lapsed buyers`
  - `"cohort_label": "90-day purchase-lapsed buyers",`
  - → Another free-text cohort_label variant (90-day, singular 'buyers') feeding the real self_evaluate gate + send-ready CampaignPlan path (test_legit_lapsed_customer_passes_gate_with_send_ready_draft). Confirms cohort_label truly is unconstrained prose across the whole SR surface — same recommendation as above: keep label free, but the underlying recency BASIS ('purchase' vs 'inbound message') this test is really pinning is a good candidate for a named constant/enum (recency_basis) since it silently changed product behavior once already (VT-485).
- **`migrations/085_l3_patterns.sql:4`** — cur=`60-90d`
  - `-- tier-2 cities: 60-90d dormants who get a discount approve at ~40-50%").`
  - → K-anon L3 pattern comment referencing the 60_90d recency band. concept-2/PRIVACY band — keep consistent with RECENCY_BANDS in l3_types.py; it's the cross-tenant cohort dimension, not the owner metric.
- **`migrations/127_vt369_agent_customer_contacts.sql:3`** — cur=`30d suppression / 2-per-90d ceiling`
  - `One row per ACTUAL agent send to a customer. Drives: recontact suppression (30d), the 2-per-90d`
  - → Schema comment stating the SEND-path suppression (30d) + 90d ceiling windows. Keep the comment in sync with the customer_send.py named constants (RECONTACT_SUPPRESSION_DAYS / MAX_AGENT_CONTACTS_PER_90D); it's the DB-side documentation of those send windows.
- **`migrations/144_vt474_business_policy.sql:30`** — cur=`"lapsed" (send-policy targeting segment)`
  - `"allowed_segments": ["lapsed", "vip", "all"],          -- absent/[] = no segment allowed`
  - → Named targeting segment in the send-authorization policy JSONB (business_policy guard). Bare token, no window, but it names the send-eligible dormant segment. Note it touches the SEND gate; if the segment vocabulary is standardized, align 'lapsed' here with the SR cohort naming so policy and cohort refer to the same thing.

## Concept 3 — trial-lapsed (30d subscription axis)  (17)

- **`apps/team-orchestrator/src/orchestrator/api/owner_dashboard.py:209`** — cur=`days=30 (hardcoded literal)`
  - `trial_ends = (trial_started + timedelta(days=30)) if trial_started else None`
  - → Recurrence of the VT-371 bug class (a stale local hardcoded trial length that already drifted once in onboarding/signup.py before being fixed to read config/trial.yaml's trial_days). This owner-facing dashboard computes trial_ends_at with a bare literal instead of the canonical trial_days config that billing/trial_evaluator.py, onboarding/signup.py::_trial_days(), and api/ops_run_control.py:963 all correctly read. If trial length is ever changed in config, this endpoint will silently show the wrong trial_ends_at to the owner. Fix: read cfg['trial_days'] the same way trial_evaluator.py does, not a re-literal 30.
- **`apps/team-orchestrator/src/orchestrator/state/__init__.py:18`** — cur=`30-day (documentation comment only)`
  - `"lapsed",  # VT-365: 30-day trial expired without subscribe — dormant, re-subscribable`
  - → Canonical Phase Literal, consistent with transitions.py and billing/graceful_exit.py. No action needed — the trial length itself is NOT hardcoded here, just described in a comment.
- **`apps/team-orchestrator/src/orchestrator/transitions.py:22`** — cur=`30-day (documentation comment only)`
  - `# VT-365 (Fazal 2026-06-09): 30-day free trial, NO card in trial, opt-in subscribe at/after day 30,`
  - → Correct — this is the canonical Phase state machine; the 30-day figure here is descriptive only (actual gating reads config/trial.yaml trial_days via billing/trial_evaluator.py). No action; confirms the 'lapsed' subscription-phase vocabulary is centralized here + state/__init__.py, distinct from concept-1's 'lapsed_count' query type and concept-2's lapsed_candidates/dormant_cohort SR vocabulary.
- **`apps/team-orchestrator/src/orchestrator/transitions.py:42`** — cur=`"lapsed" (subscription phase name)`
  - `("trial", "trial_expired"): "lapsed",`
  - → Correctly-scoped reuse of the word 'lapsed' for the trial-expiry phase, the DIFFERENT axis from concept 1/2. No numeric drift; keep the vocabulary as is — it is the existing, deliberate VT-365 naming. Flagging only so the sweep's glossary explicitly notes this third meaning exists alongside status_query.py's 'lapsed_count' and the SR 'lapsed_candidates'.
- **`apps/team-orchestrator/tests/orchestrator/billing/test_razorpay_ingress.py:164`** — cur=`30-day-expired, dormant`
  - `(30-day-expired, dormant) owner who subscribes converts straight to paid_active too."""`
  - → Same overload ('dormant' describing the trial-lapsed phase) in the Razorpay capture-conversion test docstring. Reword to drop 'dormant'.
- **`apps/team-orchestrator/tests/orchestrator/billing/test_trial_sweep.py:13`** — cur=`30-day trial`
  - `VT-365 (Fazal 2026-06-09): 30-day flat trial, NO card, opt-in subscribe at day 30,`
  - → This is the clean, correctly-scoped statement of the concept-3 window (30-day trial, no reuse of 'dormant'). Good reference wording — the other concept-3 files above should converge on phrasing like this one instead of calling it 'dormant'.
- **`apps/team-orchestrator/tests/orchestrator/onboarding/test_journey_intercept.py:7`** — cur=`onboarding/trial/lapsed`
  - `(a) LAZY-START — no journey + a FRESH phase (onboarding/trial/lapsed) →`
  - → Clean concept-3 usage listing phase values, no 'dormant' overload. No change needed.
- **`apps/team-orchestrator/tests/orchestrator/test_campaign_execute.py:516`** — cur=`phase=lapsed / 30-day trial`
  - `"""VT-328/VT-365: a `lapsed` tenant (30-day trial expired without subscribe — dormant, no`
  - → Correct vocabulary for the subscription axis (tenant.phase='lapsed' = 30-day trial expired), but the docstring itself glosses phase='lapsed' as '(dormant, no ...)' — same word Fazal wants reserved for concept 2's SR cohort. Keep the phase value 'lapsed' (renaming a DB enum is expensive/risky), but stop describing it as 'dormant' in comments/docstrings — that's exactly the cross-concept word-reuse causing the overload. Same pattern at line 568/576 in this file.
- **`apps/team-orchestrator/tests/orchestrator/test_campaign_execute.py:576`** — cur=`lapsed = dormant`
  - `# lapsed = dormant, no active subscription → blocked (the new VT-365 block).`
  - → Explicit comment equating tenant.phase 'lapsed' with 'dormant' — this IS the overload. Reword to something like 'lapsed = trial expired without subscribe, no active subscription' and drop 'dormant' from the trial-phase vocabulary entirely so grep/search for 'dormant' cleanly isolates concept 2.
- **`apps/team-orchestrator/tests/orchestrator/test_dispatch_guard_realdb.py:10`** — cur=`dormant `lapsed` phase / 30-day`
  - `tenants.refunded_at is dropped (migration 121). The dormant `lapsed` phase (30-day trial expired`
  - → Same word-overload pattern as test_campaign_execute.py — module docstring calls the trial-expired tenant phase 'dormant'. Rename the doc language away from 'dormant' (reserve that word for the SR cohort); keep 'lapsed' as the phase enum value since VT-365 already shipped it as a DB/state-machine literal.
- **`apps/team-orchestrator/tests/orchestrator/test_state.py:55`** — cur=`'lapsed' is dormant`
  - `# VT-365: 'refunded' removed; 'cancelled' is the sole terminal phase ('lapsed' is dormant`
  - → Same overload again, in the Phase-enum invariant test. Purely a comment-wording issue (line 60 correctly checks `"lapsed" in typing.get_args(Phase)` — the enum itself is fine); reword the comment to avoid 'dormant'.
- **`apps/team-orchestrator/tests/orchestrator/test_transitions.py:174`** — cur=`lapsed (dormant)`
  - `"""VT-365: trial → trial_expired → lapsed (dormant), and a lapsed owner can still subscribe."""`
  - → Same overload pattern in the state-machine transition test docstring. Reword away from '(dormant)' parenthetical.
- **`apps/team-orchestrator/tests/owner_surface/test_monthly_report.py:46`** — cur=`phase=lapsed`
  - `assert should_skip(phase="lapsed", signed_up_at=None, period_end=end) == "phase=lapsed"`
  - → Clean concept-3 usage (report-skip on trial-lapsed/cancelled phase, no 'dormant' language). No change needed; same clean pattern at test_monthly_report_runner.py:96/98.
- **`migrations/096_vt82_tenant_trial_columns.sql:5`** — cur=`30-day trial`
  - `-- 30-day trial, NO extensions). Nullable + default-0 so existing rows are unaffected.`
  - → Trial-length window (subscription axis). Keep trial vocabulary.
- **`migrations/099_vt93_refund_executions.sql:24`** — cur=`30-day (graceful-exit window)`
  - `-- 30-day graceful-exit window anchor. Set atomically with phase='refunded' by the`
  - → Subscription graceful-exit window (may be dead post-VT-365 refund removal). Subscription axis, keep separate from customer recency; verify it's not orphaned by the refund-subsystem removal.
- **`migrations/121_vt365_remove_refund_subsystem.sql:1`** — cur=`30-day trial`
  - `-- 121_vt365_remove_refund_subsystem.sql — VT-365 (Fazal 2026-06-09): 30-day free trial, NO card in`
  - → Subscription/trial window. Keep trial vocabulary; distinct axis from customer recency.
- **`migrations/121_vt365_remove_refund_subsystem.sql:25`** — cur=`'lapsed' (subscription phase)`
  - `CHECK (phase IN ('onboarding', 'trial', 'lapsed', 'paid_active', 'paid_at_risk', 'cancelled'));`
  - → DB CHECK constraint defining the subscription 'lapsed' phase — the trial/billing axis reusing the word. Keep as subscription vocabulary; this is the canonical source of the naming collision with customer-lapsed. Consider a rename (e.g. 'trial_expired') if the ambiguity keeps biting, but that's a schema change — flag to Fazal.

## Unsure / 4th axis (send-frequency, quarantine, retired detector)  (7)

- **`apps/team-orchestrator/src/orchestrator/agents/customer_send.py:100`** — cur=`90`
  - `SKIP_CAP_90D = "skipped_cap_90d"`
  - → SEND-PATH frequency cap (2-per-90d recontact ceiling, enforced by the interval '90 days' filter at customer_send.py:299 in check_agent_send_caps). Encodes a 90-day window on the send path but is FREQUENCY SUPPRESSION, not a lapsed/dormant recency window — outside the 3 overloaded concepts. Disposition: LEAVE (already clearly named SKIP_CAP_90D); flagged only because it is a 90d value on the sensitive send path and could be mistaken for the dormancy window. Do NOT fold into any dormancy constant.
- **`apps/team-orchestrator/src/orchestrator/knowledge/l2_types.py:30`** — cur=`customer_dormant_threshold_crossed (retired constant, still declared)`
  - `CUSTOMER_DORMANT_THRESHOLD_CROSSED: Final = "customer_dormant_threshold_crossed"`
  - → Per the class-level comment (lines 24-29) this event type is explicitly RETIRED — 'NO fixed-threshold DETECTOR emits these' since VT-312 moved to brain-judged percentile distributions; kept in the schema only to avoid a CHECK-constraint migration. It still carries a template with a {days_dormant} placeholder (line 62) implying an active fixed threshold that no longer exists. Low priority: consider an explicit DEPRECATED/UNUSED tag directly on the Final line (not just the class docstring) so a future grep-based dormancy-window audit doesn't re-flag this as a live threshold.
- **`apps/team-orchestrator/src/orchestrator/knowledge/l2_types.py:62`** — cur=`{days_dormant}d (retired template placeholder, no fixed value)`
  - `L2EventType.CUSTOMER_DORMANT_THRESHOLD_CROSSED: "Customer crossed dormancy threshold (cohort: {cohort}, {days_dormant}d)."`
  - → Same as line 30 — dead template for a retired detector. No functional bug (never emitted per comment) but a stray 'dormancy threshold' label with a day-count placeholder that could mislead a future reader into thinking a fixed threshold is still live. Consider marking clearly deprecated or removing once confirmed zero emit sites reference it.
- **`apps/team-orchestrator/src/orchestrator/prompts/sales_lane_system.md:47`** — cur=`30d/90d suppression`
  - `opt-out, complaint-clear, onboarded, caps, 30d/90d suppression — and the`
  - → Likely a 4th, genuinely distinct axis rather than an overload of the 3 named concepts: this is the SEND-RAIL recontact/frequency cap (confirmed in code: agents/customer_send.py RECONTACT_SUPPRESSION_DAYS=30d cooldown + a 2-per-90d send ceiling, re-enforced in agents/sales_recovery_executor.py), i.e. how often a customer CAN be messaged again -- not how long since their last purchase/dormancy. It reuses '30d/90d' vocabulary dangerously close to the SR dormancy window's own '60-90d' numbers, in the same prompt family (sales_lane_system.md sits right next to sales_recovery_v1.md), which is exactly the kind of overload Fazal is worried about even though today the two axes are implemented correctly-distinct in code. Disposition: not a bug today, but flag for a naming pass -- keep this described in prompt copy as 'recontact cooldown / send-frequency cap' rather than bare '30d/90d', so a reader/model can't conflate it with the SR dormancy targeting window one line away in intent (both live under 'Sales').
- **`apps/team-orchestrator/tests/orchestrator/knowledge/test_l3_construction.py:183`** — cur=`180-day quarantine`
  - `young = _new_tenant(pool, bt, "tier_2", signed_up_at=_NOW - timedelta(days=179)); ... old = ... days=180) ... # 180d eligible`
  - → This is a FOURTH distinct dormancy-adjacent window not covered by any of the three given concepts: a per-TENANT data-quarantine period (a tenant's own campaign data can't feed the shared cross-tenant L3 benchmark corpus until the tenant has been signed up 180+ days) — nothing to do with customer purchase recency, SR send targeting, or trial/subscription lapse. Flagging for Fazal to decide whether this L3-quarantine window is in-scope for the consistency sweep or a genuinely separate axis that should stay out of the lapsed/dormant vocabulary entirely.
- **`migrations/083_l2_episodic_events.sql:27`** — cur=`customer_dormant_threshold_crossed (DB enum value)`
  - `'customer_dormant_threshold_crossed', 'customer_high_value_threshold_crossed',`
  - → DB-side counterpart to the retired l2_types CUSTOMER_DORMANT_THRESHOLD_CROSSED constant. Carries no window value. If VT-312 detector remains parked, mark as retired/unused; otherwise it will need a defined dormancy threshold — flag to Fazal which axis (owner-metric vs SR-cohort) it should key off.
- **`migrations/093_vt320_customer_action_taken_event.sql:27`** — cur=`customer_dormant_threshold_crossed (DB enum value)`
  - `'customer_dormant_threshold_crossed',`
  - → Same retired detector-event enum in a second migration. No window value. Reconcile with 083 and the l2_types declaration when VT-312 is resolved.

## Decoy fixtures (arbitrary sample data — leave)  (6)

- **`apps/team-orchestrator/canaries/convo_harness.py:974`** — cur=`~120-270d (old/high-spend cluster) and ~2-10d (recent/low-spend cluster) synthetic day ranges`
  - `rows = [(120 + i * 25, 80_000 + i * 15_000) for i in range(n_lapsed)]  # ~120-270d, ₹800-2600+ ; rows += [(2 + i * 3, ...) for i in range(n_recent)]  # ~2-10d`
  - → Leave the values — own docstring (lines 966-971) is explicit these are NOT a fixed day count, just a distribution shaped to clear detect_lapsed_customers' recency-p75/spend-p50 percentile floors (concept 2). Naming overlap only: _lapsed_seed_rows / n_lapsed (def at line 962, vars at 972-973) reuse concept-1 'lapsed' vocabulary for a concept-2 percentile mechanism — optional VT-632 hygiene rename to n_dormant_highvalue/_sr_seed_rows, values themselves need no change.
- **`apps/team-orchestrator/canaries/drill_seed_lapsed_customer.py:75`** — cur=`120 / 60 / 0-day offsets from last_sale, building purchase history + spend total`
  - `(last_sale - timedelta(days=120), 45_000_00), (last_sale - timedelta(days=60), 30_000_00), (last_sale, 25_000_00),`
  - → Leave — synthetic ledger-history sample data to give the single seeded customer real lifetime spend; not a threshold definition.
- **`apps/team-orchestrator/tests/agent/test_finance_lane.py:303`** — cur=`days_since_last_sale=45`
  - `_FakeRow(customer_id=c1, outstanding_paise=150000, days_since_last_sale=45),`
  - → Leave. Arbitrary sample row for analyze_receivables (finance lane, outstanding-balance aging) — not a dormancy/lapsed threshold check at all (no filtering or assertion keyed to 45 as a cutoff, just an outstanding-paise sum/count-of-2 assertion). Coincidental numeric overlap with LAPSED_WINDOW_DAYS=45; not a real inconsistency.
- **`apps/team-orchestrator/tests/agent/test_finance_lane.py:304`** — cur=`days_since_last_sale=60`
  - `_FakeRow(customer_id=c2, outstanding_paise=80000, days_since_last_sale=60),`
  - → Leave. Same fixture row as line 303 — arbitrary sample data, coincidental overlap with the '60' used elsewhere for SR dormancy; no threshold logic reads this value in this test.
- **`apps/team-orchestrator/tests/orchestrator/agents/test_approval_glue.py:142`** — cur=`45 (template-param test fixture)`
  - `Jsonb({"customer_name": _CUSTOMER_NAME, "days_since_last_visit": "45"})`
  - → Arbitrary fixture value in a proposal_snapshot; not a threshold definition. Leave.
- **`apps/team-orchestrator/tests/orchestrator/agents/test_sales_recovery_executor.py:434`** — cur=`days_since_last_sale=41`
  - `days_since_last_sale=41,`
  - → Leave. Arbitrary CustomerFactBundle value for validate_draft_params grounding-literal test (also line 455) — not exercising any recency threshold, just proving the drafted params must match the bundle's literals exactly.
