---
id: VT-678
title: Co-present ask routing — "run win-back AND send the list" should deliver AND dispatch in one turn
status: open
priority: Low
owner: unassigned
created: 2026-07-18
found_by: VT-676 dev mock-drive (first drive, 2026-07-18)
authorization_basis: "Cowork 20260717T211619Z relaying Fazal: HOLD as a flagged routing-priority row — roster, don't build. Touches the VT-652/653 net-priority class; needs a deliberate call."
---

# VT-678 — co-present ask serves ONE intent per turn (roster-only, decision-gated)

## Observed (VT-676 drive, deployed dev)
"Run a win-back campaign for my lapsed customers, and send me the customer list file too" → the R7
`lapsed_list` deterministic net answered (post-VT-676: delivers the file + grounded count) — but the
campaign IMPERATIVE was not dispatched that turn. Owner must say "run it" next turn. Honest, never
silent — but one turn serves one intent.

## The decision this needs (Fazal, when he wants it)
Net priority between the deterministic count/list nets (owner_inputs/status_query, pre-brain) and
the D3/VT-657 campaign-imperative routes (triage_seam, enforce). Making co-present asks do BOTH in
one turn means either (a) the list net detects a co-present imperative and falls through to D3
(re-opens the VT-652 swallow class in reverse), or (b) D3 runs first and the list rides along (the
VT-676 co-present wiring already handles that arm). Ordering change = variance risk on the drilled
count/status behaviors — needs ×3 re-drive and a deliberate call, not a night build.
