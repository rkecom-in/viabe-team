[project]
name = "team-shared"
version = "0.1.0"
description = "Shared types for Viabe Team Python apps"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["python/team_shared"]
