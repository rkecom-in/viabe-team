The <b>signup → verified-business</b> foundation is <b>LIVE on dev</b>. Sandbox by Quicko GSTIN verification is now the <b>hard gate at signup</b> (<b>VT-408</b>, verify-then-create: no active GSTIN ⇒ no tenant, no trial — Fazal ruling 2026-06-24, CL-442), fed by entity-match candidate discovery + the bilingual signup wizard (<b>VT-406</b> A+B, found-vs-verified provenance), a widened GST parse with the <code>discover_gst</code> source seeding discovery off the <i>verified</i> entity (<b>VT-407</b>), and the <code>run_signup</code> reconciliation wire — all e2e-canary-proven against the real RKECOM GSTIN, at <code>viabe-team-dev.vercel.app/team/signup</code>. Root-caused + fixed the multi-hour Sandbox <b>500</b> (<b>VT-409</b>): <code>/authenticate</code> returns the token twice and we were reading the nested dud — fix = the top-level token. VTR Ops Console shipped: discovery panel + <b>per-field confirm</b> with VTR-asserted / owner-confirmed provenance (<b>VT-405</b> A+B). Overnight autonomous batch merged with zero stalls: <code>book_stationery</code> taxonomy (VT-388 addendum), VT-407 cosmetic fixes, CI-flake quarantine, and the <b>VT-387</b> idempotency retry-window fix (transiently-failed sends now retry, double-send-safe). Positioning locked: <b>Viabe Team = core platform</b>, Reports = awareness feature (CL-440). <b>Parked for Fazal:</b> taxonomy granularity (k-anon cohort call), <b>VT-386</b> PII-redaction-registry (plan-first), the Sales-Recovery real-send harness (<b>VT-396</b>), VT-156 privacy notice + VT-353 legal pages (counsel), <b>VT-231</b> Mumbai prod DB, Razorpay live + the signup go-live flip. <b>Reports-Jun15 milestone has passed</b> — the binding gate is now the live-cutover umbrella (<b>VT-10</b>) + VT-231 prod cutover.
