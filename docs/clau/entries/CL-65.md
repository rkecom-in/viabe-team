---
cl_id: CL-65
entry_type: Decision
topic: Process
status: Superseded
session_date: 2026-05-17
created: 2026-05-17T15:41:42.637Z
source: Claude Code 2026-05-17 PR #7 delivery; Fazal merged via gh CLI
linked_tasks: VT-3.8 done; unblocks VT-3.3 (Twilio webhook — now has gate to invoke); VT-3.2 (SubscriberState replaces Tenant stub); VT-8 (privacy admission gate — owns keyword curation + opt-out flag reads)
notion_legacy_id: 363387c2-cc5a-81e9-8b84-d971b69a67b3
title: "VT-3.8 shipped: PR #7 merged. Pre-Filter Gate (Stage 1) + 5 direct handlers live on main; 14/14 orchestrator tests green."
---

# CL-65 — VT-3.8 shipped: PR #7 merged

PR #7 merged 2026-05-17. main now contains Pre-Filter Gate (Stage 1) + 5 direct handlers per VT-3.8 Notion body.

Delivered (deterministic only — zero LLM in this subtree, enforced by grep CI gate):
- apps/team-orchestrator/src/orchestrator/types.py: WebhookEvent + Tenant stubs (marked for VT-3.3 / VT-3.2 expansion), PreFilterResult union (RouteToDirectHandler | RouteToBrain | Reject)
- apps/team-orchestrator/src/orchestrator/pre_filter_gate.py: @DBOS.step pre_filter() with deterministic routing rules: opt-out (exact match EN+HI), DSR (word-boundary EN+HI), status-callback failed/delivered/read, status ping, default → brain. Conservative matching throughout.
- apps/team-orchestrator/config/opt_out_keywords.yaml + dsr_keywords.yaml: initial drafts, marked for VT-8 admission gate ownership
- apps/team-orchestrator/src/orchestrator/direct_handlers/: registry + 5 @DBOS.step handlers (opt_out, dsr, dupe, status_ping, template_error). All Twilio sends are stubs marked TODO VT-3.3.
- migrations/010_dsr_tickets.sql: DSR ticket table with RLS inline (Pillar 3)
- migrations/011_tenant_opt_out.sql: adds opt_out column to tenants table
- CI: new orchestrator job runs all tests/orchestrator/ tests; grep lint rejects LLM imports in pre_filter subtree (Pillar 1 enforcement)
- src/dbos_config.py: reset_substrate() function added to fix VT-3.1 launch/shutdown cycling across test modules

14/14 orchestrator tests pass in CI against pgvector Postgres:
- 9 VT-3.8 tests: opt-out EN/HI → flag set, DSR → ticket created, status callback states, status ping, substantive routes to brain, ambiguous routes to brain, capture-ratio 60-80% on synthetic event mix
- 5 VT-3.1 tests still passing: end-to-end, idempotency, cross-tenant RLS, DBOS auto-resume, langgraph_supervisor import

Flags surfaced during execution:

1. tenants.opt_out column added unilaterally by Claude Code (migration 011). VT-Foundation's 001_tenants.sql did not include this column — likely because the AGE-era spec assumed opt-out lived in L1 graph. Adding it to the tenants table is correct post-AGE-revision. VT-Privacy (VT-8) admission gate will read this column.

2. pre_filter does not currently route dupes (no signal yet). VT-3.3 webhook adapter will pass dupe-status to pre_filter via WebhookEvent.dupe_status when it ships. dupe_handler exists in this PR but pre_filter has no path that routes to it. Acceptable deferral.

3. One CI iteration: dsr_handler and status_ping_handler accessed Postgres rows positionally; the shared pool uses dict_row connection factory → KeyError on tuple-style access. Fixed to column-name access, re-pushed, green. Discipline note: when using psycopg's dict_row, row access is always by column name, never positional.

4. status_ping_handler reports tenant phase honestly (no campaign/scheduled-action columns exist yet — those land in later subtasks). Pillar 7 honored.

5. template_error_handler logs structured info but cannot include run_id (no workflow context when handler invoked outside a workflow). VT-3.3 will provide workflow context when wiring the webhook.

6. Twilio status callback states 'undelivered' route to brain (added by Claude Code; brief only listed 'delivered'/'read'/'failed'). Reasonable judgment — 'undelivered' could indicate a transient issue worth brain inspection. Worth verifying in VT-3.3 webhook adapter that this state is actually surfaceable from Twilio.

## Status history
- 2026-05-17T15:41:42.637Z: created (status=Superseded)
- 2026-05-25 03:55 IST: migrated from Notion (notion_legacy_id: 363387c2-cc5a-81e9-8b84-d971b69a67b3)
