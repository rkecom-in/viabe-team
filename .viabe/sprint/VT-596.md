---
id: VT-596
title: "Specialists still run on claude-sonnet-4-6 (not upgraded to sonnet-5) + confirm dev MARKETING_CONSENT_VERSIONS admits the lapsed cohort"
status: Todo
priority: medium
type: BUILD
area: manager
created: 2026-07-04
authorized_by: fazal
authorization_basis: "Fazal 2026-07-04: upgrade sonnet-4-6 → sonnet-5; VT-592 upgraded only the manager brain + turn_brain, not the specialists."
---

# VT-596 — specialist model upgrade + consent-gate confirmation

## Findings
1. The sales_recovery specialist runs on claude-sonnet-4-6 (dev log after spawn_sales_recovery). VT-592 upgraded only the manager conversational tier (dispatch _BRAIN_MODEL_SONNET + turn_brain _TURN_MODEL). Specialists resolve their own model (see test_sales_recovery.py:192 pinning claude-sonnet-4-6, test_seams.py, _resolve_vision_model). Decide which specialist tiers to move to sonnet-5 (Fazal's general upgrade directive) vs leave.
2. detect_lapsed_customers is fail-closed on dev's MARKETING_CONSENT_VERSIONS env var (VT-396/CL-438) — if it doesn't contain the seed's consent_version, the lapsed cohort is ZERO regardless of seeded data. The seed defaults consent_version='dev-test-v0'. CONFIRM (by USE — seed + check the SR cohort is non-empty on dev, per Rule 18 no raw value reads) that dev admits 'dev-test-v0'; if not, align the seed or the env.

## Part 1 RESOLVED (landed bb02997, deployed 200bd4a); Part 2 root-caused 2026-07-04
- Part 1: models.yaml SR test slot → claude-sonnet-5 (dev manager+specialist parity);
  vision/cash-book/business-plan production pins → sonnet-5 (dev runs their Haiku slots — they ride
  to prod at the next authorized promotion); pricing registry block added; 2 test pins updated.
- Part 2 finding (live probe on 200bd4a, seeded tenant 6-lapsed/8): cohort = 0, but the cause is NOT
  the consent allowlist — it is a SALT MISMATCH in the seed: TEAM_PHONE_HASH_SALT is sealed on
  Railway (not injected to `railway run`), so the harness seeds record_of_consent.phone_token with a
  throwaway salt, while `_LAPSED_CANDIDATES_SQL`'s consent EXISTS hashes server-side with the REAL
  salt → never matches. The kickoff's "salt only affects the lookup column" assumption was wrong for
  the SR detection path. Count fast-path reads the same seeded ledger fine ("8 customers"), which
  isolates the failure to the consent join.
- Fix (in VT-598's harness build): dev-only `POST /api/orchestrator/dev-test/consent-seed` on the
  VT-582 dev-ingress guard (DEV_TEST_INGRESS_SECRET + EXPECTED_ENV=dev fail-closed) → consent
  recorded server-side with the service's own salt; the seed calls it when an ingress URL is given.
  Allowlist admission of 'dev-test-v0' then confirmed BY USE (cohort non-empty) in the VT-598 pack.
