---
id: VT-732
title: "MODEL GOVERNANCE — route every direct Anthropic call site through the tier seam; env governs ALL model usage"
status: Rostered
priority: Critical
owner: CC
exec_order: 134.8
created: 2026-08-05
created_by: clau
authorization_basis: "Fazal 2026-08-05, verbatim: 'fix now and accept a days delay, we are still away from production test.' Overrides Clau's promote-first recommendation — the promotion now WAITS for this row + its full-pack ×3 re-gate."
---

# VT-732 — no model chosen outside the env, anywhere

## Why (the Sonnet-burn finding, 2026-08-05)
All five `TEAM_MODEL_*` vars correctly hold `gpt-5.6-luna` on dev — and the bill still said
Sonnet, because several call sites construct Anthropic clients DIRECTLY and never consult the
tier seam. Fazal's intent — env governs all model usage — was configured but not enforced.

## Scope — FULL inventory (Clau independent sweep 2026-08-05T14:30Z — 16 literals / 14 files,
## ~2.5× CC's first list; proof the sweep-not-list exit gate was needed)

**Onboarding cluster (the burn):** `turn_brain.py` (`_TURN_MODEL` sonnet-5 ×3 call sites +
`_INTENT_MODEL` haiku) · `question_brain.py` (`_GAP_MODEL` haiku) · `entity_resolution.py`
(`_ADJUDICATOR_MODEL` opus) · `entity_match.py` (`_LLM_DISCOVERY_MODEL` opus) ·
`auto_discovery_sources.py` (`_EXTRACT_MODEL` haiku) · `memory_distiller.py` (`_DISTILL_MODEL`
haiku) · `shopify_onboarding.py` (`_AUTH_INTENT_MODEL` haiku) · `business_type_reconcile.py`
(`_RECONCILE_MODEL` opus).
**Manager cluster:** `manager/review.py` (`_REVIEW_MODEL` sonnet-5) · `manager/verification.py`
(`_VERIFICATION_MODEL` opus).
**Integrations:** `field_mapping.py` (opus inline).
**Agent:** `orchestrator_agent_driver.py` (opus literal — verify whether docstring or live) ·
VT-720's `compose_classified_reply` `_invoke_llm` path.
**Direct-client files needing triage** (some construct `Anthropic(` without a literal here):
`agents/sales_recovery_executor.py` · `agent/sales_recovery.py` · `agent/tools/self_evaluate.py` ·
`integrations/vision_extraction.py` · `integrations/methods/*` (apify_food, cash_book,
owner_typed) · `llm/structured.py` (may already be seam-side — verify).

**NOT violations — do not "fix":** `dispatch.py` `_BRAIN_MODEL_*` (tier intent, resolves via env
— the innocent path) · `advice_eval.py` judge default (Mac-side, deliberately Claude) ·
`twilio_send.py` / `dev_send_guard.py` / `send_whatsapp_*` `messages.create` = **TWILIO client,
not Anthropic** — a careless sweep here breaks WhatsApp delivery, not model governance.

**Then re-sweep after the fix:** zero `"claude-*"` literals outside
`provider.py`/`pricing.py`/`cost.py` + documented exceptions; the sweep output is part of the PR.

Route each through `resolve_chat_model(<tier>)`. Where a site is genuinely Anthropic-only
(server-side web_search), keep it via `require_anthropic_model` and DOCUMENT why in-line — an
exception must be legible, not incidental.

## Exit gate
- Sweep proves zero remaining direct-model literals outside the seam (+ the documented
  Anthropic-only exceptions).
- Boot log (VT-731) shows every tier resolving to the env value on deployed dev.
- **Functional proof:** one onboarding turn + one manager-review + one classified-reply on
  deployed dev, each verified from its trace to have run on the env-configured model.
- **Full-pack ×3 CLEAN** (brain-touching — the swap changes the reasoning model on live paths;
  Tier-1 = 0, no new failure class vs the 79/79 baseline pack).
- Dev key usage graph: post-fix, `viabe-team-dev` Claude burn ≈ 0 outside documented exceptions —
  the graph becomes the standing tripwire.

## Sequencing (Fazal's override, on the record)
Fazal chose fix-now over promote-first: "we are still away from production test." So:
**bulk-send silent-turn defect → VT-732 → full-pack ×3 (re-gates BOTH) → baseline → flip canary →
promotion.** One ×3 covers the bulk-send fix and the model swap together — batch them into the
same gate rather than paying for two packs.

## Boundaries
Judges stay Claude (tier_rescore sonnet-5 per Fazal 2026-07-13; o11 opus) — deliberately
cross-family from the product, out of scope. No prod anything. The 96s wait constants are
VT-730's, not this row's.
