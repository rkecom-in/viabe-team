---
id: VT-593
title: "Compose becomes the canonical author of handle-directly replies (always finalize) + FAQ never escalates"
status: Done
priority: launch-blocker
type: BUILD
area: manager
created: 2026-07-04
authorized_by: fazal
authorization_basis: "Autonomous capability program; VT-592 (sonnet-5) proved the reply truncation is STRUCTURAL not model-quality (persists on both sonnet-4-6 and sonnet-5), and sonnet-5 regressed to escalating a pricing FAQ."
---

# VT-593 — brain decides, compose writes

## Findings (deployed-dev 18a10b5, sonnet-5)
- Truncation is STRUCTURAL: explainer replies still stop after an intro on sonnet-5 ("here's the short version." then nothing; "Just let me know:" dangling). Four prompt passes + a model upgrade did not fix it. The ReAct brain reliably DECIDES but unreliably WRITES a complete final message.
- VT-591 heuristic incomplete-detection (colon-only) missed period-ended intros + bare acknowledgments → those sent truncated.
- sonnet-5 regression: "what do you charge?" ESCALATED to a human (escalate_to_fazal) instead of answering — took the escalated terminal path, so always-compose can't rescue it.

## Fix
1. dispatch.py _maybe_send_manager_reply: ALWAYS run _compose_completed_reply (no-tools → must emit a full message) for terminal handle-directly turns — the compose is now the canonical AUTHOR (good draft passes through, truncated finished). _reply_is_incomplete demoted to a log label. Fail-soft: finalized → raw → D1.
2. compose system prompt: pass a good draft through unchanged, actually GIVE the full answer, honesty rails (no invented price/number/action).
3. Manager prompt: an explicit Owner-FAQ list (pricing/how-it-works/data-safety/trust/scam/capabilities/setup-time) is ALWAYS answered directly, NEVER escalated to a human.

Validate on deployed dev: the 6-probe + a breadth sweep, reading every reply for completeness + honesty + no-escalation.

## Resolution + CRITICAL correction (CC, deployed-dev)
The "explainer colon-truncation" that drove VT-591/592/593 was NOT real — it was a VALIDATION-HARNESS DISPLAY BUG: my probe piped the reply through `grep "→ owner"`, which prints only the FIRST line, cutting every MULTI-LINE reply at the first newline. The VT-593-DIAG log proved it: draft/out/body were all `"...here's the short version:\n\nOnce you connect your Shopify..."` — the full reply was complete and multi-line. Six deploy cycles chased a phantom.

TRUE state (validated with a FIXED full-reply probe across 8 diverse scenarios — pricing/how-it-works/data-safety/scam-skeptic/Hinglish/amazon-mismatch/resume): the Team Manager answers at Claude grade — complete, structured, honest (read-only/approval-gated, never fabricates), context-aware ("I already gave you the quick version"), language-matching (full Hinglish answer), re-nudges to the pending step.

KEPT: VT-589 transmit, VT-590 tool-removal+prompt, VT-592 sonnet-5, VT-593 FAQ-never-escalate prompt (pricing now answered not escalated). REVERTED: the VT-591/593 ALWAYS-compose — redundant per-turn sonnet call (finalized-passthrough proved it just re-emitted the already-complete draft). Now: transmit the brain's raw reply as-is; compose ONLY in the rare empty-reply case. Removed _reply_is_incomplete (dead) + its test; removed the VT-593-DIAG logs (a commit-review PII-in-logs finding — dev-only, now gone).
