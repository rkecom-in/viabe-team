---
id: VT-514
title: "Team-Manager AUDIT/trace log — choke-point instrumented, no-orphan invariant, point-in-time knowledge snapshots"
status: In Review
priority: High
type: BUILD
area: observability
created: 2026-07-01
authorized_by: fazal
authorization_basis: "Fazal 2026-07-01: TM audit log (what it knows/decides/does + why) + a separate debug/failure log, real-time streamed on the VTR console; build on dev now (dev-full-parity)."
---
# VT-514 — see .running/processed/ VT-514/515/516 signals + BUILD-NOW (123000Z). Debug-first (VT-515), audit+viewer behind (VT-514/516). Completeness via choke-point + no-orphan invariant; PII by-reference via pii_redactor.

## LANDED on dev @ 5cd6edc (2026-06-30)
- mig 147 `tm_audit_log` audit spine (references pipeline_steps reasoning + debug_events failures, no dup) + `emit_tm_audit` (fail-closed conn-passed / fail-soft conn=None, PII-by-ref).
- Choke-points wired: ACTION fail-closed (draft/approval×2/escalation/autonomy×4); fail-soft memory_write (signup-safe override) + business_action×2 + send×4; KNOWS/GETS/DECIDES + snapshot_id plumbing + spawn/tool-events.
- **NO-ORPHAN invariant PROVEN on deployed dev DB** (`test_tm_audit_nonbypassability.py`, 11 passed): per-action one-row, fail-closed rollback (audit fail → action rolls back), orphan sweep, structural wiring.
- Deployed-dev verified: table + `tm_audit_operator_select` + `tm_audit_app_insert` + realtime publication all present.
- **Pending:** Cowork audit-after (PII boundary + invariant) + Fazal browser eye-check of the live feed.
