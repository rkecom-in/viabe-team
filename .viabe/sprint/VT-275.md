---
vt_id: VT-275
title: Wire match_transactions to the canonical ledger (entry_date window + amount scoring)
status: Done
priority: Medium
sprint: Sprint 3 - IntegrationInvention
type: Feature
area: [Orchestrator, Agent-Tools]
assignee: Claude Code
parent: null
blocked_by: null
sub_items: []
exec_order: 99
---

# VT-275 — match_transactions canonical-ledger wiring

NEW row (Cowork 2026-06-01): the match_transactions TOOL shipped in #138 (VT-46,
Done). VT-273 then landed customer_ledger_entries with the canonical schema, which
DIFFERS from the tool's speculative columns (it reads entry_ts/ref_vpa; canonical
has entry_date/no-ref_vpa). VT-273 broadened its graceful-degrade to UndefinedColumn
to stay safe. This row does the REAL wiring.

## Ruling (Cowork, fork option a — STANDS)
- Reconcile match_transactions to the CANONICAL schema: window on `entry_date`
  ±1 day, score on `amount_paise`. Do NOT add entry_ts/ref_vpa to the shared
  customer_ledger_entries (over-fits the general ledger; paper book has no
  timestamp/VPA).
- Matches are SCORED / LOWER-CONFIDENCE — amount+date alone false-matches (same
  amount, near date, different customer). Never assert confident attribution
  without VPA; route low-confidence honestly (field_mapping._route semantics).
  State the scoring in the PR.
- DEFER VPA/timestamp precision for UPI attribution to VT-57 (UPI), solved via a
  UPI-SCOPED path, NOT by polluting the shared ledger. (Noted on VT-57.)

In-Review at pr-open; Cowork task-merges. Real-DB test (scored match + honest
low-confidence), no mock cursors.
