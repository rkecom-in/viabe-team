---
id: VT-510
title: "Verified inconsistency — found candidate (active GSTIN) rejected after OTP via confirm-seam name-match false-reject; move verify before OTP"
status: In Review
priority: Critical
type: BUG
area: team-orchestrator
created: 2026-07-01
authorized_by: fazal
authorization_basis: "Fazal 2026-06-30 live run: selected RKECOM OPC (GSTIN 27AAKCR3738B1ZE, active) + entered OTP, THEN 'couldn't find an active GST registration'. Inconsistency of what's 'verified'."
---
# VT-510 — verify inconsistency (LAUNCH-BLOCKER, GST gate, plan-first → Cowork gate; raw-evidence bar)
confirm_and_verify (entity_match.py:481) collapses BOTH Sandbox-not-found AND name-mismatch into the same generic invalid_gstin reject (~525,544). Fazal's active GSTIN got rejected → likely the confirm-seam NAME-MATCH false-reject (candidate 'RKECOM SERVICES (OPC) PRIVATE LIMITED' vs Sandbox legal name — OPC/parens/spacing diff). Fixes: (1) DIAGNOSE Fazal's exact attempt (raw — GSTIN sent, Sandbox search_gstin response legal_name+status, which branch fired). (2) normalize the confirm-seam name-match (REUSE knowyourgst_match normalization — strip OPC/legal-stopwords/punct, casefold) so the same company with formatting diffs PASSES; keep genuine-mismatch protection. (3) move the Sandbox verify to SELECTION (before OTP) — a 'Found' card is a hint, 'verified' is the Sandbox check; never found→OTP→rejected-after. (4) fix the misleading copy (active GSTIN + name-format-diff ≠ 'no active GST'). + VT-511 addendum: SHOW the GSTIN on the card (name+state+GSTIN) + only GSTIN-bearing candidates as primary picks.
