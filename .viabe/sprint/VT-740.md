---
id: VT-740
title: "LIVE ON MAIN: the hourly wake re-drives partially-sent workflows. Containment needs a real task→campaign link first."
status: Rostered — hazard VERIFIED and documented 2026-08-10. My first fix was inert and is reverted; the reason is in this row.
priority: Critical
owner: CC
exec_order: 129
created: 2026-08-10
created_by: cc
authorization_basis: "Clau audit finding 2026-08-10T04:30Z under Fazal standing CL-2026-06-28 ('fix agreed issues immediately') + Fazal 2026-08-10 all-lanes grant. Spec: .viabe/prod-failed-workflow-handling-spec.md (Fazal 2026-07-10)."
---

# VT-740 — an automatic re-drive that never asks what already went out

## The hazard, verified three ways

`orphan_reaper.reap_stalled_manager_tasks`'s VT-560 wake:

```sql
UPDATE manager_tasks SET status = 'planned', next_retry_at = NULL, ...
 WHERE status = 'blocked' AND next_retry_at IS NOT NULL AND next_retry_at <= now()
```

1. **Effect-blind.** The predicate reads status, nullity and time. It never consults the send
   ledger. A workflow that messaged 40 of 100 customers and died is re-driven on exactly the same
   terms as one that sent nothing.
2. **Hourly, steady state.** `ORPHAN_RUN_REAPER_CRON = "0 * * * *"`, registered at
   `scheduled_triggers.py:1500`. Not boot-only.
3. **Already on `main`.** `git show origin/main:…/orphan_reaper.py` carries the same block. This is
   not a promotion risk to be caught in the package — it is live prod code.

**Send idempotency does not cover it.** `idempotency_key = f"agent:{draft_id}"`
(`agents/customer_send.py`) is keyed to the DRAFT, and a re-drive re-runs the specialist, which
mints new drafts and therefore new keys. **No per-customer cooldown exists** — the
`idx_send_idem_tenant_customer_created` index was created for one and nothing queries it.

It has cost us nothing so far only because no real customer send has been authorised. **That is a
policy holding the line, not a mechanism.**

## Why my first fix was reverted — read this before writing the second

I added `AND NOT EXISTS (… campaign_messages m WHERE m.campaign_id = c.id …)`. Adversarial review
killed it, correctly:

**`campaign_messages.campaign_id` is never populated.** The sole writer
(`send_whatsapp_template._write_campaign_message`) omits the column; there is no `UPDATE
campaign_messages` anywhere. So that join matches zero rows for every campaign, real sends
included — **the gate was inert.** This repo already documents the trap, on
`has_approved_campaign_with_no_messages` (`db/wrappers/__init__.py`): *"joining on it would find
zero rows for EVERY campaign, real sends included."* I wrote the broken join anyway, twice.

Worse than inert: `effect_state_rollup` and `prod_workflow_diagnosis` carried the same join, so
every finding would have rendered **"SAFE TO CANCEL — nothing reached a customer"**, including a
campaign that messaged 40 real people. That half is fixed and pushed (`9ae4e416`): the join now
uses the documented `{campaign_id}:{customer_id}` idempotency prefix, and — because that prefix
does **not** cover the agent draft-send path — a zero delivered-count with unattributable sends
present now classifies as **`unknown`**, not `no_effect`.

## SECOND ATTEMPT AT THE WAKE GATE — ALSO REVERTED (2026-08-10). Read before a third.

The `campaign_id` root fix landed (`cd6444f6`), so the effect join is exact now. I rebuilt the wake
gate on top of it with everything the first review asked for: the dual-arm join, a 24h ceiling to
avoid the VT-736 wedge, and a real `wake_withheld` alert through `tenant_alerts` instead of a log
line. Lint clean, tests green, dep-less green. **Adversarial review returned DO_NOT_LAND and it was
right.** Reverted.

### The defect, reproduced against the pinned psycopg

```
no params, single %   -> PARSED OK
with params, single % -> ProgrammingError: only '%s', '%b', '%t' are allowed as placeholders
```

psycopg only parses `%` when `params is not None`. The shared predicate constant contained
`LIKE c.id::text || ':%'`. The wake `UPDATE` passes **no** params, so it parsed and **the
containment applied**. The withheld-task sweep and the approval-path alert both pass params, so
they raised **on every single call**.

**Gate live, safety valve dead.** And it compounds: nothing automatically terminalizes a crashed
campaign (`_advance_campaign_status` runs only after the whole fan-out loop completes; the only
other writer is an operator cancel), so a campaign that died at recipient 40 stays `'approved'`
forever and `_DELIVERED_EFFECT_EXISTS` stays permanently true for that tenant. Every sweep would
withhold every blocked+due task; the ceiling would never fire; no alert would ever be raised. Since
`blocked` ∈ `TASK_ACTIVE`, the tenant holds its one active slot and every later objective is
admitted `queued` and never runs.

That is verbatim the VT-736 wedge — *"the tenant can still chat, but no task of any kind executes
for them again, and the admission gate tells them work is 'already in progress'. Silent, permanent,
and it looks like the product working."* **The condition the gate fires on is the condition that
never clears**, so this was not an edge case: every tenant the gate protected would eventually
wedge. Strictly worse than shipping nothing.

Two further blockers the review found, which a third attempt must handle:
- **The ceiling bypasses the VT-668 approval close.** The dead-letter UPDATE never appends to
  `dead_lettered`, so a task moved terminal that way gets no `decision='timeout'` on its open
  `pending_approvals` row, no `terminal_outcome`, no `owner_notification_status`. A later owner
  approval resolves onto a dead consumer — the exact failure the enclosing function prevents.
- **The approval-path alert ran inside the owner's transaction.** `runner.py` wraps the resolve in
  `conn.transaction()`; a server-side error there aborts it, the fail-soft `except` swallows the
  Python exception, and COMMIT silently becomes ROLLBACK. The docstring's *"must never unwind the
  owner's resolution"* would have inverted into a silent unwind. Needs a SAVEPOINT or its own
  connection.

### The conclusion I draw, having now failed at this twice

**The wake is the wrong layer, and the evidence is that two careful attempts each introduced a
worse failure than the one they prevented.** The customer-facing harm — a real person receiving the
same message twice — is already closed by VT-740's suppression at the send primitive, which is
path-independent and needs no attribution. What the wake gate adds is only the prevention of
*wasted work*, and it is buying that with a permanent-wedge risk on the tenant's only moving part.

**Recommendation for the third attempt: do not gate the wake.** Instead make the crashed campaign
terminalize (so the condition clears), and surface the partially-sent campaign to the VTR. If a
wake gate is still wanted after that, it needs its own real-DB test that executes both queries —
neither reverted attempt had one, and both were caught only by reading.

## HAZARD FOR VT-741 — do not let this be lost

My argument for NOT blocking `approval_resume.redrive_task` rests on the send-choke suppression
covering that path. **That protection is currently `FAIL_CLOSED_INTERVAL_HOURS = 7 days`, and
VT-741 is scheduled to replace it with a tiered rule whose Tier A is 24 hours** — which more than
halves the crash-duplicate window, on a path whose safety currently depends on it. Nothing in
VT-741's scope flags that this constant is load-bearing here.

This also crosses the line `send_frequency.py` draws in its own docstring: *"(b) should this
customer receive the same message twice because a process crashed is NOT a policy question"* — yet
(b) is presently enforced solely by `resolve_interval_hours`, which is explicitly the **(a)**
socket. **The crash-duplicate floor needs to be separated from the cadence tier before the tiers
land**, or the tiers will silently shrink a safety guarantee while appearing to only change
politeness.

## What a correct containment has to solve

1. **A trustworthy task→campaign link.** There is none today: `manager_tasks.source_message_ref`
   is a nullable TEXT pointer holding either a run_id or a message_sid. **The real fix is to
   populate `campaign_messages.campaign_id` at write time** — that makes the effect join exact
   everywhere and retires the prefix-matching workaround. It is a change to the send path, which
   is why it is a rostered row and not a late-night patch.
2. **Two send-key formats.** `campaign/execute.py` D1 uses `{campaign_id}:{customer_id}`;
   `agents/customer_send.py` uses `agent:{draft_id}`. Any join-based approach must cover both or
   declare what it cannot see (which is what `unknown` now does).
3. **A SECOND automatic path out of `blocked`.** `approval_resume.py` calls `redrive_task` when an
   owner replies yes to a `campaign_send` bound to a blocked/dead-lettered task — inbound WhatsApp,
   money path, no effect check. **Gating only the reaper leaves this open**, so a fix that covers
   just the sweep is not a fix.
4. **The withhold must be BOUNDED.** `blocked` is in `TASK_ACTIVE`, so it holds the tenant's active
   slot, and `settle_unretryable_block` CASes on `next_retry_at IS NULL` — which a withheld task
   never has. Withholding without a ceiling re-creates the VT-736 wedge that was closed days ago.
   Either let the ladder reach `dead_letter` (terminal, releases the slot, alerts) or add an
   explicit age ceiling.
5. **Withheld tasks must be ALERTED, not logged.** A withheld task needs a human. The sibling
   detectors in that file (`_alert_orphaned_tasks`, `_alert_dead_letter_tasks`) write
   `tenant_alerts` → Ops Console + Telegram; a `logger.warning` is pull-only and nobody is paged by
   it. Containment nobody can see is a slower silent failure.

## Boundaries

- **Fail-closed:** unreadable effect-state means DO NOT WAKE. Never "wake anyway."
- **No blind re-run, no silent disable** on a partial send (Fazal 2026-07-10, binding).
- The system CONTAINS + DIAGNOSES + REPORTS. **The VTR takes the action.** No silent auto-resolution
  of an effectful failure.
- Any re-run that would re-send needs the same approval + idempotency guarantees as an original.

## Exit gate

(a) `campaign_messages.campaign_id` is populated by every send path, proven by a real send on dev.
(b) A partially-delivered task is demonstrably NOT woken — proven by forcing the state, not by
reading the SQL. (c) The `approval_resume.redrive_task` path is gated the same way. (d) A withheld
task raises a `tenant_alerts` row and cannot hold a slot indefinitely. (e) The diagnosis is wired to
a surface a VTR actually reads.
