---
cl_id: CL-68
entry_type: Decision
topic: Process
status: Standing
session_date: 2026-05-17
created: 2026-05-17T16:28:18.743Z
source: Claude Code 2026-05-17 PR #8 delivery; Fazal merged via gh CLI; main at 47f85d5
linked_tasks: VT-3.2 done; unblocks VT-3.3 (Twilio webhook — SubscriberState now available), VT-3.4 (langgraph_supervisor), VT-3.6 (error framework reads SubscriberState), VT-3.9 (agent reads phase but doesn't mutate)
notion_legacy_id: 363387c2-cc5a-811e-81c1-fbbc091fd984
title: "VT-3.2 shipped: PR #8 merged. SubscriberState TypedDict + 21-transition machine + 4 invariants live on main; 51/51 orchestrator tests green first push."
---

# CL-68 — VT-3.2 shipped: PR #8 merged

PR #8 merged 2026-05-17. main now at 47f85d5. SubscriberState + DBOS-wrapped phase transitions live.

Delivered (deterministic state machine — zero LLM, CI grep enforces):
- src/orchestrator/state.py: SubscriberState TypedDict with 14 fields per VT-3.2 Notion body + new_subscriber_state() builder
- src/orchestrator/transitions.py: TRANSITIONS dict (21 transitions / 10 events); @DBOS.step apply_transition is the SOLE phase mutator. Atomic txn: phase_transitions INSERT + tenants mirror UPDATE + invariant checks. Rollback on violation.
- src/orchestrator/invariants.py: 4 invariants from Notion body + InvariantViolationError
- Refactor: Tenant → SubscriberState across pre_filter_gate + 5 direct_handlers + registry. types.py Tenant stub removed.
- CI: no-LLM grep extended to state/transitions/invariants subtrees. New grep bans agent code from importing the phase machine.

51/51 orchestrator tests pass in CI (no iteration needed; green first push):
- 4 state tests (pure logic on SubscriberState)
- 9 pre_filter tests (post-refactor, all still passing)
- 33 transitions tests (every valid transition fires; every invalid raises InvalidTransitionError; invariant violations roll back via DBOS; phase mirror sync; auto-resume mid-transition)
- 5 skeleton tests (VT-3.1 substrate still solid)

CONFLICT 4 surfaced + resolved with Fazal mid-execution:
Brief specified pipeline_steps INSERT for the transition record. Two real problems: (a) column names didn't match actual schema, (b) pipeline_steps requires a pipeline_runs FK row that not every transition has. Notion body had conflated domain record (belongs in phase_transitions table 002) with observability record (belongs in pipeline_steps via @observability.step from VT-122).
Resolution: domain record → phase_transitions table (matches table created in PR #3 migration 002). Observability write deferred to VT-122 when the decorator ships. Atomicity preserved via @DBOS.step boundary.

Notes Claude Code flagged in PR:
1. No get_opt_out_status helper from brief — Claude Code correctly identified it as dead code (no VT-3.8 caller reads opt_out_status). Brief overreach caught (5th catch of session).
2. graph.py's OrchestratorState left intact — separate from SubscriberState. Correct architecture: OrchestratorState is LangGraph PostgresSaver's checkpoint state (substrate concern); SubscriberState is what apply_transition mutates + persists (domain concern). Different abstractions with different lifecycles. Clau had implicitly assumed they'd merge; Claude Code kept them properly separated.

Discipline win: zero new brief overreaches in VT-3.2 (vs 3 in VT-3.1). Re-reading Out-of-scope line-by-line + surfacing all three conflicts proactively in the brief worked. Pattern is now stable.

## Status history
- 2026-05-17T16:28:18.743Z: created (status=Standing)
- 2026-05-25 03:55 IST: migrated from Notion (notion_legacy_id: 363387c2-cc5a-811e81c1fbbc091fd984)
