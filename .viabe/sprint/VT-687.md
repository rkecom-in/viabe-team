---
id: VT-687
title: Walker answered-ack + brain broad-answer-resolves-clarify — j05 cold-pivot/repeat-clarify Tier-1 class
status: In Progress
priority: Critical
exec_order: 1
created: 2026-07-19
---

## What

The b78f301 promotion-gate re-drive measured j05 breakers 2/3, two sub-classes of one
disease (owner's substantive answer not visibly incorporated):

1. **Walker cold-pivot (ignored_speech_act)** — on a turn-brain-fallback turn the walker
   RECORDS the owner's answer correctly but presents the next template question BARE.
   Skip path had `_DEFER_ACK`; the answered path had nothing. Fix: `_ANSWER_ACK` twin
   ("Got it — noted that down.") prefixed whenever this turn recorded an answer
   (`recorded_answer` flag at both record sites). Deterministic — holds LLM-down, which
   is exactly when the path runs.
2. **Brain repeat-clarify (loop_stall)** — brain re-asks the same sweets/mithai either/or
   after the owner answered at a broader level ("packaged goods in bulk"). Prompt rule
   added: A BROAD ANSWER RESOLVES A NARROW CLARIFY — record the broad value, never re-ask
   the same either/or; a talked-past draft value is a stale guess.

## Verification

- tests/orchestrator/onboarding: 388 passed (5 pre-existing DATABASE_URL realdb errors,
  non-regression class).
- Gate: j05 ×3 re-drive on deployed dev → splice over full-aggregate bundle → tier_rescore
  Tier-1=0 target (promotion gate for PR #526).
