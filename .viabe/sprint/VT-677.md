---
id: VT-677
title: LANGUAGE — per-tenant owner-language preference (design RATIFIED; build)
status: done
priority: High
owner: claudecode
created: 2026-07-18
found_by: Fazal item #2 (NEXT BUILD); design 2026-07-18, ratified Cowork 20260717T211619Z
authorization_basis: "Fazal 2026-07-18 ~02:45 IST ruled all 3 design decisions — build unblocked per the ratified design (.viabe/sprint/language-preference-design.md)."
---

# VT-677 — LANGUAGE build (design at language-preference-design.md)

## Rulings (binding)
- **D1 (Fazal 2026-07-17, Cowork 214500Z):** hinglish-preference owners → **hi-Latn (romanized)**
  register for agent-INITIATED surfaces. Templates: register Latin-script variants with Meta; **EN
  fallback until approved; NEVER Devanagari for a hinglish-preference tenant.** Free-form acks
  in-window: Hinglish directly. Pure 'hi' owners keep Devanagari templates.
- **D2:** live-turn mirroring is NEVER overridden. Preference governs agent-INITIATED + ambiguous
  turns only.
- **D3:** NO onboarding question. Signup-toggle seed + inference + verbal override
  (`set_language_preference` tool).

## Build (per design a-e)
1. Canonical resolver module (promote freeform_acks.resolve_owner_locale) + refactor the 8
   hand-rolled COALESCE call sites onto it. Value space grows to en|hinglish|hi.
2. Triage classify: `language: en|hinglish|hi` enum on TriageResult (zero marginal LLM cost);
   deterministic Devanagari codepoint override → 'hi'; inbound path persists observed value to
   `tenants.language_preference` each owner turn.
3. Signup: toggle seeds `language_preference` (OBSERVED); `preferred_language` stays NULL until an
   explicit choice.
4. `set_language_preference` manager tool (verbal override writes `preferred_language`).
5. Brain: preference line into the business-context identity block + ONE scoped sentence in
   orchestrator_agent_system.md (ambiguous-turn fallback) + same hint in turn_brain.py.
6. Agent-initiated surfaces route registers per D1 (hinglish→hi-Latn freeform / EN template until
   Meta variant approved).
7. Guards: campaign-language conformance test (cohort language NEVER sourced from tenants columns);
   judge criterion for mirroring (measure only).

## BUILD STATE (2026-07-18, running)
- **Phase-1 LANDED** (in 2656d0e push-batch predecessor): owner_locale.py canonical module
  (en|hinglish|hi, template_register D1, is_devanagari, record_observed/set_explicit) + hi-Latn ack
  variants + 2 D1 bug fixes (_floor_language startswith→exact; monthly_report raw→template_register).
- **Phase-2 LANDED** (2656d0e, deployed dev): TriageResult.language enum + manager_triage.md prompt
  block + triage_seam._persist_observed_language (Devanagari override, observed column, fail-soft).
- **Phase-3a COMMITTED local (86bfc13)**: signup seeds language_preference (OBSERVED; explicit stays
  NULL) + set_language_preference manager tool (cataloged + VT-268 allowlist reviewed).
- **REMAINING — phase-3b (the ×3-gated prompt-touch, own commit):** (1) preference line into
  knowledge/business_context.py identity block (add the 2 columns to _read_identity's SELECT);
  (2) ONE sentence in prompts/orchestrator_agent_system.md — ambiguous-turn fallback to the stored
  preference; (3) same hint in onboarding/turn_brain.py; (4) campaign-language conformance guard
  test (cohort language never sourced from tenants columns). THEN push + ×3 full-journey re-drive
  on deployed dev gates exactly that diff. (5) Meta hi-Latn template variants = Fazal/Meta-approval
  dependent, EN fallback live meanwhile (D1).

## ×3 GATE RESULT (2026-07-18, serial run on d9a4e10): CLEAN — CLOSED
33 runs (11 journeys ×3): ZERO behavioral FAILs/XPASS anomalies; j_hinglish_manager_lang 3/3 PASS.
Residual = the pre-existing latency tail (one TIMEOUT step each in j01/j02/j06/j08, j07 2of3 —
replies land late, content passes on the settled transcript) → the VT-671 work-list, now
step-mapped. First (parallel) run also surfaced+fixed two harness-infra defects: Supabase
session-pooler exhaustion under wave-of-4 runners → drive is SERIAL now; checkpoint-RLS CREATE
POLICY race → idempotent (d9a4e10). j08-r1 teardown FK crash = cleanup blip, swept.

## Risk rail (binding)
Prompt/context-block diffs touch the money-path brain (variance-dominated): keep the prompt diff to
ONE sentence; ×3 full-journey re-drive on deployed dev before trusting. No CHECK-constraint
migration without data normalization (likely skip entirely).
