---
id: VT-477
title: "Onboarding confirm-yes stall — Mumbai confirm did not advance/record (redelivery idempotency)"
status: Done
priority: Low
type: BUILD
area: team-orchestrator
created: 2026-06-29
authorized_by: fazal
authorization_basis: "Fazal 2026-06-29 (Cowork 130500Z): send-guard + reset approved; Sonnet-for-routine chosen; onboarding defects launch-blocker batch."
---
# VT-477 — Onboarding confirm-yes stall — Mumbai confirm did not advance/record (redelivery idempotency)

## RESOLUTION (plan-first investigation, data-proven) — NON-BUG (happy path)
NOT a real bug — the 'stall' was a misread of a normal cursor 0→1 advance. episodic_events.body_length proves 6 distinct-SID inbounds, 0 redeliveries: 5x greeting (152b24d re-present, no advance) + 1x 'yes' (correct category advance 0→1); city never answered. journey.py:155 idempotency never fired. DON'T change the advance/idempotency branch. Action = a regression test only (folded into VT-478). The real defect = the stale queue (VT-478). See vt477-confirm-stall-plan.md.

## EDGE-HARDEN re-analysis (CC 2026-06-28; Cowork ACCEPTED audit-after, 142500Z)
Cowork's 140000Z asked to harden the "different-sid double-advance" edge. On closer analysis it is NOT a happy-path bug: `journey._send` sends question N+1 SYNCHRONOUSLY right after `handle_reply` advances (journey.py:583-584), so the owner ALWAYS sees N+1 before they can send a 2nd reply — a 2nd "yes" is the owner confirming a question they WERE shown. The ONLY genuine gap is the narrow `_send`-FAILURE-then-"yes" case (a swallowed send at journey.py:517, "state advances regardless" — a deliberate fail-open), where the owner never saw N+1 yet a following "yes" auto-confirms its draft_value. A presentation-tracking guard there is risk-adjacent to a working 207-test path for marginal benefit. **DECISION: do NOT build the speculative standalone guard.** The send-failure edge is resolved ATOMICALLY by VT-479 buttons — a button payload IS presentation+answer in one step, so an advance-without-presentation gap cannot exist. Cowork accepted this and noted its earlier "harden now" was the wrong call.
