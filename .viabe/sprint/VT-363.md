---
vt_id: VT-363
title: dev-branch staging + merge governance cutover (dev↔main, CL-432)
status: Done
# VT-363 DONE-on-dev 2026-06-09 (retro-documented — Cowork hand-assigned the id without the allocator,
# CL-424 slip reconciled in VT-364). Established the two-long-lived-branch model:
#   dev  → Railway Dev + Vercel viabe-team-web-dev + Supabase Seoul (deployed, phase-E2E env)
#   main → Railway Prod + Vercel viabe-team-web + Supabase Mumbai (deliberate; promotion-only)
# CC self-merges [BUILD] on green into dev (CL-429 retargeted); main is Fazal-authorized promotion
# ONLY (CL-432 — the new Pillar-7 gate). Wiring shipped on dev:
#   #438 governance docs + CL-432 ; #439 deploy-dev→dev trigger + manual deploy-prod.yml ;
#   #440 ci.yml/pr-title run on dev PRs ; #441 ci-success aggregator (skip-wedge-safe required check) ;
#   + VT-362 (#437) env-guard merged to dev (F1 dev-boot verified) ; dev branch protection applied
#   (require PR + ci-success, enforce_admins=false so CC --admin self-merge works).
# Fazal-gated remainder (console): main account-level ruleset, Railway Prod EXPECTED_ENV + env→branch
# triggers, Vercel prod project values. Mumbai prod schema verified 121/121 + RLS; founding tenant
# FAZAL_TENANT_ID=d6f2510f… + FAZAL_OWNER_UUID=516efeb1… provisioned/verified.
priority: Critical
sprint: "Sprint-Fazal"
type: Infrastructure
area: [Infrastructure, CI/CD]
assignee: Claude Code
parent: VT-231
related: [VT-231, VT-362, CL-431, CL-432]
created: 2026-06-09
last_updated: 2026-06-09
---

## Concept
The launch cutover needs a deployed-and-tested staging path separate from real-customer prod. Two
long-lived branches (`dev`, `main`) map to the dev/prod environments; `main` is reached ONLY via a
Fazal-authorized promotion (CL-432), so customer-facing prod can't be touched by an autonomous merge.

## Done (on dev)
Branch governance + CI + branch protection + the VT-362 env-guard interlock — see the header for the
PR list. CC's default PR base is now `dev`.

## Out of scope (Fazal-gated, console)
main ruleset, Railway prod env-var/trigger config, Vercel prod project values.

## Status history
- 2026-06-09: shipped on dev (#437/#438/#439/#440/#441); retro-rostered + allocator drift reconciled (VT-364).
