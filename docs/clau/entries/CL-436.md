# CL-436 — VTR Run-Control Panel A–D batch CLOSED on dev (2026-06-12, Cowork)

**Status:** Standing (batch record + the surfaces it locks).
**Grant:** CL-435 (Fazal 2026-06-11, session-blanket autonomous, interactive CC).

## The batch, as merged (all → dev, every phase adversarially gated)

| Phase | VT | PR | dev merge | What |
|---|---|---|---|---|
| A — substrate | VT-374 | #458 | `b8bd111` | Controllable/observed step registry; consume-first recovery-idempotent overrides; boundary checkpointed pause (opt-out/DSR pause-EXEMPT, I6); app-level `rerun_from` (fresh identity, lineage, side-effect policy); `vtr_step_timeline` keys-only; gate manifest + import-raise + CI grep; harness CLI; VT-300 `run_controls` RETIRED. mig-131. |
| B — read-only canvas | VT-375 | #459 | `9213024` | Programs projection (past/running/upcoming-7d) + tenant→program→step canvas + binding honesty copy. C1 race test exposed a REAL rerun/approval TOCTOU → detect-and-escalate ruling (B-lock analysis recorded). |
| C — interactive controls | VT-376 (+VT-380 rider) | #460 | `0192d60` | Pause/release/override/rerun wired through the Phase-A POSTs; narrow rerun-slot advisory lock; mig-132 C2 key projection; mig-133 `ops_top_tenants_today` (latent defect — the fn existed in NO migration); ops hygiene rider killed the A13 e2e flake. |
| D — multi-VTR capability | VT-377 (+VT-381 rider) | #461 | `98560e6` | mig-134: NINE vtr_* views assignment-scoped on `operator_assignments` (recon killed the phantom `vtr_assignments` fork); NULLIF GUC helper, admin-via-role; Devanagari lexicon bridge (pre-promotion item CLOSED WITH BOUNDARY); two-operator isolation battery; F1 stale-rerun TTL, F2 revokes, F3 fn tests. Capability-complete — NO human onboarding (Fazal ruling). |

Also in-batch: VT-372 (#456 /team styling — page was BORN unstyled), VT-373 (signup
visible on dev behind Deployment Protection), VT-378 (#457 signup styling), VT-371
(welcome trial-date fix, #455, pre-batch).

## Standing surfaces locked by this batch

Single override allow-list discipline (registry `allowed_keys`, gate modules structurally
non-controllable); pause never delays opt-out/DSR; rerun = re-dispatch (never time-travel,
never inherits approvals, 409 on open approval + in-flight rerun, stale-TTL hygiene);
VTR visibility = keys-only + the frozen 4-kind→explicit-projection list; assignment
scoping fail-closed (unset/empty GUC, revoked rows, deploy windows); admin tier =
`app_vtr_admin_role` only.

## Open after the batch

- **2nd-VTR preconditions** (active-context row, 2026-06-12): `vtr_admin_connection`
  audit-or-split; Supabase privilege probes; sentence-leading validator gap (still OPEN
  on pre-promotion).
- **VT-379** (unredacted `pipeline_steps.error`) — Queued, NEEDS-FAZAL.
- Externals unchanged: Meta F1, counsel C1–C3, VT-231 Mumbai (the dev→main gate).

## Process notes for the record

Two-round plan gating killed a wrong architecture before build (v1 post-hoc seams + DBOS
fork). Build gates caught: missing-worker artifact-integrity (A), a real rerun/approval
TOCTOU (B), the mig-119 view-body trap + pooled-GUC autocommit trap (D, CC recon).
One CL-418-class incident: uncommitted Cowork substrate wiped by a tree operation during
branch churn — restored from context; rule extension recorded in the snapshot DO-NOTs.
