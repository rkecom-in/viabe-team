---
id: VT-582
title: "Server-side conversation harness — CC drives full WhatsApp conversations against deployed dev, no real messages"
status: In Progress
priority: launch-blocker
type: BUILD
area: testing
created: 2026-07-03
authorized_by: fazal
authorization_basis: "CL-2026-07-03-conversing-surfaces-and-harness (Fazal, Standing)."
---

# VT-582 — the conversation test bypass

## Design (pieces mostly exist)
Inbound: POST the DEPLOYED dev ingress with a dev-only secondary secret (DEV_TEST_INGRESS_SECRET, accepted ONLY when EXPECTED_ENV != prod — fail-closed on prod; CC sets the dev var autonomously per CL-431). Synthetic tenant with a NON-allowlisted bogus number ⇒ dev_send_guard MOCKS every outbound (no WhatsApp ever). Outbound capture: conversation_log (VT-579) already records owner-facing reply text. Harness CLI (canary-style): scenario scripts = ordered inbound messages + assertions over the replies/state; transcript dump; permutation runner. Team-Manager capabilities become fully CC-testable end-to-end on the real deployed brain (valid LLM keys live there).
