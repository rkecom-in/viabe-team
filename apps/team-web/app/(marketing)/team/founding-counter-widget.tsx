'use client'

/**
 * Founding-counter widget.
 *
 * Phase 1 scaffold: placeholder client component. The live seat count and
 * its data source land in a later ticket.
 */
export function FoundingCounterWidget() {
  return <div data-widget="founding-counter">Founding counter — scaffold</div>
}
