---
vt_id: VT-381
title: Run-control hygiene — stuck-'running' rerun rows (F1) + mig-133 anon/authenticated REVOKE (F2) + ops_top_tenants_today real-DB test (F3)
status: Queued
priority: High
sprint: "Sprint-Fazal"
type: Bug
area: [Orchestrator, Ops Console, Privacy]
assignee: Claude Code
related: [VT-376, VT-374, VT-377]
exec_order: 4
cl_decisions_checked: []
created: 2026-06-12
last_updated: 2026-06-12
---

## Source: VT-376 gate conditions (APPROVE-WITH-CONDITIONS, 2026-06-12)

1. **F1 (MEDIUM):** a hard-killed SYNCHRONOUS rerun (plan_generate / plan_deliver — not
   the DBOS-recovered agent_dispatch arm) leaves a `status='running'` lineage row that
   `_refuse_on_inflight_rerun` 409s on FOREVER — no cleanup path exists. Fix: stale-TTL
   leg in the refusal check (running + older than TTL ⇒ close `aborted_stale` + proceed)
   OR an orphan-close sweep leg; optionally a manual close API. Real-DB test: kill →
   TTL elapse → rerun succeeds.
2. **F2 (LOW):** mig-133 relied on "REVOKE FROM PUBLIC covers anon/authenticated" — on
   Supabase, platform defaults grant EXECUTE directly; the direct grant survives. RLS
   saves it today (defense-in-depth gap only). Fix: existence-guarded
   `REVOKE EXECUTE ON FUNCTION ops_top_tenants_today(...) FROM anon, authenticated`
   in the next migration + add the MULTI-VTR PRECONDITION note mig-133 lacks.
3. **F3 (LOW):** `ops_top_tenants_today` has zero function-level real-DB coverage. Test:
   zero-run tenant IS listed (LEFT JOIN claim), ranking, EXECUTE denied for
   app_vtr_role/anon.

## Dispatch

**RIDES THE PHASE-D (VT-377) DISPATCH** as a rider — closes the VT-376 gate conditions
inside the batch. [BUILD]-class individually, but lands under VT-377's merge class.

## Status history
- 2026-06-12: rostered from the VT-376 gate verdict; folds into Phase D.
