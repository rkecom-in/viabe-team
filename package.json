{
  "name": "viabe-team",
  "version": "0.1.0",
  "private": true,
  "description": "Viabe Team monorepo — Phase 1 scaffold",
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint:env": "node scripts/lint-cross-product-env.mjs",
    "lint": "pnpm lint:env && pnpm --filter @viabe/team-web lint",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
