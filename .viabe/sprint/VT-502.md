---
id: VT-502
title: "SupportBot VT-88 escalation + FATIGUE bypass VT-489 dev-routing → page ViabeOps for bogus tenant"
status: Todo
priority: Critical
type: BUILD
area: team-orchestrator
created: 2026-06-30
authorized_by: fazal
authorization_basis: "Fazal 2026-06-30: getting CRITICAL error_envelope + SupportBot VT-88 escalation + FATIGUE on ViabeOps (PROD ops) for the bogus re-drive tenant f0000bcd. Test traffic paging prod ops."
---
# VT-502 — SupportBot escalation dev-routing (same leak class as VT-489, different emit path)
support_bot.py (VT-88 escalation + FATIGUE) emits to ViabeOps DIRECTLY, NOT through alerts/dispatch.py is_dev_routed (VT-489). Fix: route the support_bot emit through is_dev_routed (dev/canary/bogus tenant → DEV bot only, never ViabeOps). AUDIT every alert emit path for the same bypass (error_envelope severity, any owner_surface/ops emitter) — principle: a dev/canary tenant NEVER pages ViabeOps for ANY alert family. PROD UNAFFECTED (real prod escalation still pages + FATIGUE still fires). Confirm owner ...0901 is synthetic (Fazal's set ends 3267/9946/6965; if a real leaked number → PURGE). Minor: don't digit-redact the synthetic tenant UUID in alert bodies.
