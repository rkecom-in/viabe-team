---
vt_id: VT-298
title: Autonomous watchdog → immediate Telegram VTR alerting
status: Review
priority: High
sprint: Sprint 2 - Owner Surface
type: Feature
area: [Orchestrator, Observability, Infrastructure]
assignee: claudecode
parent: VT-189
blocked_by: VT-290
sub_items: []
exec_order: 9
related: [VT-189, VT-296, VT-297]
created: 2026-06-02
---

# VT-298 — Autonomous watchdog → immediate Telegram VTR alerting

## Why
Fazal: "If [watchdog] found [an issue] report to VTR on Telegram immediately." A crash /
stall / misbehaviour must reach the assigned VTR without them watching a board.

## What
- Watchdog detectors (crash / stall / misbehaviour) → severity classification → IMMEDIATE
  Telegram alert to the **assigned** VTR.
- Detectors shared with the VT-296 board (board = visual; this = push).
- Routing uses the VTR↔business assignment (VT-295) to pick the recipient.

## Acceptance criteria
- A detector fires (sandbox/mock) → severity assigned → Telegram alert dispatched to the
  ASSIGNED VTR only (not unassigned VTRs).
- Alert path exercised in canary (sandbox/mock Telegram acceptable until bot token lands — DR-15, flag if needed).
- No PII in the alert payload (CL-426 / CL-390).

## Out of scope
- The board UI (VT-296); the interactive bot (VT-297).

## Notes
Build deferred behind launch queue. Pairs with VT-296 + VT-297. One PR (CL-424).

Implementation: built ON the VT-202 alerts substrate (detectors + dispatch + PII scrub already
exist). New = recipient routing to the assigned VTR. `migrations/075_operator_telegram.sql`
(deny-all RLS; chat_id + verified_at — Cowork DECISION 1b: verify/opt-in gates sends, never
message an unverified chat). `alerts/vtr_routing.py::resolve_assigned_vtr_chat_ids` chains
operator_assignments (072) → operator_telegram (075, verified-only). `alerts/dispatch.py`
`_dispatch_telegram` now fans out to BOTH the OPS chat (existing, retry-tracked) AND each
assigned VTR's verified chat (DECISION 2 = BOTH); canary tenants stay DEV-only (canary lock).
Per-recipient retry tracking is a follow-up (OPS channel is the durable retry path).

**Bot-token flag:** real sends need `TELEGRAM_OPS_BOT_TOKEN` + each VTR verifying their chat
(operator_telegram.verified_at). Canary uses mocked send_telegram. The verify/bind UI/flow is
not in this PR — rows exist, sends are gated on verified_at, so unverified bindings are inert.

Canary (DR-15, real-PG, send_telegram mocked): `tests/orchestrator/test_vtr_telegram_routing_realdb.py`
— resolver returns assigned+verified only (unverified/cross-tenant/revoked excluded); dispatch
fans out to OPS + assigned VTR, excludes cross-tenant VTR; canary tenant → DEV only, no VTR.

## Status history
- 2026-06-02: Backlog (rostered VT-290 plan, exec_order 9)
- 2026-06-03: Review (built — Cowork-approved answers DECISION 1b + 2; mig 075 + vtr_routing +
  dispatch fan-out; 4 real-PG canary + mig clean-apply; ruff clean; PR open, awaiting Pillar-7)
