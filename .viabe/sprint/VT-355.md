---
vt_id: VT-355
title: Freeze the approved sprint-dashboard layout into build_sprint_dashboard.py (layout-drift fix)
status: Done
priority: Medium
sprint: Sprint 9 - Launch Surface
type: Chore
area: [Tooling]
assignee: unassigned
parent: ""
sub_items: []
tags: [tooling, dashboard, layout-drift]
---

# VT-355 — sprint-dashboard: layout frozen, data live

Fazal approved a new sprint-dashboard layout after the generator clobbered his preferred view
(layout drift, flagged twice). Rework `scripts/build_sprint_dashboard.py` so every regen emits
EXACTLY the approved template's structure/CSS/JS, only data interpolated.

Approved template staged UNTRACKED at `.viabe/dashboard-approved-template.html` — bring it into
the PR (don't lose it).

## Contract (Cowork 20260606T103600Z)
1. KPI strip (overall % + rows closed / merges-this-batch / open launch gates / blocked-on-Fazal)
   — from `.viabe/sprint/VT-*.md` + git log.
2. Sprint-brief paragraph — inline a NEW Cowork-maintained `.viabe/sprint-brief.md` (create +
   seed from the template), NOT generated prose. Cowork owns updates.
3. Phase status table — per-sprint closed/total + state badge; Sprints 0–5 collapsed to one row.
4. Working-state two-column — left: non-done by exec_order (top 6–8); right: launch-gate /
   needs-fazal rows + status-drift detections.
5. Recent merges — last 8 from `git log --oneline main`.
6. Sprint board + filter pills (All/Done/In-flight/Backlog/Closed, live counts, "Showing X of Y",
   client-side JS per the template). Status→filter: Done→done; In Progress/Blocked/Queued→flight;
   Backlog/Deferred→back; Cancelled→closed.
7. REMOVE the mock banner div. Keep the light-mode-only hard lock.

## Acceptance
- Two runs on unchanged data → byte-identical (deterministic ordering everywhere).
- Emitted HTML diffs the approved template ONLY in data-bearing text (structural diff in the PR desc).
- Template + generator + `.viabe/sprint-brief.md` land together.
- Do NOT touch the PM-dashboard script/flow — SPRINT dashboard only.

Sequence AFTER VT-334 (serial-on-main). [BUILD].

## Origin
Cowork task 20260606T103600Z — Fazal-approved layout, drift fix.
