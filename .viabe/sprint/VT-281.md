---
vt_id: VT-281
title: PII-encrypted-from-VTR — de-identified VTR view (RLS/crypto enforcement)
status: Done
# VT-281 BUILT (Fork A, Cowork plan-ack 20260606T231500Z) — DB-enforced VTR de-identification.
# mig 115: app_vtr_role (NOLOGIN NOINHERIT) + de-identified views (vtr_customers / vtr_escalations,
# explicit columns, keyed REF#=HMAC(id,secret)) + NO grant on customers / phone_token_resolutions /
# the decrypt fn (REVOKEd PUBLIC EXECUTE — the canary caught that leak) → PII UNREACHABLE, not
# masked. orchestrator/privacy/vtr.py: bootstrap_vtr_ref_secret (env VT_REF_HMAC_KEY) + vtr_connection.
# Canary (synthetic, CL-422): denied-probe every PII table + decrypt fn; view de-identified; REF#
# keyed+stable; bootstrap fails-loud. Multi-VTR assignment-scoping is a documented HARD precondition
# in the migration (Phase-1 = Fazal-as-VTR#1 sees all). pii/crypto → Cowork subagent at PR.
# --- original roster note ---
# AOL WAVE 1/4 (Fazal "pull the full AOL layer", Cowork 20260606T211500Z) — queues BEHIND the
# backlog-close buildables (Jun-15 gates); the AOL layer does not gate launch. [PLAN-FIRST:
# pii/crypto → Cowork subagent at PR]. Build vs DEV synthetic only (CL-422). CL-425 leans on this.
# Plan must cover: VTR (Fazal=VTR#1, Telegram) can/cannot-see, key management, decrypt-audit trail.
priority: High
sprint: Backlog - Agent Operation Layer
type: Feature
area: [Privacy, Orchestrator, VTR]
assignee: unassigned
blocked_by: null
sub_items: []
exec_order: 99
related: [VT-279, VT-280, VT-231, VT-272]
---

# VT-281 — PII encrypted from the VTR (de-identified view)

Agent-operation-layer row from CL-426 (VTR standing decision, Fazal 2026-06-01).
Fazal locked: **"customer PII information encrypted so the VTR cannot see it."**

Enforce that the VTR sees de-identified / business-level data ONLY — never raw
customer names / phones. Mechanism: RLS + crypto so the VTR role cannot decrypt
customer PII (a distinct role from owner/app_role). This removes the human-PII-
access surface, which is WHY no separate human-access consent is needed beyond
CL-425 — so the enforcement must be real, not advisory.

Any escalation that genuinely needs customer identity routes to the OWNER (VT-279),
not the VTR. High-priority within the VTR layer (it's the privacy guarantee the
CL-425 framing leans on). Ties to VT-231 (Mumbai prod) + VT-272 (privacy notice).
