---
id: VT-608
title: "Loop P5 — Integration specialist real: context-scoped phase tools, Shopify fixed mapping, Sheets mapping reasoner, persist-every-phase, resume-everywhere; stubs removed"
status: Done
priority: high
type: BUILD
area: manager
created: 2026-07-05
authorized_by: fazal
authorization_basis: "Fazal 2026-07-05 loop-program grant — execution plan Package 5."
---
# VT-608 — integration becomes a real specialist (expert Package 5)
Tools (all context-scoped tenancy): list_supported_connectors, read_integration_state, start_oauth,
check_oauth_status, pull_sample, propose_mapping, confirm_mapping, commit_ingestion,
schedule_recurring_pull, verify_connector. Shopify: validated shop domain, single-use OAuth state,
fixed canonical mapping, server-side pull/ingest/verify/schedule. Sheets: OAuth without manual
paste (CL-421), owner selects sheet/tab via supported UI, sample → mapping reasoner → confirm →
commit → cadence; raw customer rows never reach the model. Both: persist every phase before
replying; resume from callback/reply/restart; needs_owner_input|blocked|failed|completed-with-
evidence; config/connector failure NEVER reported as missing owner input; field-mapping + non-
Shopify placeholder stubs removed. Real dev canaries (Rule #15): Shopify + Sheets end-to-end incl.
recurring-pull + restart recovery; OAuth replay/expiry/wrong-tenant/duplicate-callback fail closed.
ALSO IN SCOPE (vt603-builder flag, 2026-07-05): shopify_onboarding.py (start_shopify_setup,
pull_and_ingest_shopify) + integrations/connectors/shopify.py still write via the raw
get_pool()/BYPASSRLS pool — the tool-boundary trust is closed by VT-603 (only the resolved context
tenant reaches them now), but move these onto tenant_connection as defense-in-depth while the
phase tools are rebuilt. Exec order 6.

## RESOLVED 2026-07-06 — landed (6095a8a, f4ce139, db6e6a0, 1ecd167, 416d511), migration 168
Integration is a real specialist: 10 context-scoped phase tools + escalate (commit_ingestion =
proposal-only; execution server-side post-review via the outcome allowlist); Sheets OAuth + picker
endpoints + mapping reasoner wired (the field_mapping.py reasoner finally consumed); durable
owner-confirmed mapping (tenant_connector_status.field_mapping, mig 168) driving BOTH the initial
commit and recurring pulls; phase persistence UPSERT-before-reply; connector-resume dispatcher
(routes on connector_id — the Sheets flow no longer dead-ends nor cross-fires the Shopify ingest);
runner-gate defer for loop-owned turns; activation entry (+ the fail-open unmapped-specialist skip
closed); the entire raw-pool class dead across both connectors (11 sites).
Review: 9/9 confirmed findings fixed (2 critical incl. the decorative-mapping defect; the
stale-pending re-fire killed via arming-identity + expiry semantics). Re-verify: 2/2 criticals
fixed=true. Battery 3,540/0 (the recurring test_vt384_l3 ordering flake excepted — 4th occurrence,
isolation-proven; rostered into VT-611 pre-work). Non-blocking residuals for VT-611's pack:
mapping write→read round-trip join test; partial-mapping all-or-nothing sharp edge; canonical-value
validation. Team-web picker PAGE split to a follow-up row (endpoint contract stable + documented).
Live merchant-account canaries: pending on deployed dev (canaries/vt608_integration_specialist.py).
