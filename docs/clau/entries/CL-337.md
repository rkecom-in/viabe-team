---
cl_id: CL-337
created: 2026-05-22T13:20:11.022Z
notion_legacy_id: 368387c2-cc5a-816f-a828-d0cadf32edcf
title: "NEXT ACTION (owner_inputs one-PR brief — DRAFTED, persisted here verbatim, awaiting two Fazal gates before ship): Brief drafted 2026-05-22 per scope-lock CL-331 and authoritative decision CL-330. Pers"
---

# CL-337 — NEXT ACTION (owner_inputs one-PR brief — DRAFTED, persisted here verbatim, await

NEXT ACTION (owner_inputs one-PR brief — DRAFTED, persisted here verbatim, awaiting two Fazal gates before ship): Brief drafted 2026-05-22 per scope-lock CL-331 and authoritative decision CL-330. Persisted in full so it survives the session boundary — do NOT redraft; this IS the brief.

=== BRIEF: owner_inputs — structured-intent substrate + extraction writer + Composer read-path ===
Subtask: VT-7-adjacent, owner_inputs. ONE subtask, ONE PR (Fazal-locked, CL-331 — do not split). Area: Knowledge Architecture. Sprint: Sprint 7 - Knowledge Architecture.

SHIP GATE — may be executed + PR-opened, but MUST NOT MERGE until both clear: (1) Fazal signs off exact owner-facing privacy-notice wording, which must distinguish 'raw message text is not retained' from 'message content is transiently transmitted to a third-party LLM for extraction' — both are true, the extraction path makes the second one true. (2) Meta-terms pre-flight check confirms WhatsApp Business Platform / Meta terms permit transmitting message content to a third-party LLM sub-processor (a transmission question, not a storage question). Both gates Fazal-owned. Claude Code opens PR then STOPS — does not merge.

STEP 0 (mandatory, real, verify against main before any code): (a) locate the Twilio ingress handler, confirm where inbound WhatsApp body is received and confirm the EXISTING deliberate drop of the Twilio Body field — extraction must read body in-process at ingress and introduce NO new persistence of it; report file path + exact drop point. (b) confirm migrations dir convention: repo-root /migrations/, inline RLS per table, runner apps/team-orchestrator/scripts/apply_migrations.py (per VT-7.1-R / PR #44). (c) confirm the Composer's current read surface — what it queries for recent_campaigns context — so owner_inputs read-path attaches at the same seam. If any differs from brief assumptions, STOP and surface.

COMPONENT 1 — owner_inputs table (migration, repo-root /migrations/, next sequence number after 019, inline RLS, tenant_id NOT NULL): columns tenant_id (FK, NOT NULL, RLS-scoped), intent (enum/text, classified owner intent), segment (text, nullable), occasion (text, nullable), captured_at (timestamptz NOT NULL), source_message_sid (text NOT NULL — Twilio SID for provenance, NOT the body), standard id/created_at. NO body / NO raw-text column ever. Inline RLS: SELECT/INSERT/UPDATE/DELETE policies tenant-scoped, same 4-policy pattern as base schema + l1 tables. Retention: lifetime of tenant relationship, no timer. Purge on tenant offboarding / DSR — wire row deletion into existing offboarding/DSR purge routine; Step 0 confirms that routine exists, if absent FLAG it (gap, not this PR's scope to invent).

COMPONENT 2 — extraction writer (PII-sensitive; MUST be a clearly delineated, separately-reviewable diff section per CL-331 term b): at Twilio ingress, before Body is dropped, pass raw body TRANSIENTLY to an LLM classifier returning {intent, segment, occasion}; write one owner_inputs row with those + captured_at + source_message_sid. Raw body NEVER written to disk, logged, or persisted — exists only in the request-scoped variable already present at ingress. Classifier model: cheap classifier tier from models.yaml, NOT Opus (classification not reasoning). Failure mode: on classifier error / low-confidence, write no row OR row with intent='unclassified' (Claude Code's call, must be explicit, no silent provenance drop); ingress path must NOT fail/block on classification failure — ingress stays resilient.

COMPONENT 3 — Composer read-path: Composer, assembling agent context, reads owner_inputs for the tenant (recent rows, tenant-scoped), includes structured intent alongside recent_campaigns. Attach at the read seam Step 0 identified.

CI REQUIREMENT (neutralizes the one-PR serialization gap, CL-331 term a): PR CI must exercise the Composer read-path against REAL extracted rows produced by Component 2, not fixtures alone — flow: sample body -> extraction writer produces real row -> Composer reads it -> assert structured intent surfaces in assembled context. Fixture-only read-path tests are NOT acceptance. Plus one NEGATIVE test: after extraction runs, assert no raw body text appears in the owner_inputs row, in logs, or anywhere in persistence — prove the no-raw-body guarantee in CI.
=== END BRIEF ===

CLAU CAVEAT carried for Fazal: CL-330's privacy-notice sketch ('raw message text is not retained') UNDERSELLS what happens — Component 2 transmits the raw body to Anthropic's API. The honest owner-facing line must cover BOTH not-retained AND transiently-third-party-processed. Do not sign off the simpler version. RECOMMENDATION: run the Meta-terms check BEFORE Claude Code builds — a terms prohibition blocks the feature regardless of code quality, so checking first avoids a wasted build cycle. Open sub-gap: DSR/offboarding purge routine existence — Claude Code Step 0 confirms; if absent, separate gap.

NEXT SESSION: when both gates clear, hand this brief to Claude Code as-is.

## Status history
- 2026-05-22T13:20:11.022Z: created (status=)
- 2026-05-25 04:00 IST: migrated from Notion (notion_legacy_id: 368387c2-cc5a-816f-a828-d0cadf32edcf)
