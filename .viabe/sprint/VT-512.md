---
id: VT-512
title: "OTP screen shows stale GST-reject error block despite gstin_verified=true — single source of truth + clear stale error"
status: In Progress
priority: Critical
type: BUG
area: team-web
created: 2026-07-01
authorized_by: fazal
authorization_basis: "Fazal 2026-07-01 live run: GST verify PASSED (VT-510, gstin_verified=true) but the OTP screen STILL renders the gst_reject error block. Inconsistency one screen later."
---
# VT-512 — stale GST-reject error on the OTP screen (LAUNCH-BLOCKER; same 'verified inconsistency' class)
signup-form.tsx: the gst_reject error block renders on the OTP/verify screen even though verifiedEntity/gstin_verified is set. Likely DEFECT (a): a stale `error` state from an EARLIER reject (the pre-VT-510 attempt in the same session) NOT cleared when the verify later succeeded → the block renders. FIX (single source of truth): when the verify succeeds / on the step transition to OTP, clear setError(null); render the gst_reject block ONLY when GST is genuinely unverified (verifiedEntity==null), never when gstin_verified. Reconcile to ONE verify point: the create gate (verify_gstin_for_signup) just re-checks active (no typed-name match w/o CIN) — confirm it trusts/rides the verified GSTIN, no second failing verify. DIAGNOSE Fazal's session (raw — is the error stale-from-earlier vs a fresh create-422?). PROOF: after verify passes, the OTP screen shows NO error block → enter OTP → account CREATES.
