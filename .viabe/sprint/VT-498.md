---
id: VT-498
title: "SR PII-in-personalization leak (customer first name in template params) + L4 win-back grounding failure"
status: Todo
priority: High
type: BUILD
area: team-orchestrator
created: 2026-06-30
authorization_basis: "Win-back re-drive #2: when a plan parsed, self_evaluate legitimately REVISEd it — (1) SR put the customer's real first name into message_plan.personalization params (should be a placeholder), (2) L4 win-back-benchmark retrieval failed → thin grounding → can't verify cohort_size/objective."
---
# VT-498 — SR PII-in-params + L4 grounding (confirmed sub-defects)
(1) PII leak: SR (the composing model) put the customer's real first name into message_plan.personalization template params; the gate correctly flagged it should be a placeholder ({{name}}). Fix: the SR prompt/validation must enforce placeholders in personalization params, never literal customer PII. (2) L4 retrieval failed ('L4 retrieval failed … proceeding without') for kirana win-back benchmarks → no attribution baseline → even a well-formed proposed plan can't pass self_evaluate. Investigate the L4 win-back-benchmark retrieval path. Plan-first (money-path). Depends on VT-496 (see the field) + VT-497 (validate on the right tier).
