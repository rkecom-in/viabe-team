# CL-437 — Three Fazal privacy rulings (2026-06-12, close-the-open-points session)

**Status:** Fazal-issued, STANDING (all three).

## 1. VT-379 dispatch GRANTED
The unredacted `pipeline_steps.error` column fix is authorized: route the 3 direct-INSERT
writers (error_router, self_evaluate_gate, collapse) through the redacting writer, redact
`error` at write with the name registry, extend nightly Detector-5 to the error column,
backfill-redact existing rows.

## 2. VTR-PII posture REAFFIRMED + STRENGTHENED (closes the masking question)
Fazal initially proposed UI masking with partial visibility + VTR update rights; after the
conflict with locked CL-425/426 was flagged (masking ≠ unreachability; maskForVtr deleted
for cause; the no-separate-consent DPDP conclusion rests on unreachability; identity
updates are OWNER-lane), Fazal ruled (verbatim): **"Keep locked posture, the VTR cannot
update or see the customer PII fields."** This EXTENDS the lock explicitly to UPDATES —
no VTR write path may touch customer PII fields, masked or otherwise. CL-425/426
unchanged and reinforced; the UI-masking model is REJECTED, do not re-propose.

## 3. Outbox at-rest retention RULED (closes the 2026-06-06 open ledger line)
Fazal (near-verbatim): outbox bodies may be retained **only while needed for delivery,
retry, replay, or drain. After successful drain / terminal completion, redact the body
fields in the outbox, keeping metadata and hashes.** Exact owner-facing text may be
retained only in the tenant-scoped owner conversation/audit surface, under the normal
tenant retention + DSR policy. Implementation rostered as **VT-382**.

*The former ledger line "Open privacy ruling (2026-06-06, pending Fazal)" is RESOLVED by
ruling 3.*

## Addendum (Fazal, 2026-06-12, verbatim "accept") — ruling 3 interpretation

**The "exact owner-facing text" clause is satisfied by the RECONSTRUCTIBLE capture:**
template-ref + resolved Twilio SID + ordered send-resolved variable values in
`owner_message_audit`, with the exact text recoverable via the registry's pinned approved
bodies (VT-383 body_sha256 + recorded copy) — NOT literal per-send body duplication.
Corollary (Cowork-accepted policy reading, recorded): never-sent text (halted children of
terminal batches) is redacted WITHOUT audit capture — capturing never-sent text would
itself violate the retain-while-needed clause.
