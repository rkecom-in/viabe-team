---
id: VT-563
title: "Attribution-outcome writer — un-sever the outcome-learning leg (recovered/lost produces data)"
status: In Progress
priority: launch-blocker
type: BUILD
area: learning
created: 2026-07-02
authorized_by: fazal
authorization_basis: "Fazal 2026-07-02 (terminal): fix order #4."
---

# VT-563 — attribution-outcome producer

## Problem (audit findings, CONFIRMED cluster)
Zero `INSERT INTO attributions` in production src (canaries only) — VT-176 shipped only the aggregator. Nothing writes `terminal_state_metadata.attribution_outcome`/`attribution_baseline`, so the daily implicit-feedback sweep (`implicit_attribution.py:46`) is a permanent no-op and three consumers read a dead substrate. `context_builder.py:480` hardcodes `recovered_paise=0` behind a stale "substrate does not exist" comment while the read wrappers (VT-306) exist.

## Fix
Build the production attributions writer from the existing matched-transaction substrate (read `match_transactions.py` / VT-240 lineage for the intended design); back-annotate the originating run's `terminal_state_metadata` with `attribution_outcome` + `attribution_baseline` at attribution close so the implicit sweep sees production rows; point `_build_recent_campaigns` at the existing per-campaign attribution reads and flip the completeness flag when populated. If the matching substrate itself is absent, report honestly — do not fake a producer.
