---
vt_id: VT-161
title: DBOS conductor recovery path — re-verify ~2.5h retention if cloud conductor wired
status: Done
# VT-161 DONE 2026-06-09 (Cowork 20260609T113000Z; reopened when Fazal adopted DBOS Conductor, app
# "viabe-team" on console.dbos.dev, creds .viabe/secrets/dbos.env). Wired in dbos_config.py: env-driven
# app name (DBOS_APPLICATION_NAME, default viabe-team) + opt-in conductor_key, graceful-degrade.
#
# CONNECTION CANARY GREEN (CC Mac, live): _build_dbos_config → DBOS.launch with the filled key binds
# the Conductor websocket — "Connected to DBOS conductor" (conductor.py:145), app=viabe-team. Console
# flips UNAVAILABLE→AVAILABLE. The mismatch that caused UNAVAILABLE was the hardcoded name
# "team-orchestrator" (dbos_config.py:99); bind URL is .../websocket/<app_name>/<key> so the name must
# match the registered app. Gated test test_dbos_conductor_canary (DBOS_LIVE_CANARY=1) institutionalizes it.
#
# RENAME DUE-DILIGENCE (3-agent workflow incl. adversarial verifier, all risk=none): renaming
# team-orchestrator→viabe-team is RECOVERY-SAFE — app name is a Conductor/console/SQLite-fallback
# identifier ONLY, never in a recovery/dequeue WHERE clause. DBOS recovery keys on Postgres DB +
# executor_id (DBOS__VMID) + application_version (workflow-source MD5; name not an input). NO prod
# drain needed for the rename. Conductor + key absent in prod (Railway) until Fazal sets it → prod is
# unchanged (local-recovery) until then; the name default flips to viabe-team on next deploy (safe).
#
# RETENTION RE-VERIFY (~2.5h SLA): HOLDS under Conductor. Our dbos_purge (PR #47) deletes terminal
# workflow_status.inputs after 2h (privacy SLA) — an independent scheduled sweep, unaffected by
# Conductor. Conductor's managed retention (conductor.py:568 RETENTION → _sys_db.garbage_collect)
# only deletes TERMINAL rows older than an operator-set cutoff (never PENDING/ENQUEUED/DELAYED → no
# recovery conflict), is opt-in (console-pushed, not default), and can only TIGHTEN retention, never
# extend it. So our 2h inputs-purge remains the authoritative privacy floor; no conflict.
priority: Low
sprint: Hardening
type: Infrastructure
area: [Privacy, Infrastructure]
assignee: Pipeline Engineer
parent: VT-8
sub_items: []
branch: ""
version: ""
notion_legacy_id: 369387c2-cc5a-81e3-aab5-e6ca1f617175
last_updated: 2026-05-25T03:45:00+05:30
---

# VT-161 — DBOS conductor recovery path — re-verify ~2.5h retention if cloud conductor wired for production

(This page is blank in Notion; metadata captured above.)

Expected Outcome (from Notion):
Conditional task — only applies if production deployment uses DBOS cloud-managed conductor (not local-recovery-only). If yes: verify the ~2.5h terminal-row purge SLA holds in the managed environment. If still local-only (current state per CL-383), this task closes with a written-not-implemented marker.

Notes (from Notion):
Source: CL-391 open item 9 (CL-386 in PM brief). Conditional: deployment is local-recovery-only today per CL-383 so PR #47's purge suffices; revisit if conductor changes. Rostered by Cowork 2026-05-23.

## Status history
- 2026-05-25 03:45 IST: migrated from Notion (notion_legacy_id: 369387c2-cc5a-81e3-aab5-e6ca1f617175)
