---
vt_id: VT-359
title: Batch-2 template consumer-wiring (trial_subscribe_link / support_resolved / dsr / breach)
status: Done
# VT-359 RECONCILE-CLOSE 2026-06-09 (Cowork 20260609T090000Z, Rule #14 drift fix): all code shipped
# in #416 (trial_subscribe_link gated compose + support_resolved /resolve endpoint + breach runbook);
# the only open item (site-2 dsr_deletion_completed audience) was RULED 20260607T142500Z + shipped
# #417 (ops-invoked manual single-customer erase, automated send → VT-344). No code was outstanding;
# the row just never flipped. → Done.
priority: Medium
sprint: Sprint 9 - Launch Surface
type: Feature
area: [Billing, Owner Surface, Privacy, Templates]
parent: VT-108
related: [VT-332, VT-357, VT-353, VT-156]
tags: [templates, money, privacy, ops]
---
# VT-359 — wire the 5 batch-2 templates to their consumers

Dispatched by Cowork (20260607T093000Z PR-2). The SIDs landed via #413 (registry). This row wires
the consumers.

## Done (this PR — Cowork review)
1. **trial_subscribe_link → trial-end send (GATED).** trial_sweep.py exhaust branch composes the
   send (mints the VT-332 single-use token + build_subscribe_deep_link + owner_name) and routes it
   through the EXISTING owner-WABA notify seam, which stays the gated stub (logs until go-live).
   The gate is NOT flipped — only the compose is wired. Dormant if OWNER_JWT_SECRET unset.
2. **support_resolved → ops resolve-escalation endpoint (VT-357-p3 minimal).** New
   `/api/orchestrator/ops/resolve-escalation` (internal-secret + operator-JWT gate, mirrors
   resolve-phone): marks status='resolved' + resolved_by/at + record_ops_audit + best-effort
   send_template_message(support_resolved) to the owner (dormant pre-WABA-live). Idempotent.
   A Telegram `/resolve` ingress is a later nicety (greenfield) — NOT built (anti-gold-plating).
3. **breach_notification_owner / _customer → REGISTRY-ONLY + runbook.** No send path (incident-use,
   agent_selectable:false). docs/runbooks/breach-response.md now documents the manual
   Fazal-authorized send_template_message invocation + params. No auto-fan-out.

## site 2 (dsr_deletion_completed) — RESOLVED (ruling 20260607T142500Z, shipped #417 / 93368c2)
Option (a): dsr_deletion_completed is OPS-INVOKED on a manual single-customer erasure, sent to THAT
customer from the phone captured pre-deletion (CL-390); registry entry stands; the automated send
deferred to VT-344. Recorded in docs/runbooks/breach-response.md + the VT-344 scope note. No code
outstanding here.

## Verified
trial compose (deep-link + token; dormant→None) + resolve-escalation (resolved + audit + send;
403; idempotent) — 8/8 real-PG + trial_sweep regression; ruff/mypy clean.
