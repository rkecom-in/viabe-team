---
vt_id: VT-434
title: Prod-guard — fail-closed boot assertion against TEAM_TWILIO_VERIFY_MOCK_MODE (OTP 123456) in prod
status: Queued
priority: Critical
sprint: "Sprint-Fazal"
type: Feature
area: [Backend, Auth, Safety, Launch]
related: [dev-activation, VT-362]
exec_order: 103
created: 2026-06-25
last_updated: 2026-06-25
---

## Contract (Cowork GATE 2026-06-25, dev-activation Group-C prod-guard). LAUNCH-SAFETY. Risk(auth) → Cowork gate.

`TEAM_TWILIO_VERIFY_MOCK_MODE` (`auth/twilio_verify.py:14`) makes mock OTP `123456` approve ANY code (dev convenience).
If it ever leaks to prod, any owner authenticates with `123456`. MUST be impossible in prod.

## Scope
- Add a fail-closed BOOT assertion: if `EXPECTED_ENV=prod` (the VT-362 env sentinel) AND `TEAM_TWILIO_VERIFY_MOCK_MODE`
  is on → REFUSE to boot (raise, like the VT-362 env guard). Same for any other mock/test-bypass auth flag that's
  dangerous in prod (audit the auth module). Dev (EXPECTED_ENV=dev) unaffected — mock stays usable.
- Canary: prod-env + mock-on → boot refusal; dev-env + mock-on → boots fine.

## Gate
RISK (auth / launch-safety). Build → Cowork gate, NOT self-merge.

## Status history
- 2026-06-25: Rostered + ID allocated (Cowork dev-activation prod-guard).
