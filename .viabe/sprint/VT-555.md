---
id: VT-555
title: "team_welcome4 — strictly-transactional UTILITY quick-reply welcome (replaces force-converted team_welcome3)"
status: In Review
priority: launch-blocker
type: BUILD
area: onboarding
created: 2026-07-02
authorized_by: fazal
authorization_basis: "Cowork 20260702T0930Z + 0945Z (Fazal): don't appeal the force-converted team_welcome3 — create a fresh strictly-transactional UTILITY template; final copy = no free/trial, drop {{2}}, add a Complete Setup quick-reply button."
---

# VT-555 — team_welcome4 (UTILITY quick-reply welcome)

## Why
`team_welcome3` was Meta-approved then **force-converted UTILITY→MARKETING**, so it would be declined (63049) like team_welcome2. Fazal dropped the appeal path for a fresh, cleaner template.

## Created (Content API — canary `vt555_welcome4_create.py`, real create + UTILITY submit)
- **`team_welcome4`** en `HXc8188616b2e97b557f4c7330157c4f8f` / hi `HXd8a8d5945c79c75d373d9c24edd4b183` — created + submitted **category=UTILITY**, both `status=received` (Meta approval ASYNC).
- Content type **`twilio/quick-reply`**: ONE var `{{1}}` = owner name; a **Complete Setup** button, id/payload **`COMPLETE_SETUP`** (same both langs). Copy leads with the account-created FACT + the button — no free/trial/welcome-brand wording (what the auto-classifier flags MARKETING).

## Wired
- `_default_welcome` (signup) → `team_welcome4`, **name-only params** (dropped the trial-date var); `trial_end` kept in the signature for caller compat, no longer sent.
- `config/twilio_templates.yaml` + `config/template_routing.yaml` (`welcome`→`team_welcome4`) + `.viabe/templates.md` — registered team_welcome4, **DEPRECATED team_welcome3**.
- **Continue-trigger (confirmed, no code change):** the onboarding journey lazy-starts on the tenant's FIRST inbound (journey.py:576-591) — so a `COMPLETE_SETUP` button tap (Body=title) AND a typed reply both continue setup. The confirm-yesno quick-reply proves button taps reach the journey in prod.

## Verify
ruff clean; both configs parse + resolve (`welcome`→`team_welcome4`, 1 var, en SID wired); **111/111** — composer welcome route → team_welcome4, `_default_welcome` sends team_welcome4 with `{owner_name}` only (no trial_end_date), + the pre-existing composer/signup/twilio tests.

## Follow-up (not blocking)
Meta approval is async — flag if it returns **non-UTILITY** (tighten further). On approval + Fazal's go, his signup re-run → button tap → onboarding continues → delivery callback `delivered` (VT-524 ledger). **CC did NOT send to Fazal's number.**
