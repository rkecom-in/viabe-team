---
id: VT-572
title: "Team Manager self-evolving memory — system-written learned rows + compaction"
status: Rostered
priority: launch-blocker
type: BUILD
area: manager
created: 2026-07-03
authorized_by: fazal
authorization_basis: "CL-2026-07-03-agentic-fleet-mandate (Fazal, Standing): every agent gets brain-commanded tools + self-evolving compacting memory."
---

# VT-572 — the manager's memory writes and compacts itself

## Current honest state (pipeline audit + VT-566)
Tools: LARGELY DONE — the dispatch brain is a LangGraph agent with roster/query tools. Memory READ exists (VT-556 directives + VT-566 lessons/outcome read-back, gated). Memory WRITE is human-only: `upsert_learned` has ZERO callers — the system never distills its own experience into memory; no compaction anywhere.

## Scope
(1) System-written learned rows: producers that distill confirmed outcomes + accumulated corrections into `agent_memory` source='learned' entries (validated, tenant-scoped, C3 authority rules; Decision-C weighting for implicit signals). (2) Compaction: old lessons/learned rows distilled into denser understanding on a cadence (never silently expired). (3) Everything read-gated per the existing MANAGER_MEMORY_RETRIEVAL family.
