---
vt_id: VT-364
title: Apify ingestion — async start-poll-fetch (replace the blocking 120s run-sync)
status: Done
# VT-364 DONE-on-dev 2026-06-09 (allocated via vt_id_allocate.py after the VT-363 drift). The Apify
# fetch used run-sync-get-dataset-items with a single timeout=120.0 httpx POST — a held connection
# that ReadTimeout'd on slow actors (Zomato >120s at 100 reviews) → fail-soft EMPTY context (Fazal:
# "120s timeout, thats too much to rely on"). Replaced with a shared client (apify_client.run_actor):
# START POST /acts/{actor}/runs → POLL GET /actor-runs/{id} with backoff under a budget cap (480s) →
# FETCH GET /datasets/{dsId}/items on SUCCEEDED. Short per-call timeouts; the wait is the poll loop,
# not a 120s connection. Wired into BOTH apify_food._default_fetch + apify_gbp._default_fetch (one
# code path, all 3 actors). Fail-soft preserved (non-success terminal / budget → []); review caps
# kept; CL-390 strip-at-ingest unchanged. 4 behavioral tests (start-poll-fetch, FAILED, budget,
# no-run-id). Canary: the Zomato 100-review run now COMPLETES (37.4s, overall 2.8 + sentiment) where
# the old sync path ReadTimeout'd.
priority: High
sprint: "Sprint-Fazal"
type: Infrastructure
area: [Integrations, Ingestion]
assignee: Claude Code
related: [VT-110, CL-390, CL-422]
created: 2026-06-09
last_updated: 2026-06-09
---

## Concept
Ingestion is a background DBOS workflow, not a request path — a multi-minute wait is fine, but a
single 120s held connection is not (slow actors silently time out → empty L1 context). Async
start-and-poll makes ingestion reliable without removing the cost (review) caps.

## Status history
- 2026-06-09: built + canary green (async Zomato 100-review completes); [BUILD] dev self-merge.
