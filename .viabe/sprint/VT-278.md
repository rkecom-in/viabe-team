---
vt_id: VT-278
title: Live Sarvam ASR canary (real transcription + consent-gate) — prod-blocking
status: Backlog
priority: High
sprint: Sprint 4 - Ingestion Methods 3-5
type: Chore
area: [Ingestion, Privacy, Launch]
assignee: unassigned
blocked_by: null
sub_items: []
exec_order: 8.5
related: [VT-59, VT-231, VT-272]
---

# VT-278 — Live Sarvam ASR canary (prod-blocking)

Follow-up from VT-59 (Cowork merge-signal 2026-06-01). VT-59 shipped the Sarvam
voice-transcription primitive with the HTTP call + key ENV-GATED on
`TEAM_SARVAM_API_KEY` (only team-ingestion-worker config has it; not orchestrator-
accessible). The non-API logic is fully tested; the REAL Sarvam leg has not run.

**Prod-blocking (not merge-blocking)** — same posture as the Twilio live-canary:
before Sarvam voice ingestion is enabled against REAL customer data at Mumbai prod
(VT-231), a live canary MUST run green:
1. Fazal adds `TEAM_SARVAM_API_KEY` to `.viabe/secrets/team-dev.env`.
2. Real Sarvam STT call on SYNTHETIC audio (CL-422) → non-empty transcript,
   fail-not-skip on error (Rule #15).
3. Consent-gate-before-transmit verified against the live path (owner_inputs
   disabled → ConsentRejectedError, no transmission).

Until green, Sarvam voice stays dev/synthetic only (CL-422). Ties to VT-231
(Mumbai prod) + VT-272 (sub-processor framing).
