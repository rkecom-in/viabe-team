---
id: VT-641
title: "Manager intent mis-route: specific request dropped to a generic answer (systematic on Devanagari, intermittent on EN/Hinglish) — Tier-1 ignored_speech_act"
status: Mis-route FIXED + dev-verified (d9f5506); j08 residuals are DISTINCT issues (see below)
priority: Critical
type: Bug
exec_order: 1
owner: claudecode
created: 2026-07-15
source: "10-journey e2e sim fast tier (Fazal 2026-07-15 greenlit) — j08 Devanagari 3/3 + j04/j09 1/3, blind judge Tier-1 breakers"
---

## Problem (measured on deployed dev, ba280a6)

The manager intermittently DROPS the owner's specific request and serves a generic/adjacent answer
instead of routing to the asked action — a Tier-1 breaker (#4 ignored_speech_act, #6 clearly-wrong action).

**Systematic on DEVANAGARI (j08, 3/3 runs):** onboarded tenant, 8 seeded lapsed customers. Owner (Hindi
script) asks for the lapsed list, then "इन 8 ग्राहकों के लिए वापसी ऑफर तैयार कर दो, अभी भेजना मत, पहले दिखाओ"
(prepare a win-back offer for these 8, don't send yet, show me first). Manager:
- Reply 1: "You currently have 8 customers in your ledger." — in ENGLISH, total-framed (owner asked lapsed).
- Reply 2: "Aapke business mein abhi kis cheez par kaam karun—sales badhana, win back, payments, ya
  Shopify/Sheets connect?" — a GENERIC ONBOARDING/CAPABILITY MENU. It IGNORED the win-back request and did
  NOT delegate to sales-recovery. Then re-served the count (loop_stall).
The IDENTICAL journey in Hinglish/Roman (j01) correctly delegates to SR + drafts a campaign — so the
DEVANAGARI SCRIPT flips the routing.

**Intermittent on EN/Hinglish (same class):** j04 run2 — "top customers by spend" answered with "8 lapsed
customers" (wrong question); j09 run1 — "store 2's sales" answered with generic "your profile is set up,
connect data"; j01 — lapsed-count deflected to total in 2/3 runs. 1/3 rate = the router mis-classifies or
low-confidence-falls-back to a generic answer.

## Tier-1 impact
Fast-tier judge (18 runs, 6 journeys x3): 6 scenarios with >=1 breaker (target 0). j08 is 3/3 (systematic);
j04/j09/j10 are 1/3. Tier-2 quality of clean = 12/12 (100%). So the ONLY thing off the two-tier bar is
this mis-route/ignored-speech-act class.

## Scope (root-cause first, then fix — classifier is a correctness gate = risk row, self-gate before land)
1. Pin the inbound intent classifier/router (dispatch_brain -> classify -> lane); find the script/language
   gap (Roman/Hinglish keyword coverage without Devanagari) + the generic-menu fallback trigger for an
   ONBOARDED tenant. [Explore root-cause in flight 2026-07-15.]
2. Fix so a clear Devanagari request routes to the correct lane (SR win-back) — and answers in the owner's
   script. Prefer a deterministic/script-agnostic classification fix over a prompt nudge (prompt nudges
   don't land — META). Keep the count answer in-register (mirror owner language).
3. Re-drive j08 (+ j04/j09) x3 on dev; Tier-1 for those -> 0.

## FIX LANDED + DEV-VERIFIED (d9f5506, deploy d63b0980)
Added Devanagari coverage (via keyword_match's Devanagari-safe boundary matcher) to the two Roman-only
pre-brain nets: campaign_first_contact D3 (win-back verb∧noun) + status_query classify_status_query
(list/inactivity/create-verb/dormancy cues). 11/11 routing probes + 33 unit tests + ruff clean.
**Dev re-drive j08 x3 (deployed) — the targeted mis-route is GONE (transcript-proven):**
- turn-1 (Devanagari lapsed-list ask): pre-fix "You have 8 customers in your ledger" (total, English) ->
  post-fix "6 of your customers are lapsed … last 45 days" (correct lapsed classification).
- turn-2 (Devanagari win-back request): pre-fix generic onboarding menu (NO delegation) -> post-fix an
  actual SR campaign DRAFT + approval request. Delegation restored.

## j08 STILL trips the judge 3/3 — but on DISTINCT issues, NOT the VT-641 mis-route (verified by reading):
1. ignored_speech_act "owner asked to be SENT THE LIST; assistant drafted instead" = the **CD2 gap**
   (lapsed_list deliberately never dumps names inline; the file-attachment path is VT-79/CD2, pending).
2. fabrication "lapsed-120d-plus contradicts the 45-day threshold" = the **cross-cutting SR cohort-label
   finding** (the draft invents/varies its cohort filter label every run; also seen in j01). Real, separate.
3. fabrication "₹250–₹750 recovery ungrounded" = **likely a judge FALSE-POSITIVE** — grounded in the seeded
   order sizes the tier_rescore ground-truth block doesn't state (same instrument gap class as VT-640).
4. loop_stall "verbatim repeat of step-1" = **CONFIRMED judge/harness ARTIFACT** — the late-reply-sweep
   re-lists the turn-1 messages with IDENTICAL message_sids; the judge should dedup by sid but doesn't.
   This inflated the fast-tier Tier-1 count too (j10's loop_stall was the same artifact).

## Follow-ons (separate, teed up — NOT this row)
- Judge instrument: dedup transcript messages by message_sid before scoring (kills the false loop_stall) +
  state seeded order/spend in the ground-truth block (kills the ₹ FP). Cheap; improves ALL journey scoring.
- SR cohort-label consistency (the 45d-vs-shifting-label fabrication) — real, the j01 finding.
- CD2 / VT-79 list-delivery (send the actual lapsed list on request).
- Tier-2 register: status_query count/lapsed replies are hardcoded English — answer in the owner's script.

## Notes
Adjacent to DF5 (bypass-Haiku count net), VT-329 (Devanagari-safe approval/negation matcher — that was the
deterministic matcher; THIS is the higher-level intent ROUTER), and the SR cohort lapsed-vs-total finding.
