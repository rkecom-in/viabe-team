---
vt_id: VT-295
title: Ops Console — Assignment (VTAdmin: business↔VTR)
status: Review
priority: Medium
sprint: Sprint 2 - Owner Surface
type: Feature
area: [Frontend, Owner Surface, Privacy]
assignee: claudecode
parent: VT-189
blocked_by: VT-290
sub_items: []
exec_order: 6
related: [VT-189, VT-290]
created: 2026-06-02
---

# VT-295 — Ops Console — Assignment (VTAdmin)

## Why
VTAdmin controls which VTR is responsible for which businesses — the assignment that
every other page's RLS scoping reads from.

## What
- VTAdmin-only page: all-business view; assign / reassign businesses to VTRs inline.
- The assignment is the source of truth for VTR→tenant RLS scoping across the console.
- Deep view → overlay.

## Acceptance criteria
- Only VTAdmin can access/mutate assignments (role gate; fail-closed).
- Reassigning a business immediately rescopes that VTR's visibility (RLS canary: VTR's
  assigned set changes; real-PG).
- Every assignment change audit-logged (Pillar 7).

## Out of scope
- The per-page listings that consume the assignment (VT-291..294, VT-296).

## Notes
Build deferred behind launch queue. Foundational for multi-VTR scoping but VTAdmin-only,
so sequenced after the VTR core pages. One PR (CL-424).

Implementation: `lib/ops/assignment-admin.ts` (VTAdmin-gated CRUD over operator_assignments,
mig 072) + `app/(app)/team/ops/assignment/{page,actions}.tsx` + `components/ops/assignment-admin.tsx`
(nav entry already in ops-side-nav from VT-290). Every read + mutation is VTAdmin-only
fail-closed; unassign resolves the target operator/tenant FROM the row id server-side (the
VT-293/294 IDOR rule, applied up front); every mutation appends ops_audit (Pillar 7). The
real-PG RLS-rescoping canary already lives in VT-290's `test_operator_assignments_substrate.py`
(`test_reassign_rescopes`); VT-295 adds 13 vitest over the CRUD/authz/audit logic.

## Status history
- 2026-06-02: Backlog (rostered VT-290 plan, exec_order 6)
- 2026-06-03: Review (built — VTAdmin CRUD + IDOR-safe unassign + ops_audit; 13 vitest, tsc/lint clean; PR open, awaiting Pillar-7)
