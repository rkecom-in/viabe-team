---
vt_id: VT-429
title: Single-plan launch gate — offer STANDARD only (server-side offered-tiers allowlist, fail-closed + UI)
status: Queued
priority: Critical
sprint: "Sprint-Fazal"
type: Feature
area: [Backend, Billing, Money, Frontend, Launch]
related: [VT-424, VT-416, VT-89]
exec_order: 98
created: 2026-06-25
last_updated: 2026-06-25
---

## Contract (Cowork 2026-06-25, Fazal: launch offers ONE plan — STANDARD, ₹4,999/mo). LAUNCH-BLOCKER money gate. Risk(money) → Cowork gate. Stacks on VT-424 (#514, same razorpay_subscribe.py).

Fazal: at launch, owners may subscribe to **STANDARD only** (`STANDARD_RZP_PLAN_ID` = plan_SzFYaeZxl4DByj). Founding +
Pro stay in `plans.yaml`/env (NOT deleted) but are NOT offered.

## Scope
- **Server guard (load-bearing):** a config **offered-tiers allowlist** (`OFFERED_TIERS=["standard"]` or equiv —
  config, not a code fork), checked SERVER-SIDE in `razorpay_subscribe` **before** `resolve_plan`. Fail-closed: a
  non-offered / unknown tier → 4xx, NO subscription (never trust the client to send only `standard`). Widening
  post-launch = add a tier to the allowlist (one config change).
- **Owner-facing UI (team-web):** the plan-selection surface presents ONLY Standard; Founding/Pro hidden.
- Stacks on VT-424's `razorpay_subscribe.py` (the real create) → branch from `vt424-razorpay-subscription`; depends on #514.

## Gate
RISK (money — the offer/billing surface). Build → Cowork gate, NOT self-merge.

## Status history
- 2026-06-25: Rostered + ID allocated (Cowork single-plan-launch directive). Server allowlist + UI; depends on VT-424.
