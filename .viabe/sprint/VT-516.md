---
id: VT-516
title: "VTR-console real-time trace viewer — grouped summary-first feed + drill-down over audit+debug"
status: In Review
priority: High
type: BUILD
area: observability
created: 2026-07-01
authorized_by: fazal
authorization_basis: "Fazal 2026-07-01: TM audit log (what it knows/decides/does + why) + a separate debug/failure log, real-time streamed on the VTR console; build on dev now (dev-full-parity)."
---
# VT-516 — see .running/processed/ VT-514/515/516 signals + BUILD-NOW (123000Z). Debug-first (VT-515), audit+viewer behind (VT-514/516). Completeness via choke-point + no-orphan invariant; PII by-reference via pii_redactor.

## LANDED on dev @ 5cd6edc (2026-06-30)
- `TmActivityFeed` on the VTR Ops Console tenant page: real-time (Supabase Realtime), timeline GROUPED BY run/turn (collapsed default), merges `tm_audit_log` + `debug_events` over the shared trace_id; layer/kind/severity/failures/search controls, failures badge, lazy drill-downs, 200-row cap, light-mode locked.
- `tm-audit-events.ts` (fetch + summary) + `/api/team/ops/tm-audit` route + `subscribeTmAuditEvents` in `stream.ts` + `deIdentifyTmAuditForVtr` (nulls input/result/reasoning_ref for non-full-read VTRs).
- mig 147 back-filled `debug_events_operator_select` — fixes VT-515's dead browser realtime (mig 146 deny-all) so the audit-vs-debug toggle's debug stream works for operators.
- Gate: team-web tsc clean, vitest 555 passed (8 new `_matchesTmAuditFilters`/`composeTmAuditSummary`), eslint 0 errors. Realtime substrate (operator policy + publication) confirmed on deployed dev.
- **Pending:** Vercel dev build of the viewer + Fazal browser eye-check (grouped feed render, drill-down, live correlation). The visual is the eye-check (as VT-515's feed was).
