# CL-439 — The 2026-06-12 double batch CLOSED: CL-437 privacy + CL-438 F1/Act-mode (Cowork)

**Status:** Standing (batch record).

## CL-437 privacy batch
- **VT-379** #462 → dev `fc66400`: error-column write-redaction (3 writers through the
  redacting writer, registry-redacted `error` + `decision_rationale`), Detector-5
  extended, sentinel-guarded backfill. Gate: APPROVE-WITH-CONDITIONS (VT-386 rostered).
- **VT-382** #463 → dev `44f8e25`: outbox redact-on-terminal + `owner_message_audit`
  (mig-135). Two-cycle gate (BLOCK: stranded children under approval-resolved terminal
  batches → halt-children fix; narrow: sweep Leg 3 + the ruled doc correction). Fazal
  "accept": reconstructible capture (template-ref + SID + values; exact text recoverable
  via the pinned approved bodies). Never-sent no-capture = recorded policy.

## CL-438 F1 + Act-mode batch
- **VT-383** #464 → dev `87fcfd2`: the 10 Content SIDs wired lockstep; **ALL TEN
  Meta-APPROVED at canary time** (the MARKETING long pole evaporated); body_sha256 pins;
  drift vs the Cowork drafts = ZERO (Fazal submitted unedited; F4 counsel caveat applies
  to the live HI winback bodies). **CL-434 STOP #3 FELL.**
- **VT-384** #465 → dev `20a7106`: the L3 auto-send wire (Act mode for sends) — delivery-
  anchored config-driven hold, both-sided FOR-UPDATE CAS, money floor before arming, kill
  keywords + structural rule-order pin, demote re-arm via the real uuid5 derivation, L3
  daily cap. Gate BLOCK → the strong-arm fix: **owner opt-out/DSR now freezes autonomy +
  cancels armed holds (the "say STOP to turn this off instantly" promise is REAL)**;
  owner-level freeze approved. **CL-434 STOP #1 FELL.** The C2 centerpiece proves the
  remaining stop against the FULL wire (falsifiable). **Counsel C1–C3 is now the ONLY
  gate between the system and real customer sends.**
- **VT-385**: the Ask/Act generalization DESIGN approved as written (5-class taxonomy,
  per-tenant policy, precedence FLOOR > trip > class_override > mode > ladder, one-way
  sticky trip). Artifact: `.viabe/queue/VT-385/design.md`. **Build NOT dispatched** —
  awaits Fazal's grant (successor row; Act-offer template = an F1-class Meta cycle).

## Open after the batches
- **Fazal:** owner_phone on the founding dev tenant (arms the deferred live owner canary
  through the VT-384 arm path); counsel C1–C3 (THE last customer-send gate, + F4 wording
  risk on the live HI winbacks); VT-231 Mumbai; the VT-385-build grant when wanted.
- **Queued rows:** VT-386 (registry health), VT-387 (VT-262 retry window) — Cowork-
  sequenced, NEEDS no Fazal action.
- Process: recurring origin/dev refs-lock races (3×) = Cowork fetch polling vs CC pushes;
  Cowork adopted fetch-on-substantive-events-only.
