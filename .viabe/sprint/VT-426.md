---
vt_id: VT-426
title: trial_sweep hardening — wire notify_fn (trial-ending owner WhatsApp) + per-tenant language
status: Queued
priority: High
sprint: "Sprint-Fazal"
type: Feature
area: [Backend, Billing, Owner-Comms, Launch]
related: [VT-416, VT-393, CL-431]
exec_order: 95
created: 2026-06-25
last_updated: 2026-06-25
---

## Contract (Cowork GATE 2026-06-25, VT-416 sweep Rows C + D). LAUNCH gap. Build the wiring now; the trial-ending Meta SID is NEEDS-FAZAL (drops into .viabe/templates.md when provided).

The daily 7AM-IST DBOS trial sweep (`billing/trial_sweep.py` + `scheduled_triggers.py:246`) calls
`run_trial_evaluation_body` with NO `notify_fn` → `_default_notify` only LOGS → owners get NO trial-ending /
conversion-link WhatsApp. And it hardcodes `"en"` (`:159/161`).

## Scope
- **C — notify wiring:** wire `notify_fn` → `owner_surface.send_owner_template` (the VT-393 seam). Build the wiring;
  the owner-WABA **trial-ending Meta template SID is NEEDS-FAZAL** (Cowork requesting) — registry-driven via
  `.viabe/templates.md` (canonical name→SID; NO hard-coded SID, CL — template registry rule). Until the SID lands, the
  wiring is testable with a stub send_fn.
- **D — per-tenant language:** resolve language via `runner._load_preferred_language` (PR-3 added it) instead of the
  literal `"en"`.
- Canary: a sweep over a trial-ending tenant → `notify_fn` invoked with the owner's preferred-language template.

## Gate
Owner-comms (template send) → Cowork gate (send-adjacent). NO live send until the SID + a real onboarded tenant.

## Status history
- 2026-06-25: Rostered + ID allocated (Cowork VT-416 gate, Rows C+D). Build wiring; trial-ending SID NEEDS-FAZAL.
