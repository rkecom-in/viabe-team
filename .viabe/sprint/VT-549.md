---
id: VT-549
title: "B3-wiring 2 — finance lane pushback runs the manager decision loop (observe-only) + codify observe-only-rails"
status: In Review
priority: high
type: BUILD
area: agent-graph
created: 2026-07-01
authorized_by: fazal
authorization_basis: "Phase-1 overnight build (CC owns execution). Extends VT-548's B3-wiring to the 2nd real lane (finance) + codifies the observe-only-rails Standing decision the pattern now follows 3×."
---

# VT-549 — B3-wiring slice 2 (finance) + observe-only-rails ledger entry

## Built (0 migrations)
- **`finance_lane.finance_pushback`** now runs the same `observe_specialist_return` bridge as sales (VT-548): a real finance pushback → `decide_next_action` → `manager_decision` tm_audit row (observe-only, tagged `finance`). The pushback envelope is **byte-for-byte unchanged** (backward-compat); the manager decision loop now observes 2 live lanes.
- **`decisions-ledger.md` CL-2026-07-01-observe-only-rails** — codifies the two-step pattern the B3-wiring + OC1 rails now follow (observe-only slice records the would-be decision on the live path; enforcing flip is a later gated slice once the shadow data proves it safe). So future sessions understand why the flips are deferred, not missing.

## Verify
ruff clean; 21/21 (finance_lane incl the new wire-fires-tagged-'finance' assertion + envelope-unchanged + the bridge tests).

## NOT in scope
Only sales + finance expose a `push_back_to_manager`-class tool today (grep-confirmed) — the "9 lanes" goal completes as the other lanes gain pushback tools. The action-path live wire + the enforcing routing-steer flip remain (per CL-2026-07-01-observe-only-rails).
