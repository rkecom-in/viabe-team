---
id: VT-725
title: "O8 SERVING — wire card retrieval into the Manager's turn (shadow-first). The engine is built and nothing calls it."
status: In Progress — exit gates (b) per-tenant flip and (d) specialist narrowing PROVEN on dev 2026-08-09 (VT-725 CANARY PASS), unblocked by the measured floor. Retrieval is live: 100-card pool, 20 cards injected across the 6 O11 cases (was 0). Remaining gates: (a) real owner turn trace, (c) decision_evidence_links populated, (e) forced-failure degrade, (f) full-pack x3.
priority: Critical
owner: CC
exec_order: 135
created: 2026-08-03
created_by: clau
authorization_basis: "Fazal 2026-08-03: 'Finish everything, then we will promote everything to main.' Plus CL-2026-07-28-launch-with-rag (launch WITH the RAG — it is the moat) and CL-2026-07-28-manager-owns-memory."
---

# VT-725 — connect the knowledge engine to the Manager

## The gap this closes (audited 2026-08-03)
The O8 engine is COMPLETE and MERGED: `card_retrieval.py` (applicability filter, hybrid
retrieval, weighted ranking, independence-cluster diversity, per-tenant assignment overrides,
conflict surfacing, scoring traces), `admission.py`, `rollout.py`, `specialist_memory.py`,
twelve tables, 118 eligible cards.

**`grep` across `agent_graph/` returns ZERO references to any of it.** Nothing calls the
retrieval engine. The Manager cannot read a single card. O8 is built and unreachable — that is
the whole distance between "we have a moat" and "the moat does anything."

This row is the consumer. It was never rostered; that is a Clau miss, not a CC one.

## Scope — SHADOW FIRST, non-negotiable
1. **Retrieval call site at the Manager's turn.** The Manager's reasoning arc (understand →
   identify capability → define outcome → assemble framing context) gains a retrieval step when
   assembling framing context. Per-identity budget declared alongside the ACF manifest
   (o8 design §5.6): corpus domains, layers, top-k, token budget.
2. **Mode `shadow` first:** cards are retrieved, scored and LOGGED — **never injected into the
   prompt**. This produces the attribution substrate with zero behavioural risk. The flip to
   `active` (injection) is a SEPARATE step gated on the baseline comparison and on Fazal (D3).
3. **Assignment resolution honoured at retrieval:** global `knowledge_cards.default_assignment`
   overlaid with the per-tenant `knowledge_card_assignments` row. This is the flip mechanism
   Fazal ratified — `manager_global` | `manager_tenant` | `specialist:<agent>` | `disabled`,
   evaluated per retrieval, changeable at runtime without a deploy.
4. **`decision_evidence_links` populated** on every retrieval: which card versions were
   retrieved / selected / rejected, for which run and decision. This is the causality substrate
   §6 needs for ablation — without it, demotion can never be evidence-based.
5. **Specialist retrieval is narrow by construction** (§5.7): a specialist gets only its lane's
   assigned cards plus its thin tenant/task customisation memory. It never gets the Manager's
   breadth. The Manager gets specialist conclusions and cross-functional evidence, not deep
   domain corpora.

## Boundaries — the ones that must not bend
- **Retrieval is ADVISORY TO REASONING and NEVER authorizes an effect.** No customer send, no
  money action, no consent change, no gate bypass — deterministic gates and Pillar-7 remain the
  sole effect authority (ARCHITECTURE §0.1.1). A retrieved card that says "offer a discount"
  does not make a discount sendable.
- **No injection in this row.** Shadow only. If the flip to active is tempting because shadow
  looks good, that is exactly the reasoning the baseline exists to prevent.
- **Retrieval failure must be silent-safe:** a retrieval error degrades to no-cards, never to a
  failed turn. The Manager worked without cards for months; it must still.
- **No card content in logs beyond ids + scores** (`decision_evidence_links` is PII-free by
  construction; keep it that way).
- Brain-touching → full-pack ×3.

## Exit gate
On deployed dev in shadow: (a) a real owner turn produces a retrieval trace with non-zero
candidate cards and a recorded top-k; (b) a per-tenant assignment override demonstrably changes
what is retrieved for that tenant and not for another — **canary, proving the flip works at
runtime**; (c) `decision_evidence_links` rows land with retrieved/selected/rejected populated;
(d) a specialist dispatch retrieves ONLY its lane's cards — proven, not asserted; (e) forced
retrieval failure degrades to no-cards with the turn completing normally; (f) full-pack ×3 shows
no new failure class — expected, since nothing is injected; a regression here means something is
leaking into the prompt that should not be.

## FLOOR RECALIBRATION — the derivation, recorded here as Clau required (2026-08-09)
Fazal authorized it verbatim: **"Recalibrate."** (relayed 2026-08-07T12:00Z). The requirement was
that the floor "remain a measured claim anyone can re-derive", so the method and the numbers live
here and in `apps/team-orchestrator/canaries/floor_calibration/` (scripts + `labels.json`).

**Why it was necessary.** The floor was `0.62`. Measured across **600 (case, card) pairs** on the
real 100-card dev corpus with real Voyage embeddings, the **single highest-scoring card scored
0.2867**. The bar sat 2.2x above the best attainable score — the corpus was inert by arithmetic, not
by any judgement about the knowledge in it. Gate (b) below could never have passed.

**Labels.** Every card in all 6 O11 cases labelled relevant/irrelevant **blind**: the labeller saw
the case's agent view only (never `acceptable_characteristics`, never the target answer), never a
score/rank/card-id, with card order shuffled per pass. 3 passes, majority vote.

| case | split | relevant | unanimous | pairwise agreement |
|---|---|---|---|---|
| dev-compliance-gstr-readiness | dev | 10/100 | 95/100 | 0.58–0.83 |
| dev-finance-cashflow-wholesaler | dev | 11/100 | 96/100 | 0.69–0.83 |
| dev-sales-dormant-boutique | dev | 14/100 | 91/100 | 0.59–0.81 |
| val-d2c-returns-growth-tradeoff | val | 19/100 | 93/100 | 0.78–0.82 |
| val-restaurant-festival-capacity | val | 21/100 | 88/100 | 0.61–0.72 |
| val-tailor-price-increase | val | 10/100 | 89/100 | **0.50–0.53** |

**Distributions.** Pooled development **AUC = 0.753** (chance 0.5) — the scorer does discriminate.
Per case it runs 0.689–0.813 on dev and 0.599–0.686 on validation. The weakest AUC
(`val-tailor-price-increase`, 0.599) is also the weakest label agreement; those two cannot be
separated from this data and neither is being blamed.

**Chosen floor: 0.250.** Swept on DEVELOPMENT only; VALIDATION held out and never used to pick.
Precision measured on cards actually INJECTED (floor, then top_k), because injected cards are what
reach the Manager.

| floor | dev precision | val precision (held out) | cases retrieving |
|---|---|---|---|
| 0.000 (no floor) | 0.375 | 0.174 | 3/3, 3/3 |
| 0.245 | 0.471 | — | 3/3 |
| **0.250** | **0.533** | **0.500–0.600** | 3/3 dev, 2/3–3/3 val |
| 0.255 | 0.538 | 0.500 | 3/3 dev, 2/3 val |
| 0.265 | 0.800 (n=5) | — | 2/3 dev |
| 0.290 | nothing retrieves | nothing retrieves | 0/3 |

The mandate was to bias the margin toward precision. Above 0.255 the apparent precision gain rests
on n<=5 while whole cases silently go dark — the exact failure the bias exists to prevent.

**Honest limits.** Recall at 0.250 is **0.229** — that is the RANKING, not the floor, and a floor
cannot promote a card the scorer ranked 40th. **9 of 35** dev relevant cards are hard-excluded by
applicability before scoring. Specialists inherit the Manager's number (their 0.58 was the same
unreachable guess); it is borrowed, flagged, and needs its own lane measurement.
**Voyage is non-deterministic** (+/-0.0019 semantic across identical runs), which is enough to flap
one held-out case whose best card sits at 0.2500–0.2505; that card is labelled irrelevant, so val
precision *rises* when it drops out.

**Coupling guard.** `card_retrieval.SCORE_WEIGHTS` is pinned by
`tests/orchestrator/knowledge/test_o8_floor_calibration.py`. A weight or metric change now fails
with a pointer to the re-derivation instead of leaving a floor fitted to a scale that no longer
exists — which is precisely how 0.62 rotted unnoticed.

## What comes after (NOT this row)
The `active` flip (injection), measured against the sealed baseline. That is D3, Fazal's, and it
needs the treatment run to beat the frozen no-O8 baseline on the sealed set before it is even
askable.
