---
id: VT-565
title: "B2 manager_tasks LIVE PRODUCER — every live run creates + tracks durable task/step state"
status: In Progress
priority: launch-blocker
type: BUILD
area: manager
created: 2026-07-02
authorized_by: fazal
authorization_basis: "Fazal 2026-07-02 via Cowork 20260702T163000Z: close the flywheel loop-closure gaps BEFORE the drill — gap 1."
---

# VT-565 — B2 spine live producer

## Problem (vision-audit finding, verified)
`task_store.create_task`/`add_step` have ZERO production callers — `manager_tasks`/`manager_task_steps` are never written on a live run. The whole B2/B6 stack (VT-557 retry ladder, VT-560 wake, redrive endpoint, dead_letter alerts) operates on an empty table. "Every live run must produce durable task/step state under the canonical hierarchy."

## Scope
Wire the live dispatch flow to mint a `manager_task` per delegated objective and `manager_task_steps` per specialist delegation, with status transitions at the real seams (delegated → running → done/blocked) — STATE TRACKING ONLY. Routing enforcement (record_decision / MANAGER_ENFORCE_ROUTING) stays observe-only per CL-2026-07-01-observe-only-rails; this row does NOT flip it. Fail-soft: task-state writes must never break dispatch.
