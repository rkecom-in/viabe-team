---
id: VT-603
title: "SECURITY: integration_agent + onboarding_conductor take model-supplied tenant_id — context-derived tenancy + kill the privileged-pool cross-tenant write"
status: Done
priority: launch-blocker
type: BUILD
area: manager
created: 2026-07-05
authorized_by: fazal
authorization_basis: "Fazal 2026-07-05 loop-program grant. Expert review finding, CC-verified: VT-599 covered only *_lane.py; the two LIVE specialists still trust model tenant_id. Worst: integration_agent.py setup_recurring_ingestion_stub (L204-242) writes tenant_connector_status via get_pool() — the BYPASSRLS pool — keyed purely on the model's string: a genuine cross-tenant WRITE with zero RLS backstop."
---
# VT-603 — close the live tenant-trust gap NOW (standalone, ships before the loop program)
Scope: apply resolve_lane_tenant semantics (agent/lane_tenant.py) to EVERY tenant_id-taking @tool
in integration_agent.py and onboarding_conductor.py (ambient context always wins; model value
observed never trusted; None → structured tool_error, never a raise). setup_recurring_ingestion_stub:
replace the raw get_pool() write with tenant_connection(resolved_tenant) (RLS-scoped) — no raw
privileged-pool writes keyed on any model value, ever. Tests mirror test_marketing_lane_tenant_scope:
name-instead-of-uuid, foreign-real-UUID (context wins + mismatch warn), no-context (structured error),
+ an adversarial test proving the recurring-ingestion write CANNOT land on a foreign tenant. Exec order 1.

## RESOLVED 2026-07-05 — landed bcb623b, pushed (standalone security checkpoint)
All 5 integration_agent tools + both onboarding_conductor tools resolve tenant from the run
context (VT-599 pattern); setup_recurring_ingestion_stub moved off the BYPASSRLS pool onto
tenant_connection (RLS verified present, migration 034). 19 new tests incl. the adversarial
foreign-UUID write test with get_pool monkeypatched-to-raise (raw pool proven dead on the path).
Suites green (7 known local-only failures unchanged); isolated smoke green; mypy --strict clean.
Downstream shopify raw-pool writes flagged → folded into VT-608 (defense-in-depth; tool-boundary
trust closed here).
