[2026-05-26T13:33:00+05:30] PICKUP: VT-175 brief-ready (critical-path schema + day-39 evaluator + attribution-close). Sprint 1 exec 4.9. Unblocks VT-176. Rule #16 compliant (22 cl_decisions_checked).
[2026-05-26T13:34:00+05:30] STEP-0 git: HEAD=74ff0a1 (VT-174 just merged). On main. Latest migrations: 022_tenant_cost_views.sql.
[2026-05-26T13:35:00+05:30] STEP-0 RLS pattern audit (KEY FINDING): brief example RLS policy uses `current_setting('app.tenant_id')::uuid` GUC, but existing 20+ tables use `app_current_tenant()` helper which reads `app.current_tenant` GUC (000b_rls_helpers.sql). `tenant_connection()` wrapper sets `app.current_tenant` not `app.tenant_id`. Brief's example would NOT work with current substrate. Plan must use `app_current_tenant()` (matches every other table). CL-82's spirit is "RLS via session GUC" — same architecture; just the helper-function form.
[2026-05-26T13:36:00+05:30] STEP-0 deps: VT-28 ✓ (05837e3), VT-171 ✓ (39ea42c), VT-104 ✓. customers table NOT in repo (per brief — customer_id NULLABLE; VT-170 follow-up). billing/ directory doesn't exist — will be NEW.
[2026-05-26T13:37:00+05:30] PLAN: wrote .viabe/queue/VT-175/plan.md. ~95K / 100 min (well under 180K ceiling). 7 risks; 2 plan-ready Qs (GUC convention `app_current_tenant()` vs brief's `app.tenant_id` literal; CI gate extension to billing/). Status flipped planning→review.
