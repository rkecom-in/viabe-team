---
# DESIGN (2026-07-18, post-×3 evidence — the latency tail is step-mapped)
# EVIDENCE: serial ×3 on d9a4e10 — j01 explicit-send-confirm leg TIMEOUT 3/3 (reply late, content
# passes on settled transcript); one late step each j02/j06/j08; j07 2of3. Fazal's real-tenant
# 81-min approval→execution gap = the dead-letter-redrive cadence, same theme.
# ROOT CAUSE: resolution NEVER WAKES the waiting consumer. Both owner-wait loops
# (workflow.py:1133 ask_owner, :1189 paused_approval) POLL on the VT-633 ladder (15s×40 then 300s);
# the approval/answer lands between ticks → the ack/execution trails by up to a full interval.
# FIX (wake-on-signal, ladder becomes fallback — ZERO money-mechanics change):
# 1. Both loops: DBOS.sleep(interval) → DBOS.recv(topic="owner_signal",
#    timeout_seconds=interval). Message content DISCARDED — a wake HINT only; the DB condition
#    (_approval_still_pending / _question_still_open) stays sole authority. Replay-safe (recv is
#    DBOS-recorded).
# 2. Resolution seams send the hint (best-effort, never-raise):
#    - approval_resume.mark_approval_resolved → DBOS.send(<live wf id>, "resolved", topic)
#    - the correlate_reply answer flip → same send (its row knows task_id).
# 3. THE WRINKLE — redriven workflows have SUFFIXED ids (VT-668 mechanics #2), so the base
#    manager_task_workflow_id() can MISS. Stamp the LIVE DBOS.workflow_id at park time
#    (_park_task_awaiting_approval / ask_owner entry) into a readable slot (prefer an existing
#    JSONB on the task/step row — NO migration unless nothing writable exists), and the senders
#    read THAT. Fallback: base id when no stamp (pre-fix rows).
# 4. Tests: recv-woken fast path; timeout-fallback path preserved; redrive-suffix id delivery;
#    replay determinism.
# NON-GOALS here: the j02/j06/j08 single-late-step turns that are plain BRAIN latency (not
# wait-loop) — measure separately after this lands; may shrink to nothing once acks wake fast.
id: VT-671
# BUILT 2026-07-18 (pending re-measure): recv-with-timeout in BOTH owner-wait loops +
# wait_workflow_id stamp at park + wake-send at mark_approval_resolved (any decision).
# CLOSE gate: re-drive j01 x3 — the send-confirm leg must land in-window.
# RE-MEASURE VERDICT (a57514b, j01 x3): WAKE PROVEN — the send-confirm/wait-loop leg landed
# in-window 3/3 (was 3/3 TIMEOUT); r3 = first-ever full 4/4 j01 pass. The residual timeout MOVED
# to the SR plan-DRAFT phase (run_status=running at window edge = raw specialist-LLM chain
# duration, 2/3) — the named non-goal. ROW RE-SCOPED: wait-loop latency = DONE; remaining scope =
# brain-turn duration on the SR draft (+ the j02/j06/j08 single-late-step class, likely same).
# Levers: prompt diet / tool-chain count / model tier / #84 batching — measure-first.
title: SR campaign-turn latency + j02 persona-variance — the reliability tail behind a clean j02x3 (VT-667 follow-on)
status: open
priority: Medium
owner: unassigned
created: 2026-07-18
found_by: claudecode (VT-667 fix-4 dev validation)
authorization_basis: "VT-667 Option-A follow-on (Fazal 2026-07-18): core money goal done; this is the reliability tail to a clean j02x3 Tier-1=0. Not money-path."
---

# VT-671 — the reliability tail to a clean j02x3

VT-667's money-content fix is DONE + proven (brief→draft, honest send, no deflection). The clean j02x3 Tier-1=0 gate is blocked by NON-money reliability:
1. **Turn latency / timeouts**: the SR campaign turns (esp. the fix-4 revision: cancel loop + supersede + fresh SR proposer) exceed the harness 150s. On WhatsApp = the owner waits too long for a draft. Investigate SR proposer latency (model tier / tool round-trips) + whether the revision re-dispatch can be lighter (the start_manager_task_workflow is async, so the SLOW part is the SR loop itself — profile it).
2. **SR step-0 intermittent decline** ("I couldn't complete it on my own — stopped"): a first-attempt SR failure on some runs; find the trigger.
3. **Harness mechanics**: the inline drive chain must use the exit-hang WATCHDOG (kill runner after report parses — like scratchpad/postcutover3c/drive_pack.sh) so a x3 doesn't hang; use --timeout 240 for the revision-heavy j02.
4. **Persona variance**: j02's persona flips correction/cooperative/decline each run; the ×3 gate is noisy — consider a pinned-variant j02 or a larger N for a stable read.
Gate: watchdog'd j02 (240s) ×3 → tier_rescore Tier-1=0; the revision turn under a reasonable latency budget.
