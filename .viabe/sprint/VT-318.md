---
vt_id: VT-318
title: Inbound consumer STOP handler + opt-out immediate effects (gate-live)
status: Blocked
priority: High
sprint: Sprint 7 - Knowledge Architecture
type: Feature
area: [Privacy, Specialist Agent, Orchestrator]
assignee: unassigned
parent: VT-76
sub_items: []
exec_order: 9.7
branch: ""
version: "v1.0"
last_updated: 2026-06-04T03:25:00Z
blocked_by: "Live customer-inbound path (WABA) — customer STOP messages can't arrive until then"
---

# VT-318 — inbound consumer STOP handler + opt-out immediate effects

Split out of VT-76 (Cowork ruling 20260604T032500Z). VT-76 builds the
reconstitution SWEEP + SLA + columns NOW (the privacy mechanism over the VT-66
hook). This row is the RECEIVE side — it needs the live customer-inbound path
(WABA), so it's gate-live (same posture as the rest of customer-comms).

## RE-SCOPE (Cowork 2026-06-05): the STOP classifier is NOT a gap.
`customer_inbound.py` (VT-287) ALREADY classifies STOP / opt-out keywords
(`_OPT_OUT_KEYWORDS` ← `opt_out_keywords.yaml`) and calls `consent_service.opt_out`
— always honored, never lost. So what actually REMAINS for this row: the
**live-WABA delivery path** (so STOPs can arrive at all), the **per-tenant opt-out
scoping** (a phone may belong to several tenants), the **immediate-effects wiring**
(#3), and the **opt_out_confirmation / reconstitution_completed templates** (#4).
Do NOT re-build the classifier.

## What to produce (when WABA/customer-inbound is live)
1. **Inbound STOP classifier** — keyword detection DONE in VT-287
   (`customer_inbound.py`). REMAINING here: per-tenant scoping when resolving the
   sender phone → customer_id (a phone may be a customer of several tenants;
   opt-out applies per-tenant by the prompting listing/campaign; ambiguous → all
   tenants, conservative). Only the multi-tenant resolution is new.
2. **Set opt-out:** `customers.opt_out_status='opted_out'`, `opt_out_at=now()` for
   the affected tenant(s). (The columns + the 7-day sweep land in VT-76.)
3. **Immediate effects:** campaign send-skip (partly done, mig 045 — confirm the
   send gate checks opt_out_status); `query_customer_ledger` `exclude_opted_out`
   default; pending clarifications/approvals auto-resolve with
   `decision='customer_opted_out'`.
4. **Templates:** `opt_out_confirmation` + `reconstitution_completed` — free-form
   interim (VT-272/Meta), same as VT-303.

## Dependencies
- VT-76 Done (the opt_out columns + the reconstitution sweep + SLA).
- Live customer-inbound path (WABA) — the blocker.

## Acceptance criteria
- Inbound STOP → opt_out set + confirmation sent; Hindi/mixed recognized.
- Cross-tenant phone → conservative all-tenant opt-out when ambiguous.
- Active campaign skips the opted-out customer; ledger excludes; clarifications auto-resolve.

## Status history
- 2026-06-04 03:25Z: rostered + Blocked (Cowork ruling 20260604T032500Z split it from VT-76). Allocated up-front (CL-424). Blocked on the live customer-inbound path (WABA).
