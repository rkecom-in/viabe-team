---
vt_id: VT-220
title: Brief cleanup — replace TEAM_ORCHESTRATOR_BASE_URL with TEAM_ORCHESTRATOR_URL in VT-218 sprint brief; doubles as deploy-trigger PR
status: Done
priority: Low
sprint: Sprint 2 - Integration Agent (re-anchored)
type: Hygiene
area: [Documentation, DevOps]
assignee: Claude Code
parent: VT-218
sub_items: []
exec_order: 14
branch: "feat/vt-220-orchestrator-url-brief-cleanup"
version: "v1.0"
last_updated: 2026-05-28T19:15:00+05:30
---

# VT-220 — TEAM_ORCHESTRATOR_URL brief cleanup + deploy trigger

## Why this row exists

The VT-218 sprint brief (`.viabe/sprint/VT-218.md` line 78) catalog uses `TEAM_ORCHESTRATOR_BASE_URL` in the Vercel env-var matrix. That name doesn't exist in the codebase — the actual env var is `TEAM_ORCHESTRATOR_URL` (used in `lib/orchestrator-client.ts` + `app/api/ops/resolve-phone/route.ts`). The runbook (`docs/clau/dev-env-runbook.md`) is already correct.

Cleanup costs nothing and doubles as a deploy-trigger PR: Fazal asked Cowork to ship something to `main` so Railway + Vercel re-auto-deploy with the latest infrastructure substrate fully exercised.

## Scope

1. **`.viabe/sprint/VT-218.md` line 78** — replace `TEAM_ORCHESTRATOR_BASE_URL` with `TEAM_ORCHESTRATOR_URL`. Single-line edit.
2. **Sweep**: grep entire repo for `TEAM_ORCHESTRATOR_BASE_URL`. If anything else turns up (Cowork's prior dispatches in `.running/processed/`, anything in docs, etc. — DO NOT touch signal archives, only live docs/code), fix it.
3. **No code changes expected** — this is doc-only.

## Acceptance criteria

1. `grep -rn "TEAM_ORCHESTRATOR_BASE_URL" .viabe/ docs/ apps/` returns zero (excluding `.running/processed/` historical signals)
2. PR merges cleanly + triggers Railway + Vercel re-deploy
3. Railway redeploys without code change → smoke check post-deploy: `curl https://vt-orchestrator-service-development.up.railway.app/health` returns 200 (Fazal verifies)
4. Vercel redeploys + this time the commit author is rkecom-in (per the git identity fix landed earlier) → preview deploy passes Vercel's GitHub-account check

## Rule #15

No new canary — this is doc-only. Verification = the deploy-trigger ACs above.

## Out of scope

- Rewriting historical signals under `.running/processed/`
- Renaming the actual `TEAM_ORCHESTRATOR_URL` env var
- Anything beyond the one-line brief + any straggler doc references

## Dependencies

- VT-219 ✓ (related env-var cleanup)
- VT-218 ✓

## Branch & PR

- Branch: `feat/vt-220-orchestrator-url-brief-cleanup`
- PR title: `docs: replace TEAM_ORCHESTRATOR_BASE_URL with TEAM_ORCHESTRATOR_URL in VT-218 brief (VT-220)`

## Status history

- 2026-05-28 19:15 IST: filed by Cowork per Fazal request for a deploy-trigger PR
