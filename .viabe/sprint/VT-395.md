---
vt_id: VT-395
title: OTP channel — WhatsApp-primary + SMS-fallback via Twilio Verify (enable post-DLT)
status: Backlog
priority: Medium
sprint: "Sprint-Fazal"
type: Feature
area: [Infrastructure, Frontend]
assignee: Claude Code
related: [VT-326, VT-250, VT-394]
exec_order: 70
created: 2026-06-13
last_updated: 2026-06-13
---

## Contract (Fazal 2026-06-13 — decisions made)

Two Fazal rulings to land once the external dependency clears:
1. **Opt for Twilio Verify** (already the built path) — provision the Verify Service, set
   `TWILIO_VERIFY_SERVICE_SID` on Railway, turn OTP mock off.
2. **WhatsApp primary, SMS fallback** — enable the already-built SMS channel
   (`VT250_SMS_CHANNEL_ENABLED=1`) + wire Verify channel-fallback (WhatsApp → SMS on
   delivery failure / user request); instrument which channel delivers.

**Blocked on (Fazal, external):** Airtel DLT registration — Entity ID (have) + **Header /
sender registration (Fazal paying the PE registration charge now)** + an approved **OTP
Content Template**, linked to Twilio. The SMS leg cannot go live until those are active.
(The "validity-extension" pack + Business Name Display are NOT this — see CL note.)

## Acceptance
- Verify Service provisioned; mock off; real WhatsApp OTP delivers.
- Once DLT Header + OTP template are approved + Twilio-linked: SMS fallback fires when
  WhatsApp delivery fails; per-channel delivery instrumented.

## Notes
- Pairs with VT-394 (endpoint rate-limit): Verify's native per-number + brute-force is the
  abuse backstop; VT-394 is only the per-IP/enumeration/cost cap.
- Awaits Fazal: DLT completion + the Verify-provisioning grant before dispatch.

## Status history
- 2026-06-13: Rostered (Cowork). Fazal proceeding with Airtel DLT Header registration payment.
