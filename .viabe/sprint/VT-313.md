---
vt_id: VT-313
title: L4 skill corpus authoring (≥30 docs) — Fazal/Clau content deliverable
status: Done
pr: 309
reconciled: "2026-06-04 Done — 69-doc seed merged (#309); CI live-seed→retrieve canary PASSED at tier-1 (test_live_seed_and_retrieve, the acceptance gate — agent retrieves a real note in a live dispatch)"
priority: High
sprint: Sprint 7 - Knowledge Architecture
type: Documentation
area: [Knowledge Architecture, Content]
assignee: fazal+clau
parent: VT-70
sub_items: []
exec_order: 7.5
branch: ""
version: "v1.0"
last_updated: 2026-06-04T01:30:00Z
blocked_by: "Human authoring (Fazal + Clau) — NOT a code task"
---

# VT-313 — L4 skill corpus authoring (≥30 docs)

Split out of VT-70 (Cowork ruling 20260604T013000Z, Call 1). VT-70 ships the L4
**pipeline** (schema + `l4_seed.py` + retrieval + MCP tool + Composer wire +
canary) with a small SYNTHETIC placeholder corpus. This row is the **real
domain-knowledge corpus** — authored by Fazal + Clau, NOT generated by CC
(Pillar 4/5: domain wisdom is authored, not computed; fabricating it is worthless
and a violation).

## NOT a code task
The ≥30 markdown docs are a human-authoring deliverable. They load via VT-70's
`l4_seed.py` (already built) — no further code needed unless authoring surfaces a
schema gap. Cowork is flagging the dependency to Fazal directly.

## What to author (≥30 docs, per the VT-70 spec §2)
- Cohort selection patterns (5+): which dormancy bands convert; F&B vs retail vs services; seasonal timing.
- Message tone for Indian SMB (5+): formal vs friendly Hindi/English; festival framing; family-business voice.
- Pricing/discount discipline (5+): when discounts work, free-item vs no-incentive.
- Attribution interpretation (5+): reading low-attribution cases; channel caveats.
- Failure modes (5+): campaigns that backfire; fatigue; when NOT to campaign.
- Founding-tier-specific (5+): early-stage vs mature tenant differences.
- Each doc 200–1500 words; markdown in `apps/team-orchestrator/data/l4_seed/*.md` (final path per VT-70 build); reviewed by Fazal before commit.

## Dependencies
- VT-70 Done (the pipeline + seed script + schema + the embedding path).

## Acceptance criteria
- ≥30 reviewed docs committed to the seed dir.
- `l4_seed.py` loads them → ≥30 rows in `l4_documents` with real voyage embeddings.
- Retrieval canary (real corpus): a domain query returns the relevant authored docs in top-k.

## Launch gating
- Gated to before launch (Reports-Jun15 / VT-231). The agent runs without L4 priors until the corpus loads (empty-but-live Composer marker — VT-70).

## Status history
- 2026-06-04 01:30Z: rostered + Blocked (Cowork 20260604T013000Z split it from VT-70). Allocated up-front (CL-424). Blocked on Fazal+Clau authoring (content, not code).
