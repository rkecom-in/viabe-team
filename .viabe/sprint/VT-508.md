---
id: VT-508
title: "Deployed-version stamp (web sha+time | api sha+boot) bottom-left of /team + /team/signup — Fazal's deploy ground-truth"
status: In Progress
priority: Critical
type: BUILD
area: team-web
created: 2026-06-30
authorized_by: fazal
authorization_basis: "Fazal 2026-06-30: 'show a version number at bottom-left so we know what version is running and if changes deployed.' Deploys have been flaky (env-change-needs-redeploy); Fazal needs a glance instrument."
---
# VT-508 — deployed version stamp (do FIRST; the diagnostic instrument)
team-web: a small fixed bottom-left build-info on /team + /team/signup: `web <short-sha> · <build-time>` via NEXT_PUBLIC_BUILD_SHA (VERCEL_GIT_COMMIT_SHA, build-time). orchestrator: /version (or extend /health) → {git_sha, booted_at}; the page fetches it + shows `api <short-sha> · <boot-time>` next to the web stamp. So bottom-left = `web abc123 · 19:50 | api def456 · 19:42`. Every deploy bumps it.
