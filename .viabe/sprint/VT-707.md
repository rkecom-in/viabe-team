---
id: VT-707
title: O8-WP4 — quarantined ingestion pipeline + candidate creation (Codex)
status: Granted (build order AFTER VT-705 harness skeleton; ingestion outputs are candidates only)
priority: Medium
owner: Codex
exec_order: 102
created: 2026-07-22
created_by: clau
---

# VT-707 — Ingestion pipeline (Codex WP4)

Per o8 design v0.2 §3 + Codex plan §4. Acquire → verify rights → hash/dedupe → quarantine raw
→ tool-less structured extraction → schema validation → normalize claim/applicability → PII
redaction → cluster common origins → embed → candidate registry.

## Binding restrictions (from the spec)
Raw external text never retrieval-eligible · extraction tool-less/side-effect-free · source
text cannot self-assign authority/confidence/applicability · taint persists · paid/restricted
material only under recorded UsageRights · regulatory cards require jurisdiction + effective
dates · T4 stays research-only until independently corroborated.

## FIRST INPUT (rights-check before anything): the existing `archives/business-knowledge/`
extracted corpus (120+ docs) — enters as CANDIDATES through this pipeline only, never
bypasses admission; usage-rights review is step zero on it.

## Exit gate: adversarial-ingestion + real-source canaries pass (one official source, one
archived research doc, one adversarial forum-style source; fetch/embedding failures FAIL the
canary, never skip). Egress-requiring canaries: hand the run to CC (Codex flags, CC executes).
## Note: persistence lands against CC's VT-708 registry — coordinate schemas via the VT-706
contracts, do NOT invent a parallel store.
