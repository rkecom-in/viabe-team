---
vt_id: VT-259
title: Per-path CI job gating (skip web jobs for orchestrator-only PRs + vice versa)
status: Done
priority: Medium
sprint: Sprint 1.5 - Hardening
type: Chore
area: [CI, Cost]
assignee: Claude Code
parent: null
sub_items: []
exec_order: 99
pr: 197
---

# VT-259 — per-path CI job gating (Actions-diet)

Biggest GitHub-Actions minute saver: gate each ci.yml job on changed paths
(dorny/paths-filter) so an orchestrator-only PR skips the web jobs (lint/
typecheck/vitest/playwright) and a web-only PR skips orchestrator/migrations.
~halves jobs on single-app PRs. Plan: `.viabe/queue/VT-259/plan.md`.
APPROVED by Cowork, HOLD build for Fazal prioritization (Cowork leans VT-260
first). Medium risk (touches ~20 job ifs); conservative defaults (shared/ci →
run all). Branch-protection caveat: gated jobs vs required-checks-by-name —
safe today (checks OFF) but flag before re-enabling required checks.
