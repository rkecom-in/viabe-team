---
id: VT-529
title: "B6 (slice) — orphaned_task alert: the stalled-task reaper surfaces reaped tasks to ops"
status: In Review
priority: launch-blocker
type: BUILD
area: observability
created: 2026-07-01
authorized_by: fazal
authorization_basis: "Phase-1 overnight build (CC owns execution). B6 recovery/safety/ops — first slice (the no-orphan alert), completing the B2 stalled-task work + the VT-547 alert arc."
---

# VT-529 — B6 slice: orphaned_task alert

## Gap
B2 added `reap_stalled_manager_tasks` (flips a task stranded active with no runnable step → `blocked`), but the reap was SILENT — the `orphaned_task` detector the B6 no-orphan requirement calls for did not exist (recon: `silent_terminal`/`orphaned_task`/`rail_bypass` all absent from `alerts/triggers.py`). A hung task got quietly parked with no human told.

## Built (0 migrations)
- Added `orphaned_task` (severity `warning`) to the `alerts/triggers.py` TriggerKind taxonomy.
- `reap_stalled_manager_tasks` RETURNING now carries `tenant_id`; on any reap it fires ONE `orphaned_task` alert per reaped task via the existing `dispatch_alert` pipeline (tenant-scoped, dev-routed so a dev/canary tenant never pages Fazal). Fail-soft per alert AND on the alerts import — an alert failure can never break the reaper (which must never block boot).

## Verify
ruff clean; dep-less `orphaned_task` in the taxonomy; 10/10 task_store — reaping a stalled task fires an `orphaned_task` warning alert carrying the reaped task_id (spied `dispatch_alert`); the existing reaper behaviour (blocked + stall_metadata, healthy-task protected) unchanged.

## NOT in scope (remaining B6 slices, noted)
Periodic reaper scheduling (today boot-only — a runtime stall waits for the next boot; wire into the 5-min DBOS scheduler). Retry budgets — `backoff.py`/CircuitBreaker exist with NO live caller → wire into a real retry path. Dead-letter generalization (Razorpay-only today). Global customer-SEND freeze-kill + per-function/per-campaign freeze + reviewer takeover. The `silent_terminal` detector (needs the run terminal-outcome enum — B1-part-2b, touches load-bearing `pipeline_runs`) + `rail_bypass`.
