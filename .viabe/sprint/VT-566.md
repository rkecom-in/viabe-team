---
id: VT-566
title: "Flywheel read-back — lessons/corrections/outcomes retrieved into the manager's next-run context (C3 controls; Decision C weighting)"
status: In Progress
priority: launch-blocker
type: BUILD
area: learning
created: 2026-07-02
authorized_by: fazal
authorization_basis: "Fazal 2026-07-02 via Cowork 20260702T163000Z: close the flywheel loop-closure gaps BEFORE the drill — gap 2 + Decision C (implicit-feedback-weak-signal)."
---

# VT-566 — read-back closes the flywheel

## Problem (vision-audit findings, verified)
Capture is built (VT-561 trainable pairs, VT-563 outcomes, VT-556 directives) but NOTHING reads lessons back: `agent_corrections` write-only (sole reader = DSR purge), `owner_feedback` never read, `upsert_learned` zero callers. A captured lesson cannot influence the next decision — the flywheel doesn't turn.

## Scope
Retrieve captured corrections + outcome signals into the manager's dispatch context on the next run, under the C3 contamination controls (tenant-scope, provenance, authority, retrieval gating) and **CL-2026-07-02-implicit-feedback-weak-signal**: explicit human corrections = authoritative weight; implicit outcome rows (`owner_feedback.tier='implicit'`) = down-weighted, distinctly-tagged context that can never read as correction-grade. Config-gated like MANAGER_MEMORY_RETRIEVAL (observe-first posture). PROOF on deployed dev: a captured lesson demonstrably present in a subsequent run's context (GETS audit / rendered block).
