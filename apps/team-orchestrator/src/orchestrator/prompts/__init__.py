"""System-prompt assets for the orchestrator (VT-3.9).

Holds prompt text as ``.md`` files — no code. Kept a package so the prompt
files are bundled with the wheel and resolvable via ``Path(__file__)``.
"""
