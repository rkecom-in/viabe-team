---
vt_id: VT-431
title: Wire register_agent_coordinator() into main.py — the autonomous dispatch loop (dark on dev)
status: Done
priority: Critical
sprint: "Sprint-Fazal"
type: Feature
area: [Backend, Agents, Orchestration, Launch]
related: [VT-421, dev-activation]
exec_order: 100
created: 2026-06-25
last_updated: 2026-06-25
---

## Contract (Cowork GATE 2026-06-25, dev-activation Top-3 #1 BLESSED — the headline fix). Risk(activation) → Cowork gate (wants the diff). Caveat CLEARED.

`register_agent_coordinator()` (`agents/coordinator.py:816`, applies `@DBOS.workflow`+`@DBOS.scheduled`) is NEVER called
in `main.py` → the core autonomous dispatch loop never runs on dev. This is why "nothing's triggering" autonomously.

## Scope
- Call `register_agent_coordinator()` at app startup in `main.py` (where DBOS workflows/schedules register).
- **Caveat CLEARED:** the coordinator's `AGENT_REGISTRY` (coordinator.py:135) dispatches the REAL
  `agents/sales_recovery_executor.SalesRecoveryAgent.execute_item` — NOT the `agent/sales_recovery.py:28` placeholder
  (a separate supervisor-path module; still live-imported, not dead, but not the coordinator's dispatch target).
- **Verify no double-dispatch:** the coordinator (scheduled detect→draft→arm) and the supervisor
  (`run_sales_recovery_agent`, live inbound-message routing) are DISTINCT triggers — confirm the wiring doesn't make
  both fire the same SR work.
- **Downstream customer sends STAY gated** (VT-421 onboarded-gate + consent + no-send-until-Fazal-tenant). Activation =
  the dispatch runs; sends remain feature-gated.
- Canary: the scheduled coordinator registers + fires on dev (a sweep tick runs; 0 sends for non-eligible/no tenant).

## Gate
RISK (activation — the autonomous loop). Build → Cowork gate (diff review), NOT self-merge.

## Status history
- 2026-06-25: Rostered + ID allocated (Cowork dev-activation Top-3 #1, BLESSED). Caveat cleared (real executor, not placeholder).
