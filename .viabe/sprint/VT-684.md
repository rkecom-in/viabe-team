---
id: VT-684
title: "j05 ack-the-answer class — onboarding must acknowledge the owner's clarification before the next queue question (1/6 variance residual)"
status: open
priority: Medium
size: S
owner: claudecode
created: 2026-07-18
---

# VT-684 — the last j05 variance class (VT-662 family)

## Evidence (651cb75 gate, 2026-07-18)
Full-pack ×3: ONE Tier-1 in j05 r2 — owner clarified business type ("packaged goods bulk
distribution to retail stores"), assistant pivoted straight to the next queue question ("your
shop is in Chennai — is that right?") with NO acknowledgment of the answer
(ignored_speech_act). j05 re-drive ×3 same HEAD: 3/3 CLEAN → sampled variance (~1/6), not a
regression; batch graduated with this row rostered per the persona-variance standing (fix the
CLASS, not the sampled string).

## Direction (class fix, not prompt nudge)
The VT-662 net covered re-asking an OPEN about-gap; the residual is the transition beat — when
the owner's turn ANSWERS the pending question, the next-question composer must lead with a
one-clause acknowledgment/reflection of the answer before asking the next queue item.
Deterministic candidate: the turn-brain prompt already carries ALREADY COLLECTED; the walker
transition (journey.py next-question compose) is the seam — prepend the ack clause from the
just-recorded answer (grounded, no LLM invention). Prove ×3 on dev; add the sampled phrasing
as a guard case.
