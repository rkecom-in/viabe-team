---
id: VT-692
title: "WhatsApp-journey completion + name-anchored discovery (LLM+WebSearch → GST/details) — the Fazal screenshot ruling"
status: In Progress
priority: Critical
owner: claudecode
created: 2026-07-22
authorization_basis: "Fazal terminal 2026-07-22 (first-customer screenshot): 'When we got the company name, the manager could have used the LLM+Websearch and fetched the business details including GST and other detail, as we had discussed earlier in the Onboarding UI' + the visible opener-loop bug"
---

# VT-692 — close the WhatsApp-journey dead-end + wire name-anchored discovery

## The live evidence (Fazal's own first-customer journey, prod tenant 46d38684…)
All 4 seeded answers captured (Rkecom Services Pvt Ltd / Lubna Khan / Business Intelligence
Services / Mumbai) then: (1) NOTHING promoted to the tenant row (business_name still '',
type/city NULL); (2) queue exhausted (cursor 4/4), journey still 'active', next inbound →
`_opener()` holding-message loop — the WhatsApp-seeded journey has no draft, the off-taxonomy
free-text type blocks completion, and no queue remains → dead end.

## Build (one coherent arc)
1. **Name-anchored discovery kick (the Fazal ruling):** when a WhatsApp-journey tenant's
   `business_name` (+`city` when present) answer lands, kick the SAME auto_discovery the web
   signup uses, anchored on name+city, including the LLM+WebSearch candidates leg
   (ENABLE_LLM_DISCOVERY / entity_match.fetch_candidates). Draft lands → the EXISTING
   draft-driven machinery surfaces confirms (GSTIN candidates → the Sandbox GST verify stays
   the SOLE authoritative gate — discovery yields HINTS, never asserted facts; the no-fabrication
   rule unchanged).
2. **Answer promotion:** the collected free answers promote to canonical (business_name →
   tenants/business_profile; city → coarsened city_tier; owner_name → profile;
   business_type free text recorded, taxonomy-reconciled via the existing
   business_type_reconcile — never asserted off-taxonomy).
3. **Completion belt:** queue-exhausted + active + no pending confirms → run the DONE beat
   (recap + honest next step), NEVER the opener. The opener is for pre-questions state only.
4. Gates: unit per seam + dev journey re-drive (name → discovery draft → confirms → GST
   candidate surfaced → completion recap) + adversarial-verify (GST gate unbent; no
   fabricated facts; off-taxonomy never asserted) + owner-facing ×3 slice.

## Log
- 2026-07-22 rostered + build started (CC, immediate-fix standing).
