---
vt_id: VT-121
title: Daily Launch Tracker Telegram alert (overdue + due-this-week)
status: Cancelled
priority: Critical
cancelled_reason: superseded-by-Cowork-PM-role
cancelled_at: 2026-05-26T16:30:00+05:30
cancelled_by: fazal
sprint: Sprint 1 - Foundation
type: Infrastructure
area: [Observability, DevOps]
assignee: Pipeline Engineer
parent: VT-12
sub_items: []
exec_order: 6
branch: "feat/vt-observability-launch-tracker-alert"
version: "v1.0"
notion_legacy_id: 35e387c2-cc5a-8101-86b2-fcd2dfda22c6
created: 2026-05-12
last_updated: 2026-05-26T16:00:00+05:30
depends_on: VT-177
amended_by: VT-177
amended_at: 2026-05-26T16:00:00+05:30
---

## ROW CANCELLED — 2026-05-26 by Fazal

**Reason: category error.** This row designed a GitHub Actions cron + Python script + Telegram bot to track overdue launch milestones. Authored in early May before Cowork existed as a team role. With Cowork now managing the project (PM role), tracking overdue launch milestones and surfacing them to Fazal is Cowork's job — not a system feature.

**Replacement (no new VT row):** Cowork extracts the 45 Launch Tracker milestones from Notion into a Cowork-managed file (`.viabe/launch-tracker.md`), reads it as part of session-bootstrap context, and surfaces overdue items when Fazal opens the chat. Daily phone-push via Telegram is deferred (Fazal-confirmed 2026-05-26: chat-surfacing is sufficient for now).

**Net architectural change:** zero Notion API dependency in the system. Zero `NOTION_API_TOKEN` provisioning. Zero GitHub Actions workflow. Zero Python alert script. The PM-state lives in repo markdown + Cowork operational awareness.

**Sister row VT-177 (Notion migration script) ALSO CANCELLED** under the same reasoning.

The body below is preserved AS-MIGRATED for audit-trail integrity per CL-176 Rule #9; do NOT edit it to remove the Notion-based design — that was the actual original spec.

## Brief artifacts amendment — Path B reframing (Fazal 2026-05-26)

The body below assumes Notion is the source of truth for the Launch Tracker. **That assumption is OVERRIDDEN as of 2026-05-26.** Per Fazal's directive: zero Notion read-write deps in the system. The Launch Tracker is migrating to `.viabe/launch-tracker/MS-*.md` repo files under **VT-177** (sister row, Critical, Sprint 1, exec 5.5). **NOTE: above-this-amendment, the entire row was subsequently CANCELLED 2026-05-26 — see cancellation block at top.**

**Effective spec for THIS PR (when CC implements):**

- **No Notion API call.** Drop `NOTION_API_TOKEN` from GitHub Actions secrets requirements. Drop `requests` HTTP call to Notion. The data source ID `413be4ab-...` becomes irrelevant.
- **Read from `.viabe/launch-tracker/MS-*.md`** via `pathlib.Path` + YAML frontmatter parse. Same pattern as `scripts/build_dashboard.py` reading `.viabe/sprint/VT-*.md`.
- **Filter logic stays:** parse `target_date` + `status` from each milestone's frontmatter; surface overdue (`target_date < today AND status NOT IN ['Completed', 'Deferred']`) + due-this-week (`today < target_date <= today + 7 days`).
- **GitHub Action checkout step IS the data fetch.** No live external query. Faster, more reliable, no API rate limit, no token to rotate.
- **Telegram send is the only external API.** TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID (Fazal's existing values; mirror to GitHub Actions secrets if not already).
- **Failure-Telegram** still fires on any error (file parse failure, schema drift, Telegram send fail) — discipline preserved.
- **Team-model normalization (per CL-151):** CodeX/CoderC/CoderX retired; PR review = Cowork plan-review + Fazal Pillar-7 merge auth.
- **PR title:** `feat(observability): daily Launch Tracker Telegram alert (VT-121)` (NOT `(VT-Observability-Cost)`).
- **Merge target:** `main` (NOT `dev`).
- **Target deployment date in original body (2026-05-20) is stale** — date now becomes ASAP-post-VT-177-merge.

**The original Notion-based body below is preserved for audit trail (CL-176 Rule #9 supersession-from-both-ends). Do NOT edit it; the amendment above is the authoritative spec.**

## Parent task
VT-Observability-Cost.
## Concept reference
- Joint launch tracker (`Viabe_Launch_Tracker` Notion database, data source `413be4ab-870d-4895-bf35-dfd579142001`) is the calendar-anchored source of truth for 45 milestones across Reports-Jun15, Team-Jul15-Soft, Team-Aug15-Full launch gates.
- `concept-team.md` §15 (observability surfaces Fazal needs daily for solo-founder operations).
- `concept-team-pillars.md` Pillar 7 (operational visibility cannot be silently degraded).
## Why this subtask exists
Fazal has a 95-day window across three launch gates with 45 milestones, ~13 of which Fazal personally owns. Manually checking the Launch Tracker every morning is the gap that will get missed first. This subtask delivers a deterministic daily-alert mechanism: every weekday at 9 AM IST, scan the Launch Tracker for overdue + due-soon milestones and post a structured Telegram message to Fazal. Missed milestones become impossible to silently ignore.
## Dependencies
- `Viabe_Launch_Tracker` database (already created; data source ID above).
- Telegram bot already in use for Reports' OpsAssistant.
- GitHub Actions on the `viabe-team` repo (per VT-23).
- Notion API access (read-only query is sufficient).
## What CoderC must produce
### 1. GitHub Action workflow at `.github/workflows/launch-tracker-daily-alert.yml`
- Schedule: cron `30 3 * * 1-5` (9:00 AM IST = 3:30 UTC; weekdays only Mon-Fri; weekends excluded).
- Manual trigger via `workflow_dispatch` for ad-hoc runs.
- Job: `daily_alert` on `ubuntu-latest`.
- Steps: checkout, setup Python 3.12, install deps (`requests`, `python-dotenv`), run `scripts/launch_tracker_daily_alert.py`.
### 2. The alert script at `scripts/launch_tracker_daily_alert.py`
Deterministic Python script. No LLM calls. ~150 lines.
Flow:
1. Query Notion API: `POST /v1/databases/413be4ab-870d-4895-bf35-dfd579142001/query` with filter `{Status NOT IN [Completed, Deferred]} AND {Target Date < today}`.
2. Parse response into list of overdue milestones (name, target_date, owner, launch_gate, track, linked_vt_tasks, blocking_notes).
3. Sort by days_overdue descending.
4. Build Telegram message: header + OVERDUE section + DUE THIS WEEK (next 7 days) section + 'ALL CLEAR' fallback if both empty.
5. POST to Telegram via `https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/sendMessage` with `chat_id={FAZAL_TELEGRAM_CHAT_ID}` (split into multiple messages if >4096 chars).
6. Log output: count of overdue, count of due-soon, success/failure.
7. On Notion API failure: post SEPARATE Telegram failure-alert. Never silently fail.
### 3. Telegram message format example
```javascript
LAUNCH TRACKER DAILY DIGEST  2026-05-19 (Tuesday)

OVERDUE (2):
  Fazal     | Reports-Jun15    | Razorpay Live KYC submitted              | 4d overdue
  Fazal     | Reports-Jun15    | Resend DNS records deployed              | 4d overdue

DUE THIS WEEK (3):
  Fazal     | Team-Jul15-Soft  | Meta Tier-A templates authored           | due in 0d
  Fazal     | Team-Jul15-Soft  | Meta Tier-A templates submitted          | due in 2d
  Claude C  | Reports-Jun15    | Viabe Router Vercel project created      | due in 3d

Full tracker: https://notion.so/544492b9484b457b8415630e0362bfda
```
### 4. Secrets configuration
GitHub repo secrets:
- `NOTION_API_TOKEN` (existing).
- `TELEGRAM_BOT_TOKEN` (existing).
- `FAZAL_TELEGRAM_CHAT_ID` (existing).
- `LAUNCH_TRACKER_DATA_SOURCE_ID = 413be4ab-870d-4895-bf35-dfd579142001` (hardcoded constant OK).
### 5. Testing
- Unit test at `tests/test_launch_tracker_daily_alert.py` with mocked Notion API response.
- Smoke test: Fazal manually triggers `workflow_dispatch`; verifies real Telegram message within 30s.
- Failure-injection test: invalid Notion token triggers failure-Telegram correctly.
### 6. Manual stopgap until VT-121 ships
Fazal runs daily-check manually each morning via the Launch Tracker's 'OVERDUE (alert source)' saved view. Updates the hardcoded date filter weekly. Stopgap retires when VT-121 deploys (target 2026-05-20).
## Architectural rules to enforce in this subtask
- Pillar 7: visibility cannot be silently degraded. Failure-Telegram MUST fire on every Notion API error.
- Pillar 8: one alert mechanism. Parallel cron jobs rejected.
- No LLM calls (deterministic only).
- Weekend exclusion default; override via `workflow_dispatch`.
- Telegram message factual; no AI commentary on milestones.
- Secret discipline: no tokens in code or commits.
## Acceptance criteria
- GitHub Action workflow file in repo.
- Script at `scripts/launch_tracker_daily_alert.py` mypy-strict clean.
- Unit test passing.
- Smoke test: real Telegram message at 9 AM IST on a weekday.
- Failure-injection test: invalid token triggers failure-Telegram.
- Manual stopgap procedure at `docs/runbooks/launch-tracker-alert-manual.md`.
- PR title references `(VT-Observability-Cost)`; reviewed by CoderX; Fazal smoke-tests.
- Target deployment: 2026-05-20 (per Launch Tracker MS-34).
## Out of scope
- Slack alternative (Phase 2).
- Per-milestone owner notification routing (Phase 1 all alerts go to Fazal).
- Alert acknowledgment / snooze (Phase 2).
- Trend dashboard / weekly velocity report (Phase 2).
- Auto-creating Reports milestones in this tracker (out of scope; Launch Tracker is Team-led with Reports milestones embedded for joint launch only).
- Email alerts as backup channel (Phase 2).
## Branch & PR
- Branch: `feat/vt-observability-launch-tracker-alert`.
- PR title: `feat(observability): daily Launch Tracker Telegram alert (VT-Observability-Cost)`.
- Reviewers: Pipeline Engineer (primary); CoderX (secrets + failure-injection); Fazal personally smoke-tests.
- Merge target: `dev`. Production deploy upon successful smoke test.

## Status history
- 2026-05-25 03:45 IST: migrated from Notion (notion_legacy_id: 35e387c2-cc5a-8101-86b2-fcd2dfda22c6)
