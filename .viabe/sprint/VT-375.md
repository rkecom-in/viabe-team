---
vt_id: VT-375
title: VTR Run-Control Panel Phase B — programs projection + read-only canvas (tenant tiles → program tiles → step timeline)
status: Dispatched
priority: Critical
sprint: "Sprint-Fazal"
type: Feature
area: [Ops Console, team-web, Orchestrator]
assignee: Claude Code
related: [VT-374, VT-376, VT-377, VT-370]
exec_order: 2
cl_decisions_checked: [CL-4, CL-24, CL-26, CL-29, CL-33, CL-35, CL-36, CL-52, CL-56, CL-79, CL-81, CL-82, CL-88, CL-130, CL-132, CL-137, CL-220, CL-249, CL-252, CL-260, CL-424, CL-426, CL-428, CL-429, CL-431, CL-432, CL-433, CL-434, CL-435]
created: 2026-06-12
last_updated: 2026-06-12
---

## Contract (plan §9 — `.viabe/queue/VT-374/plan.md` — + Phase-A gate fold-ins)

1. **Programs projection** (orchestrator, read-only): past = `pipeline_runs` (+ rerun
   lineage); running = DBOS status + step rows; upcoming-7d = COMPUTED forecast (trial
   sweep dates from `trial_started_at` + `config/trial.yaml`; queued agent work items;
   roadmap month items). One GET surface under the Gap-6 auth pattern.
2. **Read-only canvas** (team-web Ops Console): tenant tiles → program tiles
   (past / running / upcoming-7d) → step timeline reading ONLY the Phase-A GET surfaces
   (`vtr_step_timeline`, `vtr_workflow_controls`, the timeline API). NO mutations in
   Phase B — controls ship in VT-376.
3. **Copy obligations (binding, from the plan + gates):** observed-only steps labeled
   non-controllable; re-run described as re-dispatch (no time-travel claims); keys-only
   envelope visibility disclosed; pause-state-unverifiable surfaced when the control read
   degrades; concurrent-release no-ordering note; legacy run_controls 410 copy retained.
4. **C1 fold-in (Phase-A gate test debt):** the F10 RACE acceptance — a rerun racing an
   approval-create converges to refusal, never two open approvals (real DB).
5. **Orphaned run-bound override sweep** (hygiene): extend `expire_overrides_sweep` to
   cancel run-bound pins whose run reached a terminal state without consuming them.
6. Light-mode consistent with the Gap-6 console; bilingual labels EN/HI where
   owner-facing strings exist (this is a VTR surface — EN primary acceptable, note it).

## NOT in scope
Mutations (pause/override/rerun buttons) = VT-376. C2 view hardening = VT-376. Multi-VTR
scoping = VT-377. Anything touching gate modules.

## Merge class
[BUILD] self-merge on the 3 real gates ONLY IF the diff contains zero migrations, zero
view/RLS changes, zero gate-module touches, and the canvas stays mutation-free. Any of
those present → pr-ready → Cowork gate. Rendered-output verification required for the
canvas (screenshots, console-error-free) per the VT-372 standard.

## Status history
- 2026-06-12: dispatched on VT-374 merge (b8bd111) per CL-435 batch sequencing.
- 2026-06-12: built by CC (3 builders → 2 adversarial verifiers → 6 majors fixed →
  re-verify → C1 escalation). The falsifiable C1 race test exposed a REAL unserialized
  check-then-act in rerun.py (_refuse_on_open_approval) — rerun could succeed while an
  approval armed concurrently. Cowork ruling: OPTION A (post-completion re-check →
  overlap closes the rerun run 'escalated' + run_control_rerun_overlap alert + outcome
  surfaced + panel disclosure copy; version mint stands). **B-analysis recorded:** a
  gate-window advisory lock does NOT deliver "converges to refusal" (approvals landing
  during rerun EXECUTION still overlap); a full-execution lock would block the live
  owner-approval arm behind an ops rerun — unacceptable inversion. Guarantee stack =
  mig-128 (never two open) + detect-and-escalate (never silent). **Phase-C note for the
  VT-376 contract:** when the interactive rerun button ships, revisit the gate-window
  TOCTOU edge with the lock-scope analysis above as the starting point.
  Out-of-scope flags rostered to Cowork: assignments.ts eager serverSecretClient() 500;
  sticky-banner hydration #418; run_control_rerun_overlap event-schema registration.
