---
vt_id: VT-178
title: VT-122.1 — pipeline_runs + pipeline_steps + phone_token_resolutions schema review + RLS hardening
status: Done
priority: Critical
sprint: Sprint 1 - Foundation
type: Infrastructure
area: [Observability, Database]
assignee: Claude Code
parent: VT-122
sub_items: []
exec_order: 7.1
branch: "feat/vt-observability-tables-rls"
version: "v1.0"
created: 2026-05-26
last_updated: 2026-05-26T19:18:00+05:30
authorized_by: fazal
follow_up_to: VT-122
merged_pr: 67
merge_commit_sha: f8dbf127cd86c74830008ced504379217805261f
merged_at: 2026-05-26T13:46:31Z
unblocks_filed_followups: [VT-187, VT-188]
---

# VT-178 — pipeline_runs + pipeline_steps + phone_token_resolutions schema review + RLS hardening

## Scope

The three tables already exist on main: `migrations/005_pipeline_runs.sql` + `migrations/006_pipeline_steps.sql` + `migrations/007_phone_token_resolutions.sql` (VT-Foundation era; CL-415 + VT-122 body confirmed). This row VERIFIES + HARDENS:

1. **Column-level audit** vs the design doc Section 2.1 spec. Any drift documented (the existing `pipeline_steps.tenant_id` denormalization is intentional per VT-122 body — verify other deltas).
2. **RLS policies** — confirm all 3 tables enforce `current_setting('app.current_tenant')::uuid` via `app_current_tenant()` helper (per CL-82 substrate; see active-context-summary CL-82 row). `phone_token_resolutions` MUST have STRICTER RLS (operator role required, not just tenant role).
3. **Indexes** — confirm `pipeline_runs (tenant_id, started_at DESC)`, `pipeline_steps (run_id, step_seq)`, `pipeline_steps (tenant_id, started_at DESC)`, `phone_token_resolutions (tenant_id, phone_token)` all present. Add missing.
4. **Documentation** — module docstring at top of each migration file pointing at this row + the design doc.

## Dependencies

- VT-Foundation (migrations 005/006/007 already on main)
- VT-104 (PII redactor — shared phone-hashing logic; will be reused by VT-184)
- VT-175 (`app_current_tenant()` helper substrate)

## Acceptance criteria

- Cross-tenant RLS test: insert under tenant A's GUC, attempt SELECT under tenant B's GUC → returns 0 rows for all 3 tables.
- Phone-token resolutions stricter-RLS test: operator role allowed; tenant-app role denied.
- All design-doc Section 2.1 columns present OR documented deltas inline.
- Canary: real Supabase + cross-tenant isolation + index-presence assertion.

## Canary (Rule #15 — mandatory)

Real Supabase dev DB + cross-tenant RLS verification + index audit. Pure DDL inspection + RLS round-trip; NO LLM (deterministic SQL only). Defense-in-depth: ANTHROPIC_API_KEY ABSENT at PREFLIGHT.

**Canary file:** `apps/team-orchestrator/canaries/vt178_pipeline_tables_rls.py`
**Wall-clock budget:** ≤ 30 seconds.
**Cost budget:** **0 paise** (no LLM dispatch).

**PREFLIGHT:**
- Supabase host resolved; print stripped DSN.
- ANTHROPIC env ABSENT (defense-in-depth).
- All 3 migrations (`005_pipeline_runs.sql`, `006_pipeline_steps.sql`, `007_phone_token_resolutions.sql`) applied to dev DB (verify via `information_schema.tables`).
- `app_current_tenant()` helper exists (per CL-82 substrate-note in active-context-summary).

**Group A — column-level audit (3 assertions):**

1. **pipeline_runs columns match design doc §2.1:** verify `run_id`, `tenant_id`, `trigger_kind`, `trigger_source_ref`, `started_at`, `ended_at`, `status`, `final_outcome`, `total_cost_paise`, `step_count`, `error_summary` all present with correct types via `information_schema.columns`. Document any drift inline.
2. **pipeline_steps columns:** verify `step_id`, `run_id`, `tenant_id` (denormalized per VT-122 body note), `step_seq`, `step_kind`, `step_name`, `parent_step_id`, `started_at`, `ended_at`, `duration_ms`, `input_envelope`, `output_envelope`, `decision_rationale`, `tool_calls`, `status`, `error`, `cost_paise`, `model_used`, `tokens_input`, `tokens_output`.
3. **phone_token_resolutions columns:** verify `phone_token`, `tenant_id`, `customer_id`, `phone_e164`, `created_at`, `last_accessed_at`.

**Group B — RLS isolation (3 assertions):**

4. **pipeline_runs cross-tenant isolation under `app_current_tenant()` GUC:** insert 2 rows under tenant A's GUC → SELECT under tenant B's GUC returns 0 rows.
5. **pipeline_steps cross-tenant isolation under `app_current_tenant()` GUC:** same pattern.
6. **phone_token_resolutions STRICTER RLS:** verify operator-role can SELECT under any tenant; tenant-app-role under tenant A's GUC cannot SELECT tenant B's resolutions; tenant-app-role under tenant A's GUC CAN SELECT only its own resolutions.

**Group C — index presence (1 assertion):**

7. **Required indexes present per design doc §2.1 + performance contract:** verify via `pg_indexes`:
   - `pipeline_runs (tenant_id, started_at DESC)`
   - `pipeline_steps (run_id, step_seq)`
   - `pipeline_steps (tenant_id, started_at DESC)`
   - `phone_token_resolutions (tenant_id, phone_token)` (or equivalent unique constraint)
   If any are missing, the canary documents which + the PR adds them.

**Group D — zero LLM (1 assertion):**

8. **Defense-in-depth runtime:** assert `os.environ.get('ANTHROPIC_API_KEY')` is `None` at canary start. Pure-SQL row.

**Audit artifact** (in `pre-merge-result`):
- Total wall-clock from `time`
- Resolved Supabase host at PREFLIGHT (credentials stripped); ANTHROPIC env ABSENT confirmed
- Per-assertion observed values for all 8
- **6-canary regression sweep:** VT-102 + VT-103 + VT-104 + VT-171 + VT-175 + VT-176 byte-identical (these all run against the same 3 tables; ensures no schema drift broke them)
- Sample `information_schema.columns` output for all 3 tables (verbatim)
- Sample `pg_indexes` output for all 4 expected indexes
- Full stdout tail ≥ 150 lines + log at `/tmp/vt178-canary-evidence.log`

Summary-only `pre-merge-result` will be bounced.

## Branch & PR

- Branch: `feat/vt-observability-tables-rls`
- PR title: `chore(observability): VT-122.1 tables + RLS hardening (VT-178)`
- Review path: Claude Code + Cowork plan-review + Fazal Pillar-7
- Merge target: `main`

## Status history

- 2026-05-26 17:35 IST: filed by Cowork per Fazal directive — sequencing VT-122 substrate into Sprint 1 critical-path before Team-Jul15-Soft. Sub-row of VT-122.
- 2026-05-26 18:50 IST: status flipped Backlog → Queued. Rule #15 Canary section appended (8 assertions across 4 groups). Rule #16 ledger check ran (see brief-ready signal cl_decisions_checked). brief-ready dispatched.
- 2026-05-26 19:18 IST: PR #67 merged. sha `f8dbf127`. Canary 8/8 PASS @ 29.21s; 6-canary regression sweep 54/54 byte-identical; CI 14/14 PASS. Dev DB migrations registered + canary re-run 8/8 PASS against dev. Status → Done.
- 2026-05-26 19:20 IST: STEP-0 schema audit surfaced design-doc drift (3 tables, missing per-field columns now in JSONB envelopes) + grant-exclusion-vs-policy stricter-access pattern. Path-b shipped (document drift; defer normalization). Cowork files follow-ups VT-187 (schema normalization, α-sequencing per Clau review) + VT-188 (operator-role JWT substrate, client-direct-ready per CL-88).
